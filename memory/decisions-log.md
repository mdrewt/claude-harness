# Cross-project decisions log

Append-only summary of notable decisions (one line each) with a pointer to the
full ADR in the project repo.

| Date | Project | Decision | ADR |
|------|---------|----------|-----|
| 2026-06-23 | (harness) | Workspace setup per WORKSPACE-PLAN.md v2.1 | — |
| 2026-06-23 | (harness) | Built all 7 stack templates, sync-templates drift tool, harness test suite (5/5), SDD spec template + example | — |
| 2026-06-23 | (harness) | Fixed justfile syntax (just uses indented bodies, not `recipe: ; cmd`); added per-stack CI toolchain setup; added justfile-syntax guard test | — |
| 2026-06-23 | (harness) | Adopted from external CLAUDE.md: tiered+inverted principles, mechanical-enforcement map + /simplify, cost-aware doc routing, compaction/curation/honesty/diff rules, impact-analysis + code-graph MCP note | — |
| 2026-06-24 | (harness) | Ship PreToolUse destructive-command guard hook to generated projects | `docs/adr/0002-pretooluse-destructive-command-guard.md` |
| 2026-06-24 | (harness) | Auto-format edited files via PostToolUse hook (Biome/Ruff/rustfmt) | `docs/adr/0003-posttooluse-auto-format-hook.md` |
| 2026-06-24 | (harness) | Pin GitHub Actions to digests via Renovate preset | `docs/adr/0004-pin-github-actions-digests-via-renovate.md` |
| 2026-06-24 | (harness) | Cancel superseded CI runs via workflow concurrency group | `docs/adr/0005-cancel-superseded-ci-runs.md` |
| 2026-06-24 | (harness) | Mechanically enforce mutation/coverage/SCA gates (CI + invariants tests) | `docs/adr/0006-mechanical-enforcement-of-test-and-supply-chain-gates.md` |
| 2026-06-27 | monster-realm | M8c: wild individuality stored as `individuality_seed` in private `battle_wild` side-table (1:1 by `battle_id`), NOT on the public `battle` row; `battle` is client-subscribable (ADR-0042) and raw RNG-derived genes are must-never-leak (ADR-0015) | `monster-realm/docs/adr/0045-wild-individuality-private-table.md` |
| 2026-06-27 | monster-realm | M8.5a: `start_battle` opponent-provenance authz rejects non-self/non-WILD_IDENTITY (reject-not-clamp), P0 closes conscription/grief/XP-farm; party bounds on both sides (O(1) guards before DB read); write-back asserts positional coupling via checked `.get(i)`; side-B no-write intentional PvE semantics deferred to M16/PvP | `monster-realm/docs/adr/0048-start-battle-opponent-provenance-authz.md` |
| 2026-06-27 | monster-realm | M8.5a: ADR-0042 amended — side-B no-write is intentional, not a bug; symmetric write-back only with real PvP + per-side authority (M16/ADR-0017) | `monster-realm/docs/adr/0042-private-battle-table-design.md` (amended) |
| 2026-06-27 | monster-realm | M8.6d closed/subsumed by M8.5b (BST relocated to `game_core::base_stat_total`, ADR-0049 §4); doc-comment verified accurate; no code/behavior change, no new test | — |
| 2026-07-02 | monster-realm | M11a: zone-map warp data shape — warps-in-TileMap as serialized overlay (not glyph, not side-table); content.rs data layer / world.rs rule layer split (one-way import); std-only Tiled JSON importer (no serde_json); `validate_zone_maps` standalone gate called by M11b from `sync_content` | `monster-realm/docs/adr/0065-zone-map-warp-data-shape.md` |
| 2026-07-04 | monster-realm | M10.5a: empty-moveset guard closes panic path — 3-point defense (validate_content load-time check, wild_battle_monster boundary guard, battle_monster_from_row owned-monster defense-in-depth); ADR-0049 amended | `monster-realm/docs/adr/0049-empty-moveset-panic-guard.md` (amended) |
| 2026-07-10 | monster-realm | M13.5d: content parse caching — hot-path registries cached via `LazyLock<Result<Vec<T>, String>>` statics in server-module; client-wasm two-level cache (registry + thread_local active-zone); game-core stays pure (functional-core/imperative-shell); determinism/parity evals green | `monster-realm/docs/adr/0089-content-parse-caching.md` |
| 2026-07-10 | monster-realm | M13.5e: client UX correctness — bait save/restore across re-renders [e-1], zone-switch early-return + N=3 reload msg [e-2], rationale comment on unfiltered battle subscription [e-3], sortableChildren+zIndex O(n log n) [e-4], EWMA adaptive interp delay + depth-4 ring buffer (ADR-0090, supersedes ADR-0075) [e-5] | `monster-realm/docs/adr/0090-adaptive-interp-delay.md` |
| 2026-07-13 | monster-realm | M14.5d: client battle UX completeness — weather pipeline (StoreWeather + VM + DOM banner; value→turnsRemaining rename; zero-falsy trap), BattleOutcomeTag literal union + exhaustive switch with never-check, bindings-derived parity guards (length-anchor + known-member proof-of-teeth), battleVMsEqual VM-compare + shouldSkipBattleRefresh visibility guard (JSON.stringify rejected for bigint); 14.5d-1 cure-item UI PARKED as spec gap | `monster-realm/docs/adr/0101-m14.5d-client-battle-ux.md` |
| 2026-07-18 | monster-realm | M17.5b: trade swap debits-before-credits ordering — `SwapPlan::ordered_steps() → Vec<ApplyStep>` SSOT contract (ALL ItemDebit/CurrencyDebit before ANY ItemCredit/CurrencyCredit); bilateral same-item trades near cap silently destroyed items (credit-before-debit window + silent clamp); currency headroom netted shell-side (`balance.saturating_sub(outgoing)`) symmetric with items; `confirm_trade` single 4-arm exhaustive match (no legacy loops); Credit variants carry `to_initiator` boolean; enforcement: game-core clamp-mirror tests, EA-CONSERVATION-ORDER-01 source-guard (inline netting + no legacy loops), APPLY_ORDER eval criterion | `monster-realm/docs/adr/0123-trade-swap-debits-before-credits-ordering.md` |
| 2026-07-27 | harness | ADR-0009: ADR `## Confirmation` + adr-lint mechanical gate (`just adr-gate`, strict, in `just ci`; 8 ADRs backfilled) + codified spec skeleton (Non-goals, closure dispositions) + doc-keeper knowledge-promotion dispositions; format-field doctrine: a field needs a consumer + a single home + a mechanical check | `docs/adr/0009-adr-confirmation-and-spec-skeleton.md` |
| 2026-08-14 | monster-realm | 14r-b: trading negative paths gated dynamically — 3-context Playwright e2e (`client/e2e/trade-zz-negative.spec.ts`, 10 self-contained tests) driving `__mrTrade` + `spacetime sql` server truth, auto-discovered by playwright `testDir` so it runs in CI with zero justfile/workflow edits (merge-doctrine-enforced, NOT a required check); `hasCancelPartyCheck` tightened (string-strip + anchored `&&`, RT-SEC-02/03 fixtures) and declared a shape tripwire whose semantic authority is 5a/5b/5c; new mutation-VISIBLE in-crate pins (`check_authorize_call` operator pin, EA-CANCEL-PARTY-01, EA-REAPER-03) + new eval criterion REAPER_SCHEDULE_PRIVATE; B1-B13+E1 proof-of-teeth register executed twice; mutation cap 324 untouched; EARS exact-boundary-ACCEPT clause left PARTIALLY GATED (no item faucet reaches a browser identity) with the surviving-mutant ledger recorded | `monster-realm/docs/adr/0184-trade-negative-path-dynamic-suite.md` |
| 2026-08-16 | harness | **W0-0 operator decisions (7), recorded from a direct interview rather than `mr-ask-drew` issues.** (1) **Build the harness-repo slice path** (`lp-00`): harness slices get their own worktree and a PR on `mdrewt/claude-harness`, gated by `mr-selfcheck` + per-tool `--selftest` — NOT harness `just ci`, which cannot fail for `memory/projects/**`. Rejected: supervisor-direct commits (no gauntlet; plausible cause of RF-0), attended-only (against D5), deferring loop-infra. (2) **Q-B4 ANSWERED: accept a third mandatory client hard-refresh now** for `15r-sec-a`; `15r-net-a` gets a definite Wave-6 slot rather than an event trigger. (3) **Q-B2 ANSWERED: auto-purchase of extra credits is ON** — so a lookahead miss silently buys rather than failing loudly; `lp-12` ships a **ramped ceiling** (95% until p90 has ≥3 samples per mode/tier, then toward 99%) and treats any overage row as a loud alert, not a routine band. Note the unexplained contradiction: 29 ledger rows carry `overageStatus:"rejected"` / `overageDisabledReason:"org_level_disabled_until"`. (4) **Interim pacing: run UNPACED, disable by hand** through Waves 0–4 (~25 slices, before `lp-12` exists). Operator's call, made against the measured 29%→90% precedent; rejected 3/week hand cap, 2/week, gauge-first. (5) **Consequence of (4): pull `lp-09` AND `lp-01` to the front** — the un-clearable kill switch and the real utilization gauge land before any other remediation, because manual disable is now the only control. Execution order is `lp-00` → `lp-09` → `lp-01` → `15r-sec-a` → rest; PLAN §9 reordered so `M-loop-infrastructure` precedes `M-postgate-fifteenth-review-residuals`. (6) **Tier-C allowlist is NOT empty**: seed with every incomplete milestone in scheduled order — measured as `[M18, M19, M22, M23, M24, M25]`, the six that are both sketch-sized and entirely unbuilt. MILESTONE mode uses `mr-ask-drew` for design decisions, and **a milestone with an unresolved blocking decision is barred from BOTH spec-writing and implementation** until answered; every tick checks issue state for answers first. This overrides the plan's empty-by-default design. (7) **Next action: build `lp-00` attended**, loop stays paused. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` §4, `M-postgate-fifteenth-review-residuals.spec.md` §4 |
| 2026-08-16 | monster-realm | **Sequencing against the imminent SpacetimeDB 2.8.1 bump (operator context + judgement call, not a menu choice).** The bump is of the *installed SpacetimeDB*, had not yet occurred, and a concurrent session was mid-migration in the project repo (`Cargo.toml`, `server-module/src/lib.rs`, ADR-0197, skills, ARCHITECTURE.md). Decisions: (1) **`M-stdb-2x-module-sdk` queued into PLAN §9 ahead of `M-postgate-fifteenth-review-residuals`** — it rewrites `schema.rs` attributes (`name =` → `accessor =`, 61 sites), 16 Rust source-scanning teeth and several `evals/` scanners, which is the exact surface most 15r slices read. (2) **`15r-sec-a` and `15r-sec-vis` gated `blocked:stdb-2x`** — the CRITICAL battle-privacy fix waits ~3 slices because **view primary keys arrive with 2.x**, deleting its hardest half (the no-PK client-ingest reshape), and because building first guarantees rework; `15r-sec-vis` additionally waits on `battle-schema-snapshot` being re-baselined for the 2.7.1 V10 output shift. (3) **Attribute-sensitive slices named explicitly** (`15r-d-0`, `15r-d`, `15r-consolidate`) as needing re-derivation post-bump; `15r-a2`/`15r-g`/`15r-f`/`15r-i-1`/`15r-sec-mig-*` confirmed attribute-agnostic. (4) **W0-6's answer STANDS** — ADR-0197 verified `client_visibility_filter` is byte-identically `unstable`-gated and unimplemented at 2.8.1, so private-table + owner-scoped `#[view]` remains the pattern; do not spend a slice re-checking. (5) **`M-loop-infrastructure` is bump-independent** (harness-only) and therefore leads §9 — `lp-00` → `lp-09` → `lp-01` is exactly the work that can proceed during a project-side freeze. (6) **`adr_next_free` corrected 197 → 198**; 0197 is taken by the upgrade ADR. Correction to my own earlier record: "crate version ≠ product version" was FALSE — the crate tracks the product since 2.0.0; 1.12.0 was simply a pin that never followed the CLI. | `specs/monster-realm-v2/M-stdb-2x-module-sdk.spec.md`, `monster-realm/docs/adr/0197-spacetimedb-2.8.1-upgrade.md` |
| 2026-08-16 | harness | **lp-00 DELIVERED (attended): the loop can now build harness-repo slices.** New `memory/projects/mr-repo-of` is the routing SSOT (declared `touches:` → `harness｜project｜MIXED｜EXTERNAL`), read by BOTH `mr-spawn` and `mr-launch.sh` so the rule cannot drift. `mr-spawn` routes cwd/base-branch/PR-target and refuses four ways (`REPO-MIXED` — never split a cross-repo slice silently; `REPO-EXTERNAL` — e.g. `~/.claude/**`, needs an attended session; `REPO-UNRESOLVED`; `REPO-OUT-OF-SYNC`). `mr-launch.sh` gained RUNDIR/OTHERDIR/PRREPO and **`terminal_pr_open` now polls the run's own repo** — the latent bug that would have burned all 3 attempts on a finished harness run. Brief tokenised (`<PR_REPO>`/`<BASE_BRANCH>`/`<WT_ROOT>`/`<REPO_BLOCK>`), zero hardcoded repo/branch strings left. **Two findings not in the spec:** (1) harness local `main` was **9 commits ahead of origin** (unpushed `chore(mr-sup):`) while monster-realm was 0/0 — a harness slice branching from `origin/main` would have built on a base missing all of it; now the `REPO-OUT-OF-SYNC` gate, protecting both repos. (2) `mr-selfcheck`'s two hand-maintained lists were **derived away** rather than checked — the old 19-path list had already drifted (omitted `mr-selfcheck` itself and `mr-feedback`); deriving also made the heredoc scanner scan itself and exposed a real bug, counting a `python3 - <<'PY'` example **inside a comment** as an opener (comments cannot open heredocs; fixed generally, not carved out). Teeth: 15 `mr-repo-of` fixtures incl. live declarations + a degeneracy check; all **43** live slice declarations route as intended; `mr-selfcheck` demonstrated RED on 7 injected defects incl. a negative control, then restored. **NOT exercised: `git push` + `gh pr create` on the harness** — local half proven end to end, outward half awaits the first real harness slice. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` §2.6 |
| 2026-08-17 | harness | **lp-01 DELIVERED: rate-limit telemetry reader.** Defect: ARMED heredoc pre-filters on `"resetsAt"` + `"rejected"` substring, so `allowed_warning` rows carrying `utilization` never parsed (not merely discarded — pre-filter blocks at input, no reader). Solution: `mr-cost-watch telemetry LOGFILE GATE NSLICES [OUTFILE]` one-shot appends 12-key JSONL to `$MEM/mr-rate-limit-telemetry.jsonl` (8 EARS fields + `rateLimitType`/`source`/`uuid`/`ri=verbatim-object`); ARMING heredoc path md5-byte-identical (verified over `PYRL` block plus behavioral test). Two tick call sites in `mr-native-tick.sh`, both in flock: `:280-281` before gate-1 live-chain standdown (samples live pass-logs; closes blind-spot — most ticks stand down before spawning, so tick-log-only reader samples nothing exactly when budget burns), `:407-409` after CENSUS/outside RC branches (rate-limited tick's rows matter most). Persists ALL windows `rateLimitType`-columnar; premise ("`overageStatus` five_hour-only") refuted by evidence.json:2753 showing `seven_day` row carrying it, but decision stands on reset-cycle (lp-12/13 blocked on ≥1 full cycle; widening later restarts week). Dedup by `uuid` under exclusive `flock` (idempotent against re-read/restart/truncation). Review findings: two proven cheats (basename-keyed lookup table, fabricated stdout counters) fixed by runtime-generated random corpus + cross-checking counters; static call-site pins (dead code + smuggled write) fixed by requiring `rate_limit_resets_at`/`rate_limit_armed_by` writes from exactly one place inside extracted `PYRL` range; reader hardened by red-team PoCs: uuid-persist asymmetry, 190-row TOCTOU (50 uuids), hot-path unbounded read hang/OOM-killed on `/dev/full`, NaN/Infinity producing non-RFC-8259 JSON, short write leaving torn line, 1 MiB per-line cap (468 MB RSS risk measured). Teeth: `mr-telemetry-selftest` (15 fixtures / 150 assertions) wired to `mr-selfcheck`, RED on 3 injected defects (status-filter, None-check, arming-predicate-width), demonstrated against spec's 3 attack vectors, belt-and-braces asserts `mr-state.json` and production telemetry file unchanged. Output gitignored (appended production hourly; conflicts every parallel slice branch); durability deferred to lp-12 reader owner. | — |
| 2026-08-21 | harness | **lp-tester-tools design: scoped Bash for tester via `agent_type`-keyed PreToolUse allowlist, not frontmatter pattern-scoping (impossible — `Bash(cmd:*)` only exists in settings.json rules).** Inverted from `guard-bash.mjs`'s denylist: whole-string-anchored allowlist of four non-executing shapes (`bash -n`/`sh -n`/`node --check`/the exact `mr-selfcheck` `python3 -c ...ast.parse...` idiom), rejecting any compounding/redirect/substitution metachar, confined to a relative non-traversing path with `.env`/`.pem`/`.key` targets blocked (mirrors the Read tool's own deny-list). `--selftest` deliberately excluded from the allowlist even for tools tester touched — for a loop-infra tool it IS the test suite (guard-bash.mjs's own fixtures are tester-authored TDD red phase), so allowing it would reopen the reward-hacking peek the no-Bash rule existed to close. Design red-team executed the code against this repo and caught two real defects before either shipped: a metachar guard that made the one allowed Python shape unreachable, and a path pattern that was a genuine content-disclosure oracle against `.pem`/`.env`/`.key` files — both fixed, both regression-tested in the 33-fixture `--selftest`. Split into two spec entries (`lp-tester-tools` harness / `lp-tester-tools-project` monster-realm) per `lp-00`'s `REPO-MIXED` refusal — delivered as two PRs. Known unclosed gap: `NODE_OPTIONS=--require` can make `node --check` non-inert; nothing sets it here today. Verification gap: the Agent tool resolves subagents from the session's original root, so this session could not live-spawn a tester subagent against its own worktree's edited `tester.md` to confirm the real `agent_type` payload value — relied on the 33-fixture `--selftest` (which exercises the real hook process end-to-end) plus sourced Claude Code documentation instead; first production use is the true proof. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` (`lp-tester-tools`, `lp-tester-tools-project`) |
| 2026-08-21 | harness | **Post-implementation review of `lp-tester-tools` (fresh, independent adversarial pass after both PRs shipped, run separately from the pre-ship design red-team) found two real defects by actually running the shipped code and constructing working exploits, not by reasoning about the regex.** (1) BLOCKER: tester's pre-existing, unchanged `Write`/`Edit` is completely unscoped by Claude Code (no path-scoping mechanism exists for those tools either), so tester could simply `Edit` `.claude/hooks/guard-tester-bash.mjs`'s allowlist or `.claude/settings.json`'s hook registration to disable the Bash restriction within the same session, then use the harness's own pre-existing `Bash(npm:*)`/`Bash(cargo:*)`/`Bash(pytest:*)` permission grants to run the real test suite — the exact reward-hacking channel this whole feature exists to close, one Edit call away. Closed with a new companion hook, `guard-tester-write.mjs`, blocking tester's Write/Edit/MultiEdit from ever targeting anything under `.claude/`. (2) The `.env`/`.pem`/`.key` suffix denylist from the first ship was proven, with a real working exploit, to still disclose the content of any secret-shaped file under a different name (`.npmrc`, `id_rsa`, `.git-credentials`) via the syntax-error message the "non-executing" checks emit on failure — plus a structural gap the suffix approach could never close: a safe-looking, correctly-extensioned relative path can be a symlink resolving anywhere the OS user can read (proven with a real repo artifact, `node_modules/.bin/biome`), and a path resolving to a FIFO hangs `bash -n`/`sh -n` indefinitely (proven, required `SIGKILL`). Closed by replacing the suffix denylist with a two-layer check: no dotfile path segment (except the explicit `.claude` traversal allowance tester's own legitimate use needs), then `realpathSync`+`statSync().isFile()` to require the resolved target be a regular file inside the project root, gated by a per-shape extension/shebang allowlist (`statSync` never blocks on a FIFO — blocking is `open()`-time behavior, not `stat()`-time — so the hang is structurally impossible, not merely avoided). Both fixes regression-tested against the exact proven exploits (36 Bash-guard fixtures, up from 33; 14 new Write-guard fixtures). A first attempt at the path-safety fix over-blocked `.claude/hooks/*` entirely (a real usability bug — tester couldn't check its own guard file's syntax) before landing on the directory-vs-filename distinction now shipped. `mr-selfcheck`'s `A9` block extended to verify both hooks. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` (`lp-tester-tools`, `lp-tester-tools-project`) |
| 2026-08-21 | harness | **lp-queue DELIVERED: `queue[]` reshaped from a 58-entry narrative journal (duplicating `monster-realm-handoff.md`, read by zero code — RF-3) into a `{slice, spec_file, reason, added_utc, added_by, derived_from_master_sha}` fast-path pointer cache, mechanically capped at 5, consumed by deletion on launch/merge/invalidation — never authoritative, always re-verified live.** `mr-record queue-add`/`queue-remove` are the sole writers, each re-reading `mr-state.json` fresh under `flock` and writing back atomically — deliberately decoupled from a tick's own end-of-tick write of every other key (design review found the naive version would let a stale end-of-tick snapshot silently undo a mid-tick removal). Stays strictly out of `lp-08`'s lane: no mechanical `touches:`/`blocked:`/retirement parsing, only a heading-existence check at write time. 58 legacy entries confirmed present in `monster-realm-handoff.md` (54 by exact rid match, 4 more by content) before deletion; pre-migration state recoverable via the parent commit. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` §2 (`lp-queue`) |
| 2026-08-21 | harness | **lp-handoff-rotate DELIVERED: `mr-record handoff` now auto-rotates.** Parses each entry by its own `## <timestamp> ...` header (bare, no em-dash requirement — an earlier draft that required one silently mis-split 11 of 197 real live entries, caught at red-team by running it against the live corpus) — never file position, since the live file had drifted to two regions with opposite order from direct hand-edits bypassing `mr-record`. Rotates over a ~40 KB byte budget (operator decision), keeps newest live, archives oldest-first per calendar month, pins unparseable-timestamp entries rather than losing them. Archive writes are idempotent (a crash between the archive write and the live-file swap — a real vector under the loop's own SIGKILL-on-rate-limit-trip behavior — can't duplicate a row on retry; caught at red-team). Direct Edit/Write on the handoff is now against doctrine, though no hook enforces it yet. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` §2 (`lp-handoff-rotate`) |
| 2026-08-21 | harness | **Post-implementation review of `lp-queue`/`lp-handoff-rotate` (fresh, independent adversarial pass after both shipped) caught a real blocker before merge: `SLICE_ID_RE` required a letter-first slice id, rejecting virtually every real game-milestone slice (`15r-sec-a`, `13r-c-2`, `15r-a2`, ...) — the entire review-residuals naming convention is digit-first. `queue-add`'s own `die()` message even cited `15r-sec-a` as a valid example while the regex rejected it. Fixed (`^[A-Za-z0-9][A-Za-z0-9.-]*$`), regression-pinned in `mr-selfcheck` against a real digit-led heading (`15r-sec-a` in `M-postgate-fifteenth-review-residuals.spec.md`), reproduced-then-reverified against the live spec corpus. Also fixed: `memory/projects/mr-weekly-review-prompt.md:256` carried stale doctrine calling `queue[]` "a narrative journal, not a work queue" — directly contradicted this slice's own new doctrine text in the native-tick prompt; two SSOT prompts fed to two different recurring processes had disagreed about live shared state. Both fixes verified against `SELFCHECK-OK`. | `specs/monster-realm-v2/M-loop-infrastructure.spec.md` §2 (`lp-queue`) |
| 2026-08-22 | monster-realm | **PLAN §9 reordered (operator directive, interactive session): `M-postgate-sixteenth-review-residuals` slices 16r-a/16r-c/16r-d/16r-e/16r-f/16r-g pulled forward ahead of their listed position, launchable now despite sitting textually after the wave-1-exit-blocked `M-loop-infrastructure`/`M-postgate-fifteenth-review-residuals` chain.** Root cause: six consecutive native ticks (2026-08-22T12:00Z–18:00Z) re-derived only as far as "every remaining 15r slice is `blocked:wave-2/3/4-exit`" and stood down for what would have been several days (the boundary is the next Thu 20:00 ET reset cycle) — a shallow re-check pattern-matching on the first blocked chain in PLAN §9 listing order, not a genuine full derivation; 16r's own spec declares no `blocked:` tag on those six slices and nothing in it actually depends on wave-1-exit. Annotated in-place (not decision-defaulted — direct operator instruction): `PLAN.md` gained an operator-directive blockquote between the 15r and 16r entries; `M-postgate-sixteenth-review-residuals.spec.md` §6 gained a matching runner note. Launched this tick: 16r-a (doc-truth sweep) + 16r-c (changelog-freshness gate blind spot), verified file-disjoint via `mr-disjoint` (SAFE, no overlap). Queued as fast-path hints for subsequent ticks: 16r-d, 16r-e, 16r-f, 16r-g. Left in original position: `M-loop-infrastructure` Wave-2+, every `blocked:wave-*-exit` 15r/13r-c-2 slice, and `16r-b` (keeps its own SERIAL-REQUIRED note against the still-blocked 15r scanner-migration family) and `16r-h` (`after: 16r-c`). | `specs/monster-realm-v2/PLAN.md` (2026-08-22 operator-directive note), `specs/monster-realm-v2/M-postgate-sixteenth-review-residuals.spec.md` §6 |
| 2026-08-22 | harness | **lp-gates DELIVERED (attended): the acceptance ledger + a residual sink WITH A DRAIN, mined from the `unlazy` v2 skill.** Diagnosis first: the loop's slices execute well (15/15 merged, 14/15 first-attempt) and then honestly announce their remainder into prose nothing consumes — 61% of a 46-slice sample exists because an earlier slice left DECLARED work undone, 12 of 18 continuation ids never got a spec section, 13.1-day mean disclosure-to-remediation latency, 49.5% of slice spend in remediation milestones. So the mined idea is NOT unlazy's anti-laziness framing (this loop is not lazy) but its two enforcement primitives: acceptance in a FILE with runnable checks, and an honest machine-readable surrender line. New `mr-gates` seeds `$MEM/gates/<slice>.gates.md` from the slice's spec `SHALL` criteria (two-shape parser: 131/132 sections, 522 criteria, 242 reusing the spec's own ids), hashes them in `Seed:` so the graded party cannot reword or drop one, and `verify` re-executes every CHECK through `guard-bash.mjs` comparing the deciding line. **The load-bearing anti-gaming rule**: a seeded gate's CHECK must invoke a real runner — CHECKs are authored before tests exist, so the only thing writable then is a static `grep`, which passes every other lint and certifies nothing. `DEFER:` is the only other legal exit and emits a residual promoted into a real `### <id>` section in the new standing `M-residual-backlog.spec.md`; aging (T1=3d/T2=14d, constants defined ONCE in the tool and read from it) drives it onto the existing `queue[]` fast path — **no new queue**. REJECTED from unlazy: the `tree N` depth dial (its own measurements disowned the arithmetic; depth>2 is unexecutable here under depth=1, and per-criterion leaves would re-merge the tester/specialist/verifier role split), self-authored self-checked gates (a reward-hack surface in an unattended loop), a general report-number auditor (lp-04: a detector that fires on everything carries no information), and splitting the SSOT prompt (spec §5: out of scope, 0.087% of spend). **Posture split per R13:** the `mr-audit` acceptance block ships ADVISORY and `gates-stop.mjs` ships OBSERVE-ONLY; arming is `lp-gates-arm` against a pre-registered 1-to-4-of-10 FLAGGED band. **Divergence from lp-registry, recorded not silent:** `mr-ready` will NOT read residual rows as ready-set input — the invariant is *nothing is launchable until it is a spec section*, so intake drains INTO the corpus and one derivation orders everything. Empirically established before designing: Stop hooks DO fire in headless `claude -p`, hooks DO inherit env, `stop_hook_active` flips true after the first block (so bailing on it — as the existing advisory hook does — would permit exactly one block), multiple hooks compose, a throwing hook is isolated, and hooks are NOT inherited across the ancestor walk (hence USER-level registration + an adoption-drift guard mirroring lp-09's B2). Two bugs fixed en route: `mr-spawn` never cleared `/tmp/mr_pass_<slice>.done` (+`.done.recorded`; removing either alone is worse than neither) and `mr-unlock` now reaps them with the lock. One of my own fixtures was caught VACUOUS by injection testing and repaired. | `memory/projects/monster-realm-lp-gates-plan.md`, `specs/monster-realm-v2/M-loop-infrastructure.spec.md` §2.6 + `lp-gates-arm`, `specs/monster-realm-v2/M-residual-backlog.spec.md` |
| 2026-08-23 | harness | **Two operator-directed corrections.** (1) **`guard-tester-write.mjs` carve-out for slice worktrees.** The guard blocked all of `.claude/`, which includes `<repo>/.claude/worktrees/<slice>` — where every slice's work happens — so tester staged every test file through `/tmp` on every slice (operator-reported; same finding as 16r-c, unfixed since). Worktrees are now writable EXCEPT the worktree's own `.claude/`: editing that does not disarm the running session (hooks load from `CLAUDE_PROJECT_DIR`) but it is what the PR merges, so it would disarm every future session — the same exploit on a delay. Also hardened non-existent-leaf path resolution, which became an escape once a subtree was allowed. 23 fixtures incl. a real symlink sandbox; 5 injected defects RED; both repo copies synced. **(1b) `mr-selfcheck` A8 could MUTATE the operator kill switch, not merely false-alarm on it.** Its "unforced" fixture built its env from `dict(os.environ)` without scrubbing `MR_FORCE`, so with `MR_FORCE=1` ambient (which `mr-supervisor-run` sets before the tick runs selfcheck) the sandboxed tick wrapper got PAST gate -1 and re-stamped the REAL hold — `mr-hold`'s MEM is a hardcoded machine singleton, so a sandboxed copy still writes production state. Reproduced by triggering it with my own diagnostic (`MR_FORCE=1 mr-selfcheck`), then proved fixed: with the scrub in place the same command leaves the flag untouched. `by=operator` survived throughout so the loop stayed held and nothing unsafe occurred, but the operator's recorded `at=` and the flag mtime — what the HOLD-AGED nag ages from — were overwritten and are NOT restored: writing to the kill switch is exactly what lp-09 doctrine forbids an agent session to do, so it is reported rather than repaired. **A selfcheck must never be able to mutate the switch it checks.** (2) **`lp-15` reframed from 'ceremony retirement' to REPAIR.** The retirement rested on '189 rows, 0 terminal'; folding the event-sourced ledger gives **91 items, 79 DISPOSED, every one with an action and a target, 76 of 79 targets resolving to a real spec file or `### <slice>` heading**. The triage WORKED — what never happened is the transition to terminal when the target merged (median DISPOSED age 27 days). Operator: *"patch the holes and keep the valuable parts"*. So `mr-feedback` keeps its ledger, doctrine, state machine, reconciler, covermap and Disposition Brief, and gains the drain, the gate-proved terminal transition and the aggregated aging alarms `lp-gates` built for residuals. **This also corrects my own analysis**: lp-gates plan §13.1 called the two intake ledgers a 'genuine duplicate pair'. They are not — a feedback item and a residual are typed records with different lifecycles; what was missing was a shared drain. Two typed doors, one drain, one line. The migration field-mapping drafted 2026-08-22 is withdrawn. | `.claude/hooks/guard-tester-write.mjs`, `specs/monster-realm-v2/M-loop-infrastructure.spec.md` (`lp-tester-tools`, `lp-15`), `memory/projects/monster-realm-lp-gates-plan.md` §13 |
