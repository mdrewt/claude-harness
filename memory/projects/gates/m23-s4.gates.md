# Gates: m23-s4
Spec: (none)#m23-s4
Seed: e3b0c44298fc1c14
Seeded: 2026-08-24T21:02:27Z
Repo: project
Scope: M23 accessibility S4 — constructed-shell wiring (M23-accessibility.spec.md §4 S4). battleView/boxView/raisingView/evolutionView share the #app mount; claimView is body-appended via its own ensureElement (shares the mechanism, not the nesting concern). Wire each to openOverlayA11y/closeOverlayA11y (or the render(vm|null) null<->non-null edge, per whichever mechanism each view already uses — the same two mechanisms S3 wired into the ten static-shell views) per M23 §2.2. Plus: add the canvas region ARIA attributes after client/src/render/world.ts:71. after: S1, S2 (both merged: S1=PR#364, S2=PR#363). NEVER pair with S3 in fan-out (both consume S1's helper surface) -- moot here since S3 already merged (PR#365); launch S4 solo. Co-located tests ride with this slice per spec §4 note (a slice deferring its own tests to S10 is mis-scoped).
Criteria: 0 seeded, 0 shall-uncaptured
Note: SPEC-SECTION-NOT-FOUND — author X* gates for this slice's acceptance.


AUTHORING NOTE — the ledger was seeded `SPEC-SECTION-NOT-FOUND / 0 criteria`. The criteria DO exist:
M23's EARS block is **§6**, not a §7.x section, and the seeder looks for §7.x (m23-s0/s1/s2/s3 all hit
this and flagged it — still worth fixing in the seeder). **X1–X5 are a 1:1 transcription of the five
S4-scoped criteria A11Y-13, A11Y-14, A11Y-16, A11Y-17 and A11Y-15** (spec §6, "S3/S4 — view wiring").
X6–X9 were authored in the PLAN phase, from spec §2.2/§2.3, ADR-0205 D1/D2/A3, the merged S3 record,
and the three plan-review lenses, for the S4-owned behaviours those five do NOT cover: delegation vs
reimplementation, the repeat-open focus steal across all four open doors, the open-after-paint
obligation S3 handed over, and the four-distinct-roots claim. **A11Y-13/14/15/16 are shared with slice
S3; the transcriptions here are SCOPED to this slice's five constructed-shell views.** A11Y-34 is NOT
in this ledger: its subjects (dialogueView, healView, questLogView) are S3's and are merged.

TOOLCHAIN NOTE — a PATH export IS required and `npm ci` must have been run in `client/`. Each CHECK
exports `PATH="$HOME/.asdf/shims:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"` into the child env itself,
so `check`/`verify` need no wrapper — but they MUST run from the slice worktree ROOT (`cmd_check`
uses `os.getcwd()`; every CHECK passes `cwd:'client'` to the child). Default PATH resolves node to
v18.19.1, the wrong runtime for this project.

GATE-SHAPE NOTE — inherited verbatim from m23-s3, including its three MEASURED false-greens.
`-t '<tag>'` filtering is DELIBERATELY NOT USED (substring-matched, so a decoy title passes; and it
marks every non-matching test in the file *pending*, which would red `numPendingTests===0`). Every
CHECK runs the WHOLE file set with `--reporter=json` and asserts `success===true` AND
`numFailedTests===0` AND `numPendingTests===0` AND `numTodoTests===0` AND `numTotalTests>0` AND exit
code 0, then that each REQUIRED tag appears in the passing `fullName` set **EXACTLY ONCE**.
  1. `it.todo('<TAG>')` satisfies `success`, `numTotalTests>0`, `numFailedTests===0` AND
     `numPendingTests===0` — vitest counts `.todo` in a SEPARATE `numTodoTests` field. Closed by
     asserting `numTodoTests===0` and by the per-tag `status==='passed'` filter.
  2. `npx vitest run <missing-file> --reporter=json` prints a well-formed report with
     `numTotalTests:0` and EXITS 0. Closed by `numTotalTests>0` plus the exactly-once tag filter.
  3. `assertionResults[].fullName` is the describe-chain + title, never the bare title. Closed by
     matching with `indexOf(tag) >= 0` over unique per-file tags, counted exactly-once.

WHAT NO GATE SHAPE CAN SEE (disclosed, not closed) — a correctly-named test with a tautological body.
`it('S4-boxView-OPEN-ARIA', () => expect(true).toBe(true))` satisfies every mechanical clause above.
A runner can verify a name and a status; it cannot verify that a body attempts the right assertion.
That residual is closed by the `tester` being a DIFFERENT agent than the implementer, by the red-team
pass that WRITES each wrong implementation and confirms it reds, and by the `verifier`'s body review —
never by the JSON. Stated so a green ledger is not read as more than it is.

DOCUMENTED RESIDUAL, PINNED BY X7 BUT NOT FIXED HERE — claimView can re-open after a manual dismiss.
`ClaimPhase` never transitions back to `'hidden'`, and `main.ts`'s `KeyC` close calls
`claimView.hide()` directly rather than through `applyClaim`, so the model still believes the overlay
is open. A later reconnect-driven `onClaimPending`/`onClaimAwaitingAccount`/`onClaimResult` calls
`render(vm)` with `vm.visible === true` while the DOM reads hidden. **Today that silently re-shows the
overlay (a pre-existing display bug); after S4 it also announces and steals focus.** The a11y layer is
behaving correctly for the DOM state it observes — the defect is upstream, in a model that cannot
represent "dismissed". Fixing it needs `claimModel.ts` (a new `ClaimEvent`) or `client/src/main.ts`
(route the `KeyC` close through `applyClaim`); **both are outside this slice's `touches:`, and
`main.ts` is reserved for S5 by spec §4.** S4 therefore PINS the composed behaviour instead of
hiding it, and flags it upward.


- [x] X1: A11Y-13 [UNIT] WHEN any of the FIVE constructed-shell overlays becomes visible THE SYSTEM SHALL set `role` and `aria-modal="true"` on its root to the values `OVERLAY_A11Y` declares for that id — asserted TOGETHER WITH `aria-label === t(OVERLAY_A11Y[id].labelKey)` in the SAME it(). Every expected value is DERIVED from OVERLAY_A11Y/t() at assert time, never a literal, so a hardcoded 'Battle' reds. Unlike S3's ten static shells (whose index.html already ships role/aria-modal as literals, making those two clauses vacuous there), these five roots are document.createElement'd bare and carry NO static ARIA — so the value oracle bites on its own here, and X6 is the independent mechanism partner.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-battleView-OPEN-ARIA','S4-boxView-OPEN-ARIA','S4-raisingView-OPEN-ARIA','S4-evolutionView-OPEN-ARIA','S4-claimView-OPEN-ARIA'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X1:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X1:TEETH-BITE
  EVIDENCE: m23-s4-X1:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=5 bad=0

- [x] X2: A11Y-14 [UNIT] WHEN any of the five becomes visible THE SYSTEM SHALL move `document.activeElement` to the element its `initialFocusSelector` resolves to, and that element SHALL be the natively-addressable anchor this slice creates. IDENTITY assertion (`activeElement === root.querySelector(sel)`), never `root.contains(activeElement)` (which passes on any decorative wrapper), after ONE REAL macrotask. Also pins the anchor itself: tagName, `tabindex` attribute VALUE === '-1' on the four `<h2>` titles, and `textContent` byte-unchanged (client/e2e/recruit.spec.ts keys on the literal 'Party & Box' heading text). The tabindex VALUE assertion is the only oracle for it — happy-dom focuses a bare <div>, so the focus move alone is green with no tabindex at all. DECLARED RESIDUAL: a green A11Y-14 here is NOT browser proof of focusability (spec section 9, nightly axe/Playwright is the compensating control).
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-battleView-ANCHOR-FOCUS','S4-boxView-ANCHOR-FOCUS','S4-raisingView-ANCHOR-FOCUS','S4-evolutionView-ANCHOR-FOCUS','S4-claimView-ANCHOR-FOCUS'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X2:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X2:TEETH-BITE
  EVIDENCE: m23-s4-X2:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=5 bad=0

- [x] X3: A11Y-16 [UNIT] WHEN one of the five becomes hidden THE SYSTEM SHALL restore focus to the element focused immediately before it became visible AND leave `role`, `aria-modal` and `aria-label` all ABSENT from the root — AND `hide()` SHALL call `closeOverlayA11y` UNGUARDED (plan D2). One it() per view proving both halves: focus an outside <button>, show(), flush, hide(), assert the three attributes are gone and focus returned; then hide() on a never-shown view still calls close and does not throw, and show/hide/hide yields exactly two close calls. The unguarded half is load-bearing: S3's red-team MEASURED that guarding hide() on six views ships 62/62 GREEN while permanently leaking a live capture listener, a pending timer and a stale return target (overlayA11y.ts's named A13 leak); closeOverlayA11y with no record is a documented pure no-op, so unguarded is the self-healing path.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-battleView-CLOSE-RESTORE-UNGUARDED','S4-boxView-CLOSE-RESTORE-UNGUARDED','S4-raisingView-CLOSE-RESTORE-UNGUARDED','S4-evolutionView-CLOSE-RESTORE-UNGUARDED','S4-claimView-CLOSE-RESTORE-UNGUARDED'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X3:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X3:TEETH-BITE
  EVIDENCE: m23-s4-X3:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=5 bad=0

- [x] X4: A11Y-17 [SCAN] THE SYSTEM SHALL set `role="application"`, `tabindex="0"` and a non-empty `aria-label` on the Pixi canvas, and SHALL NOT set any `role` on `#app`. client/src/render/world.ts is coverage-excluded (client/vite.config.ts) and cannot be executed under vitest without dragging PixiJS in, so spec section 9.3 makes this scan-verified — and requires the scan to FAIL LOUD if its anchor line is not found rather than pass vacuously. The scan is comment-stripped (a decoy in prose must not satisfy it), anti-vacuity-pinned (`export class WorldRenderer` must survive stripping), and — closing a red-team CRITICAL — BRACE-SCOPED to `init()`'s matched body, because 'the three writes appear somewhere after the anchor' passes an implementation that leaves init() untouched and parks the setAttribute calls in a private method nothing calls (PoC'd green while the real canvas gets zero ARIA). It also asserts every `t('...')` key extracted from the stripped source resolves in the REAL imported a11yCopy with an extraction count >= 1 — one assertion doing four jobs: the boot-crash guard (t() is deliberately untyped and throws on a miss, so a missing key kills the client with just ci green), the a11y.world.* orphan obligation the S0 catalog assigns to the slice owning the consumer, spec section 2.8's no-raw-literal tooth (a raw literal yields zero t() matches and reds the count), and key validity.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/render/world.test.ts';var NEED=['S4-WORLD-CANVAS-REGION'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X4:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X4:TEETH-BITE
  EVIDENCE: m23-s4-X4:TEETH-BITE total=1 pass=1 fail=0 pend=0 todo=0 need=1 bad=0

- [x] X5: A11Y-15 [SCAN] IF any of the FIVE S4 view files contains a literal `.focus(` call THEN THE SYSTEM SHALL fail — the single deferred focus lives only in overlayA11y.ts. S3's merged scan enumerates only ITS ten files, so the five constructed-shell views are uncovered today. Rather than build a second ~250-line comment/string stripper (this repo has been bitten twice by hand-rolled strippers — once by a regex literal containing a quote), this gate adds a distinct it() to client/src/ui/renameView.test.ts that REUSES that file's already-hardened `s3Strip`/`s3CountFocusCalls` and its comments-only-vs-string-stripped divergence tooth over the five S4 files. S3's own it() is left byte-identical. PROVISIONAL by design, exactly like S3's: superseded by evals/overlay-a11y-manifest.eval.mjs [A11Y-15] when S10 lands.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/renameView.test.ts';var NEED=['S4-VIEW-LOCAL-FOCUS-5'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X5:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X5:TEETH-BITE
  EVIDENCE: m23-s4-X5:TEETH-BITE total=36 pass=36 fail=0 pend=0 todo=0 need=1 bad=0

- [x] X6: [S4-MECH] THE SYSTEM SHALL delegate to the S1 helpers rather than reimplement them: each view SHALL call `openOverlayA11y` exactly once with ITS OWN OverlayId and its own root, and `closeOverlayA11y` with its own id and a literal `null` fallbackFocus (ADR-0205 A3 — no S4 view holds a canvas handle). The MECHANISM oracle, independent of X1's VALUE oracle: S3's red-team MEASURED that a view hand-writing role/aria-modal/aria-label with the correct copied literal passes every value assertion while never calling the helper — no focus trap, no return-focus record, no timer. Only a spy call assertion reds it, and the id argument simultaneously kills the copy-pasted-wrong-id implementation. Uses vi.mock('./overlayA11y', { spy: true }), which records calls AND calls through, so X1/X2/X3's real-effect assertions still hold in the same file.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-battleView-HELPER-CALLED','S4-boxView-HELPER-CALLED','S4-raisingView-HELPER-CALLED','S4-evolutionView-HELPER-CALLED','S4-claimView-HELPER-CALLED'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X6:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X6:TEETH-BITE
  EVIDENCE: m23-s4-X6:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=5 bad=0

- [x] X7: [S4-EDGE] WHEN an open path runs against an ALREADY-visible overlay THE SYSTEM SHALL NOT re-open it — no second openOverlayA11y, and in particular no second deferred focus. A re-open tears down and re-schedules the deferred focus, dragging focus off whatever control the player Tabbed to; invisible to every attribute assertion, since a re-open rewrites the same values. Proven by focusing a sentinel INSIDE the root, repeating the open, flushing a macrotask, and asserting the sentinel still holds focus. Covers all four open doors this slice wires: show() on all five; toggle() on the four views that expose one (no production caller today, so a toggle() that duplicates the helper call instead of delegating ships green and traps whoever wires it next); battleView.refresh(vm)/refresh(null), which main.ts drives every store batch; and claimView's THREE doors — the real production sequence renderClaim -> show() -> renderClaim means the true open edge fires inside render(), one statement BEFORE show() is even called, so both must be guarded and the close arm of render() must be guarded while hide() must not. S4-claimView-REOPEN-AFTER-HIDE additionally PINS the documented residual (see the residual note below): ClaimPhase never returns to 'hidden' and main.ts's KeyC close calls hide() directly, so a later reconnect-driven render(visible:true) re-opens exactly once — examined, not silent.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-battleView-REPEAT-NO-REOPEN','S4-boxView-REPEAT-NO-REOPEN','S4-raisingView-REPEAT-NO-REOPEN','S4-evolutionView-REPEAT-NO-REOPEN','S4-claimView-REPEAT-NO-REOPEN','S4-boxView-TOGGLE','S4-raisingView-TOGGLE','S4-evolutionView-TOGGLE','S4-claimView-TOGGLE','S4-battleView-REFRESH-EDGES','S4-claimView-THREE-DOORS','S4-claimView-REOPEN-AFTER-HIDE'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X7:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X7:TEETH-BITE
  EVIDENCE: m23-s4-X7:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=12 bad=0

- [x] X8: [S4-D7] THE SYSTEM SHALL invoke `openOverlayA11y` only AFTER the root is painted — at the instant of the call, root.style.display SHALL be neither 'none' nor ''. In a real browser `.focus()` on a display:none node is a silent no-op, so an open-before-paint implementation ships an overlay that announces but never receives focus; S3 measured that shape at 62/62 GREEN and handed the obligation to S4 explicitly. THE MECHANISM IS PINNED, not left to the author: a post-hoc read of mock.calls[0][1].style.display is PROVABLY VACUOUS (red-team PoC: it passes the wrong impl and the correct impl identically, because JS is synchronous and both statements have run by assertion time). The capture happens SYNCHRONOUSLY at invocation — by intercepting the first attribute write (openOverlayA11y sets `role` first) or via mockImplementationOnce delegating to getMockImplementation(), NEVER via vi.importActual, which returns a SECOND module instance with its own OPEN_OVERLAYS map and silently breaks every close/restore assertion in the file. Applies to claimView's render() door specifically, not only show(). Bite-proved by the tester writing the open-before-paint implementation and confirming RED.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-battleView-OPEN-LAST','S4-boxView-OPEN-LAST','S4-raisingView-OPEN-LAST','S4-evolutionView-OPEN-LAST','S4-claimView-OPEN-LAST'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X8:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X8:TEETH-BITE
  EVIDENCE: m23-s4-X8:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=5 bad=0

- [x] X9: [S4-F1] WHERE two `#app`-mounted overlays are open at once THE SYSTEM SHALL leave each one's ARIA claim and focus record intact — S4 SHALL NOT implement close-before-open. overlayA11y.ts's cross-slice contract (a) asserts the four views 'share ONE #app-mounted root' and that S4 must therefore close-before-open; that is a MISSTATEMENT of the code, falsified by reading it: each view document.createElement's its OWN <div> and appends it into the shared #app MOUNT, so there are four distinct roots, four distinct OverlayIds, four distinct OPEN_OVERLAYS records and four distinct capture listeners. Acting on the stale comment would close an overlay the player still has open. This is the single most load-bearing correctness claim in the slice and every other gate here is single-view-scoped, so it gets its own cross-view test (in boxView.test.ts, a sibling of a declared file): open boxView, construct and open battleView, assert boxView's root STILL carries role/aria-modal/aria-label, and that closing battleView leaves them intact.
  CHECK: node -e "var cp=require('child_process');var Q=String.fromCharCode(34);var env=Object.assign({},process.env,{PATH:process.env.HOME+'/.asdf/shims:'+process.env.HOME+'/.cargo/bin:'+process.env.HOME+'/.local/bin:'+process.env.PATH});var F='src/ui/battleView.test.ts src/ui/boxView.test.ts src/ui/raisingView.test.ts src/ui/evolutionView.test.ts src/ui/claimView.test.ts';var NEED=['S4-CROSS-VIEW-DISTINCT-ROOTS'];var out='';var code=0;try{out=cp.execSync('npx vitest run '+F+' --reporter=json',{cwd:'client',env:env,maxBuffer:134217728,stdio:['ignore','pipe','pipe']}).toString();}catch(e){out=String(e.stdout===undefined?'':e.stdout);code=1;}var i=out.indexOf('{'+Q+'numTotalTestSuites');if(i<0){console.error('NO-JSON-REPORT '+out.slice(-2000));process.exit(1);}var j=JSON.parse(out.slice(i));var names=[];j.testResults.forEach(function(t){t.assertionResults.forEach(function(a){if(a.status==='passed')names.push(a.fullName);});});var bad=NEED.filter(function(n){return names.filter(function(x){return x.indexOf(n)>=0;}).length!==1;});var clean=code===0&&j.success===true&&j.numFailedTests===0&&j.numPendingTests===0&&j.numTodoTests===0&&j.numTotalTests>0;var g=clean&&bad.length===0;console.log('m23-s4-X9:'+(g?'TEETH-BITE':'TEETH-MISSING')+' total='+j.numTotalTests+' pass='+j.numPassedTests+' fail='+j.numFailedTests+' pend='+j.numPendingTests+' todo='+j.numTodoTests+' need='+NEED.length+' bad='+bad.length);if(!g){console.error(JSON.stringify(bad));process.exit(1);}"
  EXPECT: m23-s4-X9:TEETH-BITE
  EVIDENCE: m23-s4-X9:TEETH-BITE total=119 pass=119 fail=0 pend=0 todo=0 need=1 bad=0
