# EG1 build plan — essence-graph schema & core-type freeze (planner output, 2026-08-03)

Slice: EG1 (M-evolution-essence-graph.spec.md EG1-1..EG1-12) · Branch: `feat/eg1-essence-schema-freeze` · Worktree: `projects/monster-realm/.claude/worktrees/EG1` · ADR: 0174 · CONTENT_VERSION: 17→18.

## Corrections to orchestrator dossier (planner-verified)
- C1: CONTENT_VERSION already 17 (uxd2/ADR-0161) → EG1 bumps to **18**.
- C2: pub_from_monster tier sourcing = **4 fresh / 9 copy-forward** (spec's 3/10 prose is wrong; total 13 correct). Fresh: taming.rs:165, movement.rs:109 (join_game starter), evolution.rs:188 (evolve), content.rs:277 (sync). Copy-forward: battle.rs:994/1237, monster_mgmt.rs:44/87 (both non-creation), pvp.rs:557, raising.rs:105/251/372, trading.rs:651 (**needs new monster_pub read** in confirm_trade loop).
- C3: ADR path = `docs/adr/0174-essence-graph-schema-and-type-freeze.md` (4-digit).
- C4: battle-schema-snapshot already snapshots ALL tables → `evals/baselines/table-schemas.json` regen required; NEW eval only pins index+edge_id+Vec<EssenceRequirementRow> type.
- C5: undeclared hard blockers on deleting evolutions.ron/fusion.ron: `evals/pt-d2-roster-wave-2.eval.mjs` (reads evolutions.ron), `game-core/tests/pt_d1_roster.rs:734-736`, `game-core/tests/item_evolution_content.rs` (T-B* tests on load_evolutions/load_fusion), `server-module/src/content.rs:747-754` (fusion_registry test), `game-core/src/content.rs:3187,3198` inline parse tests.
- C6: `movement.rs:90-104` hand-builds a `game_core::Species` literal → compiler-forced tier edit.
- C7: sdk-enum-exhaustiveness does NOT force a TrustTier client mapping (registry-driven iteration).
- C8: composite unique index unsupported (schema.rs:329-331 precedent) → D1: `#[index(btree)]` on from_species; R1 via gate + sync_content duplicate check.
- ADR-0173 D5 (measured, live 2.6.0): nested SpacetimeType widening REJECTED; only tail-append+#[default] works → **EssenceRequirementRow shape is one-shot** (top risk R-2).
- `just smoke-republish` (scripts/smoke-republish.sh, justfile:178) = live migration gate; migration-smoke-test eval is static-only.

## Ordered steps S0–S9
- S0 red-phase: tester files + type/signature stubs (unimplemented!()) in one commit → tests compile & fail RED (stub-first, D10). Compile-fail-as-red only for: 13-call-site assertion, Affinity::index exhaustive match, Species{tier} fixture sweep.
- S1 game-core types/content: Affinity::index()+ALL (types.rs, exhaustive match); MonsterInstance −bond +essence:[u32;8] +trust_favorable_count +trust_unfavorable_count +quality_time_ticks_total; Bond type STAYS. content.rs: delete EvolutionTrigger/RawEvolutionTrigger/EvolutionCondition/SpeciesEvolutions/FusionRecipe/FUSION_RON/EVOLUTIONS_RON/load_fusion/load_evolutions/parse_*/validate_evolution_fusion (+inline tests); add EssenceRequirement, EvolutionPath (edge_id/from/to/min_level:Level per D4/essence/3 Option gates), TrustTier (Ord asc, cfg_attr SpacetimeType), Species.tier u8 serde(default), ItemDef.essence_affinity+essence_amount serde(default); load_evolution_paths (glob); validate_evolution_paths (R1-R12, R12 in-set uniqueness only); lib.rs + evolution/mod.rs re-exports.
- S2 game-core rules: eligibility.rs delete fusion_eligible/FusionError/MIN_FUSION_*/resolve_evolution/evolves_to; add path_satisfied (5 gates AND), eligible_evolution_paths (filter from_species + path_satisfied → Vec<usize>), trust_tier_of (K=10 Bayesian, u64 cross-multiplication, bands [30,45,60,80]%, D5), quality_time_tier_of (ticks bands [10,50,150,400], D6), nutrition_pct_of(&EVs) delegating to total-based inner (D3). transform.rs: delete fuse/FUSION_EFFICIENCY/LEVEL_RETENTION_FLOOR/scale_u32/avg_u32 + tests; evolve() zeroes essence internally (D2). rolls.rs constructor. Species{tier:0} fixture sweep (taming/m8a, monster/battle_redteam, monster/m8d, combat/m14c, rolls, content.rs, evolution tests).
- S3 content files: delete evolutions.ron+fusion.ron; create content/evolution_paths/000-core.ron `[]`; build.rs REGISTRIES += evolution_paths (verify empty-part guard, R-9); C5 fallout (delete item_evolution_content T-B*, pt_d1_roster evolutions arm, server content.rs:747-754 test).
- S4 schema.rs: Monster +16 (append after evolves_to, #[default(0)]; window_start_ms/last_essence_train_at_ms i64), MonsterPub +12 (tier u8, 8×essence u32, trust_tier #[default(TrustTier::Neutral)], quality_time_tier u8, nutrition_pct u8), SpeciesRow +tier u8; EssenceRequirementRow (derive SpacetimeType Clone Debug PartialEq Eq); evolution_path table (EvolutionPathRow: path_id u64 PK auto_inc, edge_id u32, from_species #[index(btree)], to_species, min_level u8, essence Vec<EssenceRequirementRow>, min_trust_tier Option<TrustTier>, min_quality_time_tier Option<u8>, min_nutrition_pct Option<u8>). **Run `just smoke-republish` immediately.**
- S5 marshal.rs: monster_from_instance/monster_to_instance both ways (server-only cols→0); species_from_row +tier; pub_from_monster(m,tier)→computes trust_tier/qt_tier/nutrition_pct via game-core helpers (infallible); evolution_path_from_row → Result<EvolutionPath>.
- S6 server call sites: 13 sites (C2 split); evolution.rs: delete fuse reducer/reject_if_not_fusable/FuseEffect/find_fusion_recipe/compute_evolves_to; rewrite evolve(ctx,monster_id:u64,to_species:u32) — guards unchanged, targeted index lookup, path_satisfied gate, game_core::evolve, MonsterPub.tier fresh target lookup, dual-write, NO apply_evolution/check_and_evolve (EG2). compute_evolves_to callers dropped (raising.rs:104, battle.rs:~1232, content.rs:373, content_cache EVOLUTIONS→EVOLUTION_PATHS/cached_evolution_paths). sync_content: drop fusion seeding+validate_evolution_fusion, add validate_evolution_paths BEFORE writes (M2), seed evolution_path clear-and-reinsert, clear stale fusion rows (D8). lib.rs CONTENT_VERSION=18.
- S7 evals: content-integrity rewrite (R1-R12 mirror + per-rule bad fixtures); reducer-security (drop fuse, 2-arg evolve); no-idle-accrual GROWTH_WRITERS −'fuse' (GROWTH_FIELDS unchanged — EG5-3); pt-d2-roster-wave-2 re-anchor; baselines: table-schemas.json regen (dedicated commit, R-4), spacetime-types.json +TrustTier+EssenceRequirementRow; bsatn-compat-smoke += 28-col append+default checks + EssenceRequirementRow derive; NEW evolution-path-schema-pin.eval.mjs (index+edge_id+Vec type + teeth).
- S8 bindings: `just bindings`; fuse_reducer.ts vanishes, fusion_table.ts survives; client compile-compat ONLY: main.ts:2108 evolve({monsterId,toSpecies}), onFuse stub (~:2110), grep conn.reducers.fuse; store/boxModel/tradeProposeModel UNTOUCHED (bond stays in Migration A).
- S9 docs: ADR-0174 (+ digest regen), ARCHITECTURE.md:193-194 registry-table 2-line correction only (EG5-7 owns prose).

## Decisions D1–D10
D1 btree(from_species) only; D2 essence-zero inside game_core::evolve; D3 nutrition total-based inner fn; D4 EvolutionPath.min_level = Level (row u8, from_row Result); D5 TRUST_K=10, bands [30,45,60,80] cross-mult u64; D6 QT bands [10,50,150,400]; D7 Affinity::index+ALL in types.rs; D8 clear stale fusion rows in v18 sync; D9 evolve keeps monster_id u64 + to_species u32; D10 stub-first red.

## Test plan skeleton
(See PR/tests; full matrix in planner output.) game-core: path_satisfied gate tests incl. inclusive boundaries, affinity-indexing transposition kill, AND-combination per-gate, monotone property; trust teeth (1,0)→Neutral, (0,0)→Neutral, reachability 30fav→Devoted/14unfav→Hostile, band-constant pin, overflow property (u32::MAX); qt/nutrition boundary tests; eligible_evolution_paths both-indices + species filter + index-addressing + oracle-vs-path_satisfied; evolve zeroes essence/preserves trust+qt; R1-R12 one biting bad fixture each + empty-set positive + count-pin meta-test. Server: evolve guard/reject suite vs empty content, reject-names-failing-requirement, dual-write, fresh-tier, shared-predicate source-scan (0x22-safe), marshal 28-field round-trip field-by-field distinctive values, 13-call-site count source-scan. Evals with proof-of-teeth each.

## Anti-patterns (enforced)
gate logic in reducer; floats in game-core; mid-struct insert / missing #[default]; widening EssenceRequirementRow later; editing run.mjs; dynamic RegExp; unpaired slash-asterisk or raw quote in comments of scanned files (0x22); weakening tests; touching guards.rs; doing EG2 work; combining Migration A+B; deleting Fusion table/fusion_table.ts; extending GROWTH_FIELDS early.

## Risks
R-1 publish rejects migration (HIGH → smoke-republish right after S4); R-2 EssenceRequirementRow one-shot (HIGH → ADR review pre-S4); R-3 undeclared blast radius (HIGH → S3 sweep + repo-wide re-grep); R-4 baseline churn (dedicated commit); R-5 client fallout (typecheck right after bindings); R-6 unreachable trust bands (reachability tests in S0); R-9 build.rs empty-part guard (verify at S3 — red-team verified: guard is file-count, `[]` in one file passes).

## AMENDMENTS after 3-lens plan review (reviewer APPROVED-WITH-FIXES · red-team SURVIVES-WITH-AMENDMENTS · simplify TRIM(6)) — 2026-08-03
A1 (**CRITICAL**, all 3 lenses): EG1-3 tier:1 authoring was MISSING. Resolution: (i) author explicit `tier: 1` for species 4,5,6,9,10,22,23,30,31 in game-core/content/species/{010-derived,020-playtest-wave1,051-wave2-derived,060-item-evo-derived}.ron (new S3b step; content-version bump already planned); (ii) EG1-3 proof-of-teeth test: fixture asserting all 9 ids carry an EXPLICIT tier entry in the RON source (not serde default); (iii) **R10 gets an explicit empty-set carve-out** (`if paths.is_empty() { skip R10 }`, commented, + teeth: non-empty set with an orphan tier>0 species → reject) — required by EG1-10's own "runnable against an empty content set"; without it sync_content deadlocks + fresh-DB init panics during the EG1→EG3 window (red-team #1). ADR-0174 documents the carve-out.
A2 (red-team #2): TWO more evals hardcode `fuse`: evals/battle-reducer-security.eval.mjs (CHAIN_GUARD_REDUCERS 'fuse' entry, ~:1592) and evals/trade-escrow-guards.eval.mjs (GUARD_SITES 'fuse' entry, ~:155) — drop those entries in the SAME commit as the reducer deletion; add both files to touches-delta.
A3 (red-team #3): the 9 copy-forward sites MUST fail loud (or follow the site's existing missing-row convention) on a missing monster_pub row — NEVER `.unwrap_or(0)`/fabricated tier 0. Tester adds coverage (shared shape ok).
A4 (red-team #4): R1 has NO DB-level backstop (composite unique unsupported) — spec EG1-4's "doubling as DB-level enforcement" claim is void; enforcement = Rust gate + sync_content duplicate-pair check (tested via bad fixture). Named deviation in ADR-0174.
A5 (red-team #5+#6): ADR-0174 records TWO presumptive one-shot freezes: EssenceRequirementRow field set (AND-only semantics; OR-groups foreclosed) AND TrustTier's 5-variant set (enum variant append presumed rejected by BSATN automigration, same D5 mechanism — untested).
A6 (red-team #7+#8): trust boundary fixtures at ALL four exact ties: (5,25)→30% Wary edge, (8,12)→45% Neutral edge, (5,0)→60% Friendly edge, (30,0)→80% Devoted edge; overflow fixture must overflow the SUM (fav=u32::MAX, unfav=1) — widen to u64 BEFORE adding.
A7 (red-team #9): interim regression accepted + documented (ADR/PR): live Evolve button is a guaranteed-reject no-op from EG1 merge until EG3 content lands (evolution intentionally dark; fusion permanently removed).
A8 (simplify #1): content-integrity eval = transitional NEUTRALIZATION only (delete dead parsers, re-point sync-wiring check at validate_evolution_paths, stay green on empty registry); full R1-R12 JS mirror stays EG5-1's. Rust R1-R12 tests unaffected (EG1-10 fully delivered in Rust).
A9 (simplify #2): pt-d2-roster-wave-2 → minimal RETIRE of evolutions.ron checkers (delete parseEvolutionBlocks/orphan/BST-monotonic arms + their teeth; keep species/sprite checks). Handoff note to EG5: BST-monotonicity has no R1-R12 successor — propose as general rule if still wanted.
A10 (simplify #3): new eval file pins the INDEX only (evolution-path-index-pin); edge_id + Vec<EssenceRequirementRow> column type are already frozen by the table-schemas.json regen; EssenceRequirementRow internals by spacetime-types.json. EG1-11's pin is distributed across the suite — documented in ADR/PR.
A11 (simplify #4-#6, advice to tester): skip the 13-site count source-scan (compiler + monster-dual-write eval cover it), skip the R-rule meta-count pin, skip the separate band-constant pin (exact-tie boundary tests ARE the pin).
A12 (simplify D3 caveat): the total-based nutrition helper must be `pub` (cross-crate: server-module calls it), not `pub(crate)`.
A13 (reviewer minors): implementer brief restates the literal 16/12 column lists in order; evolution_path table is `public`; content.rs C5 cite is :749-756.

## AMENDMENTS after test review (reviewer STRONG · red-team GAMEABLE→patched) — 2026-08-03
A14 (relocation): `unmet_requirement(instance, path) -> Option<String>` moves to game-core eligibility.rs (ADR-0174 D4b): SSOT for gate messages (EG4 panel ports it) + makes the whole-file gate-needle ban on server evolution.rs sound. Reducer: `if !path_satisfied { Err(unmet_requirement(..).unwrap_or_else(default)) }`.
A15 (scan tightening): delegation scan requires load-bearing `if !((game_core::)?)path_satisfied(` in evolve body; gate-needle ban covers the ENTIRE production region of evolution.rs (pre-#[cfg(test)]), not just evolve's body.
A16 (oracle proptests): trust/qt/nutrition helpers each get a reference-recomputation proptest over wide domains (kills lookup-table gaming).
A17 (A3 sibling check): `MonsterPub {` struct-literals are banned outside marshal.rs across the 9 scanned files (+teeth).
A18 (from reviewer): transform.rs evolve() doc already claims D2 essence-zeroing but body doesn't yet — implementer lands the zeroing (test evolve_zeroes_every_essence_pool is the red pin); do not ship the doc/body mismatch.
A19: second-path fixtures for unmet_requirement (different thresholds kill hardcoded-canonical-message implementations).
