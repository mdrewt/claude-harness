# EG5 plan memo — eval-gate migration + docs/ADR tail + Migration B

Branch `feat/eg5-eval-gates-and-migration-b`, worktree `.claude/worktrees/EG5`, base master `7398266` (EG1–4 merged). ADR reserved: **0177**. HARD tier (schema.rs live-table migration). One PR (right-sizing confirmed; fallback seam = EG5-1 alone could split off, never split tasks 4–9).

## Ground truth (verified live 2026-08-07)

- Master CI green (run 31143323283), no open PRs, no sibling worktrees → touches-delta per EG1/EG2/EG4 precedent, no hidden-dependency STOP needed.
- Official SpacetimeDB 2.6 docs: column/table removal is **always forbidden** in automatic migration. Live-DB mechanism for Migration B = `spacetime publish --delete-data` (dev-sanctioned) or new-table incremental pattern. **No CI/nightly/e2e path exercises master→branch automigration** (smoke-republish.sh publishes branch module fresh; e2e uses --delete-data; migration-smoke-test.eval.mjs is static-only). ADR-0177 must carry the deployment runbook.
- EG2 already did most of EG5-2/EG5-3's headline work: E1–E5 already rewritten against evolve(monster_id, to_species); GROWTH_WRITERS already correct (fuse gone, essence_train/consume_crystalized_essence/accrue_quality_time present); GROWTH_FIELDS already has all 16 EG1 Monster columns. Remaining: EG5-2 = ADD essence_train/consume invariants; EG5-3 = GROWTH_FIELDS −bond +4 MonsterPub fields + re-point 5 bond-exemplar teeth fixtures.
- evolution-fusion-content-integrity.eval.mjs is an EG1 transitional 2-check file (G1 sync-gate, G2 RON-Vec parse); full R1–R12 JS mirror is EG5-1's to build. EG3's game-core/tests/eg3_evolution_graph.rs (T1–T11) gates via the Rust loader — the eval is the independent textual lens + the ONLY home of edge_id append-only. Don't duplicate T2's field pin or T11.
- edge_id append-only: MIRROR append-only-string-ids.eval.mjs precedent (local parser + local masking-comment refusal + own baseline). `\bid:` does NOT match `edge_id:`; append-only-ids.eval.mjs is ADR-0173-pinned byte-for-byte by pt_d1_roster.rs/pt_d3_tuning.rs — DO NOT EDIT IT. New baseline `evals/baselines/evolution-path-edge-ids.json` key "evolution_paths" value [1..10], hand-derived then cross-checked.
- Rename SAFE: run.mjs globs *.eval.mjs; nothing enumerates the filename; eval's own name string says `evolution-content-integrity` since EG1. Rename to `evals/evolution-content-integrity.eval.mjs`. Do NOT touch `game-core/content/species/060-item-evo-derived.ron:22` (content-hash trap → CONTENT_VERSION bump for a comment byte).
- Residual bond/evolves_to/fusion sites (all commented "removal is Migration B"): raising.rs:3–9,30–39,42–74,76–80,105–113 (evaluate_care → cooldown-only `(last_care_at_ms, now) -> Result<(), String>`); marshal.rs:65–68,91,231,240,258; content.rs:14,260–267,364–365,404–406; schema.rs:219,249–254,320,330–332,432–445.
- bsatn-compat-smoke.eval.mjs REAL scan anchors on evolves_to at :1618/:1622 → move anchors: Monster→`last_care_at_ms` (schema.rs:248), MonsterPub→`party_slot` (:329). H-1…H-5 + C-5 teeth are self-contained synthetic strings — leave them (fictional anchor OK), one-line note.
- battle-schema-snapshot is bidirectional exact-match over evals/baselines/table-schemas.json → tester pre-edits baseline (drop monster.bond/evolves_to, monster_pub.bond/evolves_to, whole "fusion" entry) = **the red-first oracle for Migration B**. gate-teeth TOOTH 1/2 derive from the baseline (follows automatically).
- spacetime-type-snapshot unaffected (keys on #[derive(SpacetimeType)]; Fusion is a table struct). monster-dual-write/monster-privacy/zoned-schema/evolution-path-index-pin unaffected. pvp_tests.rs:57 + m14_5d_1a_tests.rs:34 assert table PRESENCE only — safe.
- EG2-9 companion ALREADY landed (evolution_tests.rs:2372+ `eg2_9_no_scheduled_reducer_body_calls_growth_triggers`, one-hop, concat!-idiom). EG5-3 adds only a header cross-reference in the eval.
- Knowledge regen: `just knowledge` (scripts/okf-export.mjs → docs/knowledge); **regen never deletes** → must `git rm docs/knowledge/tables/fusion.md` by hand (top-ranked omission risk; caught only by DRIFT-extra). 8 knowledge files move.
- game_core::Bond/apply_care/CareError/CARE_BOND_AMOUNT become server-unused → LEAVE in game-core (lib crate, pub = reachable root, no gate fires; keeps mutate-core out of this PR). Named follow-up in ADR-0177.
- CONTENT_VERSION: no bump (no content bytes change). vite.config.ts: do not touch (exact-set coverage guard).
- adr-digest.mjs errors if Status: Superseded lacks Superseded-by, and rejects dangling Supersedes targets → flip status + backlink in same edit on 0060/0061/0062/0063/0064/0147/0149; regen DIGEST via `just adr-digest`. README.md untouched (supervisor), CHANGELOG untouched (git-cliff).

## Ordered tasks (owner · red→green)

1. **tester** EG5-1: rewrite+rename content eval → R1–R12 JS mirror + edge_id ledger + keep G1/G2. GREEN on arrival vs EG3 content; teeth = tamper fixture + GOOD control per rule, masking-comment refusals, absence-is-fail edge count. String.indexOf/literal-regex only.
2. **tester** EG5-2: add essence_train/consume invariants S1–S6 (S2 via NEW narrow `is_in_ongoing_battle(` checker — do NOT widen checkBattleGuard; local parameterised checkDecisionBeforeConsume; item-escrow block for consume). GREEN on arrival; BAD fixture per invariant.
3. **tester** EG5-3: GROWTH_FIELDS −bond +tier/trust_tier/quality_time_tier/nutrition_pct; re-point 5 bond fixtures (BAD_SCHEDULED_INLINE_BOND :582, BAD_SCHEDULED_COMPOUND_BOND :604, GOOD_INTENT_WRITER :697 → post-B care shape, GOOD_COMPARISON_NOT_WRITE :721, tooth-7 :844); NEW BAD fixture writing `.trust_tier =` in scheduled reducer (anti-vacuous); header 30→33; EG2-9 cross-ref.
4. **tester** pre-edit evals/baselines/table-schemas.json (RED until task 6).
5. **tester** new Rust tooth `eg5_6_schema_rs_declares_no_bond_evolves_to_or_fusion_table` in evolution_tests.rs beside eg1_9 (concat! idiom; RED until task 6).
6. **specialist** Migration B: schema.rs removals + compiler-forced companions (marshal/raising/content + 4 _tests.rs; delete content_tests.rs:360–380 test + raising_tests.rs bond-arithmetic family, keep cooldown tests w/ 2-arg call).
7. **specialist** bsatn anchor move (:1618/:1622 + prose :41/:431/:451/:1678).
8. **specialist** `just gen` bindings regen (fusion_table.ts deleted; index/monster_pub_table/types shrink).
9. **specialist** `just knowledge` + `git rm docs/knowledge/tables/fusion.md` + knowledge-check.
10. **doc** ADR-0177 (13 topics: 9 spec + 3 EG4 pattern decisions [pathId-keyed store / TS comparison-predicate port / roster-badge zero-round-trip] + Migration B runbook + game-core Bond retirement follow-up); flip 7 ADRs Superseded+backlink; `just adr-digest`.
11. **doc** ARCHITECTURE.md: :145, :193 (stale "empty until EG3"), :310, :555, :612, :614–615, :680–687 section rewrite (687 client bullet stays verbatim). History log lines 816/828–847/1128 untouched.
12. **verifier** full `just ci` + standalone `spacetime publish --delete-data` rehearsal vs scratch DB documented in PR body.

## Risks (ranked)
1. fusion.md git-rm omission. 2. Vacuous-green R-mirror rules (mitigate: fixture+control per rule). 3. Tooth-1 mis-attribution post-bond (keep tick_accrue_bond assertion). 4. content-hash / vite.config cosmetic-touch traps. 5. parseTables poisoning via table( in new fixture strings (concat!). 6. Deployment ungated (ADR runbook). Also: ReDoS/no-dynamic-RegExp; unpaired /* in comments; 0x22 for quote chars in scanners.

## PLAN-REVIEW ADDENDUM (3 lenses closed 2026-08-07; all adopted items below are BINDING on tester/specialist)

Reviewer: APPROVE, all load-bearing claims spot-checked accurate. Master CI green re-confirmed live (run 31143323283 completed success). One-PR right-sizing confirmed.

ADOPTED CHANGES vs the base plan:
1. **(simplify BLOCKER)** `evals/raising-reducer-security.eval.mjs` joins the slice (gate-forced): g8 `checkCareSSOT` (:317-339) requires `apply_care(`/`evaluate_care(` in evaluate_care's body → genuinely red after the reshape. Update g8 to require the new SSOT delegation (`is_cooldown_ready(`), update `BAD_COOLDOWN_LEQ`(:966)/`GOOD_EVALUATE_CARE`(:1082) fixtures to the 2-arg shape. Prefer red-first (g8 requires new shape). Keep the evaluate_care seam — `evaluate_heal` (raising.rs:296-305) is the established identical pattern.
2. **(red-team H4)** R12 ledger semantics ≠ species presence-forever. R12 permits edge REMOVAL, forbids id reuse/REASSIGNMENT. New `evals/baselines/evolution-path-edge-ids.json` = ever-issued map `edge_id -> {from, to}` (not a bare list): current id absent from ledger → FAIL (forces conscious append); current id with different (from,to) than ledger → FAIL (reuse/reassignment); ledger id absent from content → OK (legal removal). Mirror only the MECHANISM CLASS (committed baseline + local numeric parser + masking-comment refusal + no self-confirming baselines). Refuse non-plain-decimal edge_id literals (hex/octal/binary/underscore — RON accepts them, JS regex doesn't) and out-of-range (>u32) — refusal, not silent skip, each with a tooth. Record the semantic deviation from the spec's "mirror species_id's mechanism" wording in ADR-0177 (R12's own text is the authority).
3. **(red-team H1)** no-idle-accrual gains **Check C**: no production fn outside allowlist {pub_from_monster, monster_from_instance} constructs `Monster {`/`MonsterPub {` struct literals (indexOf-based, comment-stripped, _tests.rs excluded) + a struct-literal BAD fixture (scheduled reducer building MonsterPub{..} → must be flagged). Closes the findGrowthWrites dot-assignment-only blindness that made the 4 new pub GROWTH_FIELDS vacuous. Verify current production has no other literal-construction sites first (grep before finalizing allowlist).
4. **(red-team H3)** GROWTH_FIELDS also gains `level` and `xp` (writers apply_evolution/battle.rs:1210-1213 are already allowlisted; tester verifies eval green vs current tree, adjusts if a stray writer surfaces).
5. **(red-team H2)** E2 hardening in evolution-reducer-security: for evolve, verify the reject_if_in_battle call's argument region covers BOTH `player_identity()` and `opponent_identity()` chains; BAD fixture = opponent chain dropped. New S2 checker pins `is_in_ongoing_battle(` (both-role by construction, guards.rs:302-307).
6. **(red-team M6)** ADR-0177 explicitly carves out SURVIVING provisions: ADR-0149 D3's 30..=39 id-band discipline; ADR-0060's registry-shape lineage. Supersession is fusion-specific per spec wording. Also (simplify m4) note explicitly that EG4 shipped without an ADR and the 3 EG4 pattern decisions are recorded here for the first time.
7. **(simplify MAJOR)** R1-R12 fixture sizing: ONE shared valid-graph GOOD baseline; ~12 BADs as single-property mutations of it; dedicated GOOD fixtures only for R4 (minimal non-vacuous edge), R8 (spec-mandated positive fan-in pin), R9 (ItemDef shape); R12 gets its own sub-suite (masking refusals, empty/absent-baseline fail, misfire controls, reassignment tooth, removal-is-legal control). Absence-is-fail edge count everywhere.
8. **(simplify m5)** The rename prose sites in game-core/tests/pt_d1_roster.rs:153,665 + pt_d3_tuning.rs:719,803 assert a BEHAVIORAL claim ("neither ... strips block comments") — if the new eval adds block-comment masking-REFUSAL that claim needs rewording, not find-replace. (Refusal ≠ stripping; check the exact wording against the new behavior.)
9. **(simplify m3)** tooth-7 re-point = cosmetic, fine; GOOD_INTENT_WRITER re-point to post-B care shape retained (fixture-fidelity convention).
10. **(red-team M5/L2, noted NOT built)** baseline-edit laundering + no eval-set manifest = pre-existing mechanism-class limitations, parity with species; flag as follow-ups in handoff/PR, do not solve here.

Tester fan-out (all opus, disjoint files): A = content-integrity rewrite+rename+ledger (+pt-d2 comment, pt_d1/pt_d3 prose). B = evolution-reducer-security (S1-S6 + E2 both-role) + raising-reducer-security (g8+fixtures). C = no-idle-accrual (fields/re-points/Check C/EG2-9 xref) + table-schemas.json pre-edit + eg5_6 Rust tooth in evolution_tests.rs.
