# monster-realm v2 — supervisor handoff (rolling; older entries in monster-realm-handoff-archive-2026-09.md, monster-realm-handoff-archive-2026-08.md, monster-realm-handoff-archive-2026-07.md)

---

## 2026-09-18T~12:10Z — rb-87 PR#475 OPEN — export_bundle_reaper emits ONE terminal `export_bundle_reap` observation line (ADR-0238 amendment, closes R-rb-48-OBS); local `just ci` GREEN (ledger X6 + verifier serial re-run); ledger 8/10 met, 2 deferred (X9 native execution proof, X10 the operator-alarm half incl. R-rb-85-X10); verifier PASS; security auditor PASS (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/475 (head 6479e28, branch `feat/rb-87-export-reaper-observation`, worktree `.claude/worktrees/rb-87`); local full `just ci` green on the final tree via `mr-gates check` (X6 CI-GREEN) and independently by the verifier (serial re-run CI-EXIT=0: 2373 Rust tests, 99/99 evals, 3147 client tests; its first run was the documented account-e2e S9 rig-contention flake — empty stderr tail + WS reset); remote CI running — supervisor owns the merge.**

Design (the crate-level decision R-rb-48-OBS asked for): NEITHER of the two options the residual named. privacy.rs's contract bans bare `log::`/print macros and quote bytes, not the blessed `observability::mr_log` wrapper (`request_data_export` has called it since rb-65 / ADR-0243 D9), and for a scheduled reducer the calling reducer IS the scheduled reducer — the rb-40/ADR-0243 "helper REPORTS, calling reducer emits ONE terminal line" doctrine applies unchanged. Ships: private `#[derive(Debug, Clone, Copy)] struct ExportReapTick { read, planned, reaped }`; `reap_expired_export_bundles -> ExportReapTick`; pure private `reap_fields(tick)`; reducer shell guard → let tick → let fields → `mr_log(stringify!(export_bundle_reap), &fields)` → Ok(()). Line `{"evt":"export_bundle_reap","read":N,"planned":K,"reaped":M}`, unconditional on success, terminal, never on the reject. Abort loop = ABSENCE of the hourly beat (dead-man); backlog = a cap at its bound as a necessary-not-sufficient HINT across consecutive ticks; nothing consumes the evt yet.

ADR: supervisor-assigned 251 is TAKEN (rb-81) → dated `## Amendment (2026-09-18, rb-87 — residual R-rb-48-OBS closed)` on ADR-0238 (rb-84/85/86 precedent); no new docs/adr file; DIGEST unchanged. **Collision flagged: assign a fresh number only if a standalone ADR is still wanted.**

Tests: 8 ordinary Rust `rb87_` tests (two EXECUTABLE value oracles — the fragment and the composed envelope through `build_log_line`; the rest source pins with clause ORDER tuned for first-failure attribution); rb-48/rb-85 equality pins re-frozen in place; `rb65p [emit/count-in-file]` 1→2 repaid per ADR-0243 D10. Suite 961→969 (962→970 dev_reducers). RED record `memory/projects/gates/rb-87.red-before.md` (Stage 1 967/958/9 on the predicted clauses; Stage 2 E0425×6 + E0422×1). Register `rb-87.mutants.py`: 30 mutants + 3 controls, 30/30 KILLED on the designated clause, 3/3 GREEN (`rb-87.x7-register.md`); X8 = 4 rows on 4 tests.

Lenses (all recorded in `memory/projects/gates/rb-87.plan.md` §9-§13): planner (opus) · plan reviewer + plan red-team (opus, measured: applying §2 verbatim reds exactly the four revised pins; a header retruth would have shifted 16 inbound privacy.rs line citations (13 outside touches) → made line-neutral and pinned by X5; 7 register rows would have died on the whole-tail equality first → clause order fixed) · /simplify (PartialEq/Eq dropped) · tester (opus; its role hook blocks cargo — the orchestrator ran fmt + both RED stages) · tests reviewer + tests red-team (opus, measured: M5 could not compile, M14's anchor matched 19 sites, X8 label order; the stamp-plan shadow, the diverging exit and the reject-token respelling were CI-clean and are now closed) · implementer (opus, general-purpose agent) · reducer-security-auditor (PASS; one MEDIUM doc retruth: 2.x scheduled functions are private by default — adopted everywhere) · verifier (PASS). `/tmp/mr_warn_rb-87` landed during the tests review → round-2 test closures + docs written by the orchestrator; no further fan-out.

Deferred: X9 → backlog (native execution proof; R-rb-85-X9 mechanism + the crate-wide delivery gap); X10 → backlog (the alarm half — **R-rb-85-X10 is re-deferred here; it is `targeted` → rb-87 in the residual ledger and needs re-targeting by the supervisor**). Residuals registered: R-rb-87-BACKLOGAMBIG, R-rb-87-MRLOGDELIVERY (NOTE: its stored `reason` lost the mutant snippet to a shell backtick — the mechanism is "an early `return` as `mr_log`'s first statement"; correct text in the ADR amendment + plan §12), R-rb-87-ADR0243D10, R-rb-87-RUNBOOKEVT.

Follow-up flags (outside touches, not edited): ADR-0243:147 "holds privacy.rs at one" superseded; `ops/observability/rules/recording.rules.yml:203` comment now history; DR runbook §9 could name the evt. Touches-delta: ADR-0238 (amendment), ARCHITECTURE.md (one paragraph), docs/knowledge (two moved stamps). Boyscout-delta: the rb-48 `[E3/guard-first]` message retruth (one hunk). Code graphs: master unchanged until merge — refresh (cbm index_repository + codegraph sync on the main checkout) belongs to the post-merge tick.

Gotchas measured this run: the tester agent's PreToolUse hook blocks cargo (orchestrator must run fmt + RED stages); a register run and a `just ci` in the same worktree collide (shared target dir + spacetime rig) → one false-RED for the verifier; `residuals add --reason` with backticks inside double quotes loses the quoted text (use single quotes).


## 2026-09-12T~21:30Z — rb-81 PR#466 OPEN — the reducer rosters of pvp.rs / battle.rs / economy.rs / ranking.rs pinned closed by one ordinary Rust test each (ADR-0251, closes R-rb-47-ROSTER-PVP); local `just ci` GREEN (ledger X5 + verifier re-run); ledger 9/9 met, 0 deferred; verifier PASS (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/466 (head c33b61d, branch rb-81, worktree `.claude/worktrees/rb-81`); local full `just ci` green on the final tree via `mr-gates check` (E1 4/4; X1 919/919; X2 920/920; X3 lint; X4 docs fresh + ADR header + 0237 reciprocal + ARCHITECTURE + four production files byte-identical + 18 stamps + name set + anti-weakening shapes; X5 CI-GREEN; X8 TWINS-KILLED 4/4) and independently by the verifier (`CI-EXIT=0 WALL=103s`, full recipe list, 99/99 evals incl. live account-e2e); X6/X7 MANUAL ticked with path:line evidence; remote CI running — supervisor owns the merge.**

**WHAT SHIPPED (test-only; zero production Rust).** Four `rb81_<file>_reducer_roster_is_closed` tests (pvp_tests.rs ~5413+, battle_tests.rs ~7977+, economy_tests.rs ~3168+, ranking_tests.rs ~2834+), each over its own file's strippers (ADR-0003), ten clauses in one order: substrate P1-P5 (block-comment tokens, BOTH quote char-literal spellings, raw openers, per-comment-region quote parity, two-order stripper equality) → tail → cfg-roster (+ battle's binding clause on start_wild_battle) → roster walk/count/SET (7/6/2/1, loud roster-parse panic) → attr-any → attr-path → attribute PARTITION per-kind + total (16/12/5/3) → mod-census → include/macro bans → attr-raw LAST (17/13/5/4). MEASURED before planning (T0, red-before.md §1): a plain bare twin SURVIVED 915/915 in pvp/battle/economy; ranking was count-covered by pvp_tests.rs::m17a_rl7 only. GREEN at HEAD by construction (hardening pin, ADR-0224); teeth = the register.

**REGISTER (X6, red-before.md §4): 52/52 rows matched at 04e807f** — 45 M-rows KILLED on their designated label (M10 the view twin INVALID on the first pass under an illegal signature, KILLED on the partition after the `Vec<u32>`/`public` re-run), M24 KILLED-BY-COUNT (919→586), C1-C4 GREEN, D1/D2 RED-DISCLOSED. Measured-and-closed during the slice: plan red-team H1/H2/H3 stripper-polarity bypasses (→ P1/P4), the vacuous squashed-view `mod` census, the tail retarget, the cfg flip; artifact red-team F1/F2 (the 4-byte escaped char literal `'\"'` opens a phantom string in every string stripper — 919/919 + fmt-clean — hiding a bare twin in pvp/battle and an aliased twin in ranking that also evaded m17a_rl7 → P2 4-byte spelling, rows M29a-e) and F3 (spaced `# [` opener → whitespace-tolerant attr-raw, row M30); second-tester B1 (battle's cfg list is a multiset; four decoy dev-gated consts + an un-gated start_wild_battle keep it identical → binding clause, row M31). Verifier's own mutant (raw-identifier `#[spacetimedb::r#reducer]`) compiled and was KILLED on the partition. Not weakened: pure-addition diff, all pins unchanged, hardening only added clauses.

**LENSES:** planner (opus) · plan reviewer + plan red-team (opus, parallel) · /simplify inline · tester (opus; /tmp staging, orchestrator spliced + RED capture) · tests reviewer + artifact red-team + second tester as adversarial critic (opus, parallel on a git-archive snapshot) · hardening implementer (general-purpose opus; 10 hunks, −147 lines net) · reducer-security-auditor (PASS-WITH-NOTES: table-`public` flip unpinned; crate-wide order accounts → privacy → movement → lib) · doc-keeper (opus, worktree-only writes) · final reviewer (docs fact-check: "eight not nine", the ranking count-cover sentence, SOLE-kills overclaim, ADR-0249 parentheticals — all fixed) · verifier (opus, PASS). NOT run: desync-guard (no game-core/wasm/client/movement surface).

**RESIDUALS REGISTERED (4, backlog):** R-rb-81-CROSSFILEMACRO (MED), R-rb-81-CHARLITERAL (MED), R-rb-81-TABLEPUBLIC, R-rb-81-SPECCITE (domain harness). NOTE for the supervisor: `mr-gates residuals add` ignores `--severity` (all four landed MED; ADR-0251 classifies TABLEPUBLIC and SPECCITE as LOW), and TABLEPUBLIC's reason lost the word `public` to a backtick shell expansion in the add command — the ADR D6 text is authoritative. R-rb-80-CRATEWIDEBARE widened in prose (eight un-rostered reducer files + the new-module-file twin).

**ENVIRONMENT NOTES:** a fresh worktree has no `client/node_modules`, so `just lint`/`just ci` fail with `biome: not found` until `npm ci` in client/ (memory card exists); `mr-gates lint` reads the JS `||[]` idiom as a trailing `||` (rewrite as an explicit null test; memory card written); inner double quotes in a `node -e "..."` CHECK are eaten by the shell — use single-quoted JS string arrays; a doc that spells the OLD citation token while describing a repoint trips an X4 "old token absent" clause (memory `doc-prose-about-a-pinned-marker-breaks-it` applied). `just ci` on warm caches is ~103 s wall. Memory cards written: escaped-char-literal-phantom-string, cfg-predicate-roster-must-bind-to-its-item, mr-gates-lint-rejects-js-or-empty-array-idiom.

**MERGE INSTRUCTION:** From `.claude/worktrees/rb-81`, `mr-gates verify --slice rb-81 --budget ≥600` (X8 runs four register rows ≈ 2 min; X5 ≈ 2 min warm); CI-EXIT must be 0; `mr-audit` orchestration as listed above; squash-merge with the PR title (scope: security-authz,ci-gates); `mr-gates residuals close --slice rb-81 --pr 466` closes R-rb-47-ROSTER-PVP; age the four R-rb-81-* rows; reconcile ADR index (0251) / CHANGELOG / ARCHITECTURE; refresh code graphs on the canonical checkout post-merge; delete branch + worktree; harness-side files under memory/projects/gates/rb-81.* + monster-realm-rb-81-plan.md + the residual rows + the three memory cards are uncommitted in the harness repo (supervisor tick commits them); scratch in /tmp (rb-81/, rb-81-redteam/, rb-81-artifact*, rb-81-review-*) safe to delete. ADR next-free: 0252.

---

## 2026-09-12T~16:30Z — rb-80 PR#464 OPEN — deletion gate reaches heal_party / talk / advance_dialogue / dev grant_bait (ADR-0250, closes R-rb-46-ERASEWRITERS); local `just ci` GREEN (ledger X5); ledger 8/8 met, 0 deferred; verifier PASS after two local fixes (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/464 (head d104956, branch rb-80, worktree `.claude/worktrees/rb-80`); local full `just ci` green on the final tree via `mr-gates check` #2 (E1 12/12 with --features dev_reducers; X1 915/915; X2 916/916; X3 lint; X4 docs fresh + ADR header + 7 stamps + 12 test names; X5 CI-GREEN); X6/X7 MANUAL ticked with path:line evidence; remote CI running — supervisor owns the merge.**

**WHAT SHIPPED (8 production lines).** `crate::guards::require_not_deleting(ctx, "<name>")?;` + one comment line at raising.rs:307-308 (heal_party, after the joined lookup), npc.rs:238-239 (talk), npc.rs:339-340 (advance_dialogue — above its four reject-path conversation deletes and the two log::warn! arms), taming.rs:284-285 (dev grant_bait, first check). MEASURED before planning: `apply_quest_trigger` (quest turn-in grants) has exactly ONE caller — `talk`, not `advance_dialogue` (the residual's attribution was wrong; the only shipped quest completes on a Talk step) — so `talk` is gated too (disclosed as ledger X7); and no recruit path calls `grant_item` (the taming call is inside dev `grant_bait`). `dismiss_dialogue` (73-byte body freeze) and `attempt_recruit` (in-battle; `consume_one(` once) classified OPEN under PRV1-10 and pinned; `train`/`care`/`essence_train`/`consume_crystalized_essence` deferred BY SCOPE (R-rb-80-RAISINGWRITERS; their only precedented placement is unreachable by the native-host matrix), visible via a file-wide gate count of exactly 1. guards.rs byte-identical. Twelve rb80_ tests (RED at HEAD on their predicted clause, `memory/projects/gates/rb-80.red-before.md`): 4 native-host five-state matrices, 4 source pins (rb-79 whole-prefix freeze per site + return census + anchors + tag; clause W for advance_dialogue), 3 per-file censuses (bare-name 1/2/1; reducer roster set + wire-name-twin ban + `#[` attribute budget 8/5/6 + `::reducer`-is-bare; `#[cfg` 2/1/3; write verbs; comment-quote and raw-string-opener substrate preconditions), 1 guards_tests control (rb78_region_anchors 7→10; ADR-0248:173's guards_tests.rs:6415 citation kept true by line-count-neutral doc edits).

**REGISTER (X6): 28/28 M-rows KILLED, 0 INVALID; C1/T1/C3 CONTROL-GREEN.** Survivors registered, not pinned: C3 below-gate `let me =` rebinding (R-rb-80-BELOWGATE), red-team M24/M25 raw `ctx.db.player_wallet()`/`ctx.db.inventory()` writes below a gate (R-rb-80-RAWWRITE — `evals/run.mjs` has no filter and the ADR-0081 evals have no main guard, so their compensating-control status is unmeasured), verifier V2 fn-pointer delegation to `spend_currency` (R-rb-80-FNPTRDELEGATE). Verifier V1 (plain `r"\"` raw string inverting the raising/npc blankers) SURVIVED at 172ef50 and is KILLED at d104956 by the widened preconditions.

**LENSES:** planner (opus) · plan reviewer + plan red-team (opus) · /simplify inline · tester (opus; /tmp staging, orchestrator spliced + RED capture) · artifact reviewer + artifact red-team (opus; 32 hardening hunks) · impl reviewer (found the un-regenerated knowledge bundle + ADR-0184:167 — both fixed) · impl red-team · reducer-security-auditor (PASS-WITH-NOTES: enumerated eight present §4.7-selected ungated reducers outside the censused files — join_game the sharpest (PRV1-8 operator-blocked), request_data_export deliberately open — folded into R-rb-80-CRATEWIDEBARE) · verifier (opus; FAIL → three local fixes → gates re-run green). NOT run: desync-guard (no game-core/wasm/client/movement surface), doc-keeper (LANDING-PATTERN flag `/tmp/mr_warn_rb-80` appeared mid-slice; ADR/ARCHITECTURE/knowledge written by the orchestrator, fact-checked by the impl reviewer).

**RESIDUALS REGISTERED (9, all backlog):** R-rb-80-RAISINGWRITERS (MED), R-rb-80-BELOWGATE (MED), R-rb-80-RAWWRITE (MED), R-rb-80-FNPTRDELEGATE (MED), R-rb-80-CRATEWIDEBARE (MED), R-rb-80-DIALOGUECOMMITMENT (LOW), R-rb-80-CITEDRIFT (LOW), R-rb-80-REASONSCOPE (LOW), R-rb-80-DEVFEATURETEST (LOW).

**ENVIRONMENT NOTES:** `evals/run.mjs` ignores argv (ran the whole suite twice while measuring M24); most eval files print nothing when run directly. nextest pads single-digit indices (`( 4/14)`) — the register runner's `fails=` regex was fixed. `mr-gates residuals add --severity` works. The reducer-security-auditor agent definition still carries the stale 1.12.0 crate premise (harness-side). Memory cards written: evals-run-mjs-has-no-filter, string-blanker-substrate-order-and-raw-openers, reducer-roster-census-blind-to-aliased-attribute, nextest-fail-line-padded-index.

**MERGE INSTRUCTION:** From `.claude/worktrees/rb-80`, `mr-gates verify --slice rb-80 --budget ≥2400`; CI-EXIT must be 0; `mr-audit` orchestration as listed above; squash-merge with the PR title (scope: security-authz,economy-quests); `mr-gates residuals close --slice rb-80 --pr 464` closes R-rb-46-ERASEWRITERS; age the nine R-rb-80-* rows; reconcile ADR index (0250) / CHANGELOG / ARCHITECTURE; refresh code graphs on the canonical checkout post-merge; delete branch + worktree; scratch in /tmp (rb-80/, rb-80-redteam*, rb-80-*.log, rb-80-review already removed) safe to delete. ADR next-free: 0251.

---

## 2026-09-12T~13:00Z — rb-79 PR pending — propose_trade gate prefix byte-frozen (ADR-0249, closes R-rb-46-TRADINGCFG)

**TERMINAL STATE: slice close readied; local `just ci` green; PR: https://github.com/mdrewt/monster-realm/pull/463 (opened 2026-09-12T05:27Z at 11f7d1d; local full `just ci` green standalone — 2298/2298 Rust, 108/108 client files, 99/99 evals; ledger 6/6 met; remote CI running — supervisor owns the merge; post-merge graph re-index owed on the canonical checkout); ledger 6/6 met, 0 deferred; verifier PASS.**

**RESIDUAL HALF-CLOSED.** R-rb-46-TRADINGCFG was registered with three remedy parts: statement-boundary, `#[`/`cfg!(` bans, and early-exit clauses. rb-47 (ADR-0237, PR #429, be3ff53) closed the attribute half — `rb47_propose_trade_gate_has_no_attribute_or_cfg_escape` + file-wide `#[cfg` count killed M1/M2 (register rows at HEAD), so ADR-0237:203-204's claim that rb-47 closed the residual was half-right. ADR-0248 D6 conversely stated rb-79 owns the attribute class for trading.rs:252, also half-right — both corrections appended as reciprocal header lines (ADR-0249 `**Amends:** ADR-0237` + `**Amended-by:**` back-link). The early-exit half stayed open and is material: seven macro-free predecessors placed directly above the gate were CI-clean at be3ff53 — fmt 0, clippy 0, 902/902 tests — a sender-keyed `if me != crate::WILD_IDENTITY { return Ok(()); }`, a file-scope `cfg!(test)`-valued const with early Ok, a plain false const, a `return Err(e).or(Ok(()))` combinator, a rejection-SHAPED `return Err(e);` (which rb-46's clause I census reads as 1==1 and PASSES), delegation to an ungated twin (rb-46 shape 1), and a `WILD_IDENTITY` shadow with no `return` at all — the mechanism is ADR-0236/ADR-0248's recorded fact: the native host's sender IS `WILD_IDENTITY`, so real players route around the gate while tests keep observing it fire.

**WHAT SHIPPED:** `rb79_prefix_verdict` pure verdict (four clauses over the file's own strippers, no third stripper per ADR-0003; [rb79/twin], [rb79/extract], [rb79/gate-count], [rb79/prefix] — the whole squashed prefix above the gate equals a hand-derived literal byte for byte, a runtime tie with three split points verifies it, mechanism is ADR-0166 D3 + ADR-0227 D3/D4 binding never pasted); two live tests in trading_tests.rs (5956-6092 a 16-row fixture matrix, 6150-6241 the oracle over real trading.rs with escrow-insert control + the seven-part runtime tie + the D5 note-presence clause); in-place rewrite of trading.rs:247-251 (the Guard-1a comment block + blank line, five lines in and five out, zero line shift, all six knowledge-bundle stamps unmoved — `just knowledge-check` clean, four content claims preserved, no double quote/macro/cfg/block-comment markers/r#/apostrophe/production needle); ADR-0249 (Extends ADR-0236 with reciprocal `Extended-by:` back-link appended to 0236's header; reciprocal `Amended-by:` appended to ADR-0237's header); ARCHITECTURE.md one tail entry (ADR next-free = 0250).

**REGISTER: 12/12 M-rows KILLED, 0 INVALID.** M3-M8 and M10 (the seven survivors of the be3ff53 pre-slice measurement, red-before.md §1) are killed by the rb-79 live oracle ALONE; M1/M2 already closed by rb-47, M9 by m22-s5 ordering (both pre-existing); M13 (wire-name rename — an ungated reducer published under this one's wire name with a `fn propose_trade` demoted to dead — GREEN on every rb-79 clause by design, KILLED by `rb47_trading_reducer_roster_is_closed` and the m22-s5 gated-reducer censuses) is a disclosed dependency measured at register row M13, not re-pinned here per ADR-0249 D7. T1 (note-deleted row) red on the note clause as predicted. Controls C1/C2/C3 (comment above gate, early Ok BELOW gate, trailing-comma gate spelling) green. Tree byte-identical after every row.

**LENSES:** planner · plan reviewer + plan red-team · /simplify inline · tester (static Bash; orchestrator executed RED/bite-proofs) · tests reviewer + artifact red-team · impl reviewer · reducer-security-auditor (PASS-WITH-NOTES; see environment below) · desync-guard · verifier · doc-keeper. `feature-isolation`, `feature-client`, `wasm-pack-rebuild` NOT run (no game-core/client/wasm/movement surface).

**RESIDUALS REGISTERED:** R-rb-79-BELOWGATE (HIGH — below-gate delegation to an ungated helper skips ownership and all later checks; unowned, measured row M8; unresolved in trading.rs:252's scope; same class live at pvp.rs:818/1004, ranking.rs:152 per R-rb-78-PLAINRETURN); R-rb-79-ADR0248D6 (LOW — stale attribution at ADR-0248:286-289 + code-comment twin at guards_tests.rs:5220, both outside this slice's touches — registered rather than edited); R-rb-79-LIBRS89 (LOW — three pre-existing citations place `WILD_IDENTITY` at lib.rs:89, declared at lib.rs:95; rb73_session_tests.rs:268/476, accounts_tests.rs:20177); R-rb-79-XFILEINSERT (LOW — nothing pins crate-wide that `trade_offer().insert(` occurs once, trading.rs:408 today; trading censuses are trading.rs-scoped, so a new reducer escrow-inserting a trade in a third file is outside all of them).

**ENVIRONMENT NOTES:** nextest fail-fast cancelled the M13 full run at 418/421, so the rb-47 roster kill needed a filtered re-run to confirm M13 death — tree clean, both runs logged. The reducer-security-auditor agent definition carries a stale premise that the module builds against spacetimedb crate 1.12.0; it is 2.8.1 per ADR-0197 — a harness-side agent-definition fix, not a slice issue.

**MERGE INSTRUCTION:** From `.claude/worktrees/rb-79`, run `mr-gates verify --slice rb-79 --budget ≥2400`; CI-EXIT must be 0; `mr-audit` with orchestration as listed above; squash-merge with Conventional Commit title matching the ADR decision (scope: security-authz,ci-gates); `mr-gates residuals close --slice rb-79 --pr <number>` closes R-rb-46-TRADINGCFG; age R-rb-79-{BELOWGATE,ADR0248D6,LIBRS89,XFILEINSERT}; reconcile ADR index (0249) / CHANGELOG / ARCHITECTURE; refresh code graphs on canonical checkout post-merge; delete branch + worktree; scratch in /tmp (rb-79/, rb-79-review-*, rb79-artifact, rb79-verify, rb79-mutants*) safe to delete. ADR next-free: 0250.

---

## 2026-09-11T~21:40Z — rb-77 PR#461 OPEN — crate-root module wiring + guards.rs cfg-freedom gated by one ordinary Rust test (ADR-0247, closes R-rb-46-LIBRSMOD); local `just ci` GREEN (ledger X4); ledger 6/6 met, 0 deferred; verifier PASS (SUPERVISOR OWNS THE MERGE)

## 2026-09-18T12:43:11Z — rb-87 merged (PR#475, bc45236)
Merged feat/rb-87-export-reaper-observation (squash, branch deleted). Slice: export_bundle_reaper now emits ONE terminal export_bundle_reap observation line (ADR-0238 amendment, closes R-rb-48-OBS). mr-gates verify (direct, pre-merge): CLEAN, 8/10 met, 2 deferred (X9,X10 -> backlog, already unpromoted residuals R-rb-85-X9/X10 predecessors). mr-audit: orchestration=CLEAN (9 agent calls incl reducer-security-auditor/reviewer/tester/verifier); gating_advisory FLAGGED on a mechanical false-positive (.chars().skip(n_chars) matched the skip-marker heuristic; actual diff review found removed_or_modified_asserts=0, suppressions_added=0, and the privacy_tests.rs changes are a widening — test literal grew 3->5 statements, emit-count assertion 1->2, matching the new log line, all with updated doc-comment justification); acceptance FLAGGED only because mr-audit's own re-run of X8 hit its global re-verify time budget (not-reverified, not failed) — X8 was independently reverified fresh minutes earlier via a direct mr-gates verify call (MUTANTS-KILLED 4/4, agrees_with_evidence true). Adjudicated CLEAN, proceeded to merge. residuals closed --force not needed (both gates properly resolved/deferred). Local checkout fast-forwarded 70b9544->bc45236. PR's own pre-merge CI (ci+e2e) was green (after one rerun of a flaky client e2e unrelated to this server-only diff, per the prior triage ledger row); the push-triggered CI run on master itself was still in_progress at tick-end — not re-verified further this tick, no PR to delegate to mr-ci-watch (direct push, not a PR). No BLOCKERs. Mutex released.
## 2026-09-18T12:32:32Z — 2026-09-18T~12:35Z — rb-87 PR#475 e2e red triaged as flake, rerun delegated
PR https://github.com/mdrewt/monster-realm/pull/475 (rb-87, export_bundle_reaper observation line) remote CI came back UNSTABLE: ci job SUCCESS, e2e job FAILURE on 2 tests — dialogue.spec.ts M13.5c and monster-privacy.spec.ts 13r-e (PvP battle overlay, help-hint element intercepting pointer events + DOM-detached retries), 73 passed. rb-87's diff touches only server-module/src/privacy.rs (an mr_log observability line for the scheduled export-bundle reaper) — no client/e2e files at all, so these two client-side failures are not caused by this change. Local just ci was already green twice pre-merge (mr-gates X6 + independent verifier serial re-run; that verifier run's own first pass hit a documented account-e2e rig-contention flake), so this reads as the same known e2e-suite flakiness rather than a real regression. Action taken: gh run rerun 35343293619 --failed, then delegated the wait to mr-ci-watch (PR#475/rb-87) rather than polling. Also confirmed rb-86 (PR#474) and rb-85 (PR#473) are already MERGED on master (70b9544, 3fe6ebc) — their stale per-run locks (session_leader dead, done:true) match completed work, no action needed there. Next tick: mr-ci-watch event resumes the merge decision once the rerun completes — if e2e goes green, proceed to squash-merge per normal DoD; if it fails identically twice more, escalate to a real triage (not a third blind rerun).
## 2026-09-18T09:40:16Z — rb-86 merged — export_bundle_reaper atomic whole-bundle reap
PR#474 squash-merged to master (70b9544), branch+worktree deleted. Closes R-rb-48-PARTIALREAP: the TTL reaper now deletes whole bundles by shared creation stamp (new plan_export_reap_stamps seam + EXPORT_REAP_MAX_STAMPS_PER_TICK=16 write bound alongside the existing 256-row read bound) instead of per-chunk-id, so a bundle can no longer be committed k-of-N deleted across tick boundaries. ADR-0238 dated amendment (no new ADR number; 251 stays reserved for rb-81). mr-audit acceptance verdict was FLAGGED (advisory-only this slice, R13) on NOT-REVERIFIED X8 — adjudicated: supervisor independently re-ran the X8 mutation-testing gate live (4/4 mutants killed, restored=OK, byte-identical to recorded evidence), confirming mr-gates' own verify pass hit an internal execution-budget cap, not a real defect. X9 (native-host execution proof) stays DEFERred to backlog, inheriting rb-85's own unmodelled datastore_index_scan_range_bsatn/datastore_delete_by_index_scan_point_bsatn gap (R-rb-85-X9) — rb-86 deliberately kept the helper signature stable so that future slice can still inject its instant below the guard. Orchestration audit CLEAN (planner/red-team/reducer-security-auditor/reviewer/tester/verifier all present). Residual R-rb-86-X9 closed via mr-gates residuals close --pr 474. Cost 114.74 (model fable, effort xhigh, 1 attempt). Governor NORMAL (d7=$190.49/$2783, fable_d7=$181.80/$2298, fable_ok=true) both before and after this merge — no new launch this tick (one action = the merge).
## 2026-09-18T09:23:45Z — rb-86 PR#474 OPEN — export_bundle_reaper reaps whole bundles atomically by creation stamp (ADR-0238 amendment, closes R-rb-48-PARTIALREAP); local `just ci` GREEN (ledger X6); ledger 8/9 met + X9 deferred, 0 unmet; reducer-security-auditor PASS; verifier PASS at b346786 (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/474 (head b346786, branch `feat/rb-86-export-reaper-atomic-bundle`, worktree `.claude/worktrees/rb-86`, base master 3fe6ebc); remote CI running (ci + e2e pending at open). `mr-gates check --slice rb-86` exit 0 on 9617c8f: X1 10/10 rb86_, X2 961/961, X3 962/962 (dev_reducers), X4 lint, X5 docs-fresh + touches (privacy.rs, privacy_tests.rs, ADR-0238, ARCHITECTURE.md, docs/knowledge/** only; no new docs/adr file; zero stranded claims), X6 CI-GREEN (full `just ci`), X8 4/4 mutants killed on four different clauses; X7 MANUAL with evidence `memory/projects/gates/rb-86.x7-register.md:50` (30-row register, 27 KILLED on designated clauses, 3 controls GREEN, runner exit 0); X9 → backlog (R-rb-85-X9, the native execution proof — same mechanism rb-85 recorded). The final commit b346786 is DOCS-ONLY after the gate run (ADR mitigation sentence corrected per the security audit; adr-digest-check + knowledge-check re-run clean). `gh pr merge` NOT run.**

**ADR NUMBER COLLISION (supervisor action):** the assigned 0251 was already taken by rb-81 (0252 by rb-83) — the ADR-0225 reservation race again. No replacement number was self-picked; the decision record is a dated `## Amendment (2026-09-18, rb-86 — residual R-rb-48-PARTIALREAP closed)` on ADR-0238 (rb-84/rb-85 precedent). ADR next-free is still 0253.

**What shipped.** Design (plan memo `memory/projects/gates/rb-86.plan.md` §9, adopted after planner → reviewer + red-team + /simplify → reviewer delta verdict): the reaper keeps rb-85's bounded range read as the READ window (≤256 decoded rows); a new PRIVATE pure seam `plan_export_reap_stamps` returns the distinct creation stamps of the ids `plan_export_reap` plans over the whole window, oldest first, capped at the new `EXPORT_REAP_MAX_STAMPS_PER_TICK = 16` (the WRITE bound, counted in STAMPS — a stamp is one request's bundle or every bundle committed in that millisecond); the helper deletes each stamp with ONE `ctx.db.export_bundle().created_at_ms().delete(stamp)` (RangedIndex point delete, no decode), so a bundle is either fully gone or untouched in one transaction and only expired rows can ever be deleted. The owner-keyed draft was REJECTED at plan review (an invariant break would delete a FRESH export); the residual's one-shot `ScheduleAt::Time` drain was rejected (still commits k-of-N; a second schedule row is surplus under `plan_reaper_arm` and can disarm the hourly singleton). Ten ordinary Rust `rb86_` tests (ADR-0224); pin revisions: rb-85 helper-body equality pin re-frozen, `m22s4_now_bound_once` TIGHTENED (the only existing assertion changed), prose retruths. Suite 951 → 961 / 952 → 962.

**Lens findings worth carrying forward.** (1) Tests red-team MEASURED three CI-clean per-chunk-stamp spellings (`let now = now + …` shadow, `zip(now..)`, closure param) that passed every pin incl. the tightened `created_at_ms:now,` — closed by T10 freezing the export reducer's whole insert loop by adjacency + one `now` binding (memory card written). (2) The seam's first `rows.iter()` spelling tripped the receiver-agnostic `[rb85/iter-census]` (3 → 4) — pure seams in privacy.rs must be loops (memory card). (3) The implementer caught that lib.rs `erase_player_sessions` is the crate's real first non-unique-index delete — the ADR says "this module's first". (4) X8's label order follows the runner's R order (memory card). (5) Security audit: a smaller stamp cap cannot help a single oversized stamp; the real TICKBOUND mitigations are admission control (R-rb-85-EXPORTADMIT) or a row-aware cap; no alarm watches the reaper today (rb-84 SLO exclusion + no emission) — an abort loop would be silent until rb-87.

**Residuals registered (`mr-gates residuals`, all backlog):** R-rb-86-TICKBOUND (MED), R-rb-86-SAMEMS (MED), R-rb-86-READCAP-NAME (LOW). Inherited untouched: R-rb-85-EXPORTADMIT, R-rb-85-X9.

**Process notes.** `/tmp/mr_warn_rb-86` landed during tests round 2 → landing pattern: the artifact-stage lens pass was limited to the DoD-mandatory verifier + reducer-security-auditor; docs were written by the orchestrator (not doc-keeper). Subagent invocations: planner ×1, reviewer ×2 (plan + delta) + tests reviewer ×1, red-team ×2 (plan, tests), tester ×2 rounds (one agent), implementer (general-purpose) ×2 turns, reducer-security-auditor ×1, verifier ×1 — PASS at b346786: independent full `just ci` re-run CI-EXIT:0 (2365 workspace tests, 99 evals PASS incl. live account-e2e, 3147 client tests, perf-budget + check-secrets clean), lint/knowledge/adr-digest clean, `mr-gates check` 8/9 met; weakening audit clean (49 deleted lines all prose/messages except the one sanctioned tightening + the three re-frozen rb-85 pin literals; assert census monotone 197→222 / 320→382 / prop 21→34; round-2 delta audited separately — no assertion deleted; implementer commits never touched privacy_tests.rs); its own unregistered mutant (dedup/truncate statement-order swap) KILLED by three behavioural clauses + the seam body pin; X8 4/4. Verifier note: the branch moved 9617c8f → b346786 (docs-only) during its first CI run — it discarded that run and re-verified at b346786; merge SHA to confirm = b346786. Also notes `just ci` carries no coverage/mutation thresholds (nightly-only) — the changed-line mutation evidence is the X7 register + X8 + its own mutant. Harness-side artifacts (gitignored `memory/projects/gates/`): rb-86.plan.md, rb-86.gates.md, rb-86.red-before.md, rb-86.mutants.py, rb-86.x7-register.md. Code-graph refresh: the canonical checkout is unchanged by this slice (worktree only) — refresh post-merge (cbm `index_repository` + `codegraph sync`). Sandbox worktree `/tmp/rb-86-sandbox` (detached) can be removed by the supervisor (`git worktree remove --force /tmp/rb-86-sandbox`).

**Next step for the supervisor:** mr-ci-watch #474 → `mr-gates verify --slice rb-86` → squash-merge (one Conventional Commit; the branch's `wip:` commits collapse) → close R-rb-48-PARTIALREAP → promote the three rb-86 residuals as policy dictates → refresh the code graphs on the canonical checkout.

## 2026-09-18T07:02:36Z — rb-86 launched — export_bundle_reaper partial-drain fix
Native tick mr-sup-native-20260918T070008Z-467918 (07:00Z, src=cron). Gate 0: rb-85 (PR#473) was already merged, audited CLEAN, and fully recorded by the prior 06:00Z tick (ledger+handoff+residuals closed); only a stale /tmp/mr_pass_rb-85.done artifact remained — removed, no further action needed for it. master CI green (3fe6ebc). Gate 3: no locks/holds live, no open PRs, no unclaimed residual past aging thresholds (the 3 fresh rb-85-disclosed residuals are 0.1d old; the 9 stale-past-t2 alarm rows are already-promoted historical entries with promoted_slice set, not unclaimed work). queue[] fast path: rb-86 (oldest entry, from R-rb-48-PARTIALREAP) verified live — spec section exists non-blocked, not merged, no deps — launched fable@xhigh (tier=hard: same privacy/export-reaper security surface as rb-85). Found and fixed a spawner bug while launching: my first vars.json omitted the now-required 'tier' field, which mr-spawn's build_brief refuses to render without (BRIEF-RENDER-FAILED, discovered via debug copy with stderr un-suppressed since mr-spawn:6 redirects stderr to /dev/null by design). Added tier:hard and relaunch succeeded (leader pid 469706, claude_pid 469709). queue-remove rb-86 done. Governor NORMAL (d7=$74.46/$2783, fable_d7=$67.06/$2298, fable_ok=true).
## 2026-09-18T06:01:35Z — Native tick rid=native-20260918T060012Z-463939: post-merge CI red triage (rb-85), not a regression
Governor NORMAL (d7=$73.52/$2783 eff., fable_d7=$67.06/$2298 allowance, fable_ok=true). No live locks/mutex/hold; prior tick (05:22Z) had already squash-merged rb-85 (PR#473, export_bundle_reaper bounded-range read, ADR-0238 amendment) and left master.ci=in_progress-post-merge-verify-pending. This tick found the post-merge run (35310888755) conclusion=failure: exactly one e2e test failed, client/e2e/ranked-forfeit.spec.ts:265 (m17c ranked-PvP forfeit, RL-18), Test timeout of 120000ms exceeded; 75 passed, 1 skipped otherwise. Investigated before treating as a real regression: rb-85's PR#473 diff touches only docs/ADR/knowledge-pages + server-module/src/{privacy.rs,privacy_tests.rs,schema.rs,accounts_tests.rs} -- nothing in pvp/battle/ranked/forfeit code paths. Same job (a two-chromium-instance e2e) failed identically on a plain Nightly run 2026-09-12T11:29Z with zero code changes, then passed every Nightly since -- precedent for a pre-existing timeout flake, not something rb-85 introduced. Local per-run 'just ci' on the rb-85 branch passed clean (3147 tests, eval PASS 99) before merge. Action taken: 'gh run rerun 35310888755 --failed' to re-run only the failed e2e job and confirm green, rather than reverting a clean unrelated merge. Did not sit and poll (no per-PR mr-ci-watch target exists for a post-merge master run) -- next tick (or the run's own completion) should re-verify master CI live before trusting this note. Queue unchanged (rb-86..rb-90 pending, fast-path). No slice launched/parked/merged this tick -- one CI-repair action only, per one-step-per-tick.
## 2026-09-18T05:28:18Z — rb-85 merged — export_bundle_reaper bounded index read
Native tick rid=native-20260918T052202Z-417684. Merged PR#473 (mdrewt/monster-realm): rb-85 adds a btree index on export_bundle.created_at_ms and rewrites export_bundle_reaper to read a bounded range (.filter(..=cutoff).take(cap)) instead of the module's last full-table sweep — closes R-rb-48-SCANCOST. Master 91e813f -> 3fe6ebc. mr-gates verify: 8/10 met, 2 deferred (X9 execution proof -> backlog, needs native_host_tests.rs range-syscall support outside rb-85 touches; X10 byte-level observability -> rb-87). mr-audit: orchestration CLEAN (red-team/reviewer/tester/verifier, 5 agent calls across fable/opus/sonnet); gating_advisory mechanically FLAGGED (5 modified asserts, 3 'skip-marker'/2 'suppression' string hits) — read the diff: the modified asserts are a legitimate TIGHTENING (rb22p sweep-census assertion 1->0 now that the sweep is gone, two now-vacuous always-0==0 per-body clauses replaced by a stronger receiver-agnostic census in rb85_reaper_reads_a_bounded_range_and_never_sweeps, confirmed present in the merged file); the skip/suppression hits were doc-comment prose describing attack patterns (#[ignore], #[allow(..)]), not real markers added. Worktree/branch cleaned, residuals closed (1). Governor NORMAL throughout (d7=$72.17/$2783 eff.).
## 2026-09-18T05:06:00Z — rb-85 PR OPEN — local gate green, ledger 8/10 met + 2 deferred, remote CI running
Resumed from the 2026-09-13 rate-limit park at 479753e (worktree .claude/worktrees/rb-85, branch feat/rb-85-export-reaper-bounded-index-read). Terminal state reached: **PR https://github.com/mdrewt/monster-realm/pull/473** open on mdrewt/monster-realm (head 68d9597, base master 91e813f), local `just ci` green on the final tree (2355 Rust / 3147 client / 99 evals PASS), `mr-gates check --slice rb-85` re-executed on the final tree → 8/10 met (X1-X6, X8 with fresh evidence; X7 MANUAL, evidence gates/rb-85.x7-register.md RESULT line), X9→backlog, X10→rb-87 deferred, 0 unmet; verifier PASS. Supervisor owns the merge (gh pr merge not run). Work done on the resume: (1) the accounts_tests.rs hidden dependency applied once touches were widened (fragment + one Kills line, numstat 2/0; the plan's 'three prose citations' were already stale on master via rb-73 — left as history, disclosed in the ADR amendment); (2) tests round 3 re-derived from plan §14 (the /tmp staging was gone): body-equality pins on plan_export_reap + marshal::now_ms, call-or-path-aware ratchet; (3) reviewer + red-team on that delta MEASURED two CI-clean survivors (brace-list import + macro_rules! splice from movement.rs; multi-line-attribute eleventh test) → round 4 closed them; (4) a bounded re-probe MEASURED two more (#[path = "../…"] module outside src/; macro-synthesized eleventh test) → round 5 closed them; /tmp/mr_warn_rb-85 appeared during round 5, so no further fan-outs — verifier verdict stands from the round-4 tree (round 5 = test clauses only). Register rb-85.mutants.py: 37 rows, 32 KILLED / 2 INVALID / 3 GREEN, exit 0; runner fixes recorded in x7-register.md (nextest 0.9.138 `stderr ───` header broke the clause detector; proptest-regressions artifact sweep; create-file edit type; RT-A1 band moved past T10's sampled row). Residuals registered AFTER the last lens: R-rb-85-EXPORTADMIT (backlog, MED-class: no admission control on request_data_export — storage growth not closed), R-rb-85-MODCENSUS (backlog: m22_declared_mod_names skips `*tests` mod names without a cfg(test) check — pre-existing M22 gate gap). CORRECTION for rb-86's spec premise: 'no client subscribes to my_export_bundle' is false since rb-53 (client/src/net/connection.ts:653). Spend: this resume session ran ~8 subagent invocations (tester ×3 rounds on one agent, reviewer, red-team ×2, verifier) on top of the parked session's $111 — over the $150 target; sizing note for the supervisor: the ratchet's bypass classes (see auto-memory raw-call-census-bypass-classes) cost three extra test rounds. Next step for the supervisor: mr-ci-watch #473 → verify ledger → squash-merge; then promote the two residuals; rb-86 needs its premise corrected before launch.

## 2026-09-18T03:04:38Z — rb-85 RESUMED — rate-limit park lifted
Native tick rid=mr-sup-native-20260918T030306Z-63878-30928 (src=cron, invoked 2026-09-18T03:00:12Z). Gate 0/1/2: no live locks, no .done, mr-hold HOLD-NONE, master green at 91e813f, harness main in sync at a9f3f18 (== origin/main). No active human session (no resident IDE claude pid, no writes in either repo in the last 6 min). Governor NORMAL (d7=$3.51/2783, fable_ok=true). rate_limit_resets_at (2026-09-18T00:00:00Z) is now in the past; mr-reset-watch process already exited (its job done) with no live watcher found. Gate 2: found rb-85 genuinely PARKED (not reflected in mr-state.json parked[] -- ground truth over breadcrumbs) -- branch feat/rb-85-export-reaper-bounded-index-read exists, pushed, worktree still present at .claude/worktrees/rb-85, HEAD 479753e (rate-limit checkpoint from 2026-09-13T09:57:21Z). Per doctrine 'if a slice is open or parked (incl. wip: branches) -> resume it before starting anything new', resumed rb-85 instead of picking rb-86 off queue[] (queue untouched, still rb-86..90 at cap). Wrote pass-vars (model=fable, effort=xhigh, tier=hard -- server-module schema/reducer surface -- resume_block summarizing the prior session's commits 11bb280/9166e52/1b7e5f7/a40a9bd and its own documented remaining work: round-3 gating-test revisions lost from /tmp, accounts_tests.rs plan-section-10-B1 widening still pending, re-verify gates fresh). Called mr-spawn rb-85: GATES-PRESENT (10 existing, seed unchanged) then LAUNCHED (leader=64536, claude_pid=64539, model=fable, effort=xhigh, repo=project, pr_repo=mdrewt/monster-realm, rid=mr-spawn-20260918T030408Z-64477). Verified detachment (ppid=1, own session/pgid) and model/effort in the launched command line. Ledger row written (outcome=launched, cost_usd null -- run in progress). NOTE FOR NEXT TICK/HUMAN: while composing pass-vars I hit the known python-docstring-shell-execution trap (memory: python-script-docstring-shell-execution-trap) -- an inline `python3 -c "..."` heredoc with backticks inside a double-quoted arg got backtick-expanded by bash before reaching python (harmless 'mr-gates: command not found', no side effect since mr-gates isn't on PATH here), caught by inspecting the written JSON before calling mr-spawn and rebuilt via a standalone .py script with no shell interpolation. No BLOCKERs. No merge/park action this tick beyond the resume-launch.
## 2026-09-18T02:31:50Z — residual-backlog classify-and-disposition pass (Drew-requested, out-of-band) — 89 wontfix, 16 promoted
Drew explicitly requested a deliberate one-time pass over the full unclaimed residual backlog (not a normal cron tick) to separate real game-defect findings from review-of-review meta findings per the 2026-09-01 anti-recursion directive (ADR-0224). Pulled mr-gates residuals list --unclaimed --json: 105 rows, all MED severity (none HIGH/CRITICAL, so no mr-ask-drew gate applied), sourced almost entirely from the rb-52..rb-84 residual-backlog review chain plus 17r-d/17r-f/18r-c. Read every row's title+reason (full text, not summaries) and classified each individually. 89 dispositioned wontfix via mr-gates residuals disposition, each with its own specific reason (not a boilerplate string) -- buckets: mechanical scanner/census/citation-checker blind spots (the rb-70 ADR-backlink-integrity edge cases, rb-77/78/79/80/81 census-evasion shapes -- same recursion class as evals/*.eval.mjs even though these are Rust #[test]s doing string/regex matching over stripped source), pure doc/citation line-drift with zero functional impact, test-oracle/harness-coverage findings (auditing whether an EXISTING test has a blind spot -- the exact recursion named in the directive), already-accepted design decisions (ADR/spec text cited in the residual's own reason), and findings already tracked as their own standing item elsewhere (M25 privacy audit, ADR-0106 W3 RLS future, ARCHITECTURE.md S10 assignment). 16 promoted to real backlog slices rb-91..rb-106 via mr-gates residuals promote: measured player-facing a11y/UI defects (invisible+broken-focus claim buttons rb-91, missing AT announcements rb-92/rb-94, privacy-surface discoverability rb-93, skill accuracy hover-only rb-98, sprite action invisible to AT rb-99, PvP opponent card exposes name not role to AT rb-100), observability-correctness bugs (unbounded error-ring eviction via defeated dedupe rb-95, distinct frame faults collapsing rb-96, a 'TOTAL, never throws' function that measurably throws rb-97), a DR-runbook privacy-documentation gap (rb-101), and security/data-integrity gaps in the deletion-gate and session/disconnect systems (transaction-abort strands a session row rb-102, stolen-token holder can suppress a victim's own disconnect cleanup rb-103, no reaper for stalled wild battles rb-104, deletion-gate loophole via mid-grace PvP challenger rb-105, PvP sibling of the trade-offer CANCELLAUNDER exploit class rb-83 just fixed rb-106). Shipped as one doc-only chore/residual-triage-pass-20260918T023035Z PR#127 (128-line diff, all 16 promoted sections), gh pr merge --squash --auto (standard doc-only path), verified MERGED live, harness main FF'd 5ef9ee6->ae4008c. Post-pass mr-gates residuals list --unclaimed --json confirms 0 remaining unclaimed. Note: the full (non --unclaimed) residuals list still shows 32 open rows / an alarm for 9 past t2_stale_days=14d (oldest: R-m23-s10-X16 at 24d, R-rb-10-X7/X6 at 19d) -- these are OLDER residuals already promoted in past ticks whose backlog slices have not yet been picked up/built (not part of this pass's scope, which was specifically the 105 UNCLAIMED rows); normal gate-3 aging-based pick-work should surface those for launch in upcoming ticks, oldest first. queue[] untouched by this pass (still rb-86..90, at its 5-entry cap) -- rb-91..106 are durably recorded in M-residual-backlog.spec.md and will be picked up by gate 3's full derivation once the queue drains, or can be queue-added explicitly by a future tick.
## 2026-09-18T02:02:33Z — 02:00Z tick — promoted R-17r-d-B2 to backlog (rb-90)
Native tick rid=native-20260918T020008Z-38849 (src=cron, forced=0). Governor NORMAL (d7=$2.55/2783 eff., fable_d7=$0, fable_ok=true). Gate 0: no live per-run locks, no .done files, no chain-owner mutex held, mr-hold HOLD-NONE, no pending events. Gate 1/2: rate_limit_resets_at (2026-09-18T00:00:00Z) already past, no active human session (no resident replay-mode claude pid, no non-wrapper file writes in last 6min). master clean at 91e813f, CI green, no inflight/awaiting_merge/parked work. Took chain-owner mutex. Gate 3: master not red; nothing open/parked to resume; ran mr-gates residuals list --unclaimed --json -- dozens of MED unpromoted residuals from the rb-52..rb-89/17r-d/17r-f/18r-c review chain, all well past t1_promote_days=3, none past t2_stale_days=14. Per aging rule this outranks queue[] fast path and new PLAN §9 work this tick. Classified the oldest (R-17r-d-B2, disclosed 2026-09-05T09:23:40Z, 12.7d) per Work-selection-scope: a real A11Y ledger gap (overlay same-key-close behavior is unit-tested at main.a11yFocus.test.ts:587/:652 but has no acceptance-tier A11Y-* criterion) -- not an eval-tooling-scanner finding, so promoted rather than dispositioned wontfix. Promoted via mr-gates residuals promote -> rb-90 in M-residual-backlog.spec.md, mr-record queue-add'd (queue now 5: rb-86,87,88,89,90), shipped as doc-only chore/residual-promote-20260918T020153Z PR#126, gh pr merge --squash --auto (standard path per doctrine, verified MERGED live), harness main fast-forwarded to 5f13c37. No slice launched this tick -- the aging bar on the residual backlog took priority; queue's existing rb-86..89 remain for a future tick once the backlog stops dominating gate 3. Pre-existing untouched local strays noted, left alone: .claude/settings.json (M, predates this tick, weakens git/.env read permissions -- not mine to touch), .claude/worktrees/ and .codegraph/ untracked dirs in monster-realm (build/index artifacts). Risks/notes for next tick: the unclaimed-residual backlog is large (40+ MED rows, mostly meta doc/citation/oracle-coverage findings from review slices 17r-d/17r-f/18r-c/rb-52..rb-67+) -- worth a deliberate classification pass to separate real game-defect findings (promotable) from review-of-review meta findings (wontfix per Drew's 2026-09-01 anti-recursion directive) rather than promoting one-by-one each tick.
## 2026-09-18T01:04:17Z — 01:00Z native tick — promoted rb-89, opened decision issue #472
rid=mr-sup-native-20260918T010319Z-27325-17783. Gate 0: no live per-run locks, no .done files, no chain-owner mutex held, mr-hold HOLD-NONE. Governor NORMAL (d7=$1.20/2783, fable_d7=$0, fable_ok=true). Both repos synced with origin, no strays beyond pre-existing .claude/settings.json (untouched) and tool-generated .codegraph/ artifacts. No resident human session. mr-selfcheck (00:00Z tick) flagged residual-unpromoted (107 past t1=3d) and residual-stale (9 past t2=14d) alarms. Investigated the oldest stale item: R-m23-s10-X16/rb-14 (24d) is blocked on two M23 sec.8 operator rulings (colorblind palette redesign-vs-opt-in; canvas sprite-tint contrast) that the spec itself flags as art-direction-not-engineering calls and that had never been asked -- opened non-blocking decision issue mdrewt/monster-realm#472 (does not stand down the loop). Promoted the oldest unclaimed/unpromoted residual, R-17r-e-VIEWHDR (13d, real defect: 5 stale present-tense contract citations across battleView.ts/boxView.ts/raisingView.ts/evolutionView.ts/ARCHITECTURE.md after 17r-e's retraction) to backlog slice rb-89 via mr-gates residuals promote, queued it (mr-record queue-add). Queue now rb-86/87/88/89, all valid. Committed spec+state change directly to main (60c8e95, matches established chore(mr-sup) pattern for residual-promote ticks -- e.g. 183f6fa) and pushed. No slice launched or merged this tick (queue entries exist but this tick's budget went to the residual-aging triage per gate-3 ordering, which outranks the queue fast path). No BLOCKER raised (issue #472 is non-blocking). Next tick: fast-path can launch rb-86 off the queue.
## 2026-09-18T00:03:48Z — 00:00Z tick — reaped stale rb-84 lock, dispositioned R-17r-e-UPDATEFLAG wontfix
Native tick rid=native-20260918T000053Z-11537 (src=cron). Governor NORMAL (d7=$0/2783 eff. — window reset at 00:00Z, budget bundle shows a fresh 7-day window start). Gate 0: the only recorded lock was rb-84 (session_leader 3763316) — pid confirmed dead, no .done, no chain-owner mutex held, mr-hold HOLD-NONE. Live ground truth check (ledger + handoff + gh pr view) showed rb-84 was ALREADY fully merged 2026-09-13T06:04:52Z (PR#471, squash-commit 91e813f, R-rb-48-SLOCLASS closed) — the lock was leftover debris from that tick's session death, never reaped, and had been re-triggering false wrapper-reconcile "CRASHED(no-done, dead leader 3763316)" ledger rows on every tick since (09-13T13:14Z, 09-15T10:14Z, 09-17T06:25Z). Reaped it via `mr-unlock stale` (UNLOCK-REAPED rb-84).

Also found four harness-repo files uncommitted from the 2026-09-13T~09:56Z tick (rb-85 rate-limit park) that crashed before committing its own bookkeeping — same failure class previously seen with rb-83 (documented in the 2026-09-13T01:58Z entry). Folded those into this tick's commit: mr-state.json (inflight cleared, rate_limit_resets_at 2026-09-18T00:00:00Z, rb-85 park note), monster-realm-handoff.md/-archive-2026-09.md (rb-83/rb-84/rb-85 entries), mr-usage-daily.jsonl (09-13/09-16/09-17 rows). Left `.claude/settings.json` alone — its uncommitted diff (broadens `Bash(git:*)`, removes `Read(**/.env)`/`Read(**/.env.*)` from the deny list) is outside supervisor doctrine's file scope and security-sensitive; not attributable to this loop's normal bookkeeping, so not mine to commit or revert.

Gate 1/2: fetch clean both repos, master green at 91e813f (Nightly success x3), no open PRs, no active human session (only .codegraph daemon file churn, no resident IDE claude pid). Gate 3: master not red, nothing open/parked. `mr-gates residuals list --unclaimed --json`: 108 unclaimed, alarms show 108 past t1=3d (promote-stalled) and 9 past t2=14d among ALREADY-CLAIMED/promoted residuals (R-m23-s10-X16 24d, R-rb-10-X6/X7 19d — the same historical stale-scanner-class rows noted in prior ticks, not newly actionable). Oldest two unclaimed residuals tied at disclosed_at 2026-09-05T09:07:18Z (12.6d): R-17r-e-UPDATEFLAG and R-17r-e-VIEWHDR. Classified per Work-selection scope before promoting: R-17r-e-UPDATEFLAG's entire scope is content-version.eval.mjs:109 advertising an unimplemented `--update` flag — purely the retired scanner-script's own correctness (ADR-0224) — dispositioned `wontfix` (MED, self-dispositionable) rather than promoted. R-17r-e-VIEWHDR (stale doc citations of overlayA11y.ts contract (a) in battleView.ts/boxView.ts/raisingView.ts/evolutionView.ts/ARCHITECTURE.md) is a real doc-correctness defect, not eval-tooling — left unpromoted for a future tick's one-action promote step.

One action this tick: the wontfix disposition (residual work, no slice launch/merge). Queue (rb-86/rb-87/rb-88, cap 5) untouched — re-read fresh, not overwritten from a stale snapshot. park_counters unchanged (14r-e:1). No BLOCKERs, no rate-limit event, no fan-out. Next tick: either promote R-17r-e-VIEWHDR (next-oldest unclaimed, real defect) or continue residual aging triage before falling through to the queue fast path (rb-86).
## 2026-09-13T09:57:21Z — rb-85 PARKED — 7-day rate-limit window rejected
Native tick rid=native-20260913T095512Z-109987. rb-85 (export_bundle_reaper bounded-index read, fable@xhigh hard-tier) crashed EXIT=1 ATTEMPTS=1 after ~3.4h / $111.15 (wrapper-reconciled). Log tail shows the real cause: api_error_status=429, result="You've reached your Fable limit." — a genuine rate_limit_event status=rejected (rateLimitType=seven_day_overage_included, utilization=0.92, overageStatus=rejected, overageDisabledReason=org_level_disabled). This is the account's real 7-day API window, distinct from the ledger-tracked weekly $ budget (d7=$933.92/2783, governor NORMAL) — the two are different axes and this one is currently the binding constraint. five_hour window is healthy (util 0.38, resets 2026-09-13T12:00Z) but overage is disabled org-wide so it cannot be used to push through the seven_day rejection.\n\nImpl was mid-flight: worktree had uncommitted round-3 test-artifact staging plus a couple of doc edits (ARCHITECTURE.md, docs/adr/0238) left dirty when the session died; committed as a supervisor checkpoint (479753e) and pushed to feat/rb-85-export-reaper-bounded-index-read. No PR opened yet for rb-85 (gh pr list confirms none). Branch has RED-GREEN progression through 1b7e5f7 (impl GREEN) plus round-2 gating tests and round-3 revision staging (not yet applied to the actual test file — only staged under /tmp/rb-85/tests/round3/ inside the run's own workspace, which does not persist; next resume must re-derive round 3 from the plan/gates memo, not from /tmp).\n\nOutcome recorded PARKED (stopped:rate-limit) — no park-counter bump per the rate-limit-park rule. mr-state.json: inflight cleared, rate_limit_resets_at set to 2026-09-18T00:00:00Z (the seven_day reset), notes updated. Stop flags touched (mr_stop_all, mr_stop_rb-85) defensively though the session was already dead (lock alive=false, .done present) — no stragglers to kill. mr-reset-watch spawned detached for 2026-09-18T00:00:00Z+2min to resume automatically; no manual relaunch of rb-85 before then. master unaffected (still green at 91e813f, rb-84's fix). Queue (rb-86/87/88) and park_counters (14r-e:1) untouched — re-read fresh, not overwritten from a stale snapshot.
## 2026-09-13T06:05:09Z — rb-84 merged — PR#471 (SLO roster reapers, closes R-rb-48-SLOCLASS)
Merged PR#471 (`fix(rb-84): classify export_bundle_reaper + account_deletion_reaper...`) squash-commit 91e813f onto master (fedd13f base). mr-gates verify CLEAN 7/7 met/0 deferred/0 unmet; mr-audit orchestration CLEAN (10 calls/7 roles/3 models); gating_advisory FLAGGED on skip_markers_added=3 — adjudicated as a scanner false-positive (matches were `.skip(1)`/`.skip(group_ln)` iterator calls and the word 'ignored' in a doc comment, not `#[ignore]`; `observability_tests.rs` is a pure 1286-line addition, 0 deletions, 46 `#[test]`s, no removed asserts/suppressions). PR body's Acceptance line matched `mr-gates render --format pr` exactly. `R-rb-48-SLOCLASS` closed via `mr-gates residuals close --slice rb-84 --pr 471`. Run filed two new MED residuals (unpromoted, fresh, left for normal aging): `R-rb-84-GLOSS` (recording.rules.yml item 5's human-prose gloss isn't cross-checked against the selectors) and `R-rb-84-ATTRSPELL` (rb84_scheduled_roster scanner keys on literal spellings of `scheduled(`/`#[spacetimedb::table(`, theoretical whitespace/cfg_attr blind spot). Master fast-forwarded to 91e813f, worktree+branch cleaned up. Governor NORMAL (d7=$821.12/$2783, fable_d7=$656.59/$2298, fable_ok=true) throughout. No new slice launched this tick — this was a single merge action; next tick should re-derive eligibility fresh (gate 3 aging: 111+ open residuals, several past t1/t2 staleness per the prior tick's note) rather than assume a composite launch was already considered.
## 2026-09-13T04:02:52Z — 04:00Z tick — launched rb-84 off the queue fast path
Native tick rid=native-20260913T040005Z-3761005 (src=cron, forced=0). Gate 0: no live per-run locks, no .done files, no chain-owner mutex, mr-hold HOLD-NONE (queued_events=0). Gate 1: rate_limit_resets_at (2026-09-11T00:00:00Z) in the past, proceed. Gate 2: both repos fetched clean and up to date with origin (harness main@dedd198, project master@fedd13f); only resident process is a `claude --resume` IDE session idle 117min with zero writes in the last 6min (idle-open, not active) plus the expected mr-decision-watch (issue #469, rb-83-cancel-sweep, still OPEN/0 comments — non-blocking, left alone) and this tick's own mr-native-tick.sh. No open PRs on either repo. Master CI green at fedd13f (rb-83 merge).

Gate 3: no red master, nothing open/parked. Checked residual aging per doctrine: `mr-gates residuals list --unclaimed --json` shows 106 open, 64 unpromoted past t1=3d, 0 past t2=14d (the historical t2 alarms — R-m23-s10-X16, R-rb-10-X6/X7 — remain the known stale-scanner false-positive / permanently-promoted-target class documented in the 02:02Z and 03:08Z entries; not re-litigated). Per the 03:08Z handoff's explicit note ("Next tick: fast-path should launch rb-84 off the queue"), used the queue fast path instead of re-doing full aging triage this tick: queue held rb-84..rb-88 (cap 5) from prior promotions. Live-reverified rb-84's spec section (M-residual-backlog.spec.md:61, R-rb-48-SLOCLASS, non-blocked, no unmet `after:` deps, not already merged) — valid, launched it.

Classified rb-84 as routine tier (opus@high): scope is ops/observability/rules/recording.rules.yml (SLO allowlist/exclude roster missing export_bundle_reaper + account_deletion_reaper) plus one ordinary proof-of-teeth test — no server-module schema/reducer, netcode, security/RLS, or M20/M25 surface touched, not a resume-after-park. Wrote pass-vars, called mr-spawn: GATES-SEEDED then LAUNCHED (leader=3763316, claude_pid=3763319, model=opus, effort=high, repo=project, pr_repo=mdrewt/monster-realm). Verified detachment + process liveness post-launch. `mr-record queue-remove --slice rb-84` succeeded (queue now rb-85..rb-88, 4 entries). Ledger row written (outcome=launched, cost_usd null — run-in-progress, wrapper/merge-time record will carry actual spend).

Governor NORMAL (d7=$792.37/$2783 eff., fable_d7=$656.59/$2298 allowance, fable_ok=true). Left the pre-existing `.claude/settings.json` stray in the harness working tree untouched (not mine, flagged by prior ticks, weakens .env read protection — not committing blind). No BLOCKERs. Mutex was never held (fast single-launch action); nothing to release beyond the per-run lock which mr-spawn already wrote.

