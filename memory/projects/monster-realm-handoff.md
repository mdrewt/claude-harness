# monster-realm v2 — supervisor handoff (rolling; older entries in monster-realm-handoff-archive-2026-09.md, monster-realm-handoff-archive-2026-08.md, monster-realm-handoff-archive-2026-07.md)

---

## 2026-09-19T~03:50Z — 20r-b: PR #481 open, local gate green (essence reward/cap SSOT + R14)

**TERMINAL STATE: branch `feat/20r-b-essence-ssot`, worktree `.claude/worktrees/20r-b`, HEAD `a459f13` pushed (ea05faa + doc fact-check commit), base master `4ba39b5`; full `just ci` GREEN locally (CI-EXIT=0). PR: https://github.com/mdrewt/monster-realm/pull/481 (opened 2026-09-19; remote ci+e2e running at open; HEAD a459f13) — next step is to open it. Main checkout untouched.** No new ADR number: the record is a self-amendment section `## Amendment (20r-b, 2026-09-18)` on `docs/adr/0175-essence-graph-reducers.md` (ADR-0104 rb-43/rb-70 precedent) plus an inline `**[CLOSED by 20r-b …]**` on follow-up (3); `just adr-digest` is a deliberate no-op (header-only digest) and `DIGEST.md` is unchanged.

Diff: `game-core/src/currency.rs` (+`ESSENCE_BST_DIVISOR: u16 = 30`, `essence_battle_reward(bst) -> u32`, `ESSENCE_SOFT_CAP: u32 = 999`, values/signatures unchanged; 8 `essence_` + 2 `prop_essence_` tests) · `game-core/src/content.rs` (R14 in the `validate_evolution_paths` roster + rule body after R12, 17 `r14_` tests) · `server-module/src/battle.rs` (const+fn deleted, `pub(crate) use game_core::currency::essence_battle_reward;` — the divisor has no server consumer) · `server-module/src/raising.rs` (line-neutral 2-for-2 swap at 436-437 to `pub(crate) use …::ESSENCE_SOFT_CAP;`). Touches-delta beyond the declared four: `docs/adr/0175-essence-graph-reducers.md` and `ARCHITECTURE.md` (3 sentence sites + the "Ten edges"→"Twelve edges" count fix, boyscout). `docs/knowledge` unchanged (the raising.rs swap is line-neutral); `game-core/src/lib.rs`, `battle_tests.rs`, `raising_tests.rs` untouched by design.

Rule id is **R14, not R13**: R13 is name-reserved for the unbuilt temporal-dominance guard by ADR-0176 D2/:200, ADR-0177:150, `evals/evolution-content-integrity.eval.mjs:13` and `game-core/tests/eg3_evolution_graph.rs` T11 (:531) — five present-tense claims in four files outside touches, all still true.

Gate/ledger: `memory/projects/gates/20r-b.gates.md` — one seeded gate B1, met, 0 deferred (27 tests: 8 `essence_`, 2 `prop_essence_`, 17 `r14_`). Tests were written RED-first by the tester in three rounds (compile-RED E0425 ×13 on the three symbols → promotion-only: 8 R14 fixtures runtime-red / 15 pass → 23/23 → 27/27). `cargo-mutants --in-diff` 8/8 caught; hand-cheat register `memory/projects/gates/20r-b.mutant-register.md` rounds 1-2 (17/20, then 3 survivors — u32::MAX carve-out, two-entry-path carve-out, largest-not-first message selection — closed by round-3 tests).

Lens record: planner(opus) → reviewer + red-team on the plan (R13→R14, package name `monster-realm-module`, module-anchored nextest filter, absence limb) → tester(opus, 3 rounds) → reviewer + red-team on the tests (u16-cast escape, R14-after-R12 pin, divisor-consumer pin) → impl by the orchestrator → reviewer + red-team(+cargo-mutants) + desync-guard(PASS) + reducer-security-auditor(PASS) + `/simplify` inline (production diff minimal; `r14_essence_amount_998_accepted` and `essence_battle_reward_may_exceed_the_soft_cap` kept deliberately as documentary) → verifier (running at hand-off) → doc-keeper.

Residual registered: **R-20r-b-B1** (backlog) — `server-module/src/raising_tests.rs:2167` still says its assertion is "the only pin of ESSENCE_SOFT_CAP's value"; `currency.rs:457 essence_soft_cap_is_999` is now a second pin, and the file is unmodifiable by this slice's own gate (B1 byte-compares both server test files against the slice base).

**Supervisor actions:** (1) re-run `mr-gates verify --slice 20r-b` FROM THE WORKTREE — B1's CHECK is cwd-relative and its EVIDENCE field is stale (`23 tests run: 23 passed` against `EXPECT: 27 tests run: 27 passed`; the box is ticked from the round-3 run); (2) keep R-20r-b-B1 in the residual queue; (3) mark spec slice §20r-b done at merge; (4) the ARCHITECTURE.md "Twelve edges" count fix is a boyscout correction (12 `edge_id:` entries: 10 in `000-core.ron`, 2 in `070-wave3.ron`), not a 20r-b behaviour claim; (5) doc-keeper fact-check flagged stale counts inside the ADR amendment's Confirmation paragraph ("23 tests, 7 / 2 / 14 by prefix") and a call-site/import mix-up — supervisor-owned fixes, not applied here.

**Resume point if interrupted:** branch `feat/20r-b-essence-ssot` @ `ea05faa` (pushed), worktree `.claude/worktrees/20r-b`, base `4ba39b5`; nothing further to implement — next step is to open the PR, then record its number here and in the ledger.

---

## 2026-09-18T~19:35Z — m23-s9 PR#478 OPEN — evolution overlay contrast tokens + `prefers-contrast: more` + em→px (ADR-0253 SELF-ASSIGNED); local `just ci` GREEN (2373 Rust / 99 evals / 108 files·3159 client tests); ledger 7/7 met; verifier PASS 12/12 mutants
Branch `feat/m23-s9-evolution-contrast` (worktree `.claude/worktrees/m23-s9`, HEAD df203cc, pushed), PR https://github.com/mdrewt/monster-realm/pull/478, remote ci+e2e pending at open. Terminal state = PR open + local gate green; supervisor owns the merge. Diff: `client/src/styles.css` (9 `--mr-evo-*` `:root` tokens + trailing AAA `@media (prefers-contrast: more)` block), `client/src/ui/evolutionView.ts` (inline `var()` tokens, `background-color` longhand, 14/13/12px), `client/src/ui/evolutionView.test.ts` (+1371: `m23s9 X1–X4` DOM-computed WCAG oracles — ADR-0224 forbids the spec §5.3 eval, so A11Y-30/31 land as vitest like S8 did), ADR-0253 + DIGEST regen, `ARCHITECTURE.md` 2 sentences. `client/index.html` declared but UNCHANGED (X7 MANUAL: viewport has no zoom block). **Supervisor actions:** (1) ADR number 0253 was self-assigned because the launch gave `None` — renumber at merge if taken; (2) the ledger's X7 EVIDENCE cites the CANONICAL checkout path `…/monster-realm/client/index.html:5` (unchanged from master, so it resolves either side of the merge); (3) `mr-gates verify` X1–X6 CHECKs are cwd-relative — run from the worktree or post-merge checkout. Lens record: planner → reviewer+red-team on the plan (dropped the 3:1 large-text tier — WCAG large is 24px/18.67px-bold, nothing here qualifies; `#999` fails 4.43:1 on the worst-case `#333`; scope by `atStack` not text slicing) → tester (2 rounds) → reviewer+red-team on the tests (red-team wrote the cheat: 9 GREEN bypasses — rule ORDER, class/element+`!important` stylesheet route, happy-dom silent drops of `var(--x,fb)`/UPPERCASE, `<style>` injection, textless cover element, border-only consumption, fp monotone — all closed round 2) → impl (orchestrator) → /simplify inline (2 inherited-redundant colours dropped) → verifier (folded impl-review lens under the `/tmp/mr_warn_m23-s9` landing flag; PASS) → doc-keeper. Domain auditors N/A (no server/game-core). One CI red→fix cycle: rb-89's `overlayA11y.test.ts` pins the `NO CLOSE-BEFORE-OPEN` header paragraph at exactly 5 `//` lines and the extractor runs to the first non-comment line — a header paragraph appended AFTER it grows the window; moved the m23-s9 paragraph above it. Follow-up flags (outside touches, not edited): `client/vite.config.ts:105` "evolution/fusion screen" (fusion retired); spec §2.7 large-text threshold conflates pt/px (ADR-0253 D4). Residuals: 1.4.11 non-text (button 1.39:1 under `more`, out of scope §3.1); `prefers-contrast: more` has no e2e.

## 2026-09-18T~17:05Z — rb-89 PR#477 OPEN — five present-tense citations of overlayA11y.ts contract (a) corrected in place (four line-neutral view headers + an m23-s1 bracket-note; five ordinary vitest tests RED→green; closes R-17r-e-VIEWHDR); local `just ci` GREEN (108 files / 3155 client tests, 99 evals PASS); ledger 5/5; verifier PASS

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/477 (head ed67174, branch `fix/rb-89-viewhdr-stale-contract-a-citations`, worktree `.claude/worktrees/rb-89`, base db95397). Remote CI (`ci` + `e2e`) pending at hand-off. Main checkout untouched on master.** No ADR (none assigned; comment-only, ADR-0224). No new eval. Touches: `client/src/ui/{battleView,boxView,raisingView,evolutionView}.ts` (comment only, 5 in / 5 out each — the files carry 72/21/27/14 external line cites), `client/src/ui/overlayA11y.test.ts` (21 → 26 tests), `ARCHITECTURE.md` (`:1970` `[rb-89: …]` bracket-note in the rb-75 idiom, historical prose byte-preserved — the plan-round reviewer rejected an in-line rewrite against ARCHITECTURE.md:5-6; new `**rb-89**` block at `:2301`). `overlayA11y.ts` unchanged.

Ledger `gates/rb-89.gates.md` (0 seeded SHALL → X1-X5): X1 vitest JSON 26/26 + 5 `rb-89-` titles · X2 `gates/rb-89.gates.mjs x2` — whole-file line counts equal, every line outside the 5-line window byte-identical to origin/master, window + bracket sha256-pinned to the lens-approved text · X3 `… x3` 11/11 mutants killed on scratch copies (m9 blank-line note + m10 in-window paraphrase are X2-ONLY kills, disclosed; m11 = the artifact red-team's :52-54 meaning flip, which an anchors-only ground-truth lock passed at 26/26 until the tester pinned the `do NOT "share ONE root"` / `must NOT close-before-open` negations) · X4 `just ci` · X5 touches ⊆ set + `**rb-89**` block names the four views + R-17r-e-VIEWHDR. Lenses: planner → reviewer (MAJOR: bracket-note not rewrite) + red-team (HIGH ×2: outside-window plant → X2 outside_equal; early-return vacuity → fail-loud asserts) + /simplify → tester → reviewer + red-team (HIGH: meaning flip) + desync-guard PASS → verifier PASS (RED-before reproduced on scratch; own mutant killed). Supervisor should re-run `mr-gates verify --slice rb-89` (X3 ≈ 13 vitest runs, ~2 min; X4 is a full `just ci`). Post-merge: `mr-gates residuals close --slice rb-89 --pr 477` for R-17r-e-VIEWHDR.

Follow-ups (registered / flagged): **R-rb-89-BOXTITLE** (registered via residuals add) — `boxView.test.ts:287` title still says the overlayA11y.ts header's "share ONE root" claim IS a misstatement (present tense); gating-test id cited by name from focusTrap.ts:62 / overlayA11y.ts:54, so a fix must keep the `S4-CROSS-VIEW-DISTINCT-ROOTS` token byte-identical. Residual spec cite `ARCHITECTURE.md:1908` had drifted to `:1970` (rb-75 class). `focusTrap.ts:1` "16 overlays" vs `:53` "seventeen" drift still open (rb-88 flag).

Environment incident, handled: the supervisor's rb-88 X4 re-verify (`just ci`, pid 1391940, cwd `.claude/worktrees/rb-88 (deleted)`) was wedged 78+ min at `ops/observability/validate.mjs` on a `docker run --rm … caddy hash-password` whose container (`agitated_keldysh`, mr-caddy-validate:local) had already printed its hash and had no processes but stayed "running (unhealthy)" — the `--rm` client never returned. `docker rm -f agitated_keldysh` released it (the stranded verify ended; its worktree was already gone so it could never have recorded evidence). One rb-89 `just ci` run in that window reddened on the documented account-e2e S9 contention flake (empty stderr + WS reset); the eval passed serially (`import('./evals/account-e2e.eval.mjs').then(m=>m.default())` → pass:true) and the full gate re-ran clean. Code graphs: no symbol change (comments + tests) — refresh on the canonical checkout post-merge as usual. New harness memories: `anchors-only-comment-lock-passes-a-meaning-flip`, `docker-rm-client-wedged-on-finished-container`.

---

## 2026-09-18T~14:05Z — rb-88 PR#476 OPEN — focusTrap.ts:58-62 retracts the false "S4 must close-before-open" prescription in place (comment-only, line-count neutral; three ordinary vitest tests; closes R-17r-e-E3); local `just ci` GREEN (ledger X4 + verifier client re-run 3150/3150); ledger 5/5 met, 0 deferred; verifier APPROVE; desync-guard PASS (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/476 (head a92ff9e, branch `fix/rb-88-focustrap-stale-close-before-open`, worktree `.claude/worktrees/rb-88`, base bc45236). Remote CI (`ci` + `e2e`) pending at hand-off. Main checkout untouched on master.** No ADR (none assigned; ADR-0224 comment-only). Diff: `client/src/ui/focusTrap.ts` (5 comment lines in/out at :58-62 — the file is line-cited at :52-56/:64/:65-73/:88-94/:136/:150 by 7 files, so neutrality is load-bearing), `client/src/ui/focusTrap.test.ts` (+154, one appended describe: `rb-88-SIBLING-ROOTS-NO-STACK`, `rb-88-SAME-ROOT-TWICE-STACKS`, `rb-88-ONE-LISTENER-PER-ROOT-ZERO-ON-MOUNT`; 19→22), `ARCHITECTURE.md` (one `**rb-88**` paragraph at :2299). Ledger X1 count 22/22 · X2 neutrality + 8/8 facts + **sha256 pin of the approved block** (`gates/rb-88.x2.mjs`; added after the artifact red-team measured a paraphrased-contradiction bypass of the presence needles — bite-proved) · X3 harness-side vitest mutant register (`gates/rb-88.mutants.mjs`, scratch client copies, 6/6 killed each by ≥1 rb-88 test, 3 controls) · X4 `just ci` · X5 touches ⊆ {focusTrap.ts, focusTrap.test.ts, ARCHITECTURE.md} + no docs/adr + knowledge/adr-digest drift 0. Lenses: planner (sonnet) · plan reviewer + plan red-team (sonnet, parallel) · tester (sonnet; tester CAN write the worktree now, still cannot execute — orchestrator ran the RED-claim proof `expected btn-1 to be btn-2`) · test reviewer · impl reviewer + red-team + desync-guard (parallel; all clean) · /simplify (nothing to collapse) · verifier (sonnet, APPROVE; own `e.target` mutant killed). Implementer = orchestrator (5-line verbatim comment swap; ≠ tester). Spend ≈ well under the $60 target (7 sonnet subagents, one `just ci`).

Follow-up flags (outside touches, not edited): **rb-89 / R-17r-e-VIEWHDR** — view headers + `ARCHITECTURE.md:1970` m23-s1 block still say "share ONE root, so S4 must close-before-open or two capture traps stack" (the last carrier outside module headers); `focusTrap.ts:1` "16 overlays" vs `:53` "seventeen" (pre-existing count drift; a header insert would break neutrality). Red-team scope note recorded in the ledger: the rb-88 block proves listener topology only — a `stopPropagation` mutant survives it and dies only to pre-existing A11Y-7-NO-STOP-PROPAGATION; never prune the older A11Y-7/S1-TRAP tests as redundant. New harness memories: `comment-truth-gate-pin-the-approved-block-by-hash`, `vitest-mutant-register-scratch-client-copy`. Code graphs: no symbol change (comment + tests) — refresh on the canonical checkout post-merge as usual.

---

## 2026-09-18T~12:10Z — rb-87 PR#475 OPEN — export_bundle_reaper emits ONE terminal `export_bundle_reap` observation line (ADR-0238 amendment, closes R-rb-48-OBS); local `just ci` GREEN (ledger X6 + verifier serial re-run); ledger 8/10 met, 2 deferred (X9 native execution proof, X10 the operator-alarm half incl. R-rb-85-X10); verifier PASS; security auditor PASS (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/475 (head 6479e28, branch `feat/rb-87-export-reaper-observation`, worktree `.claude/worktrees/rb-87`); local full `just ci` green on the final tree via `mr-gates check` (X6 CI-GREEN) and independently by the verifier (serial re-run CI-EXIT=0: 2373 Rust tests, 99/99 evals, 3147 client tests; its first run was the documented account-e2e S9 rig-contention flake — empty stderr tail + WS reset); remote CI running — supervisor owns the merge.**

Design (the crate-level decision R-rb-48-OBS asked for): NEITHER of the two options the residual named. privacy.rs's contract bans bare `log::`/print macros and quote bytes, not the blessed `observability::mr_log` wrapper (`request_data_export` has called it since rb-65 / ADR-0243 D9), and for a scheduled reducer the calling reducer IS the scheduled reducer — the rb-40/ADR-0243 "helper REPORTS, calling reducer emits ONE terminal line" doctrine applies unchanged. Ships: private `#[derive(Debug, Clone, Copy)] struct ExportReapTick { read, planned, reaped }`; `reap_expired_export_bundles -> ExportReapTick`; pure private `reap_fields(tick)`; reducer shell guard → let tick → let fields → `mr_log(stringify!(export_bundle_reap), &fields)` → Ok(()). Line `{"evt":"export_bundle_reap","read":N,"planned":K,"reaped":M}`, unconditional on success, terminal, never on the reject. Abort loop = ABSENCE of the hourly beat (dead-man); backlog = a cap at its bound as a necessary-not-sufficient HINT across consecutive ticks; nothing consumes the evt yet.

ADR: supervisor-assigned 251 is TAKEN (rb-81) → dated `## Amendment (2026-09-18, rb-87 — residual R-rb-48-OBS closed)` on ADR-0238 (rb-84/85/86 precedent); no new docs/adr file; DIGEST unchanged. **Collision flagged: assign a fresh number only if a standalone ADR is still wanted.**

Tests: 8 ordinary Rust `rb87_` tests (two EXECUTABLE value oracles — the fragment and the composed envelope through `build_log_line`; the rest source pins with clause ORDER tuned for first-failure attribution); rb-48/rb-85 equality pins re-frozen in place; `rb65p [emit/count-in-file]` 1→2 repaid per ADR-0243 D10. Suite 961→969 (962→970 dev_reducers). RED record `memory/projects/gates/rb-87.red-before.md` (Stage 1 967/958/9 on the predicted clauses; Stage 2 E0425×6 + E0422×1). Register `rb-87.mutants.py`: 30 mutants + 3 controls, 30/30 KILLED on the designated clause, 3/3 GREEN (`rb-87.x7-register.md`); X8 = 4 rows on 4 tests.

Lenses (all recorded in `memory/projects/gates/rb-87.plan.md` §9-§13): planner (opus) · plan reviewer + plan red-team (opus, measured: applying §2 verbatim reds exactly the four revised pins; a header retruth would have shifted 16 inbound privacy.rs line citations (13 outside touches) → made line-neutral and pinned by X5; 7 register rows would have died on the whole-tail equality first → clause order fixed) · /simplify (PartialEq/Eq dropped) · tester (opus; its role hook blocks cargo — the orchestrator ran fmt + both RED stages) · tests reviewer + tests red-team (opus, measured: M5 could not compile, M14's anchor matched 19 sites, X8 label order; the stamp-plan shadow, the diverging exit and the reject-token respelling were CI-clean and are now closed) · implementer (opus, general-purpose agent) · reducer-security-auditor (PASS; one MEDIUM doc retruth: 2.x scheduled functions are private by default — adopted everywhere) · verifier (PASS). `/tmp/mr_warn_rb-87` landed during the tests review → round-2 test closures + docs written by the orchestrator; no further fan-out.

Deferred: X9 → backlog (native execution proof; R-rb-85-X9 mechanism + the crate-wide delivery gap); X10 → backlog (the alarm half — **R-rb-85-X10 is re-deferred here; it is `targeted` → rb-87 in the residual ledger and needs re-targeting by the supervisor**). Residuals registered: R-rb-87-BACKLOGAMBIG, R-rb-87-MRLOGDELIVERY (NOTE: its stored `reason` lost the mutant snippet to a shell backtick — the mechanism is "an early `return` as `mr_log`'s first statement"; correct text in the ADR amendment + plan §12), R-rb-87-ADR0243D10, R-rb-87-RUNBOOKEVT.

Follow-up flags (outside touches, not edited): ADR-0243:147 "holds privacy.rs at one" superseded; `ops/observability/rules/recording.rules.yml:203` comment now history; DR runbook §9 could name the evt. Touches-delta: ADR-0238 (amendment), ARCHITECTURE.md (one paragraph), docs/knowledge (two moved stamps). Boyscout-delta: the rb-48 `[E3/guard-first]` message retruth (one hunk). Code graphs: master unchanged until merge — refresh (cbm index_repository + codegraph sync on the main checkout) belongs to the post-merge tick.

Gotchas measured this run: the tester agent's PreToolUse hook blocks cargo (orchestrator must run fmt + RED stages); a register run and a `just ci` in the same worktree collide (shared target dir + spacetime rig) → one false-RED for the verifier; `residuals add --reason` with backticks inside double quotes loses the quoted text (use single quotes).


## 2026-09-12T~21:30Z — rb-81 PR#466 OPEN — the reducer rosters of pvp.rs / battle.rs / economy.rs / ranking.rs pinned closed by one ordinary Rust test each (ADR-0251, closes R-rb-47-ROSTER-PVP); local `just ci` GREEN (ledger X5 + verifier re-run); ledger 9/9 met, 0 deferred; verifier PASS (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/466 (head c33b61d, branch rb-81, worktree `.claude/worktrees/rb-81`); local full `just ci` green on the final tree via `mr-gates check` (E1 4/4; X1 919/919; X2 920/920; X3 lint; X4 docs fresh + ADR header + 0237 reciprocal + ARCHITECTURE + four production files byte-identical + 18 stamps + name set + anti-weakening shapes; X5 CI-GREEN; X8 TWINS-KILLED 4/4) and independently by the verifier (`CI-EXIT=0 WALL=103s`, full recipe list, 99/99 evals incl. live account-e2e); X6/X7 MANUAL ticked with path:line evidence; remote CI running — supervisor owns the merge.**

**WHAT SHIPPED (test-only; zero production Rust).** Four `rb81_<file>_reducer_roster_is_closed` tests (pvp_tests.rs ~5413+, battle_tests.rs ~7977+, economy_tests.rs ~3168+, ranking_tests.rs ~2834+), each over its own file's strippers (ADR-0003), ten clauses in one order: substrate P1-P5 (block-comment tokens, BOTH quote char-literal spellings, raw openers, per-comment-region quote parity, two-order stripper equality) → tail → cfg-roster (+ battle's binding clause on start_wild_battle) → roster walk/count/SET (7/6/2/1, loud roster-parse panic) → attr-any → attr-path → attribute PARTITION per-kind + total (16/12/5/3) → mod-census → include/macro bans → attr-raw LAST (17/13/5/4). MEASURED before planning (T0, red-before.md §1): a plain bare twin SURVIVED 915/915 in pvp/battle/economy; ranking was count-covered by pvp_tests.rs::m17a_rl7 only. GREEN at HEAD by construction (hardening pin, ADR-0224); teeth = the register.

**REGISTER (X6, red-before.md §4): 52/52 rows matched at 04e807f** — 45 M-rows KILLED on their designated label (M10 the view twin INVALID on the first pass under an illegal signature, KILLED on the partition after the `Vec<u32>`/`public` re-run), M24 KILLED-BY-COUNT (919→586), C1-C4 GREEN, D1/D2 RED-DISCLOSED. Measured-and-closed during the slice: plan red-team H1/H2/H3 stripper-polarity bypasses (→ P1/P4), the vacuous squashed-view `mod` census, the tail retarget, the cfg flip; artifact red-team F1/F2 (the 4-byte escaped char literal `'\"'` opens a phantom string in every string stripper — 919/919 + fmt-clean — hiding a bare twin in pvp/battle and an aliased twin in ranking that also evaded m17a_rl7 → P2 4-byte spelling, rows M29a-e) and F3 (spaced `# [` opener → whitespace-tolerant attr-raw, row M30); second-tester B1 (battle's cfg list is a multiset; four decoy dev-gated consts + an un-gated start_wild_battle keep it identical → binding clause, row M31). Verifier's own mutant (raw-identifier `#[spacetimedb::r#reducer]`) compiled and was KILLED on the partition. Not weakened: pure-addition diff, all pins unchanged, hardening only added clauses.

**LENSES:** planner (opus) · plan reviewer + plan red-team (opus, parallel) · /simplify inline · tester (opus; /tmp staging, orchestrator spliced + RED capture) · tests reviewer + artifact red-team + second tester as adversarial critic (opus, parallel on a git-archive snapshot) · hardening implementer (general-purpose opus; 10 hunks, −147 lines net) · reducer-security-auditor (PASS-WITH-NOTES: table-`public` flip unpinned; crate-wide order accounts → privacy → movement → lib) · doc-keeper (opus, worktree-only writes) · final reviewer (docs fact-check: "eight not nine", the ranking count-cover sentence, SOLE-kills overclaim, ADR-0249 parentheticals — all fixed) · verifier (opus, PASS). NOT run: desync-guard (no game-core/wasm/client/movement surface).

**RESIDUALS REGISTERED (4, backlog):** R-rb-81-CROSSFILEMACRO (MED), R-rb-81-CHARLITERAL (MED), R-rb-81-TABLEPUBLIC, R-rb-81-SPECCITE (domain harness). NOTE for the supervisor: `mr-gates residuals add` ignores `--severity` (all four landed MED; ADR-0251 classifies TABLEPUBLIC and SPECCITE as LOW), and TABLEPUBLIC's reason lost the word `public` to a backtick shell expansion in the add command — the ADR D6 text is authoritative. R-rb-80-CRATEWIDEBARE widened in prose (eight un-rostered reducer files + the new-module-file twin).

**ENVIRONMENT NOTES:** a fresh worktree has no `client/node_modules`, so `just lint`/`just ci` fail with `biome: not found` until `npm ci` in client/ (memory card exists); `mr-gates lint` reads the JS `||[]` idiom as a trailing `||` (rewrite as an explicit null test; memory card written); inner double quotes in a `node -e "..."` CHECK are eaten by the shell — use single-quoted JS string arrays; a doc that spells the OLD citation token while describing a repoint trips an X4 "old token absent" clause (memory `doc-prose-about-a-pinned-marker-breaks-it` applied). `just ci` on warm caches is ~103 s wall. Memory cards written: escaped-char-literal-phantom-string, cfg-predicate-roster-must-bind-to-its-item, mr-gates-lint-rejects-js-or-empty-array-idiom.

**MERGE INSTRUCTION:** From `.claude/worktrees/rb-81`, `mr-gates verify --slice rb-81 --budget ≥600` (X8 runs four register rows ≈ 2 min; X5 ≈ 2 min warm); CI-EXIT must be 0; `mr-audit` orchestration as listed above; squash-merge with the PR title (scope: security-authz,ci-gates); `mr-gates residuals close --slice rb-81 --pr 466` closes R-rb-47-ROSTER-PVP; age the four R-rb-81-* rows; reconcile ADR index (0251) / CHANGELOG / ARCHITECTURE; refresh code graphs on the canonical checkout post-merge; delete branch + worktree; harness-side files under memory/projects/gates/rb-81.* + monster-realm-rb-81-plan.md + the residual rows + the three memory cards are uncommitted in the harness repo (supervisor tick commits them); scratch in /tmp (rb-81/, rb-81-redteam/, rb-81-artifact*, rb-81-review-*) safe to delete. ADR next-free: 0252.

---

## 2026-09-12T~16:30Z — rb-80 PR#464 OPEN — deletion gate reaches heal_party / talk / advance_dialogue / dev grant_bait (ADR-0250, closes R-rb-46-ERASEWRITERS); local `just ci` GREEN (ledger X5); ledger 8/8 met, 0 deferred; verifier PASS after two local fixes (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/monster-realm/pull/464 (head d104956, branch rb-80, worktree `.claude/worktrees/rb-80`); local full `just ci` green on the final tree via `mr-gates check` #2 (E1 12/12 with --features dev_reducers; X1 915/915; X2 916/916; X3 lint; X4 docs fresh + ADR header + 7 stamps + 12 test names; X5 CI-GREEN); X6/X7 MANUAL ticked with path:line evidence; remote CI running — supervisor owns the merge.**

**WHAT SHIPPED (8 production lines).** `crate::guards::require_not_deleting(ctx, "<name>")?;` + one comment line at raising.rs:307-308 (heal_party, after the joined lookup), npc.rs:238-239 (talk), npc.rs:339-340 (advance_dialogue — above its four reject-path conversation deletes and the two log::warn! arms), taming.rs:284-285 (dev grant_bait, first check). MEASURED before planning: `apply_quest_trigger` (quest turn-in grants) has exactly ONE caller — `talk`, not `advance_dialogue` (the residual's attribution was wrong; the only shipped quest completes on a Talk step) — so `talk` is gated too (disclosed as ledger X7); and no recruit path calls `grant_item` (the taming call is inside dev `grant_bait`). `dismiss_dialogue` (73-byte body freeze) and `attempt_recruit` (in-battle; `consume_one(` once) classified OPEN under PRV1-10 and pinned; `train`/`care`/`essence_train`/`consume_crystalized_essence` deferred BY SCOPE (R-rb-80-RAISINGWRITERS; their only precedented placement is unreachable by the native-host matrix), visible via a file-wide gate count of exactly 1. guards.rs byte-identical. Twelve rb80_ tests (RED at HEAD on their predicted clause, `memory/projects/gates/rb-80.red-before.md`): 4 native-host five-state matrices, 4 source pins (rb-79 whole-prefix freeze per site + return census + anchors + tag; clause W for advance_dialogue), 3 per-file censuses (bare-name 1/2/1; reducer roster set + wire-name-twin ban + `#[` attribute budget 8/5/6 + `::reducer`-is-bare; `#[cfg` 2/1/3; write verbs; comment-quote and raw-string-opener substrate preconditions), 1 guards_tests control (rb78_region_anchors 7→10; ADR-0248:173's guards_tests.rs:6415 citation kept true by line-count-neutral doc edits).

**REGISTER (X6): 28/28 M-rows KILLED, 0 INVALID; C1/T1/C3 CONTROL-GREEN.** Survivors registered, not pinned: C3 below-gate `let me =` rebinding (R-rb-80-BELOWGATE), red-team M24/M25 raw `ctx.db.player_wallet()`/`ctx.db.inventory()` writes below a gate (R-rb-80-RAWWRITE — `evals/run.mjs` has no filter and the ADR-0081 evals have no main guard, so their compensating-control status is unmeasured), verifier V2 fn-pointer delegation to `spend_currency` (R-rb-80-FNPTRDELEGATE). Verifier V1 (plain `r"\"` raw string inverting the raising/npc blankers) SURVIVED at 172ef50 and is KILLED at d104956 by the widened preconditions.

**LENSES:** planner (opus) · plan reviewer + plan red-team (opus) · /simplify inline · tester (opus; /tmp staging, orchestrator spliced + RED capture) · artifact reviewer + artifact red-team (opus; 32 hardening hunks) · impl reviewer (found the un-regenerated knowledge bundle + ADR-0184:167 — both fixed) · impl red-team · reducer-security-auditor (PASS-WITH-NOTES: enumerated eight present §4.7-selected ungated reducers outside the censused files — join_game the sharpest (PRV1-8 operator-blocked), request_data_export deliberately open — folded into R-rb-80-CRATEWIDEBARE) · verifier (opus; FAIL → three local fixes → gates re-run green). NOT run: desync-guard (no game-core/wasm/client/movement surface), doc-keeper (LANDING-PATTERN flag `/tmp/mr_warn_rb-80` appeared mid-slice; ADR/ARCHITECTURE/knowledge written by the orchestrator, fact-checked by the impl reviewer).

**RESIDUALS REGISTERED (9, all backlog):** R-rb-80-RAISINGWRITERS (MED), R-rb-80-BELOWGATE (MED), R-rb-80-RAWWRITE (MED), R-rb-80-FNPTRDELEGATE (MED), R-rb-80-CRATEWIDEBARE (MED), R-rb-80-DIALOGUECOMMITMENT (LOW), R-rb-80-CITEDRIFT (LOW), R-rb-80-REASONSCOPE (LOW), R-rb-80-DEVFEATURETEST (LOW).

**ENVIRONMENT NOTES:** `evals/run.mjs` ignores argv (ran the whole suite twice while measuring M24); most eval files print nothing when run directly. nextest pads single-digit indices (`( 4/14)`) — the register runner's `fails=` regex was fixed. `mr-gates residuals add --severity` works. The reducer-security-auditor agent definition still carries the stale 1.12.0 crate premise (harness-side). Memory cards written: evals-run-mjs-has-no-filter, string-blanker-substrate-order-and-raw-openers, reducer-roster-census-blind-to-aliased-attribute, nextest-fail-line-padded-index.

**MERGE INSTRUCTION:** From `.claude/worktrees/rb-80`, `mr-gates verify --slice rb-80 --budget ≥2400`; CI-EXIT must be 0; `mr-audit` orchestration as listed above; squash-merge with the PR title (scope: security-authz,economy-quests); `mr-gates residuals close --slice rb-80 --pr 464` closes R-rb-46-ERASEWRITERS; age the nine R-rb-80-* rows; reconcile ADR index (0250) / CHANGELOG / ARCHITECTURE; refresh code graphs on the canonical checkout post-merge; delete branch + worktree; scratch in /tmp (rb-80/, rb-80-redteam*, rb-80-*.log, rb-80-review already removed) safe to delete. ADR next-free: 0251.

---

## 2026-09-12T~13:00Z — rb-79 PR pending — propose_trade gate prefix byte-frozen (ADR-0249, closes R-rb-46-TRADINGCFG)

**TERMINAL STATE: slice close readied; local `just ci` green; PR: https://github.com/mdrewt/monster-realm/pull/463 (opened 2026-09-12T05:27Z at 11f7d1d; local full `just ci` green standalone — 2298/2298 Rust, 108/108 client files, 99/99 evals; ledger 6/6 met; remote CI running — supervisor owns the merge; post-merge graph re-index owed on the canonical checkout); ledger 6/6 met, 0 deferred; verifier PASS.**

**RESIDUAL HALF-CLOSED.** R-rb-46-TRADINGCFG was registered with three remedy parts: statement-boundary, `#[`/`cfg!(` bans, and early-exit clauses. rb-47 (ADR-0237, PR #429, be3ff53) closed the attribute half — `rb47_propose_trade_gate_has_no_attribute_or_cfg_escape` + file-wide `#[cfg` count killed M1/M2 (register rows at HEAD), so ADR-0237:203-204's claim that rb-47 closed the residual was half-right. ADR-0248 D6 conversely stated rb-79 owns the attribute class for trading.rs:252, also half-right — both corrections appended as reciprocal header lines (ADR-0249 `**Amends:** ADR-0237` + `**Amended-by:**` back-link). The early-exit half stayed open and is material: seven macro-free predecessors placed directly above the gate were CI-clean at be3ff53 — fmt 0, clippy 0, 902/902 tests — a sender-keyed `if me != crate::WILD_IDENTITY { return Ok(()); }`, a file-scope `cfg!(test)`-valued const with early Ok, a plain false const, a `return Err(e).or(Ok(()))` combinator, a rejection-SHAPED `return Err(e);` (which rb-46's clause I census reads as 1==1 and PASSES), delegation to an ungated twin (rb-46 shape 1), and a `WILD_IDENTITY` shadow with no `return` at all — the mechanism is ADR-0236/ADR-0248's recorded fact: the native host's sender IS `WILD_IDENTITY`, so real players route around the gate while tests keep observing it fire.

**WHAT SHIPPED:** `rb79_prefix_verdict` pure verdict (four clauses over the file's own strippers, no third stripper per ADR-0003; [rb79/twin], [rb79/extract], [rb79/gate-count], [rb79/prefix] — the whole squashed prefix above the gate equals a hand-derived literal byte for byte, a runtime tie with three split points verifies it, mechanism is ADR-0166 D3 + ADR-0227 D3/D4 binding never pasted); two live tests in trading_tests.rs (5956-6092 a 16-row fixture matrix, 6150-6241 the oracle over real trading.rs with escrow-insert control + the seven-part runtime tie + the D5 note-presence clause); in-place rewrite of trading.rs:247-251 (the Guard-1a comment block + blank line, five lines in and five out, zero line shift, all six knowledge-bundle stamps unmoved — `just knowledge-check` clean, four content claims preserved, no double quote/macro/cfg/block-comment markers/r#/apostrophe/production needle); ADR-0249 (Extends ADR-0236 with reciprocal `Extended-by:` back-link appended to 0236's header; reciprocal `Amended-by:` appended to ADR-0237's header); ARCHITECTURE.md one tail entry (ADR next-free = 0250).

**REGISTER: 12/12 M-rows KILLED, 0 INVALID.** M3-M8 and M10 (the seven survivors of the be3ff53 pre-slice measurement, red-before.md §1) are killed by the rb-79 live oracle ALONE; M1/M2 already closed by rb-47, M9 by m22-s5 ordering (both pre-existing); M13 (wire-name rename — an ungated reducer published under this one's wire name with a `fn propose_trade` demoted to dead — GREEN on every rb-79 clause by design, KILLED by `rb47_trading_reducer_roster_is_closed` and the m22-s5 gated-reducer censuses) is a disclosed dependency measured at register row M13, not re-pinned here per ADR-0249 D7. T1 (note-deleted row) red on the note clause as predicted. Controls C1/C2/C3 (comment above gate, early Ok BELOW gate, trailing-comma gate spelling) green. Tree byte-identical after every row.

**LENSES:** planner · plan reviewer + plan red-team · /simplify inline · tester (static Bash; orchestrator executed RED/bite-proofs) · tests reviewer + artifact red-team · impl reviewer · reducer-security-auditor (PASS-WITH-NOTES; see environment below) · desync-guard · verifier · doc-keeper. `feature-isolation`, `feature-client`, `wasm-pack-rebuild` NOT run (no game-core/client/wasm/movement surface).

**RESIDUALS REGISTERED:** R-rb-79-BELOWGATE (HIGH — below-gate delegation to an ungated helper skips ownership and all later checks; unowned, measured row M8; unresolved in trading.rs:252's scope; same class live at pvp.rs:818/1004, ranking.rs:152 per R-rb-78-PLAINRETURN); R-rb-79-ADR0248D6 (LOW — stale attribution at ADR-0248:286-289 + code-comment twin at guards_tests.rs:5220, both outside this slice's touches — registered rather than edited); R-rb-79-LIBRS89 (LOW — three pre-existing citations place `WILD_IDENTITY` at lib.rs:89, declared at lib.rs:95; rb73_session_tests.rs:268/476, accounts_tests.rs:20177); R-rb-79-XFILEINSERT (LOW — nothing pins crate-wide that `trade_offer().insert(` occurs once, trading.rs:408 today; trading censuses are trading.rs-scoped, so a new reducer escrow-inserting a trade in a third file is outside all of them).

**ENVIRONMENT NOTES:** nextest fail-fast cancelled the M13 full run at 418/421, so the rb-47 roster kill needed a filtered re-run to confirm M13 death — tree clean, both runs logged. The reducer-security-auditor agent definition carries a stale premise that the module builds against spacetimedb crate 1.12.0; it is 2.8.1 per ADR-0197 — a harness-side agent-definition fix, not a slice issue.

**MERGE INSTRUCTION:** From `.claude/worktrees/rb-79`, run `mr-gates verify --slice rb-79 --budget ≥2400`; CI-EXIT must be 0; `mr-audit` with orchestration as listed above; squash-merge with Conventional Commit title matching the ADR decision (scope: security-authz,ci-gates); `mr-gates residuals close --slice rb-79 --pr <number>` closes R-rb-46-TRADINGCFG; age R-rb-79-{BELOWGATE,ADR0248D6,LIBRS89,XFILEINSERT}; reconcile ADR index (0249) / CHANGELOG / ARCHITECTURE; refresh code graphs on canonical checkout post-merge; delete branch + worktree; scratch in /tmp (rb-79/, rb-79-review-*, rb79-artifact, rb79-verify, rb79-mutants*) safe to delete. ADR next-free: 0250.

---

## 2026-09-11T~21:40Z — rb-77 PR#461 OPEN — crate-root module wiring + guards.rs cfg-freedom gated by one ordinary Rust test (ADR-0247, closes R-rb-46-LIBRSMOD); local `just ci` GREEN (ledger X4); ledger 6/6 met, 0 deferred; verifier PASS (SUPERVISOR OWNS THE MERGE)

## 2026-09-21T~04:20Z — m25-s0 PR#130 OPEN — ADR-0034 dated amendment (DECISION #496) + security-threat-model §7 corrections (harness, doc-only); local gate green; ledger 11/12 met, X12 DEFERred → wontfix; verifier PASS (SUPERVISOR OWNS THE MERGE)

**TERMINAL STATE: PR https://github.com/mdrewt/claude-harness/pull/130 (head `d25b92b`, branch `feat/m25-s0-threat-model-adr0034-amend`, worktree `.claude/worktrees/m25-s0`, base main `3b1b8eb`). Harness has no remote CI; local gate = harness `just ci` exit 0 (106/106 tests, lint/research-gate/adr-gate clean) + mr-selfcheck no-regression (X11). Main checkout untouched on `main`.** No new ADR number (in-place amendment, ADR-0033/0175 precedent). No `evals/*.eval.mjs` (ADR-0224). Diff = exactly the two declared `touches:` paths (+146/−19): `specs/monster-realm-v2/adr/0034-security-audit-gate.md` (append-only `## Amendment — 2026-09-21 (M25 ceremony corrections; DECISION issue #496)`; lines 1-32 byte-identical) and `specs/monster-realm-v2/security-threat-model.md` (all six §7 corrections; header `M0–M25`/`Revised: 2026-09-21`; §0/§2 byte-identical). touches-delta none; boyscout-delta none; Items: none.

Ledger `memory/projects/gates/m25-s0.gates.md` (0 seeded → X1–X12 authored): 11 met with evidence via `mr-gates check --slice m25-s0 --timeout 600` from the worktree; **X12 (strict `SELFCHECK-OK`) DEFER → wontfix** — at base 3b1b8eb mr-selfcheck already prints `SELFCHECK-FAIL residual-unpromoted` (promote step stalled: R-rb-85-EXPORTADMIT/MODCENSUS/X9 at 3d) + `SELFCHECK-FAIL residual-stale` (19 rows ≥14d) and no SELFCHECK-OK; supervisor-owned; X11 gates no NEW selfcheck key (colon-optional parser). **Supervisor actions:** (1) `mr-gates verify --slice m25-s0` — probes resolve worktree→harness-main by absolute path (never cwd), so the project-repo cwd default is safe; X11 needs ~40 s warm, a cold biome npm cache can exceed the 120 s check timeout → pre-warm `just lint` once; (2) run the residual promote step (X12 greens on its own; registry 50 open vs cap 12); (3) after merge: mark M25 §4 row S0 done, clear `PLAN.md:930`'s ADR-0034 BLOCKER note; (4) delete the worktree (`.claude/worktrees/` is NOT gitignored in the harness — a `git add -A` on main would swallow it; pre-existing).

Doc-truth decisions (both plan lenses): every count the M25 spec carried was already false against the live tree (48/50 reducers → 52/54; 38 tables 18/20 → 43, 18/25; 5-entry view list → 7) and was DE-QUANTIFIED; every `file:line` cite replaced by file+symbol (the spec's own `pvp.rs:806/827`, `schema.rs:567/654/847` rotted); S2/S5/S6/S7 artifacts future-framed; `<bdi>` scoped to its one real site (leaderboard); `validate_name` order trim→NFC; the Considered-alternatives parenthetical carrying the dead RLS premise is RECORDED as a residual, not superseded (the operator authorized exactly two clauses). Gate X8 (cited symbols must exist at project PINNED_SHA `3937c588a711…`) caught a real miscitation on its first run: `buildPvpViewModel` → `buildPvpChallengeViewModel` (the spec's SEC-13 carries the wrong name).

Lens record: planner(opus) → reviewer+red-team on the plan (opus, parallel) → plan v2 assembled from origin/main + approved regions (`gates/m25-s0.assemble.py` → `m25-s0.expected.{adr,threat-model}.md`) → tester(opus, 3 rounds: `m25-s0.{lib,texts,x1..x11}.mjs`, RED-first with predicted first-failure ids, X9 teeth 34 sha-pinned mutants) → orchestrator applied the byte-copy (≠ tester) → red-team on probe mechanics (opus: symlink, committed-tree, post-merge commit resolution, fail-open selfcheck parse, block-shape under ws-collapse — all fixed) → reviewer on the delivered diff (opus: MAJOR "migration completeness" framing → §8-1 completeness-of-declaration; 6 MINORs fixed) → verifier(opus) PASS (both cwds identical, node 18 + 24, own mutants bite) → doc-keeper (memory `monster-realm-m25-s0`). Domain auditors N/A (no code). Code graphs: no symbol change — refresh on the canonical checkout post-merge as usual.

Follow-up flags (outside touches, in the PR body): M25 spec §2.8 `:55-62` cite → now 84-99; stale spec counts/lines above + SEC-13 name + §2.5(1) omits `challenge_id` + :218-220 inverted trade-off; **ADR-0224 vs the M25 spec's S2/S7 NEW-eval deliverables — delivery-vehicle decision needed before S2 is briefed**; ADR-0034:16-17 + `spec-corpus-review.md:74` + `milestone-loop-prompt.md:289` still carry "RLS-leak verification … headline check" (one residual, likely an mr-ask-drew follow-up); M24 §2.7 `<bdi>` overclaim; `M19-social.spec.md:5-6` stale cross-ref to the deleted chat row; M20 OBS-47/OBS-15 back-pointer.

**Resume point if interrupted:** nothing further to implement — terminal state reached (PR open + local gate green + ledger resolved); supervisor merges.

## 2026-09-21T22:03:21Z — 22:00Z tick — launched rb-110 (EXPORT_REAP_MAX_DELETE_PER_TICK rename)
Gates 0-2 clean: no per-run locks, no chain-owner mutex, no resident IDE claude session growing, no non-wrapper writes in last ~6min, HOLD-NONE (mr-hold status not queried directly but no .native-supervisor-disabled file present). One stray non-claude process found (pid 221114, a cargo test binary --exact privacy::privacy_tests::rb109_host_iterators_close_alias_free_and_refill_on_oversized_rows, running from the now-deleted rb-109 worktree, etime ~2h4m) -- rb-109 already merged @2f5ae9d at the prior 20:59Z tick and its worktree/branch were cleaned per that tick's record, so this is an orphaned leftover test process holding no lock and blocking nothing (free -g shows 15G free / 40G available); left alone this tick (not a doctrine-covered cleanup target, no memory pressure) but flagged in case it recurs. Both repos fetched: harness 1 commit ahead of origin/main (pushed by verifying git status was already clean going in), project up to date with origin/master @2f5ae9d. Master CI (ci+e2e) confirmed live GREEN on 2f5ae9d via gh api check-runs. Separately, the scheduled Nightly workflow failed on changelog-freshness (run 35607167983, issue #499 auto-opened by the notify job per ADR-0200) -- per docs/nightly-red-response-policy.md this job's response is 'queue a ledger-refresh slice as the next slice', but this is NOT the master-CI-red gate (ci+e2e are green) so it does not preempt this tick's action; noting the recurring pattern (issues #491/#465/#433/#425/#381, roughly weekly since 2026-08-28) as worth an eventual dedicated slice or operator attention since 'next slice' has evidently never actually been queued for it. No open PRs, no parked slices (park_counters only shows stale 14r-e:1). Gate 3: mr-gates residuals list --unclaimed showed 22 open, all MED, oldest at 2.776 days -- under t1_promote_days=3, so no residual outranks queue[]/new work this tick. queue[] had 4 entries; re-verified queue[0]=rb-110 live: spec section exists (M-residual-backlog.spec.md:101, from rb-86 READCAP-NAME, not blocked, no after: deps outstanding, not already merged/present in server-module/src/privacy.rs). Classified per work-selection-scope: this is a real crate-correctness/doc-accuracy defect (a misnamed pinned constant), not eval-tooling-only, so it proceeds as a normal promoted-residual launch, not a wontfix disposition. Confirmed source location: server-module/src/privacy.rs:1674 (EXPORT_REAP_MAX_DELETE_PER_TICK, in fact bounds the READ window per its own neighboring comments at :1662-1692) with ~10+ pin sites in privacy_tests.rs. Launched rb-110: fable@xhigh (HARD tier -- touches server-module/src/privacy.rs reducer-adjacent constant + its test pins), ADR-267 pre-allocated (pure rename/doc-consistency, no schema/behavior change), touches={server-module/src/privacy.rs, server-module/src/privacy_tests.rs, docs/adr/0238-*.md, docs/adr/0231-*.md, ARCHITECTURE.md}, repo=project, base=origin/master, PR target mdrewt/monster-realm. First mr-spawn attempt failed BRIEF-RENDER-FAILED (vars.json was missing the required 'tier' field -- added tier:hard and relaunched successfully). mr-spawn LAUNCHED cleanly: leader=361102 claude_pid=361105, brief_bytes=16126, gates seeded (criteria=0 -- residual-backlog slices don't carry EARS SHALL criteria the seeder extracts, same NO-LEDGER-shaped situation as other rb-* slices; hand-adjudicate at merge against the residual's own EARS line and the proof-of-teeth requirement). No fan-out candidate assessed beyond this single launch (single-action tick; N=1 live after this). Budget governor NORMAL throughout (d7=$1738.46/$2783 eff 62%, fable_d7=$895.07/$2298 39%, fable_ok=true, comfortably under the 90% guard). No BLOCKERs. queue[] now has rb-111/rb-114/rb-115 remaining (not re-verified this tick beyond the fast-path entry consumed).
## 2026-09-21T21:01:32Z — rb-109 merged (PR#500)
Squash-merged to master @2f5ae9d (base 786c222). Live re-verify at merge time: gh pr checks 500 both ci+e2e PASS, mergeStateStatus CLEAN/MERGEABLE. mr-gates verify --slice rb-109 --json: CLEAN, 4/4 met, 0 unmet, 0 deferred, spotcheck X2 read adversarially and held (SUITE-GREEN cargo nextest default+dev_reducers counts agree). mr-audit (--repo as filesystem path, not gh slug -- first invocation with the gh slug produced a spurious AUDIT-ERROR/exec-error:FileNotFoundError on gating+acceptance, re-ran with the local path and got real results): orchestration CLEAN (9 agent calls, full role set incl. reducer-security-auditor+verifier). gating_advisory FLAGGED (removed_or_modified_asserts=1) -- read the full diff: this is the range-scan/index-delete expansion (5->7 modelled syscalls, comment/panic-message rewording caught the assert-count heuristic), no test assertion actually deleted or weakened. Adjudicated benign, not a BLOCKER. Residuals: 3 filed (R-rb-109-ORDERMODEL, R-rb-109-WRITEWALLPROSE, R-rb-109-WALLTOOTH, all MED/unpromoted -- ages 0d, nowhere near t1_promote_days=3), mr-gates residuals close --slice rb-109 --pr 500 closed 1. Worktree .claude/worktrees/rb-109 removed, local+remote rb-109 branches deleted (remote auto-deleted on merge). Master CI (run for merge commit 2f5ae9d) was in_progress at record time -- next tick/event re-verifies before any new launch off this master sha. Governor NORMAL (d7=$1737.48/$2783, fable_d7=$895.07/$2298, fable_ok=true). No BLOCKERs, no rate-limit event. This was the tick's sole mutating action (merge only, no composite launch -- master CI not yet confirmed green).
## 2026-09-21T18:02:55Z — rb-109 LAUNCHED — export_bundle reaper native-execution proof (R-rb-85-X9)
Queue fast-path: rb-109 (promoted residual R-rb-85-X9, deferred 2026-09-18 from rb-85) re-verified live — spec heading present, non-blocked, source rb-85 already merged (PR#473, 2026-09-18). Launched fable@xhigh/hard tier (server-module native-host + privacy/export-reaper surface qualifies HARD: security/privacy data-deletion correctness, server-module schema-adjacent). touches: server-module/src/native_host_tests.rs, server-module/src/privacy.rs, server-module/src/privacy_tests.rs. Target: RED/GREEN proof-of-teeth test executing the bounded reaper read+delete over an oversized synthetic export_bundle population via datastore_index_scan_range_bsatn / datastore_delete_by_index_scan_point_bsatn in the in-memory native host (ADR-0224 — ordinary Rust test, no new evals/*.eval.mjs). run_id=mr-spawn-20260921T180238Z-120450, leader pid 120506, claude pid 120509. No fan-out this tick: rb-110/rb-111 (next in queue) both carry 'touches: (inherit from source slice — REVIEW)' from different, unverified source slices (rb-86) — ambiguous/undeclared touches treated as colliding per doctrine, serialized rather than risking an overlap. Gate-3 stale-residual check: 19 unclaimed residuals, all MED severity, ages 0.09-2.75d — none past t1_promote_days=3, so no promote action needed this tick. 3 older residuals (R-m22-s3b-X18 23d… wait 19d, R-rb-10-X6/X7 23d) are already status=promoted/targeted (not unclaimed) — already tracked, not actionable here. Budget NORMAL (d7 $1643.96/$2783=59%, fable_d7 $802.46/$2298=35%, fable_ok=true). Master CI green (786c222). No BLOCKERs.
## 2026-09-21T17:04:15Z — 17:00Z tick — promoted rb-87 residuals (rb-114..rb-119), reconciled stale 16:00Z bookkeeping
Native tick rid=native-20260921T170009Z-100763 (src=cron). Gates 0-2 clean: no locks/mutex, HOLD-NONE, both repos synced (harness main cf2e986, project master 786c222 CI success), no open PRs. Found the prior 16:00Z tick (rb-108 merge, PR#498) had left mr-state.json/handoff/rb-108.plan.md uncommitted on harness main -- committed as 642b132 (its content later folded upstream by the PR#133 squash-merge, since the residual-promote branch was cut from a checkout that already had it staged). Gate 3: mr-gates residuals list --unclaimed --json showed 6 unpromoted residuals past t1_promote_days=3 (R-rb-87-ADR0243D10, BACKLOGAMBIG, MRLOGDELIVERY, RUNBOOKEVT, X9, X10; ages 3.18-3.20d) -- all real defects (observability dead-man delivery proof, reaper backlog-signal ambiguity, stale ADR-0243 sentence, stale runbook, operator-alarm half of the reaper dead-man design), none eval-tooling-only, so classification left all 6 promotable rather than wontfix. Promoted all 6 via mr-gates residuals promote -> rb-114..rb-119 in specs/monster-realm-v2/M-residual-backlog.spec.md. queue-add succeeded for rb-114/rb-115; queue was already at its 5-entry cap for rb-116..rb-119 -- left unqueued (they remain launchable via the full PLAN-derivation path since they are no longer unpromoted, so this costs nothing per doctrine). Shipped the spec diff as a doc-only chore PR: https://github.com/mdrewt/claude-harness/pull/133 (chore/residual-promote-20260921T170234Z), gh pr merge --squash --auto, landed at cf2e986. No slice launched or merged this tick -- past-t1_promote_days residuals outrank queue/PLAN work per doctrine, and promoting them was this ticks one action. Budget governor NORMAL (d7=$1642.61/$2783, 59% effective; fable_d7=$802.46/$2298, fable_ok=true). No rate-limit event. No BLOCKERs. Next tick: front of queue is rb-109 (fast path); rb-116..rb-119 are real launchable PLAN-derivation candidates once queue drains.
## 2026-09-21T16:13:08Z — rb-108 merged: M22 module census cfg(test)-attribute rule (ADR-0266)
PR#498 squash-merged 786c222 (master fast-forwarded, worktree/branch cleaned). mr-gates verify: 6/6 CLEAN, spotcheck reverified fresh + agrees. mr-audit: orchestration CLEAN (12 agent calls, full lens roster incl reviewer/red-team/verifier); gating_advisory FLAGGED on suppressions_added=3 -- adjudicated FALSE POSITIVE: the 3 `#[allow(...)]` hits are string literals inside synthetic test-fixture data (Vec<&str> feeding the parser under test), not real suppressions on any assertion or production code; diff is test-file + ADR + ARCHITECTURE + DIGEST only, zero production change. Residuals: closed R-rb-85-MODCENSUS (1); 2 new MED backlog residuals registered by the run (R-rb-108-SUFFIXCOPIES: 3 sibling module censuses still exempt by name-suffix in trading_tests.rs/guards_tests.rs/evolution_tests.rs; R-rb-108-STALEPROSE: native_host_tests.rs:26-29 narrates the retired suffix rule). Separately at gate 3: master HEAD (985bc62, rb-107 commit) had a RED CI run -- root-caused to a transient GitHub infra failure (sbom-action's syft v1.42.3 release download returned HTTP 504), not a code defect; cargo-audit/semgrep/ci/e2e all otherwise green. Reran the failed job (gh run rerun 35614141998 --failed); retry was still in-flight at tick end, not yet resolved. Did not treat as a real master-red BLOCKER since root cause was confirmed infra, not code -- proceeded to the rb-108 merge. Next tick: re-verify that retry resolved (and the new CI on rb-108's own merge commit 786c222) before trusting master green; if the syft 504 recurs, that's a pattern worth a BLOCKER/ask-drew about the SBOM step's flakiness, not another blind retry.
## 2026-09-21T15:55:45Z — rb-108 PR#498 OPEN — M22 module census exempts by `#[cfg(test)]`, never by a `tests` name suffix (ADR-0266, closes R-rb-85-MODCENSUS); local `just ci` GREEN (CI-EXIT=0 on 625d0ee); ledger 6/6 met, 0 deferred; verifier PASS (SUPERVISOR OWNS THE MERGE)
https://github.com/mdrewt/monster-realm/pull/498 (branch rb-108 @ 625d0ee, worktree .claude/worktrees/rb-108, 9 wip/docs commits, squash on merge). Local full `just ci` GREEN on the final tree: /tmp/rb-108-ci.log CI-EXIT=0 (workspace nextest 2442/2442, 99/99 evals, client 132 files/3381 tests, fmt/clippy/biome/security/observability clean). Ledger memory/projects/gates/rb-108.gates.md 6/6 met (0 seeded → X1–X6 authored; X5/X6 = anchor-line mutants reinstating the suffix predicate / the old strings-then-comments pipeline, KILLED), re-executed from scratch on 625d0ee. PR body carries the byte-exact `mr-gates render --format pr` line, `Items: none`, `touches-delta:` (ADR-0266 + DIGEST regen, ARCHITECTURE paragraph), `boyscout-delta: none`. Delivered (one test-only file, zero production change): accounts_tests.rs — pure seam `m22_declared_mod_names_in(src)` (upward attribute walk; exempt iff a squashed attribute == `#[cfg(test)]` exactly; other cfg forms + same-line runs = production; `r#name` reported bare) over a NEW single-pass newline-preserving blanker `m22_blank_for_mod_scan` (nested block comments, strings, raw strings with ANY hash count, char literals; lifetimes untouched); three rb108_ tests with exact-vec oracles covering 5 MEASURED CI-clean escapes (stray `"` in a `//` comment gluing an earlier cfg(test) onto a later mod; `'"'` char-literal boundary inversion; nested comment with outer `*/` on the mod line hiding it; 7-hash raw string blanking to EOF under the 6-hash cap; raw identifier skipped) + the verifier's zero-hash raw-string mutant. RED record memory/projects/gates/rb-108.red-before.md. Lenses: planner → reviewer ∥ red-team (plan; red-team #1/#2 stray-quote/nested-comment merge → the blanker; #3 forgeable sed anchor → X5/X6 pre-count) → /simplify (dropped a predicate helper) → orchestrator stage-1 extraction (disclosed) → tester (opus; NO Bash — orchestrator ran fmt/clippy/RED) → tests red-team (`contains("cfg(test)")` cheat → paired-attribute fixture; lifetimes) → implementer (0 test edits, 2 rounds) → artifact reviewer ∥ red-team-writes-the-cheat (7-hash HIGH) ∥ reducer-security-auditor PASS ∥ desync-guard N/A (haiku) → verifier PASS (own mutant survived → fixture added, replayed KILLED) → doc-keeper (DISCARDED: wrote to the MAIN checkout, not the worktree, with factual errors and an invalid Subsystems vocab — main restored by file-write, no git mutation; ADR/ARCHITECTURE/2 memory cards rewritten by the orchestrator). Residuals registered (after the lenses): R-rb-108-SUFFIXCOPIES (trading_tests.rs:5061 / guards_tests.rs:3365 / evolution_tests.rs:6608 still exempt by suffix), R-rb-108-STALEPROSE (native_host_tests.rs:26-29 narrates the retired rule) — both backlog; R-rb-85-MODCENSUS closes on merge (`mr-gates residuals close --slice rb-108 --pr 498`). Gotchas measured this run: an ORPHANED `just ci` from rb-107's supervisor verify (ppid 1, cwd = the deleted rb-107 worktree, hung 81 min in validate.mjs → `docker run … caddy hash-password`) was found and killed by PID before launching this slice's CI; the hand-authored ledger needed `EVIDENCE: pending` placeholders (first check printed 6× PASS but wrote nothing); giving the write-capable red-team and the reviewer the SAME pinned clone produced a phantom BLOCKER (memory card shared-pinned-clone-lenses-tear-each-other). SUPERVISOR TODO at merge: (1) `mr-gates verify --slice rb-108` FROM the worktree (X5/X6 rewrite accounts_tests.rs and restore from an in-memory copy — never run concurrently with a `just ci`); (2) commit harness-side edits — ledger, rb-108.plan.md, rb-108.red-before.md, this handoff, mr-residuals.jsonl (2 new rows), 2 memory cards (attribute-walk-needs-newline-preserving-blanker, shared-pinned-clone-lenses-tear-each-other) + MEMORY.md pointers; (3) mr-state.json adr_next_free is already 267 — verify; (4) mark M-residual-backlog.spec.md#rb-108 done with the PR ref; (5) post-merge cbm index_repository + codegraph sync on the canonical checkout; (6) /tmp/rb-108-review-*, /tmp/rb-108-verify-* (shared-object clones), /tmp/rb-108-review-target and /tmp/rb-108-*.log are disposable and LEFT IN PLACE (the bash guard blocks recursive rm) — delete at leisure. No BLOCKER. Worktree left in place for the merge.

## 2026-09-21T14:44:59Z — rb-107 merged: export admission cap (ADR-0265)
PR#497 squash-merged to master @985bc62 (rb-107: account-tiered global live-row admission cap on request_data_export, closes R-rb-85-EXPORTADMIT). Pre-merge adjudication found and resolved a false alarm: mr-audit's fresh acceptance re-run flagged X2 EVIDENCE-MISMATCH (944-947/1008 tests reported as not run due to a failure in accounts_tests::g5_writes_only_owned_tables) even though the run's own recorded evidence showed 1008/1008 passing. Root-caused live: a stale incremental-compilation artifact in the rb-107 worktree's target/ dir (accounts.rs/accounts_tests.rs are byte-identical to master and untouched by this slice's diff, so the failure could not be a real code regression — confirmed by forcing a touch+rebuild, which reproduced a clean 1008/1008 pass reliably across repeated runs). Independently reverified live post-rebuild: X1-X3 (full + dev_reducers suites green), X6 (remote CI ci+e2e both pass), X7/X8 (mutation register — reran the X8 4-mutant subset live, 4/4 killed on designated labels, tree restored byte-exact), orchestration audit CLEAN. gating_advisory flagged 1 modified assert (a nested-return count pin moved 1->3 in privacy_tests.rs); read the full diff — legitimate re-truthing attributed to the 2 new admission-gate early-exits this slice adds, independently covered by a new dedicated pin (rb107_reducer_admits_twice_before_the_first_write [rb107/exit-shape]), not a weakening. mr-audit's disposition-scan findings (missing/orphan spec dispositions) are pre-existing corpus-wide items in files this slice never touched — not blocking. Residual R-rb-85-EXPORTADMIT closed via mr-gates residuals close --pr 497. Worktree + branch + per-run lock cleaned (mr-unlock reaped rb-107, session_leader 3708319 confirmed dead). Lesson for future ticks: an acceptance-gate EVIDENCE-MISMATCH is a real fraud signal in general, but is also exactly what a stale worktree build cache produces — always force a clean rebuild before trusting a FLAGGED re-run verdict on a Rust gate, rather than treating the mismatch alone as proof the slice regressed.
## 2026-09-21T14:20:27Z — rb-107 PR OPEN — account-tiered GLOBAL live-row admission cap on request_data_export (ADR-0265, closes R-rb-85-EXPORTADMIT); local `just ci` GREEN twice (ledger X6 + verifier logged run); ledger 8/8 met, 0 deferred; verifier PASS (SUPERVISOR OWNS THE MERGE)
https://github.com/mdrewt/monster-realm/pull/497 (branch rb-107 @ 5148235, worktree .claude/worktrees/rb-107, 5 wip/feat/docs commits, squash on merge). Local full `just ci` GREEN: X6 inside `mr-gates check` (~14 min wall) and the verifier's logged run /tmp/rb-107/logs/verifier-ci.log (CI-EXIT=0: workspace nextest 2439/2439, 99/99 evals incl. account-e2e live export 268 chunks/17 tables, client 132 files/3381 tests, fmt/clippy/biome/tsc/secrets/observability clean). Ledger memory/projects/gates/rb-107.gates.md 8/8 met (0 seeded → X1–X8 authored; X1 roster-shaped with full nextest paths + --run-ignored all; X7 MANUAL ticked at rb-107.x7-register.md:53; X8 ROW-IDS=[M2,M6,M7,M16]), 0 deferred, 0 unmet. PR body carries the byte-exact `mr-gates render --format pr` line, `Items: none`, `touches-delta:` (ADR-0265 + DIGEST regen, ARCHITECTURE paragraph, 3 knowledge files), `boyscout-delta: none`. Delivered: privacy.rs +86/−8 (two gates after the purge: pre-gate vs EXPORT_MIN_BUNDLE_ROWS=EXPORTERS.len()=17 before the walk, exact gate vs total before the insert loop; EXPORT_LIVE_ROW_CAP = 256×168 = 43 008 derived from the reaper constants; EXPORT_ANON_LIVE_ROW_CAP = cap/2 = 21 504 for callers with no account row via crate::accounts::is_account_holder (ADR-0189 D2 SSOT); pure export_admission_open(live,requested,cap) exact+saturating; one reason export_reject_admission; Table::count() O(1) tx-aware — first use of datastore_table_row_count in the module, proven by `just build` + account-e2e), six rb107_ tests (44 clause labels) + 8 pin re-freezes as attributions, RED record rb-107.red-before.md (Stage 1 1004/1001/3 both rounds; Stage 2 E0425×9), register rb-107.mutants.py 40/40 KILLED + 3/3 controls (two rows re-run after a runner span bug, recorded), ADR-0265 (Extends 0238/0226/0189, Amends —), DIGEST regen, ARCHITECTURE paragraph, knowledge regen. Lenses: planner → reviewer ∥ red-team (plan; B1 = every protected privacy.rs:<line> citation already stale → placement freed; C1 = flat cap is a permanent sybil lockout → tiering) → /simplify → planner rev 2 → tester (opus, /tmp staging) → tests reviewer ∥ tests red-team (live cheats: let cap: u64 shadow, hollowed-test X1 forge, nested block comment, cfg const twin, unpinned SSOT body — all closed in round 2) → implementer (opus; green first run, 0 test edits) → LANDING PATTERN /tmp/mr_warn_rb-107 after commit d4afe40 → artifact reviewer + /simplify + security checklist INLINE over pinned clone /tmp/rb-107-review@d4afe40 → ONE verifier (PASS; own mutant = ungated export_bundle writer in accounts.rs, KILLED by [rb85/accessor-ratchet]). Orchestrator test edit disclosed: one needless_range_loop reshape in [rb107/label-census]. Residuals registered (after the lenses): R-rb-107-LOCKOUT (HIGH, anonymous-share lockout ~181 pairs/day), R-rb-107-REJECTWALK (post-walk band for above-minimum bundles), R-rb-107-BYTEBOUND, R-rb-107-CAPOBS, R-rb-107-ARMSKIP — all target backlog; R-rb-85-EXPORTADMIT closes on merge (`mr-gates residuals close --slice rb-107 --pr N`). SUPERVISOR TODO at merge: (1) `mr-gates verify --slice rb-107` FROM the worktree (X6/X8 need the rig free; X1 roster uses full nextest paths); (2) commit harness-side edits — ledger, rb-107.plan.md, rb-107.red-before.md, rb-107.mutants.py, rb-107.x7-register.md, this handoff, mr-residuals.jsonl (5 new rows + the promoted marker), 3 memory cards + MEMORY.md pointers (nextest-exact-name-filter-needs-module-path, mutant-runner-fn-span-ends-at-column0-brace, verify-inbound-line-citations-before-constraining-a-design); (3) bump mr-state.json adr_next_free 265→266; (4) mark the M-residual-backlog.spec.md#rb-107 section done with the PR ref; (5) post-merge cbm index_repository + codegraph sync on the canonical checkout (NOT run pre-merge — graphs track master); (6) follow-up flags outside touches: stale privacy.rs:<line> citations in ADR-0231 / exportAssembly.test.ts / privacyModel.test.ts, ADR-0238's rb-85 amendment naming EXPORTADMIT as open, security-threat-model.md DoS row; (7) /tmp/rb-107-review pinned clone removed; /tmp/rb-107/, /tmp/rb-107-rt*, logs disposable. No BLOCKER. Worktree left in place for the merge.
Acceptance: 8/8 met, 0 deferred, 0 unmet — rb-107 seed:e3b0c44298fc1c14. No deferrals.

## 2026-09-21T11:02:32Z — rb-107 launched (queue fast-path)
Native tick rid=native-20260921T110004Z-3706419 (src=cron). Gates 0-2 clean: no per-run locks, no chain-owner mutex, HOLD-NONE, no resident IDE claude pid, no non-wrapper writes in last 10min, no .done files, no open PRs either repo, both repos synced (harness main, project master @3937c58), no wip branches. Gate 3: residuals check — 18 unclaimed, all MED severity, max age 2.95d (< t1_promote_days=3), none outrank PLAN work this tick. queue[] non-empty (5 entries from prior residual-promotion ticks): re-verified rb-107 (queue[0]) live — spec section exists at M-residual-backlog.spec.md:77 (R-rb-85-EXPORTADMIT), not blocked:, no unmet after: deps, not already merged (checked git log + merged-PR search). Launched rb-107 via mr-spawn: fable@xhigh, tier=hard (touches server-module/src/privacy.rs request_data_export reducer + possible new admission-control table under ADR-0221 automigration-freeze), ADR-265 pre-allocated. queue-removed rb-107 post-launch. Did not fan out a 2nd slot this tick: rb-108 (next queue entry) touches accounts_tests.rs manifest-census logic which is plausibly file-disjoint from rb-107's privacy.rs, but rb-107's scope may require touching schema.rs (structural/always-serial set per Fan-out safety) and that uncertainty wasn't worth resolving pre-launch — downgraded to serial per doctrine (never upgrade toward parallel on uncertain grounds). Budget governor NORMAL (d7=$1516.63/$2783 weekly, fable_ok=true). No BLOCKERs.
## 2026-09-21T10:03:58Z — 10:00Z tick — residual promotion (rb-109..rb-113)
Gates 0-2 clean: no per-run locks, no chain-owner mutex, no resident IDE claude session, no non-wrapper writes in last 10min, HOLD-NONE. Both repos synced (master@3937c58, harness main). No open PRs, no parked slices at gate entry. Found and reconciled uncommitted stray bookkeeping from the 09:00Z tick (handoff entry, archive rotation, usage-daily row) that never got committed -- committed directly to main (8e71d0e), pushed. Gate 3: mr-gates residuals list --unclaimed showed 23 open; 5 (R-rb-85-X9 3.19d, R-rb-86-READCAP-NAME/SAMEMS/TICKBOUND 3.04d, R-rb-86-X9 3.02d) crossed t1_promote_days=3, outranking queue-launch and new PLAN Sec.9 work per gate-3 aging doctrine. Classified all 5 per the 2026-09-01 work-selection-scope directive: all are real game/security-correctness defects (export-bundle reaper execution-proof test, write-set-bound correctness, EXPORT_REAP_MAX_DELETE_PER_TICK misnomer) -- none eval-tooling-only, none dispositioned wontfix. Promoted -> rb-109..rb-113 in M-residual-backlog.spec.md. Queued rb-109/110/111 via mr-record queue-add; queue[] hit its 5-entry cap (already held rb-107/rb-108 from the prior tick) before rb-112/113 could be added -- those two remain valid promoted sections, discoverable on a future full derivation pass even though not in the fast-path cache. Shipped as doc-only chore PR#132 (chore/residual-promote-20260921T100548Z), squash-merged clean (no CI configured on this branch), local main fast-forwarded to b3341d8, branch auto-deleted. This tick's ONE action = promote (gate-3 residual outranking); no rooted run launched. Next tick fast-path: queue has rb-107, rb-108, rb-109, rb-110, rb-111 ready to launch in that order (re-verify each live before trusting the queue hint) -- rb-107/rb-108 remain the front of the queue and are HIGH-signal candidates: rb-107 (request_data_export sybil admission control, touches server-module/src/privacy.rs -- HARD tier, fable@xhigh, budget.fable_ok=true at d7=$1515.20/2783 eff., fable_d7=$709.49/2298 allowance) was live-verified ready to launch this tick (spec section exists, non-blocked, not already merged) but gate-3's residual-aging step took priority as this tick's one action.
## 2026-09-21T09:02:35Z — Native tick 09:00Z — residual promotion (rb-107/rb-108)
Gates 0-2 clean: no per-run locks, no chain-owner mutex, no resident IDE claude session, no non-wrapper writes in last 10min, HOLD-NONE. Found /tmp/mr_pass_m24-s6.done from an already-fully-merged chain: PR#493 (m24-s6) verified live as MERGED with ci+e2e both SUCCESS (the 09:00Z situation bundle's recent_blockers list still showed a stale e2e-FAILURE entry from an earlier CI attempt on the same PR -- superseded, re-verified green). master is actually 2 slices further ahead, at m24-s8/PR#495 (M24 i18n milestone fully closed S0-S8) -- reconciled by cleaning the stale .done/.vars.json, no live chain to reap. No open PRs, no parked slices, queue[] empty. Gate 3: mr-gates residuals list --unclaimed showed 25 open, all MED, all unpromoted -- but R-rb-85-EXPORTADMIT and R-rb-85-MODCENSUS both crossed t1_promote_days=3 (3.17d, up from 2.999d at the prior tick) so they now outrank new PLAN §9 work. Classified both per the 2026-09-01 work-selection-scope directive: EXPORTADMIT (unbounded anonymous request_data_export growth, a real security/economy defect) and MODCENSUS (m22_declared_mod_names *tests-suffix census escape, a real crate-census correctness defect) are NOT eval-tooling-only findings, so both promote rather than disposition wontfix. Promoted -> rb-107/rb-108 in M-residual-backlog.spec.md, queued via mr-record queue-add, shipped as doc-only chore PR#131 (chore/residual-promote-20260921T090135Z), squash-merged to main @a323ce5. This tick's ONE action = promote (gate-3 residual outranking new-milestone launch); no rooted run launched. Next tick: fast-path queue has rb-107 then rb-108 ready to launch (re-verify live per doctrine before trusting the queue hint).
## 2026-09-21T08:20:17Z — m25-s0 merged — ADR-0034 dated amendment + threat-model §7 corrections (PR#130)
PR#130 squash-merged to main @05a9f8e. Decision #496 (issue already closed by the 06:08Z tick that consumed it) authorized exactly two ADR-0034 corrections, both landed via an in-place dated Amendment section (byte-identical original text preserved, ADR-0033/0175 precedent, no new ADR number): (1) dead RLS-verification consequence replaced by the two-channel model (subscription + Err(String) rejection payload) with M25's channel-1 job reframed as completeness of the visibility_note declaration, never RLS verification; (2) 'untrusted chat' struck from Context, set_profile_name (ADR-0132) substituted as the real UGC surface. security-threat-model.md got the matching §7 corrections (Chat/social row → User-generated-text row, Reducer-rejection-payloads channel-2 row added, §3 M25-scope rewritten to what's actually missing, §4 accepted-risks expanded with trade_offer/battle_challenge split verdicts + public-table-composition + second-identity-issuer + CVSS-lite-gaming residuals). touches-delta: none (exactly the two declared paths). Acceptance ledger 11/12 met, X12 deferred wontfix (pre-existing mr-selfcheck registry debt, unrelated to this two-file docs diff). mr-audit: orchestration CLEAN (8 agent calls incl. reviewer/red-team/verifier/tester), gating_advisory CLEAN. Hard-tier so mandatory_read=true; supervisor read the full diff — content is well-cited against live evidence (ADR-0197 FF3, ADR-0194/0198, ADR-0132/0255/0261) and matches the accepted decision exactly. Worktree/branch cleaned, stale per-run lock reaped.
## 2026-09-21T06:09:47Z — m25-s0 launched — ADR-0034 amendment decision consumed (issue #496)
Operator accepted the recommendation on DECISION issue #496 (m25-s0-adr0034-amend): adopt the spec's own drafted amendment to ADR-0034 in place (no new ADR number) — (a) replace the dead RLS-verified consequence with the two-channel model + completeness-of-private-table-migration framing, (b) strike 'untrusted chat' from Context, substitute set_profile_name. Closed #496 with a <!--mr-system--> comment. Launched m25-s0 (fable@xhigh, tier=hard, repo=harness, pr_repo=mdrewt/claude-harness, leader pid 3482779/claude pid 3482782) to write the amendment plus apply the §7 threat-model corrections to specs/monster-realm-v2/security-threat-model.md. M25 milestone now unblocked past S0; S1-S8 remain serial/fan-out per §4 spine. Gate-seeding for m25-s0 returned SPEC-SECTION-NOT-FOUND (advisory) — the spec names S0 only in a §4 table row.

## 2026-09-21T05:03:55Z — M25-S0 BLOCKED on operator decision — ADR-0034 amendment (issue #496)
M24 fully closed this tick's predecessor (m24-s8/PR#495, the ICU round-trip shim — final of 9 slices, S0-S8 all merged). Gates 0-2 clean this tick: no per-run locks, no chain-owner mutex, HOLD-NONE, no resident IDE session, no non-wrapper writes, both repos fetched/in-sync (harness was 1 commit ahead — pushed). No open PRs, no parked slices, queue[] empty, no residual past t1_promote_days=3 (closest R-rb-85-EXPORTADMIT/MODCENSUS at 2.999d — under threshold by hours).

PLAN §9 order `[M18, M19, M22, M23, M24, M25]`: M18/M19 remain post-gate-blocked (Phase C, playtest-3 gate not raised for them); M22/M23/M24 CLOSED; M25 is next and its ceremony is CEREMONY COMPLETE (2026-08-24) with Phase D implementation greenlit (2026-08-08 operator override skips playtest-3 for Phase D specifically).

M25-security-audit.spec.md §8-1 flags slice S0 as a hard BLOCKER in the spec's own text: two clauses of the *accepted* ADR-0034 (security-audit-gate) are now factually false (RLS-verified premise dead per ADR-0197; "untrusted chat" context — no chat system exists), and amending an accepted ADR is explicitly stated as not a spec author's call. Every M25 slice S1-S8 depends on S0 directly or transitively, so this one decision currently blocks the entire milestone. The spec already drafts the recommended amendment text (§8-1a/b) — handed to Drew as the --recommend.

Raised `mr-ask-drew m25-s0-adr0034-amend --blocking` → https://github.com/mdrewt/monster-realm/issues/496. `.blocked-on-human` written, `mr-decision-watch` spawned (5-min poll, resumes via event tick on answer). Note: §8-2 (release-tag convention, gates only S6) is a second independent M25 blocker not yet raised — out of scope until S0-S5 land.

Next tick: if issue #496 answered, the decision-watch event tick applies the ADR-0034 amendment as part of launching m25-s0 (or a small doc-prep step first) and proceeds down the S0→S1→S2→{S3,S4}→S5→S6→S7→S8 spine. If unanswered, re-check residual aging (closest items were 2.999d against a 3d promote threshold — will cross within hours) before falling back to standdown.
## 2026-09-21T04:38:34Z — m24-s8 merged (PR#495) — i18n ICU round-trip shim
PR#495 (feat/m24-s8-icu-round-trip) squash-merged to master @ 3937c58 (ADR-0264). catalog-export.mjs/catalog-import.mjs (source-parsed, zero-RegExp, live-CLDR plural, @translated marker), nightly-only gap-ratcheted i18n-locale-completion.json baseline, 4 just recipes, catalogRoundTrip.test.ts (I18N-29) through the real formatter. Orchestration CLEAN (full role set: general-purpose/planner/red-team/reviewer/tester/verifier). Acceptance ledger 12/13 met, X13 legitimately deferred to backlog (nightly.yml/policy-doc wiring outside declared touches; the recipe it calls, X6, ships here). Worktree+branches cleaned (local+remote). Master CI still in_progress on the merge commit at tick end — next tick re-verifies before any new launch.
## 2026-09-21T04:22:13Z — m24-s8 CI-watch delegation (04:21Z tick)
EVENT consumed: m24-s8 run finished rc=0 attempts=1 model=opus, opened PR#495 (mdrewt/monster-realm) 'feat(m24-s8): i18n ICU round-trip shim'. Local gate green (99/99 evals incl. RT-01..RT-14, 8/8 observability), ledger 12/13 gates met + 1 deferred (X13, decision-defaulted:m24-s8-icu-shim-earned-with-no-named-vendor=YES-BUILD-IT, D9), lint-clean, verifier teeth added (4 mutants killed: plain-value trim, EMIT-MISMATCH/write ordering, banner-bug) on ef1fe67. Re-verified live at 04:2 2Z: no chain-owner mutex, per-run lock leader 3226878 confirmed dead, done file present, HOLD-NONE, no resident IDE pid -- took mutex, gates 0-2 clean. Live gh pr view 495: state OPEN, mergeStateStatus UNSTABLE, mergeable MERGEABLE, both ci+e2e checks IN_PROGRESS (started 04:19:28Z, run 35560534389) -- checks NOT yet complete, so this tick does NOT merge. Delegated CI-wait via mr-ci-watch (pid recorded, detached setsid) per doctrine -- resumes via event tick on completion. This is the sole mutating action; mutex released after. Governor NORMAL (d7=$1360.24/$2783 eff., 49%; fable_ok=true). No BLOCKERs, no rate-limit event. Note: uncommitted mr-state.json/handoff diffs found at tick start from the 02:02Z launch tick (never committed to git) -- folded into this tick's commit along with the untracked m24-s8-plan.md memory card.
## 2026-09-21T02:03:01Z — m24-s8 launched (M24 final slice — ICU round-trip)
Native tick 2026-09-21T02:00:44Z (rid=native-20260921T020044Z-3221974). Gates 0-2 clean re-verified live: no live per-run lock, no chain-owner mutex, no resident IDE claude pid, no unaccounted harness/project writes, mr-hold HOLD-NONE. mr_pass_m24-s6.done was stale /tmp exhaust from the already-merged-and-recorded 22:00Z tick (PR#493) -- left in place, harmless, no live lock referenced it.

Both repos synced (master@436aa74 = m24-s7/PR#494, merged+recorded by prior tick). No open PRs either repo. mr-state.json: inflight=[], awaiting_merge=[], queue=[].

Gate 3: 24 unclaimed residuals, all severity MED, all status unpromoted, max age 2.87d -- none past t1_promote_days=3 or t2_stale_days=14, so none outrank new PLAN §9 work this tick. Fell through to full derivation: M24 (i18n) is the open milestone, slices S0-S7 all merged (confirmed via git log ADR-0255..0263), only S8 (ICU round-trip interchange shim: scripts/catalog-export.mjs, scripts/catalog-import.mjs, evals/baselines/i18n-locale-completion.json, justfile) remains, `after: S7` satisfied.

M24 spec §8 item 5 is an operator escalation ("Is the ICU round-trip shim earned with no named vendor?") but is explicitly marked "[DEFAULTS to building it in S8]" if unanswered -- a reversible scope choice per its own framing (deferring costs nothing), not architectural/irreversible/security/spec-contradicting. Per BLOCKER discipline, took the documented default (build it) rather than opening a blocking mr-ask-drew round-trip; recorded as decision-defaulted:m24-s8-icu-shim-earned-with-no-named-vendor=YES-BUILD-IT in the ledger notes.

Launched m24-s8: opus@high, routine tier (client/scripts/evals only -- no schema/reducer/netcode/security/M20/M25 surface), ADR-0264 reserved, repo=project, base=origin/master, PR target mdrewt/monster-realm. mr-spawn LAUNCHED cleanly (leader=3226878 claude_pid=3226881, own session, detachment+model asserted). No fan-out candidate -- S8 is the only unblocked slice this tick.

CAVEAT for the merging tick: mr-gates seed reported `SPEC-SECTION-NOT-FOUND` / criteria=0 for m24-s8 -- the spec's slice table (§4) has no dedicated `### S8` heading the seeder can extract EARS criteria from mechanically (unlike prior slices whose criteria live in numbered `### <id>` subsections elsewhere). This means `mr-gates verify --slice m24-s8` will likely read NO-LEDGER or empty -- an absent measurement per doctrine, not a failed one, but it means the merging tick CANNOT lean on the acceptance-ledger gate as usual and must hand-adjudicate against spec §6 I18N-29 (round-trip key-set/arity identity, unit test not eval per ADR-0224/0257 vehicle) and I18N-30 (no `new RegExp(` in the two scripts), plus §4.1's milestone-wide post-integration invariants (bindings-drift=0, battle-schema-snapshot unchanged, CONTENT_VERSION still 21) since S8 is M24's last slice.

Budget: governor NORMAL, d7=$1280.11/$2783 eff. (46%), fable_d7=$640.34/$2298 fable-allowance (fable_ok=true, unaffected -- this launch is opus-tier). No rate-limit event, no BLOCKERs. Mutex released after this action; next tick resumes from the launched chain (event tick or standard poll).
## 2026-09-21T00:09:31Z — m24-s7 — CI-watch delegated (PR#494 checks pending)
PR https://github.com/mdrewt/monster-realm/pull/494 (feat/m24-s7-i18n-fr-catalog -> master), mergeStateStatus=UNSTABLE (checks: ci pending, e2e pending), mergeable=MERGEABLE. Ledger m24-s7.gates.md re-verified 12/12 met, 0 deferred, 0 unmet (seed:e3b0c44298fc1c14). No chain-owner mutex was held (stale/absent) at tick start; verified live PR state, no other in-progress action. Delegated CI-wait to mr-ci-watch (pid 3195200, detached, own session) per doctrine — will resume merge via event tick once checks complete. Did not merge without live-green confirmation. Budget governor NORMAL (d7=$1278.11/$2783, fable_d7=$640.34/$2298, fable_ok=true). No new launch this tick (single action = CI-wait delegation).
