# m20c — ADJUDICATED plan (binding). Raw planner draft: monster-realm-m20c-plan-draft.md

Slice: client real OTel Web SDK wiring. Branch `feat/m20c-client-otel`, worktree `.claude/worktrees/m20c`, base `d45b3c3` (master, CI green). ADR: 0180 body-only amendment (no new number). HARD tier.

Three plan-review lenses ran (reviewer / red-team / simplify). Every finding adjudicated below as AM1–AM22; the planner draft stands except where amended.

## Rulings (AM = adjudicated modification; binding on tester + implementer)

- **AM1 (reviewer H1 — ACCEPTED):** `zone_id` must track zone switches. New façade method `telemetry.setZone(zoneId)`; 7th main.ts hook in `switchZone` (~main.ts:692) or wherever `rawMap.zone_id` is committed (implementer verifies the real anchor; tester pins it with a `W-*` needle). `attributes.ts` gains a rebuild path + unit test. Initial zone set at first map commit.
- **AM2 (reviewer H2 — ACCEPTED):** `maxRemoteGapMs` logic gets a named home: `client/src/observability/interpGap.ts` — pure, takes an iterable of remote `{jitterEwma}` + `stepMs`, imports `adaptiveInterpDelayMs` from `render/interpolation.ts:133` (never copies it), reduces to max; sibling test covers own-entity exclusion handled at call site (main.ts passes remotes only), zero-remotes → no sample, max-not-sum. 8 source files total now.
- **AM3 (reviewer M1 + red-team #4 — ACCEPTED):** T-16b's credential scan covers `observability/*.ts` AND the new main.ts hunks. Additionally the RUNTIME seam is the primary tooth: T-16a asserts the captured exporter config's `headers` is exactly `{}`/undefined by deep-equality (not substring absence).
- **AM4 (reviewer M2 — ACCEPTED):** T-25b asserts fps boundaries contain **50, 55, AND 60** (55 is the OBS-25 threshold), strictly increasing.
- **AM5 (reviewer M3 + simplify #2 — ACCEPTED, KEEP the third counter):** `mr_client_intent_reject` stays (distinct netcode-health class: server-rejected intents → visible drop/rubber-band; seam exists at `noteMoveRejection`). ADR amendment must name it explicitly as a deliberate addition beyond the six spec-named signals AND spell out both PromQL mappings: prediction-divergence rate := `rate(mr_client_reconcile_correction[5m]) / rate(mr_client_reconcile[5m])`; reconcile-correction rate := `rate(mr_client_reconcile_correction[5m])` (per-second); intent-reject rate := `rate(mr_client_intent_reject[5m])` (companion, spec-extra).
- **AM6 (reviewer M4 — ACCEPTED):** exporter version resolved at build time with `npm view` + peer check; the plan's `^0.22x` is directional only. Red-team resolved a real tree at 0.221.0/2.10.0 — use that as the starting point; lockfile pins.
- **AM7 (reviewer M5 + red-team #10 — ACCEPTED):** R-1 premise softened: sdk-metrics 2.10.0 `MeterProvider` nullish-coalesces `resource` (no merge), no auto `service.instance.id`. Mitigation UNCHANGED: explicit resource via `resourceFromAttributes({'service.name':'monster-realm-client'})`, and T-R1 pins the resource attribute set by **exact set equality**.
- **AM8 (red-team #1 — ACCEPTED):** T-W2 additionally asserts the new `.then` body in `sendIntent` contains `try {` and `} catch` (the call-site guard is mandatory, tested, defense-in-depth beyond telemetry.ts's internal guards). A telemetry throw must never reach the rejection `.catch` → `predictor.dropRejected` (manufactured desync).
- **AM9 (red-team #2 — ACCEPTED):** T-16c asserts the resolved export URL by **exact equality**: `new URL(u).origin === configured origin && new URL(u).pathname === '/v1/metrics'` (no identity-in-path smuggling).
- **AM10 (red-team #3 — ACCEPTED):** telemetry.test.ts tooth: for each façade method, the `attrs` object passed to `instrument.record()`/`add()` is the **same reference** (`toBe`) returned by `buildAttributes` — no spread-and-append site can exist.
- **AM11 (red-team #5 — ACCEPTED):** T-A1 fixtures gain `build_sha` upper-edge cases: 40-hex accept, 41-hex reject (plus existing 7-char edge and 'unknown' reject).
- **AM12 (red-team #7 — ACCEPTED):** rate-budget math documented with the real retry behavior (otlp-exporter-base retries ≤5× on 429/502/503/504, backoff 1s→5s cap, not configurable): worst case at clamp-min 15s ≈ 24 req/min single tab — still 5× headroom under Caddy's 120/min. Thundering-herd mitigation: `resolveTelemetryConfig` takes an injected `jitter` ∈ [0,1) and applies **upward-only** interval jitter `interval × (1 + 0.1×jitter)` after clamping (pure, property-tested; main.ts passes `Math.random()`).
- **AM13 (red-team #8 — ACCEPTED):** ADR amendment lists the FULL resolved dependency set (11 @opentelemetry packages incl. transitives: api-logs, core, otlp-exporter-base, otlp-transformer, sdk-logs, sdk-trace, semantic-conventions), all Apache-2.0, zero install scripts (verified from a real lockfile resolve).
- **AM14 (red-team #9 — ACCEPTED):** `frameWindow.ts` DISCARDS anomalous windows: if the elapsed window exceeds 2× the nominal 1s (backgrounded-tab rAF suspension), reset state WITHOUT emitting a sample. New tooth T-25d: clock-jump fixture → no sample, state reset. Documented in ADR (fps SLO never polluted by tab-suspend artifacts).
- **AM15 (red-team #12/#13 — ACCEPTED):** no pagehide/visibility flush in v1 — final partial window is lost; documented as accepted data loss in ADR. Light tooth: `recordFrameSample` performs exactly 2 `record()` calls (fps + frame-time-max), no fan-out.
- **AM16 (simplify #3 — ACCEPTED):** bucket trim. `mr_client_fps: [15,30,45,50,55,60,90,144]` (8). `mr_client_frame_time_max_ms: [8,16,33,50,100,250,1000]` (7). `mr_client_interp_gap_ms: [16,33,66,100,200,500]` (6). `mr_client_reducer_rtt_ms: [10,25,50,100,250,500,1000]` (7). `mr_client_wasm_ready_ms: [100,250,500,1000,2500,5000,10000]` (7). Cardinality-budget tooth: every histogram's boundary list length ≤ 9 and strictly increasing.
- **AM17 (simplify #1 watch-item — ACCEPTED):** `instruments.ts` name fields must reference the exported constants from `names.ts` (single source); instruments.test.ts asserts the instrument-name set is identity-equal to the names.ts constant set (no independent string literals).
- **AM18 (red-team #6 — FLAG, out of touches):** Alloy's `build_sha` value regex (`[0-9a-f]{7,40}`) is a weak cardinality bound on a public path (16^7..16^40 space, mintable at 120 series/min/IP by a scripted client). m20c cannot fix (ops files out of touches). Recorded for the supervisor as an m20b/ops follow-up: pin to fixed length or deployed-SHA allowlist.
- **AM19 (reviewer L1 — ACCEPTED):** ADR RTT wording: "genuine round trip (promise settles on server Ok/OkEmpty per sdk index.mjs:6313-6338), plus up to one JS task tick of client-side outbound batching under the v3 ws protocol (index.mjs:5948-5969)."
- **AM20 (reviewer item 6, buildInfo — VERIFIED CLEAN):** `buildInfo.ts:44-48` ships real `git rev-parse --short HEAD` with `'unknown'` fallback; `'unknown'` fails the sha predicate → key omitted (T-A1 exercises it). Dev behavior correct by construction.
- **AM21 (test-env constraint — BINDING):** vitest runs in NODE env (no happy-dom default; `vite.config.ts:47-53`); every observability module imports with no ambient window/document/navigator at module scope; host access injected. No `.d.ts`/`.spec.ts`/`.tsx` under client/src (spec-gap-revival.eval.mjs:232). No new RegExp anywhere (T-A2). No coverage exclusions (SANCTIONED_EXCLUDES is an exact set).
- **AM22 (increments + RED discipline):** four gated increments per the draft (pure core → shell+deps → main.ts hunks one-at-a-time with full `npm test` after EACH → live-stack verification + docs). Tester ≠ implementer; tester owns all gating tests incl. the wiring teeth (append-only describe block in main.wiring.test.ts); implementer never edits them (corrections go back through the tester). Orchestrator runs the RED proof and the bite-proof mutations (tester has no Bash).

## Deferred/parked (unchanged from draft)
Browser traces (no criterion forces; ingest live; reopen = a consumer exists) · window.__mrObs · wasm-init isolation (metric = ms from timeOrigin to exports-callable, ADR-documented) · snap/teleport metric · per-reducer RTT beyond enqueueMove · evals/** (m20e owns).

## Touches (final declaration for the PR)
Declared: `client/src/observability/**` (new), `client/src/main.ts`, sibling tests (incl. append-only block in `client/src/main.wiring.test.ts`).
touches-delta: `client/package.json`, `client/package-lock.json` (the SDK dep — unavoidable, spec-mandated by OBS-16 "via the OTel Web SDK"; no concurrent sibling slice is running — verified at launch: no open PRs, no other worktrees), `docs/adr/0180-*.md` (body-only amendment), `ARCHITECTURE.md` (minimal).
Live-stack risks R-2 (Alloy suffix), R-3 (CORS origin default mismatch: MR_OTLP_ALLOWED_ORIGIN default https://localhost:5173 vs vite dev 5290/preview 4173), R-6 (tls internal cert), R-7 (transport) → verified at increment 4 against `docker compose up -d` where feasible; NOT gating `just ci` (per slice brief).
