# m22-s9 plan — M22 post-integration verification (the milestone's real DoD)

Status: IMPLEMENTED (2026-09-02) — live e2e green 6× consecutive (S9-CASCADE-VERIFIED classified=22 seeded=785 vacuous=[battle_action,player_dialogue_state]); 5 m22s9_* Rust pins green; X6 mutants 2/2 caught by designated tags; measured finding F1 (ADR-0077 keep-latest GC vs cascade — challenge direction flipped so the terminal battle survives on the opponent axis; export_bundle pre-count sampled at the post-export census). Worktree `.claude/worktrees/m22-s9` @ origin/master 7bd5940, branch slice/m22-s9.
Touches (hard): `server-module/src/accounts_tests.rs` · `evals/account-e2e.eval.mjs` · `docs/adr/0232-*.md` · `ARCHITECTURE.md` (+ docs/knowledge, handoff/progress memos in harness memory).
ADR number: **0232** (supervisor-assigned; confirmed free on disk, highest = 0231).

## Deliverables
1. Full `just ci` green-and-meaningful; bindings-drift = 0; schema-snapshot intact (all three are existing gates — S9 proves them on the integrated tree and records evidence in the ledger).
2. Live e2e in `account-e2e.eval.mjs` PHASE 2 (`runLivePhase`): loaded-account deletion cascade + terminal-cancel + multi-chunk export, real scheduled reaper.
3. Rust contract pins `m22s9_*` (T1–T10) appended to `accounts_tests.rs` after :11691 — the 5 named cross-slice contracts at the type-system level.
4. ADR-0232; ARCHITECTURE.md M22-closure note; M22 closure in the harness handoff (PLAN.md §9 status edit is supervisor-owned doc-aggregation — recorded in handoff, NOT edited here).

## Design (planner output, reviewed)
### Injected clock (D1)
Patch IN THE E2E'S TMPDIR WORKSPACE COPY ONLY (issuer-patch precedent, N4 needle-throw + sole-occurrence assert):
- `game-core/src/accounts/deletion.rs:40` `604_800_000` → `15_000` (grace 15 s → real one-shot reaper fires).
- `game-core/src/accounts/deletion.rs:145` `EXPORT_CHUNK_ROWS: u32 = 500` → `2` (multi-chunk split provable with 3 rows; distinguishes chunks(K) from one-per-row).
Shipped tree untouched (deletion-grace-wasm-ssot + G24 read the real declaration).

### Driver flow (WS only; committed bindings; HTTP reducer calls BANNED — mr_load_driver.rs:76-89: each HTTP call is an ephemeral connection whose disconnect runs resolve_all_live_interactions and deletes player/character)
Two subjects: A = loaded+cascaded; D = trade counterparty, PvP target, PRV1-3 disarm proof, AND bystander control (a cascade erasing everything must FAIL the run).
S9-01 D-join · S9-02 A-join · S9-03 A-buy×3 (inventory sub-chunk vehicle) · S9-04 A-propose-trade(D) · S9-05 A-challenge(D) · S9-06 A-wild (grass-shuttle; fallbacks below) · S9-07 2nd challenge → D-accept (live PvP battle) · S9-08 one PvP action (battle_action) · S9-09 handshake `S9-seed-ready` → eval does SQL seeding + PRE-CASCADE SNAPSHOT → `<tmp>/s9-go` · S9-10 A-export → poll my_export_bundle stable · S9-11 D-delete → PendingDeletion · S9-12 D-cancel → Active (PRV1-3) · S9-13 A-delete · S9-14 A-export must Err (pending-deletion reject) · S9-15 A-cancel → Active (disarm on the LOADED account) · S9-16 A-redelete (re-arm) · S9-17 poll my_account terminal_at_ms Some (deadline 120 s) · S9-18 A-cancel-late must Err === "this account has already been permanently deleted" (PRV1-4) · S9-19 A-export-late must Err · S9-20 D-still-active (terminal None — disarm held).
A stays CONNECTED through S9-17 (disconnect would resolve her interactions early).

### Post-cascade truth pass (eval side, owner SQL `--format json` — parseSqlOutput NOT widened, its docstring forbids TEXT)
Manifest-driven over a transcribed constant `M22S9_MANIFEST_TRANSCRIPTION` (single delimiter-joined string literal, biome-stable). Honesty: Rust T7 derives expected content FROM `DATA_LIFECYCLE_MANIFEST` (never a second literal) and byte-compares. Owner columns derived from imported `REKEY_MANIFEST` (S0 export surface) — no second enumeration (T10 pins the precondition).
- ERASE(13): zero A-rows per identity column. ANONYMIZE(4): survive + tombstoned (account: auth_issuer==TOMBSTONE, terminal Some, provenance retained; player/profile: name==TOMBSTONE_DISPLAY_NAME; battle: A-side==0xFF…, D-side byte-identical to pre-snapshot). ViaJoin(5): unreachable via A's pinned pre-snapshot parent keys. NotOwned(18): SURVIVAL asserted (world-content control). Bystander: D's rows unchanged.
- NON-VACUITY: pre-snapshot records A-scoped counts per classified table; zero-count tables go to a printed `vacuous[]` list asserted ⊆ declared allowlist-with-reasons. A run seeding nothing FAILS.

### Export assembly (PRV1-11/12/13)
Per-chunk digests (never raw payload_json): one request_id; chunk_index multiset == 0..N-1; totalChunks==N on every chunk; tableName set == exportable:true set BOTH directions (17); battle_wild/export_bundle/guest_claim absent + no seed field; ≥1 table spans ≥2 chunks with non-final chunks exactly E2E_CHUNK_ROWS rows; per-table summed rowCount == SQL-observed count.

### Wild battle
Primary: grass-shuttle via movement (recruit.spec.ts precedent, MAX_WALK_STEPS bounded, loud fail). Fallback 1 (spike S5 bad): SQL-DML synthetic battle_wild child on A's PvP battle_id (proves ViaJoin sweep; loses resolve_wild leg — named residual). Fallback 2: declared vacuity + DEFER.

### >500 playtest_event volume leg
Optional, spike S1-gated (SQL INSERT Identity-literal viability). If unviable → DEFER to backlog with named residual (PRV1-13 already carried by chunk-rows patch).

### Rust T1–T10 (append after accounts_tests.rs:11691, m22s9_* prefix, M22-S9 header)
T1 fn-ptr signature pin resolve_all_live_interactions (spike S4: may need new #[no_mangle] abort stubs; fallback = squashed-signature scan, never dropped). T2 exactly-two-call-sites census. T3 fn-ptr pin should_reject_for_deletion. T4 ExportBundle exhaustive destructure (no `..`) + type asserts. T5 bindings field parity (include_str! my_export_bundle_table.ts, camelCase derived in-test). T6 bindings expose M22 reducer surface (STATE_TRANSITION_OWNERS + request_data_export + my_account/my_export_bundle). T7 e2e transcription == manifest (derived, both directions, 40 pinned, ambiguity fatal, quote-agnostic). T8 patch needles live (exactly once in deletion.rs AND once in eval). T9 e2e expected constants derive from source (REJECT_ALREADY_DELETED, tombstones). T10 every Erase/Anonymize table has ≥1 REKEY key (e2e derivation precondition).
File-local hazards honored: no /* or glob-star anywhere; concat!-split scanner needles; per-file stripper soundness.

### Phase-0 teeth (new pure fns)
findMilestone, patchDeletionGrace, patchExportChunkRows, parseSqlJson, parseManifestTranscription, checkDeletionFlow, checkExportAssembly, checkCascadeTruth — each BAD fixture pins its failing clause TAG. Teeth must include the 8 cheat shapes: erase-everything, erase-nothing-stamp-terminal, export-drops-a-table, export-includes-battle_wild, cancel-Ok-on-terminal, disarm-noop, transcription-policy-flip, patcher-needle-miss.

## Spikes (BEFORE tests; S3+S4 blocking)
S3 grace-patch end-to-end probe (tmpdir copy, 15 s grace, real reaper fires; my_account view poll latency) · S4 T1 link probe (which extern symbols demanded) · S2 sql --format json envelope (read scripts/playtest-report.mjs:330-380 first) · S5 grass-shuttle encounter reliability · S6 shop-buy reducer name/args + starting currency · S8 driver stdout flush observability (handshake vs two-spawn fallback) · S1 (optional) SQL DML INSERT Identity literals.

## Sequencing (internal s9a→s9b; ONE PR)
Phase A (s9a boundary, park-safe): ADR-0232 + Rust T1–T10 + eval pure layer w/ teeth + `just ci` green. Phase B: patchers + sqlJson + seeding/snapshot + DRIVER_SRC flow + truth pass + adversarial pass. If parked at A: DEFER the live-phase gates to a named next slice.

## Anti-patterns (enforced, from plan §4)
Vacuous-green, over-broad-green, new RegExp, scheme literals (Semgrep matches comments), parseSqlOutput widening, backticks in DRIVER_SRC, silent patcher no-op, milestone-roster drift (count-pinned), coarse fixtures, view onUpdate reliance, HTTP player calls, shipped-tree patching, note-skip false-pass (:3387 pass:true — assert live phase RAN in CI), git checkout during bite-proofs, nextest-green ≠ just-ci-green, timeout inflation w/o named deadlines, concurrent live evals, fixture monoculture, re-spelled constants, main-checkout edits from worktree.

## Spike results (all run 2026-09-02, /tmp/m22s9-spike/probe.mjs — throwaway)
- S3 ✅ THE PREMISE HOLDS: grace patched 604_800_000→15_000 in the tmpdir copy; real one-shot fired the real reaper at +15.002 s (delete stamp 1788348696647 → terminal 1788348711649); cascade completed (monster erased, export_bundle swept, schedule row consumed). Driver-side polled `my_account.iter()` DOES see PendingDeletion and terminal (views live-update; poll ≈1.5 s grain). `cancel_account_deletion` post-terminal returned the verbatim PRV1-4 string over the SDK. Export: 17 chunks = one per exportable table (EVEN EMPTY tables get a chunk), request-wide chunk_index 0..16, single request_id (= now ms), total_chunks 17 on every chunk.
- S3 consequence: A's `player` row is PRESENCE state — deleted by on_disconnect. The ANONYMIZE asserts (player.name tombstone; character absence via entity_id) MUST run while A is still connected → the driver needs a SECOND sync point (hold connection until the eval's post-cascade SQL pass finishes, then disconnect). Also assert player.name tombstone ≠ vacuous by checking the row EXISTS with the tombstone value at that moment.
- S1 ✅ SQL DML: multi-row INSERT into private playtest_event works, `0x<64-hex>` Identity literal accepted, auto_inc honors 0 sentinel (rows 1,2,3). >500-row volume leg VIABLE (batched multi-row INSERTs).
- S2 ✅ `sql --format json`: envelope `[{schema:{elements:[{name:{some:..}, algebraic_type:..}]}, rows:[[..]], stats:..}]`; Identity cell renders `["0xhex"]` (product-wrapped); enums/Options are Sum-typed in schema (variant names camelCased e.g. `pendingDeletion`); ORDER BY unsupported (sort client-side; decodeSqlJson precedent in scripts/playtest-report.mjs). GOTCHA: player_wallet's owner column is NOT `identity` (400 "not in scope") — column names MUST derive from REKEY_MANIFEST, never guessed.
- S4 ✅ fn-pointer pins (resolve_all_live_interactions, should_reject_for_deletion, erase_character_rows) compile+link after adding 4 new #[no_mangle] abort stubs in accounts_tests.rs: datastore_insert_bsatn, datastore_update_bsatn, datastore_delete_all_by_eq_bsatn, datastore_delete_by_index_scan_point_bsatn (match real bindings-sys signatures in final version). Full 767-test suite still links+passes.
- S5 ✅ grass shuttle needs NO tile data: zone-0 art (world.rs ZONE_0_ROWS, drift-gated vs RON) — spawn (1,1), Down→(1,2), then alternate Right/Left between grass (2,2)↔(3,2); every step rolls encounter_rate 200‰; 80-step budget ⇒ failure odds ~1e-8. Detect battle via my_battle view poll.
- S6 partial: economy::buy(shop_id, item_id, qty) exists; proximity/zone + starting currency UNVERIFIED — if buy needs zone-1 travel, SQL-INSERT inventory rows for A instead (decide at test authoring).
- S8 ✅ driver stdout streams live to the parent (observed during probe) — handshake via milestone-watch + go-file is sound.

## Seed-ordering correction (from S3 findings)
A can hold only ONE live battle (escrow guards), and a resolved WILD battle GCs (no surviving row) — so the battle-ANONYMIZE assert needs a TERMINAL PvP battle. Revised seed order:
1. A-join, D-join. 2. A-challenge#1(D), D-accept → LIVE PvP battle; A submits one action (battle_action row persists while D never answers). 3. D disconnects → forfeit_on_disconnect → battle TERMINAL (persists); D's trades/challenges auto-cancel (none yet — deliberate ordering). 4. D reconnects + re-joins (player presence row recreated; account/monsters/wallet persist). 5. A-propose-trade(D) (open trade). 6. A-challenge#2(D) (pending challenge). 7. A grass-shuttles into a LIVE wild battle (last — battle lock rejects later openings; movement is blocked from here on). 8. eval-side SQL bulk seed (>500 playtest_event for A + dialogue/quest/conversation/heal_cooldown/inventory rows as needed) + pre-cascade snapshot. Then export → D-delete/cancel (PRV1-3) → A-delete → export-reject → A-cancel → A-redelete → terminal → late-cancel (PRV1-4) → post-cascade SQL pass (A STILL CONNECTED) → release driver → disconnect.
Open question for test phase: does forfeit GC pending battle_action rows? If yes, battle_action non-vacuity via SQL fixture keyed to the wild battle.

## Lens outcomes (reviewer + red-team + simplify, 2026-09-02) — ALL adopted into the design below
REVIEWER BLOCKER (seed infeasible): pending challenge + its live battle can't coexist between A/D (challenge rows deleted on accept; one-pending-per-identity; wild battle blocks later challenge/accept). FIX = reorder: terminal PvP battle FIRST (challenge#1→accept→D disconnect-forfeit→D rejoin), then currency-only trade (monster_ids EMPTY both sides — escrow conflict fix), then pending challenge#2, then wild shuttle LAST. This also dissolves the reviewer MAJOR: the forfeit's legitimate writes to D (XP/rating/battle state) land BEFORE the pre-cascade snapshot, so D-byte-identical + battle-D-side-intact are valid asserts across the snapshot→cascade window.
RED-TEAM: (1)+(2) REKEY_MANIFEST owner-column derivation UNSOUND (6/17 Erase-Anonymize tables are policy BLOCKED; a policy-filtered read yields 0 columns = silently unattempted, invisible to vacuous[]) → transcription now CARRIES per-table identity-column lists, derived+pinned Rust-side from the m22s6 typespace walk (no REKEY/G6 dependence); truth pass hard-errors on cols.length==0 for Erase/Anonymize. (3) grace race → cancel asserted exactly Ok (print Err payload), terminal None right after cancel, named bail if elapsed(delete1→cancel)≥10s. (4) bare-literal needle ambiguous (comment at deletion.rs:23; G24 carries 604_800_000 9× in the eval) → needle = FULL declaration line (unique, spike-proven); T-pin counts that needle, "once in eval" = the exported const only. (5) assert driver exitCode===0 on success + no ok:false anywhere + no unknown S9 step names. (6) pin export rejects to exact 'export_reject_pending_deletion' (also proves deletion-gate-before-cooldown ordering). (8) vacuity allowlist hard-capped (≤3, reasons) + independent seeded-rows floor (sum pre-counts ≥ 520). (9) strict JSON.parse per chunk payload; parse failure = clause-7 failure. (10) prose labels m22s9-T#.
SIMPLIFY: reuse decodeSqlJson from scripts/playtest-report.mjs (exported, main-guarded, adversarially teeth-tested; evals→scripts import precedent at playtest-report.eval.mjs:648). CUT T2 call-site census (subsumed by rb24 exact-body pin + trading_tests resolver-extraction chain). MERGE T1+T3 (one signature-contract test). SLIM T6 into T5. D never enters the deletion state machine (pure bystander); PRV1-3 live disarm proven on A via schedule-row census syncs (cancel→census 0→redelete→census 1). KEEP: >500 playtest rows (brief-mandated, spike-proven cheap, §8.3 volume probe), S9 late-export err (3 lines, unique subject-check angle), grace=15s (bail guard covers the race legibly).

## FINAL Rust test set (m22s9_*, 5 tests + 4 abort stubs)
m22s9-T1 m22s9_cross_slice_contract_signatures — fn-ptr coercion pins: resolve_all_live_interactions fn(&ReducerContext,Identity) [S3↔lib.rs], should_reject_for_deletion fn(&Account)->bool [S1↔S5], erase_character_rows. m22s9-T2 m22s9_export_bundle_struct_shape_tripwire — exhaustive no-`..` destructure + per-field type asserts [S2↔S4↔S8]. m22s9-T3 m22s9_bindings_expose_m22_surface — include_str committed bindings: 8 ExportBundle fields camelCased (derived in-test snake→camel), my_export_bundle/my_account handles, reducer modules for STATE_TRANSITION_OWNERS + request_data_export [S2/S3/S4↔S8]. m22s9-T4 m22s9_e2e_manifest_transcription_matches_manifest — expected string DERIVED from DATA_LIFECYCLE_MANIFEST + typespace-walk column names; byte-compare vs eval const; census 40; ambiguity fatal [S0↔S2↔S6]. m22s9-T5 m22s9_e2e_patch_needles_and_constants — grace/chunk declaration needles sole-occurrence in deletion.rs + equal to eval consts; eval expected strings equal source: REJECT_ALREADY_DELETED, export_reject_pending_deletion, TOMBSTONE_AUTH_ISSUER/DISPLAY_NAME/IDENTITY hex. Abort stubs: datastore_insert_bsatn, datastore_update_bsatn, datastore_delete_all_by_eq_bsatn, datastore_delete_by_index_scan_point_bsatn.

## FINAL driver flow (WS only; sync via go-files go1..go5; milestones strict: no ok:false, no unknown steps, exit 0)
S9-01 D-join · S9-02 A-join · S9-03 A-challenge1(D) · S9-04 D-accept1 → live PvP · S9-05 A submits one pvp action (optional organic battle_action; skip if arg shape costly) · S9-06 D-disconnect → forfeit → battle1 TERMINAL (poll via A's my_battle) · S9-07 D-reconnect+rejoin · EMIT presence-ready{aId,dId,battle1Id} WAIT go1 [eval: SQL seed wallet(A), inventory(A)×3, dialogue/quest/conversation/heal_cooldown(A), playtest_event(A)×501 → go1] · S9-08 A-propose-trade(D, currency-only, monster_ids empty) · S9-09 A-challenge2(D) pending · S9-10 wild shuttle (Down once from spawn(1,1) then alternate R/L over grass (2,2)/(3,2); ≤80 steps; battle via my_battle poll; record wildBattleId) · EMIT seed-ready{wildBattleId,offer/challenge ids} WAIT go2 [eval: SQL INSERT battle_action fixture on wild battle if organic absent → PRE-CASCADE SNAPSHOT (A-scoped per-col counts on all classified tables + D full snapshot + world counts + battle1 row bytes) → go2] · S9-11 A-export → poll my_export_bundle stable → emit per-chunk digests (strict JSON.parse rowCount, hasSeedField) · S9-12 A-delete (t0) → PendingDeletion · S9-13 A-export-late must Err=='export_reject_pending_deletion' · S9-14 A-cancel must be exactly Ok (bail named if t-t0≥10s) → assert Active + terminal None · EMIT cancel-done WAIT go3 [eval: census A schedule rows == 0 → go3] · S9-15 A-redelete → PendingDeletion · EMIT redelete-done WAIT go4 [eval: census A schedule rows == 1 → go4] · S9-16 poll terminal (120s) · S9-17 A-late-cancel must Err == REJECT_ALREADY_DELETED exact · S9-18 A-late-export must Err=='export_reject_pending_deletion' · EMIT cascade-done WAIT go5 [eval: POST-CASCADE truth pass while A connected → go5] · S9-19 D-still-active (Active, terminal None) · done → disconnect → exit 0. Post-cascade also asserts terminal_at_ms − deletion_requested_at_ms ≥ E2E_GRACE_MS from the final account row.

## Open items
- PLAN.md §9 M22-closure edit = supervisor-owned (out of touches) — hand off in handoff note.
- Ledger authored during PLAN (X* gates) — see gates file m22-s9.gates.md.
