# Plan — m24-s5: i18n migration batch C (the tail: 13 views + `index.html` static strings → `t()`/`tf()`, `<bdi>` at the leaderboard, `dir="ltr"`) (planner output, 2026-09-20)

Worktree `projects/monster-realm/.claude/worktrees/m24-s5` · branch `feat/m24-s5-i18n-migration-c` · from master 10d4109 (S4 merged) · ADR-0261 (`**Extends:** 0256, 0257, 0259, 0260`). Pattern = S4 (ADR-0260 + `memory/projects/monster-realm-m24-s4-plan.md`) verbatim unless a decision below says otherwise.

## Measured census (real S2 scanner, comment-stripped, at 10d4109; `memory/projects/m24-s5-scan-census.txt`)
tradeProposeView 7 sinks / 1 failing · dialogueView 5/1 · claimView 6/1 · sessionView 4/0 · renameView 4/0 · menuView 4/0 · leaderboardView 4/2 · helpView 4/0 · errorOverlayView 4/2 · questLogView 2/1 · healView 2/1 · privacyView 7/1 · evolutionNotice 2/1 → **55 sinks, 11 FAILING = the entire whole-tree remainder** → **HARDCODED_CEILING 11 → 0** (I18N-19). Two population corrections for the ADR: (1) the spec row's "45 sinks" is wrong (55); (2) ADR-0260's "the 11 that remain are S5's tail plus `main.ts`'s four (S6)" is wrong — the 11 are exactly these 13 files'; `main.ts`'s four `chrome.status.*` literals are `reportError('…')` FUNCTION ARGUMENTS (e.g. `client/src/main.ts:651`), scanner-invisible, so S6 moves no ratchet and the ceiling reaches its permanent 0 HERE.
Scanner-invisible hoisted literals (migrate, zero ratchet effect, sentinel test is the only proof — ADR-0259 R4 class): privacyView `#paintButton(this.#closeBtn, 'Close', true)` (`privacyView.ts:151`), `'Confirm deletion'` / `'Keep my account'` (`:187-188`); evolutionNotice `speciesLabel` return `` `Species #${id}` `` (`evolutionNotice.ts:71`), `evolutionNoticeLabel` returns (`:93`, `:95`). Five hoisted, 16 keys total for 11 failing sinks.
Static `index.html` strings (S1 already seeded the keys): `#help-title` "Controls &amp; Goals" (`client/index.html:94`), `#rename-submit` "Rename" (`:60`), `#tradepropose-submit` "Offer" (`:79-81`), `#help-hint` (`:143`, STAYS — see D8).

## Files
| File | Change |
|---|---|
| `client/src/ui/tradeProposeView.ts` | `:168` → `t('tradePropose.target.placeholder')`; `show()` writes `this.#submitBtn.textContent = t('chrome.tradePropose.submit')` after the `wasVisible` read (`:126`), before the display write; `import { t } from './i18n/resolver'` |
| `client/src/ui/dialogueView.ts` | `:72` → `t('dialogue.action.shop')` (render-time already) |
| `client/src/ui/claimView.ts` | `:96` DELETED; `render()` writes `this.#privacyBtn.textContent = t('claim.privacyButton')` before the open-last call at `:122`; `:97` (`style.display = ''`) STAYS in the constructor; header comment `:91-95` rewritten |
| `client/src/ui/leaderboardView.ts` | `:60` → `t('leaderboard.empty')`; `:69` → `const name = document.createElement('bdi'); name.textContent = row.displayName; li.replaceChildren(name, tf('leaderboard.row', { rating: row.rating, wins: row.wins, losses: row.losses }))` (I18N-20) |
| `client/src/ui/helpView.ts` | constructor resolves `#help-title` via `overlay.querySelector('#help-title')` into `readonly #titleEl` (throw if missing — the file's own `:26/:30/:34` convention); `show()` writes `this.#titleEl.textContent = t('chrome.help.title')` after the `wasVisible` read (`:47`) |
| `client/src/ui/renameView.ts` | `show()` writes `this.#submitBtn.textContent = t('chrome.rename.submit')` after `:106` |
| `client/src/ui/errorOverlayView.ts` | `:35` DELETED (footer node still created, class kept); `:81` → `t('errorOverlay.footer')` |
| `client/src/ui/questLogView.ts` | `:45` → `tf('questLog.entry', { name: entry.displayName, step: entry.stepIndex })` |
| `client/src/ui/healView.ts` | `:47` → `tf('heal.location', { cost })` |
| `client/src/ui/privacyView.ts` | `:145` + `:151` move into `render()` (`this.#title.textContent = t('privacy.title')`; `this.#paintButton(this.#closeBtn, t('privacy.close'), true)`); constructor KEEPS `this.#title.style.display = ''`, `this.#closeBtn.style.display = ''`, `this.#closeBtn.disabled = false` (anchor invariant, D4); `:187` → `t('privacy.confirm.delete')`, `:188` → `t('privacy.confirm.keep')`; `:141-151` comment rewritten |
| `client/src/ui/evolutionNotice.ts` | `:71` → `name === undefined \|\| name === '' ? tf('evolutionNotice.species.fallback', { id }) : name`; `:93` → `tf('evolutionNotice.reveal.nicknamed', { nickname, from, to })`; `:95` → `tf('evolutionNotice.reveal.anonymous', { from, to })`; `:209` DELETED; `render()` writes `this.#okBtn.textContent = t('evolutionNotice.ok')` |
| `client/src/ui/sessionView.ts`, `menuView.ts` | NO edit (0 failing; every sink is model data or a glyph-only compound `${row.keyGlyph} — ${row.title}`, `menuView.ts:185`) |
| `client/index.html` | `:2` → `<html lang="en" dir="ltr">` (I18N-23); `:60` `>Rename<` → empty; `:79-81` `Offer` text removed; `:94` `Controls &amp; Goals` removed. `tabindex="-1"`, `data-testid`, ids untouched; `#help-hint` untouched |
| `client/src/ui/i18n/messageIds.ts` | +16 `MessageId` members (comment: `// m24-s5 (ADR-0261) — …` per namespace), +6 `MessageParams` rows (ONE table); header `:17` "S5–S6 add the remaining call sites" → "S6 wires boot" |
| `client/src/ui/i18n/catalog.en.ts` | +16 entries with `// @desc:`; the three `chrome.help.title`/`chrome.rename.submit`/`chrome.tradePropose.submit` citation lines gain "(literal removed from index.html; resolved in HelpView/RenameView/TradeProposeView `show()`)"; header `:16-17` sentence updated |
| `client/src/ui/i18n/__fixtures__/i18n-hardcoded.json` | 11 → **0** |
| Tests (tester) | 11 NEW co-located files `client/src/ui/{tradeProposeView,dialogueView,claimView,leaderboardView,helpView,renameView,errorOverlayView,questLogView,healView,privacyView,evolutionNotice}.i18n.test.ts` (D9) + NEW `client/src/indexShell.i18n.test.ts` + whole-file edit of `client/src/ui/i18n/catalog.test.ts` (roster 96→112) |
| Docs | `docs/adr/0261-*.md` (new) · `docs/adr/DIGEST.md` (`just adr-digest`) · `ARCHITECTURE.md` one `**m24-s5**` paragraph after `:2313` + the `:2303` heading gains "S5 migration batch C — ADR-0261" |
| Harness | spec §4 S5 row as-built bracket; gates ledger X1..X10 |

## Key roster (16 new → 112 total; ★ = MessageParams row). English value = CURRENT source bytes, copy-paste never retype.
tradePropose.* (1): `tradePropose.target.placeholder` `'Select a player…'` (U+2026) — `tradeProposeView.ts:168`.
dialogue.* (1): `dialogue.action.shop` `'Shop'` — `dialogueView.ts:72`.
claim.* (1): `claim.privacyButton` `'Privacy & Account Data'` — `claimView.ts:96` (resolved in `render()`).
leaderboard.* (2): `leaderboard.empty` `'No ranked players yet'` — `:60` · ★`leaderboard.row` {rating: number, wins: number, losses: number} `` ` — ${rating} (W${wins}/L${losses})` `` — **LEADING SPACE + `—` U+2014**; the name is NOT a param, it is the sibling `<bdi>` — `:69`.
errorOverlay.* (1): `errorOverlay.footer` `'F8 dismiss · F9 bug report'` (`·` U+00B7) — `:81` (the `:35` constructor twin is deleted).
questLog.* (1): ★`questLog.entry` {name: string, step: number} `` `${name} (step ${step})` `` — `:45` (`name` = quest content id, not UGC).
heal.* (1): ★`heal.location` {cost: string} `` `Heal here (${cost})` `` — `:47` (`cost` = `formatHealCostLine(loc)`, model-owned text).
privacy.* (4): `privacy.title` `'Privacy & Account Data'` — `:145` (render) · `privacy.close` `'Close'` — `:151` (render) · `privacy.confirm.delete` `'Confirm deletion'` — `:187` · `privacy.confirm.keep` `'Keep my account'` — `:188`.
evolutionNotice.* (4): `evolutionNotice.ok` `'OK'` — `:209` (render) · ★`evolutionNotice.species.fallback` {id: number} `` `Species #${id}` `` — `:71` · ★`evolutionNotice.reveal.nicknamed` {nickname: string, from: string, to: string} `` `${nickname} evolved from ${from} into ${to}!` `` — `:93` · ★`evolutionNotice.reveal.anonymous` {from: string, to: string} `` `Your ${from} evolved into ${to}!` `` — `:95`.
Existing chrome.* gaining call sites (no catalog value change): `chrome.help.title` (HelpView `show()`), `chrome.rename.submit` (RenameView `show()`), `chrome.tradePropose.submit` (TradeProposeView `show()`). `chrome.helpHint` stays a static literal (D8).
Untouched (PASS, tier-(e) / model data): sessionView/claimView/privacyView/renameView/menuView vm fields; `${c.key} — ${c.action}` (helpView:73), `${row.keyGlyph} — ${row.title}` (menuView:185), `[${row.source}] ${row.message}` (errorOverlayView:77), `t.label` / `` ` ${m.label}` `` (tradePropose:173/195), `vm.npcName`/`vm.nodeText`/`choice.text`, `PRIVACY_PSEUDONYMIZATION_DISCLOSURE`, `showFeedback(msg)`, `''` clears, `replaceChildren()`.

## Decisions
1 Scope = 11 failing + 5 hoisted + 3 static-shell strings + `dir="ltr"`; byte-identical English at every observable moment the overlay is visible. Both population corrections → ADR.
2 Two keys for the coincident English `'Privacy & Account Data'` (`claim.privacyButton` = a button label on the claim overlay; `privacy.title` = the privacy overlay heading). Semantic, not source-derived (§2.3/B5). The routing tests pin each site's EXACT key so a cross-alias is red even though the bytes coincide.
3 Leaderboard row = `<bdi>` + one text node from ONE ★ key (I18N-20/§2.7): `name` (`createElement('bdi')` + `textContent`, zero attributes, zero children) then `tf('leaderboard.row', {rating,wins,losses})` as the second `replaceChildren` argument → `li.textContent` stays byte-identical, `li.childNodes.length === 2`, and `replaceChildren` remains a scanner-visible sink (identifier + exempt call). Leading space lives IN the key (the `shop.*.row` trailing-space precedent). Rejected: name as a param (I18N-21 violation), a placeholder-splitting post-process, a second "dash" key. Fallback if happy-dom rejects a string argument to `replaceChildren`: `li.replaceChildren(name, document.createTextNode(tf(...)))`. `<bdi>` nowhere else (spec names only leaderboard/tradeView/pvpView; questLog names are content ids, the evolutionNotice nickname is plain interpolation as `pvp.incoming.label` is).
4 Where constructor-time strings resolve — per file, by the rule "in the method every production open path traverses and that re-runs on every open, unconditionally (never `!wasVisible`-gated)": HelpView/RenameView/TradeProposeView → `show()` (ADR-0260 D3 literal; `main.ts` opens them via `show()`); claimView → `render()` (the real open edge is `render(vm.visible)` — `main.ts:493-500` `applyClaim → renderClaim` runs BEFORE `claimView?.show()` at `:669-670`, and the reconnect re-open is `render()`-only); privacyView → `render()` (`main.ts:657-658` always `renderPrivacy()` then `show()`; the close-anchor's `display=''`/`disabled=false` stay in the constructor so `RB52V-ANCHOR-NEVER-DISABLED` keeps its meaning); errorOverlayView → `render()` only (`main.ts:929-934` renders then shows; the constructor twin is deleted, not resolved — one write, one key); evolutionNotice → `render()` (no `show()`; runs every batch, cheap). Pre-show/pre-render text is '' and NOT asserted English anywhere today (verified: `privacyView.test.ts`, `claimView.test.ts`, `errorOverlayView.test.ts`, `evolutionNotice.test.ts:570-581` reads OK after `render('x')`).
5 Model data are params with model TYPES (all `number`/`string` here — no bigint in this batch). Nothing in `*Model.ts`/`privacyBanner.ts`/`helpModel.ts`/`a11yCopy.ts` is touched.
6 Every `t(`/`tf(` first arg a literal; ternary OUTSIDE (`speciesLabel`); no wrapper, no dynamic key, no unused key (S7 DEAD-KEY) — every new key has ≥1 call site; no resolver/scanner/main.ts edit.
7 `index.html`: the three view-owned literals are REMOVED (single source; the shells are `display:none` until JS shows them, so there is no pre-JS reader — verified no test/e2e/eval pins their text); `dir="ltr"` added on `:2` as S5's additive half of I18N-23 (S6 writes `documentElement.dir` at boot; the static default belongs to the only slice that touches the file). Proof = a NEW static-shell test.
8 `chrome.helpHint` STAYS static in `index.html:143`: `indexShell.test.ts` H3/S5T-HINT-BUTTON require the `?` text and `main.wiring.test.ts:2255-2293` W-UX1-HINT-NO-JS-OWNER forbids `main.ts` from naming `help-hint`, so no slice can own a `t()` write for it without amending ADR-0151 D2 — recorded in ADR-0261 as a residual (S7's DEAD-KEY gate must exempt or S6 must resolve it).
9 Test vehicle = NEW co-located `<view>.i18n.test.ts` files (auto-discovered by `client/vite.config.ts:48` `src/**/*.test.ts`; `discoverViewFiles` excludes `.test.ts`), NOT appended describe blocks: whole-file drops under the tester write guard; zero risk of corrupting the needles evals pin inside existing test files. Each new file is self-contained (`// @vitest-environment happy-dom`, its own index.html-faithful mount copied from the sibling, `vi.mock('./i18n/resolver', { spy: true })`, the S4 `SplitSentinels`/`WalkSubtree`/`AssertNoRosterWord` trio, and — for views calling `openOverlayA11y` — the sibling's `closeOverlayA11y(id, null)` + macrotask sweep in before/afterEach). `catalog.test.ts` is the one in-place edit (whole-file replacement staged by tester B).
10 ADR-0261: Subsystems client-ui, ci-gates; population corrections ×2, D2–D4, D7–D8, the `<bdi>` shape, residuals (§2.5 model copy in `privacyBanner.ts`/`helpModel.ts`/`claimModel.ts`/`sessionModel.ts`/`menuModel.ts`/`healModel.ts` 'Free'/'gold' — outside touches, scanner-invisible, S7 checklist; `a11yCopy.ts:67/72` duplicates two of these strings as accessible names; claimView's five blank sibling buttons pre-existing; `#t(` scanner bypass R-m24-s4-RT1 still open, closed per-file by the exact spy pins).

## Tests (RED at HEAD; prefix `m24s5 XX-`)
Per view V ∈ {TP, DV, CV, LB, HV, RN, EO, QL, HL, PV, EN}: **V-01 routing** (spy; exact key + params per site; DOM bytes byte-identical; pre-show/pre-render NEGATIVE for the moved strings — text '' and `i18nT` not called with that key; then a SECOND `show()`/`render()` after `mockClear()` re-requests it (RT2); model strings pinned `not.toHaveBeenCalledWith`) · **V-02 sentinel** (construct BEFORE `mockImplementation`; try/finally + `mockRestore` + post-restore control; explicit VM matrix; whole-subtree walk incl. `<option>` text; forged-span guard against the per-file key roster; roster-word absence; a `«key»`/`«key|json»` containment pin for EVERY migrated surface incl. the 5 hoisted literals; fixture-collision rule) · **V-03 scan** (copy SV-03 exactly; floors TP 7, DV 5, CV 6, LB 4, HV 4, RN 4, EO **3**, QL 2, HL 2, PV 7, EN 2).
Specifics: LB-01 pins `li.childNodes.length === 2`, `childNodes[0].tagName === 'BDI'` with `attributes.length === 0` and `children.length === 0` and `textContent === name`, `childNodes[1].nodeType === 3` with text `' — 1200 (W10/L2)'`, `li.textContent === 'Zed — 1200 (W10/L2)'`, every `tf` call's `Object.keys(params).sort()` equals `['losses','rating','wins']` and no call argument contains the name (I18N-21 mechanical half), `dataset.identity`/`own` preserved, two rows with different numbers, an XSS name inside the `<bdi>`; LB-02 roster `['No ranked players yet', ' (W', '/L']` and fixture names without uppercase W/L. TP-02's walk must include the `<option>` branch. PV-01 asserts the pre-render invariants `closeBtn.disabled === false`, `closeBtn.style.display === ''`, `title.style.display === ''`; PV-02 uses SYNTHETIC vm labels (the real `CONFIRM_PROMPT` contains the roster word `Confirm deletion`). CV-02 vm strings must avoid `Privacy`. DV fixtures avoid `Shop`. EO fixtures avoid `dismiss`/`bug report`; EO-01 also proves the constructor no longer writes the footer. EN-01/02 cover the pure functions by RETURN VALUE (nicknamed / anonymous / both fallbacks) and the banner DOM separately — the nested `«species.fallback|…»` inside the outer sentinel's JSON is asserted by string equality on the return value, never through `SplitSentinels`; `evolutionNotice.i18n.test.ts` keeps the sibling's no-regex-literal rule. HV/RN/TP-01 pin `#help-title`/`#rename-submit`/`#tradepropose-submit` text '' pre-show (fixtures mirror the NEW index.html), English after `show()`, re-requested on a repeat `show()`.
**IX** (`client/src/indexShell.i18n.test.ts`, loud reader copied from `indexShell.test.ts`): IX-01 `documentElement.getAttribute('dir') === 'ltr'` and `lang === 'en'` (I18N-23); IX-02 census — every non-whitespace text node under `<body>` (script content excluded) has parent `#help-hint`, there is exactly ONE, and it equals `CATALOG_EN['chrome.helpHint']`; explicitly `#help-title`/`#rename-submit`/`#tradepropose-submit` `textContent.trim() === ''`.
**catalog.test.ts**: +10 `EXPECTED_PLAIN`, +6 `SAMPLE_PARAMS`/`EXPECTED_PARAM_OUTPUTS` (two sets differing in EVERY field); it-titles "exactly 96 keys" → 112, CATALOG-SHAPE "m24s4 96-key roster" → "m24s5 112-key"; 7 `it(` unchanged. JSON 11 → 0.
Mutants: (a) literal beside `t()` (b) constant closure (c) key aliasing between the coincident-English pair (d) JSON-only (e) hardcoded numbers in `leaderboard.row` (f) `<bdi>` omitted / name in the text node (g) name passed as a param (h) `<bdi>` gains `aria-hidden`/`aria-label` (i) constructor-only resolve (privacy/claim/errorOverlay/evolutionNotice) (j) `!wasVisible`-gated write (k) errorOverlay constructor literal left in (→ EO-03 FAIL) (l) leading space dropped (m) `speciesLabel` left raw (n) `index.html` literal left / `dir="rtl"` or absent (o) private `#t` (RT1 class) (p) `t(cond ? …)` — review-only.

## Ledger X1..X10
X1 ratchet CEILING=0 · X2 TP/HV/RN 9 passed (9) · X3 DV/QL/HL 9 passed (9) · X4 CV/PV 6 passed (6) · X5 LB 3 passed (3) · X6 EO/EN 6 passed (6) · X7 IX 2 passed (2) · X8 catalog 7 passed (7) · X9 tsc + compile test 5 passed (5) · X10 I18N-21 MANUAL (path:line citations).

## Tasks
T1 tester RED, two parallel agents staging under `/tmp/m24-s5/tests/`: **A** = TP/DV/CV/LB/HV/RN `.i18n.test.ts` + `indexShell.i18n.test.ts`; **B** = EO/QL/HL/PV/EN `.i18n.test.ts` + whole-file `catalog.test.ts` → T2 reviewer ∥ red-team on tests → T3 specialist (messageIds → catalog → 11 views → index.html → measure → JSON → `tsc`/biome/vitest/`node evals/run.mjs`; never edits a test) → T4 ADR-0261 + `just adr-digest` + ARCHITECTURE + ledger + spec bracket → T5 reviewer(+simplify) ∥ verifier → doc-keeper.

## Risks
happy-dom `replaceChildren(node, string)` (PoC first; fallback in D3) · privacyView anchor invariants must stay in the constructor · roster-word collisions (real `CONFIRM_PROMPT`, `Shop` in dialogue fixtures, `W`/`L` in names, `dismiss` in error rows) · nested sentinel JSON in EN-02 · errorOverlay `render()` try/catch swallows a mis-keyed `t()` (only the exact spy + byte pin see it) · `-02` containment pins are param-key-ORDER sensitive (inherited) · S6 ‖ S5 both touch the JSON (0 stays 0; second merger re-measures) · ADR-0261 number race at merge · biome line width on the reveal strings · `helpView` gains a constructor throw on a missing `#help-title` (all construction sites verified to carry it) · new-file vs appended-describe deviation from S3/S4 (justified in D9) · the ARCHITECTURE `:2313` and ADR-0260 sentence about "main.ts's four" is stale — correct in prose, do not amend ADR-0260.

## Hidden-dependency flags (exhaustive grep of e2e/, evals/, main.ts, indexShell*.test.ts, overlay-a11y-manifest, keyboard-operable-rows): NONE requiring an out-of-touches edit.
`evals/keyboard-operable-rows.eval.mjs:3332-3341` FROZEN_HTML_NEG_IDS keeps `help-title` (tabindex retained); `:2135` `'Close'` is a synthetic fixture · `evals/overlay-a11y-manifest.eval.mjs:66-85` no new `*View.ts`, no `.focus(` · aria-modal count 11 unchanged · `indexShell.test.ts` H3/S5T-HINT need `#help-hint` text (kept); `indexShellCascade.test.ts:65` targets `help-title` must RESOLVE (kept, empty) · `client/e2e/dialogue.spec.ts:437`, `wallet-balance.spec.ts:956` pin rendered `quest_001 (step 0)` (byte-identical); `shop-npc.spec.ts:416` selects `[data-shop-id]`; rename/pvp-side-b/monster-privacy click `[data-testid="rename-submit"]`; no e2e on leaderboard DOM · `main.wiring.test.ts:3913-3934` needs `shopAction`/`shopId` in dialogueView.ts (kept) · `resolver.test.ts:70-86` snapshot scoped to `chrome.*` (no new chrome.* keys) · sibling fixtures `tradeProposeView.test.ts:121/389/401`, `renameView.test.ts:131/393/407`, `helpView.test.ts:141` still carry the old literals (unasserted; leave). Callers of touched exports: `evolutionNoticeLabel` ← `main.ts:2089` + its test; `formatHealCostLine` ← healView + tests — no signature changes anywhere.

## Anti-patterns
`t(cond ? 'a':'b')` · view-local wrapper · keying glyph-only rows · the name as a `leaderboard.row` param · one shared key for the two `Privacy & Account Data` sites · `<bdi>` in questLog/evolutionNotice · migrating `privacyBanner.ts`/`helpModel.ts`/`a11yCopy.ts` copy "while here" · fixing claimView's blank sibling buttons · exact sink counts · keeping the index.html literals as "fallback" · resolving in the constructor · `!wasVisible`-gated writes · resolving `chrome.helpHint` anywhere · any `documentElement.dir` write in a view (S6/main.ts) · `clearAllMocks` inside the sentinel try · splicing into existing test files · reusing the real `buildPrivacyViewModel` output in PV-02 · a second `MessageParams` list.

## Right-sizing: ONE PR. 13 files but only 16 keys and ~30 changed lines of production code; two parallel testers absorb the RED fan-out; a split would double ADR/digest/ledger/lens cost and still serialise on `messageIds.ts`/`catalog.en.ts`. Fallback: park `index.html` + the three `chrome.*` call sites + `dir` as S5b only if X7's static-shell test surfaces an eval dependency the grep above missed.

## Revisions after reviewer ∥ red-team ∥ /simplify (plan lens, 2026-09-20) — see /tmp/m24-s5/plan-revisions.md (copied below in brief)
/simplify: LB-01 XSS pin cut (sibling owns it); 12 new test files kept (a shared helper would be a non-test file outside touches); D4 reduced to ONE sentence — the door rule.
Red-team MAJOR-1 (APPLIED): show() is an independent door for privacyView/claimView → privacy.title + privacy.close in show(); claim.privacyButton in BOTH render() and show() (main.ts onSignInFailed shows before it renders). MAJOR-2 (APPLIED): errorOverlay.footer in show(), outside render()'s hostile-VM try; constructor twin + render-site write deleted (4→3 sinks). MEDIUM (APPLIED, binding on tester B): EN-02 nested sentinel asserted by exact return-value equality only. MINOR: RT1 restated in ADR-0261.
Reviewer: byte transcriptions verified exact; D4 call-order verified against main.ts (:925-934 error overlay, :657-658 privacy, :663-670 + :3099-3103 claim); D7 clean (no static-text consumer; a11y-static-shell eval read in full, clean); D9 clean (no test-file roster/count pin exists); ledger shapes OK.

## Implementation + verification (2026-09-20)
Landing pattern (/tmp/mr_warn_m24-s5) appeared after the RED checkpoint → test-phase lenses done inline by the orchestrator (no fan-out), one specialist, one verifier. Tester A/B (staged under /tmp/m24-s5/tests, spliced): 13 files, 39 tests, RED 34/39 at 7a07dfd (HV-03/RN-03 regression teeth green on 0-failing files; catalog 4/7 green). Orchestrator test edits: biome format, 2 message rewords, 1 unused interface, happy-dom CSS-fetch silencer ported into indexShell.i18n.test.ts. Specialist: 15 touches files, no test edits, measured ceiling 0, gating 71/71, full client 3338/3338, tsc/biome clean, evals 99/99. Verifier (pinned copy c9ee66a): X1–X9 reproduced, integrity clean (no test edits 7a07dfd..c9ee66a, no skip/only, it counts exact, JSON 11→0), 11/11 mutants KILLED incl. own-choice M11 (privacy.title→claim.privacyButton cross-alias). Full `just ci` GREEN at c9ee66a (CI-EXIT=0, /tmp/m24-s5-ci1.log: nextest 2433/2433, 99/99 evals, client 3338/3338 in 128 files). Ledger 10/10 (X10 MANUAL with path:line evidence). PR #492.
