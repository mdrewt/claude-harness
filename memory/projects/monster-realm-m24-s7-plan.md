# m24-s7 build plan — the proof locale: `catalog.fr.ts` + §5.3 parity gate (planner output, 2026-09-20)

Context file: /tmp/m24-s7/context.md. Worktree: projects/monster-realm/.claude/worktrees/m24-s7 (branch feat/m24-s7-i18n-fr-catalog @ 72e638d). Toolchain: export PATH="$HOME/.asdf/shims:$HOME/.cargo/bin:$HOME/.local/bin:$PATH" (node 24.13.1).

Verified facts the brief lacked: CONTENT_VERSION is **22** (server-module/src/lib.rs:87, rb-82), not the spec's 21 — S7 pins "unchanged at 22"; parameterized keys are 35 (77 plain + 35 closures = 112); bigint params: trade.side.currency.amount, shop.buy.row.price, shop.sell.row.price.
Blast radius of the CATALOGS widening: resolver.test.ts:120 (pins ['en']), catalogShape.test.ts:116 (discovery == registry), main.i18nBoot.test.ts (mocked registry, unaffected), i18nTypes.compile.test.ts:98 (typeof, unaffected).

## 1. catalog.fr.ts
Mirror catalog.en.ts:1-46 header; `import type { Catalog } from './messageIds'`; `import { cldr, selectPlural } from './plural'`; hoisted `const WEATHER_TURN_FORMS = cldr({ zero: 'tour', one: 'tour', two: 'tours', few: 'tours', many: 'tours', other: 'tours' })` with UNQUOTED property names (a quoted `'one':` line parses as a catalog entry in catalogShape SHAPE-01); `export const CATALOG_FR: Catalog = Object.freeze({…} satisfies Catalog)`; entry order identical to en; every entry has a contiguous `// @desc:` block (≥10 non-ws chars on the marker line, no blank-line gap); closures keep en's exact `p.<field>` set; never a backtick inside `${…}`; the word `oneOther` never appears in the file (raw substring scan catalog.test.ts:126 + identifier scan catalogShape:1191).
DECISION (option B): `battle.weather.banner` = `${p.label} (${p.turns} ${selectPlural('fr', p.turns, WEATHER_TURN_FORMS)})` — first real use of the plural seam; CLDR fr `one` covers 0 AND 1; en stays byte-pinned. S8 must serialise this closure as ICU plural.
French typography: U+00A0 before `:` `;` `!` `?` and inside « »; `’` U+2019; `…`; sentence case with accented capitals; keep en glyphs `· — → ✓ • ★ ×`; keep the trailing space in shop.buy.row/shop.sell.row and the leading ` — ` in leaderboard.row; key names Esc/B/M/?/F8/F9 stay; abbreviations Lv→Niv., HP→PV, W/L→V/D, Acc→Préc.; digits raw (no fmtNumber — YAGNI); params are MODEL DATA interpolated verbatim; glyph-only/proper-noun keys may equal en; chrome.helpHint ≤ 47 NFC code points (`? pour l’aide · clic ou M pour le menu` = 38); box.hint must quote fr box.card.toParty verbatim.

## 2. catalogParity.test.ts (co-located vitest, NOT an eval — ADR-0224/0257/0262)
Imports: node:fs/path/url, vitest, stripComments from ../../../../evals/dom-shell-coverage-exclusion.eval.mjs, a11yCopy, { CATALOGS, currentLocale, setLocale, t, tf } from ./resolver, CATALOG_EN. NO static import of ./catalog.fr — RED at HEAD via the registry pin `['en','fr']`.
Census (on stripComments(raw), zero RegExp): walk client/src/**/*.ts minus .test.ts; per file parse `import { … } from '<spec>'` where spec endsWith('/i18n/resolver') or === './resolver' (module i18n) or endsWith('/a11yCopy') (module a11y); record local→{module, orig} for orig ∈ {t, tf}, skip `type` items; literal mask (sq/dq/tl code-context array); a call = local name at identifier boundaries, in code, not preceded by `.`, optional ws, optional balanced `<…>`, `(`; first arg: `'`/`"` literal followed by `,` or `)` → LITERAL; anything else incl. ANY backtick → DYNAMIC-KEY. Findings: PARITY-GAP (both directions, every registered locale), DYNAMIC-KEY, UNDEFINED-KEY (per-binding: i18n ∉ MESSAGE_IDS, a11y ∉ keys(a11yCopy)), DEAD-KEY (MESSAGE_IDS − requested − EXEMPT; EXEMPT = ['chrome.helpHint'] exactly, each exempt key asserted to have ZERO sites → STALE-EXEMPTION). MESSAGE_IDS belt: parse messageIds.ts source from `export type MessageId =` to next `;`, lines starting `| '`; length === Object.keys(CATALOG_EN).length and > 100. Memoise census once.
Tests (describe(name, { sequential: true })): PARITY-01 live + fixtures (fr minus shop.title → 1 missing; extra legacy.old → 1 extra; {} → finding; {en} → []; union-parser: two-type source parses only the first; commented `| 'dead.x'` not parsed); PARITY-02 live (roster == exact 19 files; main.ts i18n keys == six chrome.status.*, a11y == ['a11y.world.region']) + fixtures (a) t(`a11y.${x}`) (b) t(`box.title`) (c) const k as const; t(k) (d) 'shop.'+x (e) ternary (f) GOOD tf('leaderboard.row', {...vm}) (g) "don't (x)" decoy (h) obj.t( (i) total(/at(/wait( (j) dual-import main.ts shape (k) no-import file with t('x') → 0 (l) import type ignored; PARITY-03 live + fixtures (shop.notAKey; a11y t('a11y.nope'); i18nT('a11y.world.region')); PARITY-04 live + fixtures (legacy.old dead; STALE-EXEMPTION); PLURAL-01 table {en:[one,other], fr:[many,one,other]} keys == registry, supportedLocalesOf non-empty, categories equal. describe catalogFr: FR-01 Proxy field-set equality per closure + every «field» present in fr output + ≥100/112 differ + isFrozen; FR-02 weather plural (0≡1 form, 1≠2, 1e6 → many); FR-03 cross-locale (box.hint contains toParty; setLocale('fr') then t('chrome.help.title') === CATALOGS.fr[...]; finally en).
Predicted HEAD: 12 tests, 5 failed / 7 passed; after specialist 12 passed.

## 3. resolver widening (disclosed, touches-delta + SCOPE WIDENING)
resolver.ts: add `import { CATALOG_FR } from './catalog.fr'`; CATALOGS = Object.freeze({ en: CATALOG_EN, fr: CATALOG_FR }); comment "(S7 adds `fr` here)" → "(S7 added `fr`; S8+ adds here)". resolver.test.ts:120 → ['en','fr'], :95 title single-entry → two-entry.

## 4. Anti-patterns
-t zero-match → pin counts; exact sets not includes; fixture diversity; no nested template literals in entries; unquoted forms keys; describe object-form; no .only/.skip; implementer never edits catalogParity.test.ts; biome import order; no RegExp; no `oneOther` word in catalog.fr.ts; no static import of catalog.fr in the test; real NBSP char; no fmtNumber/Segmenter/console.warn; do not touch messageIds.ts/catalog.en.ts/index.html/evals.

## 5. Ledger X1..X12 — see memory/projects/gates/m24-s7.gates.md.

## 6. ADR-0263 outline
Title: The proof locale — catalog.fr.ts with a real CLDR plural, and the §5.3 parity gate as a co-located vitest census (M24 S7). Accepted · 2026-09-20 · m24-s7 · Extends 0256, 0262 · Subsystems client-ui, ci-gates. D1 vehicle; D2 registry widening disclosed; D3 binding-resolved literal-masked census, backtick = DYNAMIC-KEY, `.`-receiver/`${}`-embedded = accepted fail-loud limit; D4 per-binding UNDEFINED-KEY; D5 DEAD-KEY roster exactly {chrome.helpHint} + stale tooth; D6 plural option B + six-form rule; D7 French typography; D8 proof shape (registry-pin RED, Proxy field sets, ≥100-differs floor, PLURAL table == registry); D9 I18N-31 pinned at 22 (spec stale since rb-82). Rejected: eval file; static import for RED; bare-global t( scan; no-substitution backticks; union-only UNDEFINED-KEY; option A; oneOther; per-key byte pins; fmtNumber; translating a11y copy. Consequences: fr users see English a11y names/#help-hint/model data/code-appended ASCII colons; S8 exports selectPlural closure as ICU plural; adding a locale needs a PLURAL_CATEGORIES row + resolver.test.ts pin edit; CONTENT_VERSION 22 is the M24 baseline.

## 7. Sequencing
tester (RED, + resolver.test.ts sibling edit) → red-team-writes-the-cheat on the tests → specialist (catalog.fr.ts + resolver.ts) → reviewer ∥ verifier (mutants: delete a fr key; quote a forms key; oneOther in a comment; many→''; unregister fr; 48-cp helpHint) → docs → full just ci.

## 8. Plan-review deltas (reviewer ∥ red-team, 2026-09-20) — BINDING for the tester
- R1 (CRITICAL, red-team): calls inside a `${…}` interpolation of an enclosing template MUST be scanned (the code-context mask treats `${…}` payload as code, recursively). Tooth: `` `${t('shop.notAKey')}` `` → exactly one UNDEFINED-KEY. The ADR wording "fail-loud limit" for `${}` is WRONG — drop it; only the `.`-receiver indirection remains an accepted limit.
- R2 (HIGH): `t?.(` / `tf?.(` optional-call MUST be recognised as a call. Tooth: `t?.('a11y.nope')` (a11y binding) → one UNDEFINED-KEY.
- R3 (HIGH): multi-line import lists MUST resolve. Tooth: a fixture with the resolver import split over ≥3 lines (`import {\n  CATALOGS,\n  t as i18nT,\n  tf,\n} from './ui/i18n/resolver';`) plus a planted `i18nT('shop.notAKey')` → one UNDEFINED-KEY (proves the file was not silently dropped).
- R4 (MEDIUM): specifier match accepts `/i18n/resolver`, `/i18n/resolver.ts`, `/i18n/resolver.js`, `./resolver`, `./resolver.ts` (and the same for `/a11yCopy`). Re-exports and dynamic `import()` are accepted limits — record in ADR-0263.
- R5 (LOW): first-arg literal detection tolerates whitespace incl. newlines between the closing quote and `,`/`)`; `'a.b' as const`, `('a.b')`, `'a.b' satisfies X` classify as DYNAMIC-KEY (safe direction) — one fixture proves the ws tolerance.
- R6 (HIGH, ADR only): a renamed local `oneOther` helper evades the name-based SHAPE-06 ban; FR-02's category-distinctness assertion is the real backstop for the one plural key this slice adds — record in ADR-0263 consequences as guidance for S8+/third-locale authors.
- R7 (MAJOR, reviewer): FR-01's Proxy field-set check assumes branch-free closures (true of every en closure today: one unconditional template per key). State the assumption in the test comment + ADR; additionally call each closure twice with two distinct sentinel families so a sentinel-dependent cheat cannot pass.
- R8: the ledger's seed text says CONTENT_VERSION 21; X10 pins 22 (HEAD, rb-82) — this is deliberate.
