# rb-110 plan — REV 2 — rename EXPORT_REAP_MAX_DELETE_PER_TICK → EXPORT_REAP_MAX_READ_PER_TICK

Slice: rb-110 (residual R-rb-86-READCAP-NAME, source rb-86). Worktree `.claude/worktrees/rb-110`, branch `rb-110` from origin/master @2f5ae9d. ADR reserved: 0267 (free on origin/master; draft at `docs/adr/0267-rb110-export-reap-read-cap-named-for-the-read.md` — the file name is FINAL: T2 include_str!s it).
Touches: server-module/src/privacy.rs, server-module/src/privacy_tests.rs, docs/adr/0238-*.md, docs/adr/0231-*.md, docs/adr/0265-*.md, docs/adr/0267-*.md, ARCHITECTURE.md, docs/knowledge/** (expected untouched). touches-delta: docs/adr/DIGEST.md (`just adr-digest`).
NOT touched (hidden dependency → residual R-rb-110-CLIENTCITE, registered AFTER the lenses): client/src/net/connection.test.ts:5061-5062, client/src/net/store.test.ts:5547-5548. Harness spec §rb-110 and gates/rb-109.gates.md quote the old name as frozen criterion text — seed-hashed, not edited.
REV 2 = REV 1 + plan reviewer (APPROVE-WITH-CHANGES: B1, M2-M6, m1-m8) + plan red-team (15 findings). Baseline measured on the worktree: `cargo nextest run -p monster-realm-module --run-ignored all` → 1019 passed; `--features dev_reducers` → 1020.

## 0. Ground truth (measured 2026-09-25)
Old name, contiguous: privacy.rs ×8 (:1662 :1674 decl :1692 :1705 :1846 :1908 :1932 :1978), privacy_tests.rs ×24, ADR-0238 ×5 (:59 :302 :423 :437 :537), ADR-0231 ×1 (:435), ADR-0265 ×4 (:18 :44 :46 :52; :202 names STAMPS only — no edit), ARCHITECTURE.md ×3 (:2293 rb-85, :2295 rb-86, :2303 rb-107), client ×2.
**Two more privacy_tests.rs sites are split across `concat!` boundaries and invisible to grep** (red-team F1): `:18929` `concat!("(EXPORT_REAP_MAX_DELETE", "_PER_TICKasu64)")` in `rb107_nd_cap_decl()` and `:19012` `"_ROW_CAP: u64 = (EXPORT_REAP_MAX_DELETE"` in `rb107_cap_decl_source()`. Identifier-only view (keep only `[A-Za-z0-9_]` bytes) counts 26 in privacy_tests.rs, 24 raw. Both are consumed by `rb107_caps_are_the_reapers_drain_and_the_manifest_minimum` `[rb107/cap-source]`, which goes RUNTIME RED after the privacy.rs rename unless re-frozen; privacy_tests.rs:19004 forbids deriving one from the other ("a pin constructed from its own needle proves nothing").
Squashed (`stripped_for_scan`) counts in privacy.rs AFTER the rename (honest values): new name 3 (decl, EXPORT_LIVE_ROW_CAP, `.take`), `EXPORT_REAP_MAX_STAMPS_PER_TICK` 2 (decl, seam-call arg), prefix `EXPORT_REAP_MAX_` 5.
Line-neutrality facts: `EXPORT_LIVE_ROW_CAP` flat form is 144-146 cols with the new name (> max_width 100 → stays wrapped); the `.take` chain is ~140 cols flat (> chain_width → stays vertical, :1978 becomes 43 cols). docs/knowledge anchors: privacy.rs#L1514/#L1740/#L1827 — all ABOVE :1846/:1908/:1932/:1978, so `just knowledge-check` proves neutrality only above :1827; the ledger's X3 `wc -l` check (2025 lines) owns the rest. ADR-0265:55/:298 cite privacy_tests.rs:16799-16812 and :16826-16832 (currently accurate) → EVERY privacy_tests.rs edit is line-count-neutral in place and every new line is appended at EOF.

## 1. Name: `EXPORT_REAP_MAX_READ_PER_TICK` (unchanged from REV 1)
Family shape `EXPORT_REAP_MAX_<quantity>_PER_TICK`; READ parallels STAMPS and matches the code's own vocabulary ("bounds the READ at 256 rows / WRITE at 16 stamps", `ExportReapTick.read`). 29 vs 31 chars → line-neutral. Rejected: ROWS_READ_PER_TICK, READ_WINDOW_ROWS, any alias/compat const.

## 2. Tester tasks (owns privacy_tests.rs; in-place line-neutral re-freezes; ALL new code appended at EOF under a `// ===` rb-110 banner)
### 2a. Re-freezes (existing tests; keep every assertion's KIND — assert_eq! stays assert_eq!, equality stays equality)
Value reads → new name (10): :9084, :12164, :15708, :16199, :16773, :19803, :21890, :21895, :22113, :22120.
Frozen-text pins → new name (7, not 5): :8500 (rb48 `[E1/seam-params]` negative array — must carry the NEW name or the zero-count clause is vacuous), :11770 (`rb85_nd_take`), :11844 (`rb85_helper_body_pin` squashed literal; its one-comma twin at ~:11858 is derived by `replacen` — the replacement target must stay unique), :11959 (`rb85_helper_body_source` rustfmt-shaped literal), :16851 (`[rb86/cap-wiring]` take-arg equality), **:18929 (`rb107_nd_cap_decl`, concat!-split) and :19012 (`rb107_cap_decl_source`, raw-spelled control) — re-frozen INDEPENDENTLY of each other.**
Prose retruths (no assertion change; line-count-neutral): :9039-9045 doc comment ("kept deliberately … its own slice" is now false → say the rename happened in rb-110/ADR-0267); :9064 and :13604 are INSIDE assert failure-message string literals (rename the token; rustfmt does not reflow strings); :11544 banner; :14999/:15023 rb-86 banner; :18879 rb-107 banner; :21515 rb-109 banner quotes the frozen rb-109 criterion — rename the token and mark `(paraphrased — the frozen spec text predates the rb-110 rename)`; :22261 is a DOUBLE falsehood: the backticked fragment quotes E1 (which is about the READ cap) and glosses it "the WRITE bound" over a test about the STAMP cap → do NOT fabricate a STAMPS quote of E1; write "E1 has no write clause; this is the value oracle for the STAMP cap (`EXPORT_REAP_MAX_STAMPS_PER_TICK`)".
### 2b. New tests (4). Names are FIXED (the ledger's X1 filter names them):
**T1 `rb110_read_cap_constant_is_named_for_the_read_and_valued_256`**
- `[rb110/value]` `crate::privacy::EXPORT_REAP_MAX_READ_PER_TICK == 256` (a HOST read; forces existence).
- `[rb110/decl]` squashed PRIVACY_RS contains `format!("pub(crate)const{new}:usize=256;")` EXACTLY once, `new = stringify!(EXPORT_REAP_MAX_READ_PER_TICK)` (built, not hand-typed; `== 1`, never `contains`).
**T2 `rb110_the_delete_named_read_cap_is_gone_from_the_crate`** (the name claims the crate → the census must reach the crate)
- Needle: `let new = stringify!(EXPORT_REAP_MAX_READ_PER_TICK); let prefix = new.strip_suffix("READ_PER_TICK").expect(..); let old = format!("{prefix}DELETE_PER_TICK");` — the source never spells the old name contiguously AND the identifier-only view of the source does not fuse it (`{prefix}DELETE_PER_TICK` → `prefixDELETE_PER_TICK`). NEVER two adjacent string literals whose concatenation is the old name (the defrag clause would trip on itself). Failure messages print counts only.
- `[rb110/needle-witness]` `include_str!("../../docs/adr/0267-rb110-export-reap-read-cap-named-for-the-read.md")` contains `old` ≥ 1 — ADR-0267 is the one document that legitimately quotes the old spelling in full, so the needle is witnessed independently of the census it drives. (Green pre-rename: fine, it is a witness, not a tooth.)
- `[rb110/census-prod]` IDENTIFIER-ONLY view (`chars().filter(|c| c.is_alphanumeric() || *c == '_')`) of PRIVACY_RS: count(old) == 0; the message also prints the raw contiguous count. (pre: 8 raw / 8 ident-only.) A raw hit is always an ident-only hit, so one assertion covers both views; the file's own convention splits needles across `concat!`, which is why the ident-only view is the one asserted (/simplify: folded the separate defrag clause into the two file censuses).
- `[rb110/census-tests]` same over PRIVACY_TESTS_RS: ident-only count(old) == 0, raw count in the message. (pre: 24 raw / 26 ident-only — the two rb107 concat!-split pins.)
- `[rb110/census-crate]` for every `mod x;` item declared in lib.rs (port the ~15-line `real_sources()` reader from `privacy_enforcement_tests.rs:105-120`: parse lib.rs, read `env!("CARGO_MANIFEST_DIR")/src/<x>.rs`; include lib.rs itself) identifier-only count(old) == 0 per file; anti-vacuity: the list has ≥ 20 files and names privacy.rs. Kills `use crate::privacy::EXPORT_REAP_MAX_READ_PER_TICK as <old>;` anywhere in the crate. (If `real_sources` is reachable as `pub(crate)`, reuse it instead of porting.)
- `[rb110/no-alias]` squashed PRIVACY_RS: count(new) == 3, count(`EXPORT_REAP_MAX_STAMPS_PER_TICK`) == 2, count(`EXPORT_REAP_MAX_`) == 5 — three EXACT counts (no netting hole; a `paste!`/macro/`use as` form still carries the prefix). STAMPS and prefix needles concat!-split.
- `[rb110/ban-roster-live]` every distinct `"EXPORT_[A-Z0-9_]+"` double-quoted literal in raw PRIVACY_TESTS_RS exists ≥ 1 in raw PRIVACY_RS (measured 7/7 today). A negative census over a stale literal is a zero-count clause that passes over nothing — this is the generic closer for the `:8500` class.
- `[rb110/prod-residual-closed]` raw PRIVACY_RS count(`R-rb-86-READCAP-NAME`) == 0 (production must not still disclose a closed residual as open). Needle concat!-split. (pre: 1)
- `[rb110/census-witness]` LAST: raw count(new) ≥ 1 in PRIVACY_RS and ≥ 1 in PRIVACY_TESTS_RS by the same helper (anti-vacuity; pre: 0/0 — if it ran first it would steal the RED from the censuses).
**T3 `rb110_docs_name_the_read_cap_correctly`** — DOCS = ADR-0238, ADR-0231, ADR-0265, ARCHITECTURE.md via `include_str!("../../docs/adr/…")` / `("../../ARCHITECTURE.md")` (622 KB is fine). NEVER scans client/**.
- `[rb110/doc-new-name]` per-file FLOORS from the measured pre-state: ADR-0238 ≥ 5, ADR-0231 ≥ 1, ADR-0265 ≥ 4, ARCHITECTURE.md ≥ 3 (a rename, not a deletion, must leave at least as many). (pre: 0 everywhere)
- `[rb110/doc-old-name]` MARKER RULE, line-scoped (collect the matching lines, assert on each): every line containing `old` must also contain `rb-110`; marked lines ≥ 1 overall; at least one marked line also contains `R-rb-86-READCAP-NAME`. FORBIDDEN phrasings: file-level `contains(old) && contains(marker)`, `>= 1` on old-name lines without the per-line marker check — both are GREEN pre-rename (ARCHITECTURE.md:2295 already carries old + `rb-86` + the residual id on one line). (pre: 13 unmarked lines → RED.)
- `[rb110/doc-split-token]` per doc: identifier-only count(old) == raw count(old) AND identifier-only count(new) == raw count(new) (kills `EXPORT_REAP_MAX_<!-- -->DELETE_PER_TICK` and a mid-token line break; `rb67p_strip_html_comments` exists because that class was MEASURED).
- `[rb110/doc-stale-claim]` phrase bans, each MEASURED ≥ 1 pre-rename in the file it is scoped to and needles concat!-split: ADR-0238 `name retained; it is pinned in` (:437) and `a crate-visible rename is its own slice` (:438); ARCHITECTURE.md `keeps its now-misnamed name` (:2295); PRIVACY_RS `DISCLOSED rather than fixed` (:1671); PRIVACY_TESTS_RS `is kept deliberately (a crate-visible rename` (:9044). The census can only see lines with the token; this is the only instrument that sees an identifier-free falsehood.
- Clause ORDER: doc-new-name, doc-old-name, split-token, stale-claim; compute every count first and interpolate all into the FIRST clause's message so one stage-1 run records them all.
**T4 `rb110_test_roster_is_closed`** — the rb-109 shape at its MINIMUM after /simplify (~100 lines): `rb110_test_roster()` (4), `rb110_helper_roster()` (closed), `rb110_dependency_roster()` (7: `rb48_privacy_has_exactly_one_cfg_attribute` [cfg twin-arm decoy], `rb22p_no_bare_quote_in_privacy` [string/doc-alias decoys; pins that PRIVACY_TESTS_RS is the compiled module], `rb22p_scan_hygiene` [`r#` ban, strip pipeline], `rb86_bundle_cap_is_sixteen_and_the_read_cap_is_unchanged` [`.take(` ARGUMENT equality], `rb85_helper_body_exact`, `rb107_caps_are_the_reapers_drain_and_the_manifest_minimum` [`[rb107/cap-source]`], `rb48_reap_interval_and_batch_cap_pinned`), `rb110_label_roster()` (label, owner index), preambles (vacuity, roster-dup), per-name `fn <name>(` == 1 (tests AND dependencies), adjacency flush+indented summed == 4, decl-total = SQUASHED `fnrb110_` count == tests+helpers (the squashed view sees every visibility prefix and indentation and cannot be fed by string fixtures, so the line-start census and the attribute-block walker with its six control fixtures are DROPPED: a fifth test hidden behind a second attribute is owned by the ledger's exact full-suite count X2 and `--run-ignored all`, and the decl-total still moves), label census via `rb107_test_span` (carriers == [owner]; every test ≥ 2 labels), body floor (`iffalse{` 0, `return;` 0, squashed span ≥ 300). Reuse `rb22p_count`, `stripped_for_scan`, `rb107_test_span` — reuse is the file's convention; only the PREFIX is hardcoded per slice, so NO shared parameterised roster helper. Disclose in T4's doc: `#[ignore]` and a second attribute are not caught by the roster (the ledger's exact run counts own them).
### 2c. Hygiene for the new block (rb22p_scan_hygiene scans this file): `//` comments only; no `/*` `*/` `r#` `log::` `println!` `print!` `dbg!` `\"`; every production needle concat!-split; NO `[rb110/…]` label in any doc comment (a span runs to the NEXT `#[test]`, so T2's doc comment is inside T1's span → carrier list `[0,1]`); open the block with a `// ===` banner (it also terminates `rb107_test_span(.., "rb109_test_roster_is_closed")`, which today runs to EOF — safe, still far above the 300-byte floor).

## 2d. Tests-review outcome (reviewer APPROVE-WITH-CHANGES; red-team: honest GREEN reached 1023/1024 on a scratch rename, 45 cheats measured, 13 survivors) → tester round 3 closes: label census over comment-stripped spans (I1/I2); body-floor via extracted squashed body `!starts_with("if")` + zero `return` tokens (E4/E5); span→roster label total; `#[ignore]` zero in the block (C3b); census-crate recurses one level (nested `*_tests.rs` + a module declared from privacy.rs; missing file REPORTED) with the corpus size pinned EXACTLY (M1/M2/E1f); `format!("EXPORT_` banned in the test file (E3); T3 new-name floors counted over HTML-comment-stripped text + per-doc OLD-name CEILINGS on raw text — ADR-0238 ≤ 4, ADR-0231 == 0, ADR-0265 == 0, ARCHITECTURE.md ≤ 2 (E2/F2); marker `rb-110` must not be followed by a digit (B4); prose fixes (:9038 rb-110 moved one token; :11544/:14999 do not attribute the new spelling to rb-85; :22261 duplicate sentence). Unclosable in-test and therefore ON THE VERIFIER'S CHECKLIST: `[rb110/decl]` `== 1` weakened to `>= 1` (C5); `assert_eq!(x, 0)` → `assert!(x >= 0)` on the censuses (I4); a clause deleted together with its roster entry (E9); T4 itself hollowed (C11); T1 hollowed to a label + filler above the 300 floor (E8); `rb110_label_roster().len()` must stay ≥ 22 (21 + label-total).

## 3. Implementer tasks (a DIFFERENT agent; never edits privacy_tests.rs)
**Constraints the tests impose on the DOCS (measured by the red-team — violating any of these is a RED, not a style point):** (i) every physical line that keeps the old name must ALSO carry `rb-110` on that SAME line (the marker rule is per line, and hard-wrapping a bracket note across two lines was measured RED at ADR-0238:438); (ii) new-name FLOORS per file — ADR-0238 ≥ 5, ADR-0231 ≥ 1, ADR-0265 ≥ 4, ARCHITECTURE.md ≥ 3 — counted with HTML comments stripped, so the three ADR-0238 in-place renames (:59 :423 :537) alone give 3 < 5: the :302 history note and the :437 bracket note must each ALSO spell the NEW name on their own line, and the dated amendment adds more; ARCHITECTURE.md's :2295 retruth must spell the NEW name (`:2293` + `:2303` give only 2); (iii) old-name CEILINGS per file on raw text — ADR-0238 ≤ 4 (:302 note, :437 note, one or two amendment lines), ADR-0231 0, ADR-0265 0 (rename every citation; a kept-and-marked citation is a RED), ARCHITECTURE.md ≤ 2 (:2295 + the new `**rb-110**` paragraph); (iv) no identifier split across a line break or an HTML comment; (v) the banned phrases must vanish from their files: ADR-0238 "name retained; it is pinned in" / "a crate-visible rename is its own slice", ARCHITECTURE.md "keeps its now-misnamed name", privacy.rs "DISCLOSED rather than fixed" and the residual id `R-rb-86-READCAP-NAME`; (vi) privacy.rs stays 2025 lines with zero `"` bytes added; squashed privacy.rs must name the read cap exactly 3×, the stamp cap 2×, the `EXPORT_REAP_MAX_` family 5× (no new consumer, no alias). The red-team's honest reference patch (privacy.rs + the four docs, 26/26 line-neutral, measured GREEN) is at `/tmp/rb110-tests-redteam/rb110-honest-impl.patch` — it lacks the ADR-0238 amendment and the ARCHITECTURE.md rb-110 paragraph, which the implementer writes.
privacy.rs (line-count-neutral, 2025 lines, zero `"` bytes added): rename :1662, :1674, :1692, :1705, :1846, :1908, :1932, :1978. Rewrite :1670-1673 IN 4 LINES: :1670 must still complete :1669's sentence ("…because a creation / stamp selected from inside this window carries a tail beyond its edge."), so ~3 lines are free: say what the constant IS (the READ window since rb-86; the write bound is STAMPS below), that rb-110 (ADR-0267) named it for the read; NO cross-file meta-claim ("pinned in N tests/documents"), NO `R-rb-86-READCAP-NAME`, NO "DISCLOSED rather than fixed", no double quote. `cargo fmt --check` clean.
ADR-0238: rename in place :59, :423, :537 (identifier citations in live text). **:302 sits in a sentence ADR-0238 itself declared "SUPERSEDED and left in place as history" (:412)** → do NOT rewrite it; append a same-line bracket note `[rb-110: this constant is now EXPORT_REAP_MAX_READ_PER_TICK — ADR-0267]` so the line carries `rb-110` (marker rule) while the history stays. :437-439: replace the whole "(name retained; it is pinned in ten places and three documents, and a crate-visible rename is its own slice)" parenthetical with a bracket note that spells the rename on ONE physical line together with `rb-110`, e.g. "The row cap `EXPORT_REAP_MAX_READ_PER_TICK` [rb-110 renamed it from `EXPORT_REAP_MAX_DELETE_PER_TICK` (ADR-0267; residual R-rb-86-READCAP-NAME closed) — see the amendment below] is now the READ window only, and its doc comment says so." Append `## Amendment (2026-09-25, rb-110 — residual R-rb-86-READCAP-NAME closed)` after the rb-109 amendment: what was renamed (old → new, on lines that carry `rb-110`), why (ADR-0267 D1-D4), an **Edited in place (identifier citations only)** list (:59, :423, :437, :537) and a **Left as history with a bracket note** list (:302), so :723's convention statement stays true; DIGEST unchanged (header-derived).
ADR-0231:435: rename + retruth the sentence — "so it can cut across one owner's request" has been false since rb-86 (whole-bundle stamp deletes); keep line count if possible.
ADR-0265 :18, :44, :46, :52: rename (the derivation is otherwise unchanged; :202 untouched).
ARCHITECTURE.md: :2293 (rb-85) and :2303 (rb-107) rename in place. :2295 (rb-86, a single physical line) keeps ONE old-name mention and RETRUTHS the tense: "the constant keeps its now-misnamed name because it is pinned in ten places — R-rb-86-READCAP-NAME" → "the constant KEPT its by-then-misnamed spelling `EXPORT_REAP_MAX_DELETE_PER_TICK` because it was pinned in ten places — R-rb-86-READCAP-NAME, closed by rb-110 (ADR-0267), which renamed it `EXPORT_REAP_MAX_READ_PER_TICK`" (the line must carry `rb-110` AND `R-rb-86-READCAP-NAME`). New `**rb-110**` paragraph after :2307 (before `## M24`): may spell the old name (its line starts with `**rb-110**`).
ADR-0267 (draft exists; the doc-keeper finalises): D3 = marker rule; Extends-not-Amends rationale; Consequences softened to what the clauses prove; note that privacy_tests.rs:21515/:22261 stop being verbatim quotations; Confirmation names T1-T4 + the re-frozen pins.
Gate order: `just ci-fast monster-realm-module` (clippy -D warnings, nextest, doctests) · `cargo fmt --check` · `just knowledge-check` (green WITHOUT regen) · `just adr-digest` + `just adr-digest-check` · full `just ci` once (detached, CI-EXIT marker) · `mr-gates check --slice rb-110 --timeout 2400` from the worktree.

## 4. RED/GREEN staging (record: memory/projects/gates/rb-110.red-before.md; name the exact tree for each stage)
Stage 1 (runtime RED): unrenamed tree + ONLY T2/T3 and their helpers (no T1, no T4, no re-freezes) → `[rb110/census-prod]` 8, `[rb110/census-tests]` 24, defrag 8/26, no-alias READ 0, prod-residual-closed 1; `[rb110/doc-new-name]` 0 everywhere, `[rb110/doc-old-name]` 13 unmarked lines, stale-claim ≥1 per site — first-failure-wins, so the FIRST clause of each test must interpolate every count.
Stage 2 (build RED): unrenamed tree + the FULL tester patch → E0425 ×11 on the new name (10 re-frozen value reads + T1).
Stage 2b (re-freeze necessity, implementer-measured on the RENAMED tree): revert only the :18929/:19012 re-freeze → `[rb107/cap-source]` RED; restore → GREEN. Same for `[rb86/cap-wiring]` (:16851) and `rb85_nd_take` (:11770).
GREEN: renamed tree + full patch, ZERO further test edits: 1019+4 = 1023 (1024 with dev_reducers).
Verifier asserts: every re-frozen site still assert_eq!/equality (no contains/<=); assertion count not decreased; no #[ignore]/#[cfg] on rb110_ or re-frozen tests; declaration census == 1; anti-vacuity clauses present and LAST; privacy.rs line count 2025; privacy_tests.rs edits above the new block are line-count-neutral; old name repo-wide only at: ARCHITECTURE.md marked lines, ADR-0238 marked lines, ADR-0267, the two client comments, harness spec/ledger (frozen).

## 5. Ledger (memory/projects/gates/rb-110.gates.md — 0 seeded criteria; X-gates authored in the PLAN phase)
X1 the four rb110_ tests by exact name → `4 tests run: 4 passed`; X2 full suite both feature sets 1023/1024 (node spawnSync, stdout+stderr joined); X3 privacy.rs line count == origin/master's (2025); X4 `just knowledge-check` in sync; X5 `just adr-digest-check` no drift; X6 the eleven re-frozen/consumer tests by exact name → `11 tests run: 11 passed`; X7 MANUAL — the RED record's deciding line. Run `mr-gates check` FROM the worktree with `--timeout 2400`; nextest's Summary is on stderr (`2>&1`).

## 6. Risks / anti-patterns
Alias const (D2); meta-claim comments (D4, four copies existed: privacy.rs:1671-1673, privacy_tests.rs:9044-45, ADR-0238:437-439, ARCHITECTURE.md:2295); a `[rb110/…]` label quoted in a doc comment; a `>= 1` old-name doc clause (green pre-rename); deriving one rb107 pin from the other; inserting lines above privacy_tests.rs:16799 (ADR-0265 citations) or anywhere in privacy.rs; scope creep into client/** or rb-111+; ADR-0267 reservation race (re-check `ls docs/adr/` before the commit — 0267 free at 2f5ae9d).
Workflow: tester (opus, separate) → tests reviewer + tests red-team → implementer (opus, separate) → ci-fast → reviewer + /simplify + red-team + reducer-security-auditor + desync-guard (N/A surface, run for the record) + verifier → doc-keeper → full `just ci` → mr-gates check → PR (touches-delta: DIGEST.md; boyscout-delta: none expected) → STOP (supervisor merges).
