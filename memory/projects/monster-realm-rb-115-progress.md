# rb-115 progress memo — TERMINAL STATE reached (PR open + local `just ci` green + ledger 7/8 met, 1 DEFERred, 0 unmet); written for the supervisor's audit, not a park

PR: https://github.com/mdrewt/monster-realm/pull/509 · branch `rb-115` · worktree `.claude/worktrees/rb-115` · head 6e84fb6 (base origin/master 584cce2) · remote CI running · SUPERVISOR OWNS THE MERGE (`gh pr merge` is forbidden for the run).

## DONE
- Plan REV 2 (`memory/projects/monster-realm-rb-115-plan.md`): planner + plan reviewer + plan red-team (both APPROVE-WITH-CHANGES) + /simplify. ADR-0269 (`docs/adr/0269-rb115-export-reap-tick-reports-pre-truncation-stamp-count.md`, **Extends** 0238) + ADR-0238 dated amendment (:830) + ARCHITECTURE `**rb-115**` paragraph (:2313) + DIGEST regen.
- 5 `rb115_` tests (tester, opus, sandbox) + 14 attributed re-freezes, line-neutral above privacy_tests.rs:16799 (X4). RED record `memory/projects/gates/rb-115.red-before.md`: stage 2 build RED (3 predicted errors), stage 1' `1024 passed, 12 failed`, stage 3 deciding RED (4 tests on the predicted clauses), GREEN 1036/1036 + 1037/1037 with zero test edits; register 13/13 mutants killed, 3/3 stage-4 reverts red, 2/2 controls green; verifier's 4 own mutants killed.
- Production: `count_export_reap_stamps` (pure, runs the frozen seam with cap `rows.len()`), `ExportReapTick.due`, `reap_fields` fourth key, helper binding; seam + reducer shell byte-identical; comments retruthed (no residual id in production).
- Lenses under the `/tmp/mr_warn_rb-115` landing flag (arrived after stage 2): reducer-security-auditor PASS-WITH-NOTES (nit adopted), verifier APPROVE (items 1–9 PASS). Implementer = orchestrator applying the plan's rustfmt-verified bytes; desync-guard not run (no game-core/wasm/client surface).
- Local gate: `just ci` run 1 red ONLY on `observability-validate` (Docker Desktop was down on the host — dangling `/usr/bin/docker`); Docker Desktop started; full `just ci` run 2 on the identical tree `CI-EXIT=0` (`/tmp/rb-115/logs/ci2.log`).
- Ledger `memory/projects/gates/rb-115.gates.md`: X1–X6 met by `mr-gates check` (run FROM the worktree), X7 MANUAL met (EVIDENCE → red-before.md:60), X8 DEFER -> backlog (the window-edge half; mints R-rb-115-X8, MED). `mr-gates lint`: one WARN (X8 has no CHECK/MANUAL rationale line — a deferred gate), no BLOCK.

## REMAINING (supervisor)
- Watch remote CI on PR #509 (mr-ci-watch); `mr-gates verify --slice rb-115` FROM `.claude/worktrees/rb-115`; squash-merge on the CI event (one Conventional Commit `feat(rb-115): …`).
- `mr-gates residuals close --slice rb-115 --pr 509` → closes R-rb-87-BACKLOGAMBIG (truncation half); the X8 DEFER line mints R-rb-115-X8 (MED) for promotion. R-rb-109-ORDERMODEL stays open (gains one-directional evidence only).
- Reconcile CHANGELOG (git-cliff from the squash commit) / ADR index README / ARCHITECTURE across siblings at the serial merge (no sibling ran concurrently this time).
- Post-merge code-graph refresh on the MAIN checkout (cbm `detect_changes` + `index_repository`; `codegraph sync`) — not done here (the canonical checkout is still at master).
- Housekeeping: `/tmp/rb-115/` (logs, patch scripts, `redteam/ws*` scratch copies with cargo targets, ~2 GB) can be deleted after merge; the tester sandbox worktree `/tmp/rb-115-sandbox` was removed by the run.

## BLOCKERS
- none

## exact-next-step
- supervisor: `mr-gates verify --slice rb-115` FROM `.claude/worktrees/rb-115`, then merge PR #509 on the CI event.

## mr-gates render --format park (verbatim)
# rb-115 — park memo (generated by mr-gates)

## DONE (7)
- X1: [rb-115 teeth — the truncation half of R-rb-87-BACKLOGAMBIG] WHEN the five rb115_ tests run by exact name over
  evidence: Summary [ 0.110s] 5 tests run: 5 passed, 1031 skipped
- X2: [non-regression] WHEN the full server-module suite runs with ignored tests forced on, default features and --f
  evidence: rb115-X2:SUITE-GREEN
- X3: [re-frozen pins still bite] WHEN the fourteen pre-existing tests whose assertions or fixtures this slice re-fr
  evidence: Summary [ 0.050s] 14 tests run: 14 passed, 1022 skipped
- X4: [inbound test-file citations still resolve] WHEN the privacy_tests.rs line span ADR-0265 cites for [rb86/stamp
  evidence: rb115-X4:CITATION-RESOLVES
- X5: [knowledge bundle in sync] WHEN `just knowledge-check` runs on the slice tree THE SYSTEM SHALL report the comm
  evidence: okf-export: bundle in sync (no drift)
- X6: [ADR digest current] WHEN `just adr-digest-check` runs on the slice tree THE SYSTEM SHALL report DIGEST.md up-
  evidence: adr-digest: DIGEST.md is up-to-date (no drift).
- X7: [RED before, GREEN after — ADR-0224 proof of teeth] WHEN the full tester patch is applied to the unfixed tree 
  evidence: /home/mdrewt/projects/ai-apps/claude-harness/memory/projects/gates/rb-115.red-before.md:60

## REMAINING (0) — the exact next steps

## DEFERRED (1)
- X8 -> backlog — `due` is taken over the READ WINDOW, so it inherits the window's blindness by construction: a tick that reads a full window (read == EXPORT_REAP_MAX_READ_PER_TICK) and plans every stamp it saw cannot say whether expired rows remain past the window's edge, and on the live tree (bundles >= 17 rows, ascending btree yield) due == planned on every well-formed tick, so the new count never shows a partly drained backlog there — measured on the rb-109 oversized population: (256, 16, 272) with due 16 still leaves four expired bundles. The residual R-rb-87-BACKLOGAMBIG named both halves; this slice closes the truncation half (exactly-sixteen vs truncated, direct and order-independent) and this gate is the drained-vs-partial half (the docs name it R-rb-115-X8, the id this DEFER line mints). Candidate fix (its own slice, MED): after the point deletes, ONE post-delete range probe `ctx.db.export_bundle().created_at_ms().filter(..=cutoff).next().is_some()` published as a raw observation (one range syscall, at most one buffer fill; the native host already models the range read), which re-freezes [rb85/range-census], [rb86/stamp-index-reaches] / [rb111/index-reach] and the helper body pin — a deliberate reshape of the read-side censuses that this slice must not smuggle in. Zero-cost interim: an ops rule on N consecutive full-window ticks (the rb-87 X10 alarm item).
