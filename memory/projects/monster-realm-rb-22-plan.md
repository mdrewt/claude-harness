# rb-22 plan — pre-claim export_bundle orphan purged at claim time (R-m22-s2-S3-GUEST-EXPORT-ORPHAN)

Worktree `.claude/worktrees/rb-22`, branch `slice/rb-22`, fork master@48fc867. ADR number reserved: **0220**.
Touches: `server-module/src/accounts.rs`, `server-module/src/privacy.rs` (NEW), `server-module/src/lib.rs`
(+ companions: accounts_tests.rs, privacy_tests.rs (NEW sibling), docs/adr/0220-*, docs/knowledge/**, ARCHITECTURE.md;
generated: docs/adr/DIGEST.md — declared as generated artifact of the ADR, not scope widening).

## Decision (Option B — purge at claim; ADR-0220)
Delete the GUEST's `export_bundle` rows inside `complete_guest_claim`'s write phase, via a delegated
helper in the new owning module `privacy.rs` (M22 §7.2 assigns export_bundle to privacy.rs; G5/D0 bans
the write in accounts.rs — accounts_tests.rs:2119 + eval [W/*]). NOT rekey (Option A): would falsify the
EXEMPT policy → forced edits to evals/guest-claim-integrity.eval.mjs POLICY_SHAPES/REKEY_MANIFEST
(outside touches → hidden-dep STOP) + collision risk with S4's chunk-tuple uniqueness (guard 11 never
consults export_bundle). NOT TTL-only: S4's reaper doesn't exist; TTL is not a substitute for cascade
erasure (playtest_event doctrine, the manifest's own reason text). The manifest's HONEST LIMIT reason
(eval:1864-1873) itself prescribes "delete chunks at claim time" first. Purge ONLY the guest's chunks
(claimer's identity stays live). Verified: NO writer/reader of export_bundle exists anywhere (S4 absent)
— rb-22 closes the hole structurally ahead of S4.

## Measured facts (planner, re-verified)
- F1 delete idiom: collect-PKs-then-delete-by-PK (disarm_claim_reaper accounts.rs:319-333; rekey_inventory
  inventory.rs:105-119). guest_claim.guest_identity is #[primary_key] so its .delete() is NOT a btree precedent.
- F2 forced test edit: `data_lifecycle_manifest_totality_bidirectional` (accounts_tests.rs:3465) mod census
  (:3529-3547) REDs on `mod privacy;` unless `("privacy.rs", include_str!)` row added to m22_scanned_sources
  (:3145-3169). `mod privacy_tests` skipped (`tests` suffix, :3248-3250).
- F3 RED-ordering: include_str!("privacy.rs") can't exist pre-fix (compile error). Pre-fix RED comes from
  accounts_tests scans of ACCOUNTS_RS/LIB_RS + a RUNTIME read of privacy.rs (env!(CARGO_MANIFEST_DIR) idiom,
  observability_tests.rs:439, pvp_tests.rs:734-737 "file does not yet exist" precedent). Census row lands in fix commit.
- F8 call-site: insert `crate::privacy::purge_export_bundles(ctx, guest);` between rekey_all(...)?; (:510)
  and consume_claim_and_disarm (:511). AUTH-34/[S/*] pins unaffected. TRAP: [S/reachable] (eval:1542-1555)
  bans the token `return` between rekey_all( and consume — no belt early-returns.
- F4/F5 whole-tree scanners: privacy.rs must have NO block comments, NO raw strings, no `"` in char literals,
  no `/` in string literals, no log:: token, no print macros; privacy_tests.rs needs #![cfg(test)] and
  privacy.rs declares `#[cfg(test)] #[path = "privacy_tests.rs"] mod privacy_tests;` (accounts.rs:608-610 form;
  monster-privacy eval :2759-2800 requires it). Never write table/reducer attrs contiguously in the tests.
- F6 knowledge bundle: lib.rs line shifts drift reducer pages' `#L<line>` pins → `just knowledge` AFTER code commit.
- F7 DIGEST.md regenerated by `just adr-digest` after ADR-0220.
- Highest ADR on disk = 0219 → 0220 free.

## Tasks
0. STUB PROBE (orchestrator): placeholder privacy.rs + mod line → run lint/nextest/evals → ONLY
   data_lifecycle census may red. Measures F4. Keep stub.
1. Tester (opus) authors gating tests → /tmp/rb-22-tests/ (tester can't write worktrees/run anything);
   orchestrator applies + runs RED proof → memory/projects/rb-22.red-before.txt.
   1a accounts_tests.rs: rb22_claim_purges_guest_export_bundles_call_site (count==1, exact squashed expr
   `crate::privacy::purge_export_bundles(ctx,guest)`, statement form, depth-0, order rekey_all<purge<consume<update<Ok,
   no `return` in region), rb22_purge_called_exactly_once_in_accounts_rs (whole-file count==1),
   rb22_lib_wires_mod_privacy (modprivacy; ==1, no cfg(test) guard), rb22_privacy_module_purges_by_owner_index
   (RUNTIME read, fail-loud when absent), rb22_accounts_header_names_the_privacy_delegate (header truth).
   1b privacy_tests.rs: declared-once, owner-scoped filter + ban .iter(), collect-then-delete shape,
   no-early-return, writes-only-export_bundle (write_target_accessors port), no-identity-ctor port,
   scan hygiene self-test, stripper machinery control (needle in comment/string → 0).
2. Specialist implements (privacy.rs helper `pub(crate) fn purge_export_bundles(ctx: &ReducerContext, owner: Identity)`
   — owner-generic for S3 cascade reuse; lib.rs `mod privacy;` alphabetical (playtest<privacy<pvp);
   accounts.rs call + header fix; accounts_tests.rs census row (mechanically forced, F2)).
3. Bite-proof memory/projects/rb-22.bite-proof.mjs: temp copy, 18 mutants W1-W18 (see planner output),
   each pinned to NAMED test + message substring; function-replacer + applied-count asserts; controls
   (healthy pass + literal test count). Deciding line: RB22-BITE-PROOF tests=<N> mutants=18 caught=18/18 survived=0 controls=2/2 verdict=Y.
4. Docs: ADR-0220 (decision, rejected A/C, idiom, honest limits i-iv incl. stale manifest reason DEFER,
   S3 still owes deleting-account erase, S4 still owes TTL reaper), ARCHITECTURE.md module-map row,
   just knowledge, just adr-digest.
5. Live probe INFEASIBLE (no writer exists, private table, rig edit outside touches) → EO-9 DEFER backlog.

## Review lenses
Plan: reviewer + red-team (parallel). Artifact: reviewer + red-team (test file focus) + reducer-security-auditor.
desync-guard SKIPPED — zero game-core/netcode/client surface (delete-only helper; no rule logic). verifier last.

## Environment
export PATH="$HOME/.asdf/shims:$HOME/.cargo/bin:$HOME/.local/bin:$PATH"; verify node=24.13.1 in worktree.
Fresh worktree: client npm ci + just wasm needed before full `just ci`. cargo-nextest 0.9.138 present.
just ci LOCAL needs NO spacetime running (account-e2e lock); don't run evals concurrently with `just ci`.

## PLAN-REVIEW OUTCOME (reviewer ‖ red-team, both MEASURED — run before any test was written)
Reviewer: F1/F2/F8, ledger shape, signature/visibility/mod-placement, header-fix necessity all CONFIRMED
with citations; blast radius via both graphs clean. MAJOR M1: the no-block-comment/no-unpaired-`/*` hygiene
ban must cover privacy_tests.rs TOO — ≥12 evals concat ALL .rs incl. _tests.rs with the naive
`/\/\*[\s\S]*?\*\//g` stripper (recruit-reducer-security:1479+42, battle-lifecycle-gc, practice-xp,
migration-smoke-test, raising/battle/npc security, no-idle-accrual, spacetime-type-snapshot,
zone-warp-server-runtime, zoned-schema, battle-schema-snapshot), and privacy* sorts BEFORE pvp/raising/
schema in the blob — adversarial fixture strings must be composed from concat! fragments, never a
contiguous `/*`, `*/`, `"` landmine. L1: `#![cfg(test)]` does NOT contain the `#[cfg(test)]` substring —
monster-privacy justification comes from the PARENT decl `#[cfg(test)] #[path = "privacy_tests.rs"] mod privacy_tests;`.

Red-team (standalone harness with VERBATIM-copied scan machinery + real clippy runs — /tmp/rb22-attack/):
- CRITICAL needle bug: squash_ws removes ALL ws → the loop needle is `foridinids{` (the plan's spelling
  was impossible). Rule: DERIVE every needle by running the real pipeline; never hand-type.
- CRITICAL alias bypass: `let db = &ctx.db; db.account()...delete(owner);` in privacy.rs — write_target_accessors
  rfind misattributes to export_bundle (MEASURED green, clippy-clean). The eval's strong [W/db-binding]/
  [W/ctx-binding] bans scan accountsSrc ONLY. Counter: privacy_tests.rs must ban `let<ident>=&ctx.db`/
  `=&ctx.db;`/any non-`ctx` ReducerContext binding AND carry the exact-body-equality pin.
- CRITICAL dead-branch: `if false { <correct body> }` passes every containment pin, clippy-clean.
  Counter: EXACT-EQUALITY pin of the whole squashed helper body (hand-frozen literal in the test,
  compared to pipeline-derived body; authored by generating from the shipped source once).
- HIGH shadows: `let ids = Vec::new();` re-bind / `let id: u64 = 0;` in-loop — closed by the equality pin.
- HIGH `let guest = me;` shadow between rekey_all and the purge — closed by asserting
  `body.matches("letguest").count() == 1` in complete_guest_claim's squashed body.
- MEDIUM: all privacy body scans MUST be extract_squashed_fn_body-scoped (never whole-file .contains);
  add a decoy-FUNCTION control fixture (correct text in a second fn → scoped extractor sees 0).
- LOW (pre-existing): char/byte literals blind the brace matcher — naive single injection self-defeats
  (extractor returns None = loud). Backlog note in ADR, not a gate.
- Mutants W19-W23 added: W19 dead-branch-wrapped correct body; W20 shadowed-ids-to-empty; W21 shadowed
  loop id-to-constant; W22 aliased extra write (db.account().delete) appended to a correct body;
  W23 `let guest = me;` shadow (distinct from W2's textual arg swap). Total mutants: 23.
- Tooth structure: keep SEMANTIC teeth (owner-scope, collect-delete shape, no-return, alias bans) as
  separately-named tests with their own messages (per-mutant message pinning stays possible), with the
  equality pin as the backstop tooth that owns W19/W20/W21/W22's kills where no semantic tooth reds first
  — bite-proof pins each mutant to its named tooth.
- write_target_accessors' statement-boundary blindness is PRE-EXISTING and shared with shipped G5; fixing
  the shared helper inside rb-22 risks false-REDs on accounts.rs's existing writes (Vec::insert etc.) —
  NOT changed here; new gate EO-11 DEFERred to backlog for a dedicated hardening slice.

## ARTIFACT-PHASE LENSES (reviewer ‖ red-team ‖ reducer-security-auditor, on shipped c0b3c48)
- reviewer: BLOCKERS/MAJOR/MINOR = NONE. Verified machinery copies in privacy_tests.rs are byte-identical
  to accounts_tests.rs originals; no name collisions; frozen literals satisfiable; citations truthful.
- reducer-security-auditor: PASS. `guest` derived only from server-side guest_claim.guest_identity (never
  client-steerable to a victim); purge changes no accept/reject decision; transactional; no leak/log/DoS
  (table empty today). Nit 2 → closed by rb22_purge_named_nowhere_else_in_crate (crate-wide naming census,
  16th test). Nits 3-5 (S4 must cap per-owner chunks; stale eval prose; write_target_accessors) → DEFER/ADR.
- red-team (ARTIFACT, MEASURED on a copy, clippy-clean): TWO real gate holes the plan-phase pass missed —
  * Finding 1 (CRITICAL): strings-before-comments stripper → a BARE `"` in a // comment opens a fake string
    that swallows real compiling code (an arbitrary-Identity account delete) from EVERY squashed-text pin
    incl. the frozen-body equality backstop. Closed: rb22p_no_bare_quote_in_privacy ([hygiene/bare-quote],
    production-only — privacy.rs must carry exactly the one sanctioned "privacy_tests.rs" literal, zero
    other quotes). Mutant W25.
  * Finding 2 (MEDIUM): [call/reachable] scanned rekey→purge only; an early return in the purge→consume gap
    skips AUTH-34 consume + AUTH-21 stamp while returning Ok (JS eval catches it, Rust arm didn't). Closed:
    widened the region to rekey→consume (matches [S/reachable]). Mutant W26.
  * 9 other attack classes measured DEAD (unicode ws, macro relocation, post-purge shadow, ctx.db() method,
    alias-spacing, renamed-context helper, include_str! self-scan).
Final gate set: 17 tests (5 accounts-arm + 12 privacy-arm), 27-mutant bite-proof, both counter-teeth land.

## Risks
R1 unknown census eval → Task 0 measures. R2 clippy unused import. R3 vacuous nextest filter → literal counts.
R4 nightly mutate-server cap — source-scan pins see mutated source via include_str!. R5 regen order.
R6 remote-only gates (gitleaks/semgrep/bindings-drift) — no ws:// literals, no print macros; local semgrep on changed files pre-PR.
R7 assertStripperSound on privacy.rs → Task 0 + hygiene test. R8 ARCHITECTURE row rot accepted (ADR limit).
