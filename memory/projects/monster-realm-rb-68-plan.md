# rb-68 PLAN v2 (post reviewer + red-team + /simplify, 2026-09-07)
# ADR-0230 PRV1-17/PRV1-20 evidence chain — residual R-rb-40-ADR0230 (+ R-rb-65-ADR0230-PRV120)

Worktree: projects/monster-realm/.claude/worktrees/rb-68 · branch feat/rb-68-adr0230-mrlog-evidence (from b1b4ff6)
v1 is superseded. Every change below traces to a lens finding; MEASURED findings win over speculative ones.

## 0. RESHAPING FINDING (v1, CONFIRMED by reviewer)
The accounts.rs mr_log CALL-SITE CENSUS IS ALREADY SHIPPED:
  accounts_tests.rs:14580 rb40_claim_emits_one_purge_observation (census :14609-14640; #[cfg ban :14654; breadcrumb ban :14643;
    terminality via rb40_frozen_emit_tail :14475 consumed at :14679)
  accounts_tests.rs:15697 rb65_reaper_emits_one_cascade_observation (census :15759-15792; decl-uniqueness :15702; alias :15795)
=> a NEW census tooth adds ZERO coverage. rb-68's new tooth is the DOCUMENT side. The census is a test-time DERIVATION SEAM.

## 1. FACTS (all re-verified by the reviewer; corrections applied)
claim   : decl accounts.rs:691 `pub fn complete_guest_claim(` ; emit :773 ; evt "guest_claim_export_purge" ; fields `fn purge_fields(` :786 [rb-40/ADR-0235]
cascade : decl accounts.rs:1029 `pub fn account_deletion_reaper(` (attr :1028) ; emit :1068 ; evt "account_deletion_cascade" ; fields `fn cascade_fields(` :801 [rb-65/ADR-0243]
Terminality = last stmt before the trailing Ok(()) (:774 / :1069). Claim INLINES the builder; cascade BINDS first (:1067). One frozen tail cannot cover both.
Section body = ADR-0230 lines 108-166 (heading :107; next `## ` at :167). Exactly ONE `<!--`, at :94, inside a code span, in a DIFFERENT section.
Zero \r, zero whitespace-only lines, zero ``` fences, zero chars from the hidden set.
NON-ASCII INVENTORY (red-team MEASURED; v1's survey was incomplete): U+2014 x23, U+00A7 x9, U+2013 x2, U+2019 x1. THAT IS THE ALLOW-SET.
Backtick parity: every line has an EVEN backtick count => no code span crosses a newline => a per-line span extractor is safe. (v1's "68 backticks" was a line count; corrected.)
Helper location corrections: strip_rust_strings is :58 (not :80); the M22_*_RS block is :3456-3472. All other v1 helper cites exact.
"EVERY cited file already has an include_str! const" — TRUE for all 14 files the section names.
CI-neutrality CONFIRMED: no eval reads ADR-0230's body. docs/knowledge/** is generated by justfile:531 -> scripts/okf-export.mjs which
  EXCLUDES *_tests.rs (:69,:78) and reads no ADR bodies => byte-identical is correct.

## 2. SCOPE DECISIONS (resolving lens conflicts)
D1  HEADING :107 AND HEADER :3-10 STAY BYTE-IDENTICAL. The heading ("mechanical enforcement deferred") remains TRUE after rb-68: rb-68
    gates the DOCUMENT<->SOURCE CORRESPONDENCE, not the negative CONTENT property PRV1-17/20 assert. Same for the :10 Decision line and
    DIGEST.md's copy of it. This dissolves red-team B6 (no `enforcement deferred` needle is used) and honours reviewer M3.
    Merit argument (not the over-stated digest argument, reviewer M8): ADR-0230:1,:3,:5,:9,:10 ARE digest inputs (adr-digest.mjs:177-190,
    240-char cap :311); `**Extends:**` :8 is NOT parsed. We freeze :3-10 anyway because nothing in the residual requires touching it.
D2  NO NEW ADR, NO Amends/Extends/Extended-by edit. DECLARED SCOPE DEVIATION from the seed (reviewer M7): the seed's "use Extends"
    instruction is copy-paste from rb-67 and is unexecutable here — rb-68 mints no ADR (none reserved; ARCHITECTURE.md:2247 next-free 0245),
    and 0230 is the TARGET not the extender. Provenance goes INLINE in the corrected bullets and is GATED by [emit/pair].
    ALSO DECLARED: the seed's premise is stale — it says "exactly one observability::mr_log call"; there are TWO since rb-65.
D3  PRV1-17 names delete_account, cancel_account_deletion and the reaper ONLY (0230:16-17; 0235:119 says so explicitly). The claim-time
    line is CONTEXT for the file-level census, NOT PRV1-17 evidence. The prose must mark it as such or it fabricates scope. (reviewer m4)
D4  :130 (`privacy.rs:19-28` macro ban) is a CITATION fix, not a falsehood retraction — privacy.rs:29 still reads "no logging or print
    macros" and privacy.rs:1571's mr_log is a FUNCTION CALL, not a macro. Do NOT write "this was false". (reviewer m5)
D5  DELETE, DON'T REWRITE the retracted claims (the rb-62 lesson). No "an earlier draft said ..." sentence may quote a dead claim —
    that is what makes [emit/no-stale-claim] authorable without a polarity exemption. The :145-147 parenthetical is DELETED, not re-dated.
D6  Close BOTH R-rb-40-ADR0230 and R-rb-65-ADR0230-PRV120 (the latter targets :124-125 and :149-155, both fully inside DIES). Register the
    follow-up residual AFTER the lenses (`residuals add` is add-only, not upsert).

## 3. PROSE PLAN (ADR-0230)
CONVENTION, stated once and applied uniformly: file name in bare prose in the same paragraph + declaration marker in an inline code span
  (`pub fn account_deletion_reaper(`, `fn cascade_fields(`, `pub(crate) fn log_reject(`, `const REJECT_ALREADY_DELETED`); NEVER a path:NN
  or path:NN-MM pair. Event names quoted as literals in code spans.
DIES: :111 ("git:line evidence") [reviewer M1] · :113-114 ("none is inferred from a source scan" — the new lead bullet IS source-derived)
  [reviewer M2] · :116-117 (zero mr_log) · :124-125 (reaper emits nothing) · :130 (cite only, per D4) · :134-136 (only logging is step 6a)
  · :145-147 (battle.rs:894/:936 parenthetical — DELETED per D5) · :151-153 (PRV1-20 "by absence") · :157-159 (honest caveat, re-scoped)
  · :161-165 (rewritten NARROWLY per D1/reviewer M3, NOT deleted as history) · :209-211 and :216-217 (## Consequences restate the same claims)
  · every path:NN inside :116-155 (~22).
ADDS:
 (1) Lead bullet: accounts.rs emits exactly TWO lines of its own, both through `crate::observability::mr_log(`, each as the terminal
     statement before its reducer's trailing Ok(()). Enumerate decl + evt + fields-builder + provenance for each. MARK the claim-time line
     as context, not PRV1-17 evidence (D3). Name the owning gates.
 (2) reject() -> guards `pub(crate) fn log_reject(` chain, de-lined.
 (3) delete_account / cancel_account_deletion static-literal bullet, de-lined (`const REJECT_ALREADY_DELETED`).
 (4) Reaper bullet: "emits exactly one line, PRV1-17-safe by construction"; the scheduler-only Err is still a static string, returned not logged.
 (5) 11-helper census RESTRUCTURED AS A SUB-LIST, ONE ITEM PER FILE, so [cite/decl-real]'s nearest-file binding is unambiguous
     (reviewer M5, red-team A10). privacy.rs corrected to a PER-FUNCTION claim naming request_data_export's emission explicitly (D4).
     Drop the "4-line body" count for erase_character_rows (it is 3 lines).
 (6) Step-6a paragraph de-lined; battle write-back retargeted onto `pub(crate) fn resolve_wild_battle_on_disconnect(` + evt
     "wild_disconnect_writeback_err". "exactly four resolvers" is TRUE and stays.
 (7) PRV1-20 rewritten "by absence" -> "BY CONTENT": `fn cascade_fields(` is pure (no ctx, no table read), rendering only the subject
     Identity Display (64 lowercase hex) and a usize; same for `fn purge_fields(`. Phrase it as "It holds by content, not by absence"
     — the needle is `holds by absence`, so this is clean (red-team B1 dissolved).
 (8) :161-165 rewritten narrowly: the document<->source CORRESPONDENCE is now mechanised (rb-68, accounts_tests.rs); the CONTENT property
     remains MET BY VERIFICATION. ANTI-OVER-CLAIM: the words "PRV1-17 and PRV1-20 are now mechanically enforced" must NOT appear.
 (9) ## Consequences :209-211 "git:line pointers" -> "declaration-shaped pointers"; :216-217 records that rb-68 planted the correspondence
     gate and that the criteria stay met-by-verification. MUST NOT introduce an evt literal there (see [emit/containment]).
PARKED, DO NOT FIX (reviewer confirmed both are TRUE, not drifted): 0230:53-54 `game-core/src/accounts/deletion.rs:40` and
  0230:77 `evals/deletion-grace-wasm-ssot.eval.mjs:90`. Section-scoped clauses keep them legal. -> residual R-rb-68-ADR0230CITES.

## 4. TEST DESIGN v2 (server-module/src/accounts_tests.rs)
REUSE (verified): strip_rust_strings :58 · strip_rust_comments :131 · squash_ws :159 · stripped_for_scan :165 ·
  strip_comments_keep_strings :173 · stripped_keep_strings :219 · extract_squashed_fn_body :226 · extract_squashed_fn_sig :251 ·
  idx :259 · is_word_byte :335 · is_word_char :340 · m22_count_occurrences :3496 · rb22_dq :4770 · rb40_bs :14416 · rb40_evt :14422 ·
  rb40_nd_mr_log :14431 · rb40_frozen_emit_tail :14475 · rb65_evt :15462 · rb65_nd_cascade_fields_fn :15487 · nd_complete :274 ·
  rb24_nd_reaper_decl :5523 · ACCOUNTS_RS :266 + the M22_*_RS block :3456-3472 (every cited file already has a const).
ARCHITECTURE ported from privacy_tests.rs:10334-11028 (rb-67), per the per-module local-copy convention quoted at privacy_tests.rs:46-50.
ONE new const: RB68_ADR_0230_MD = include_str!("../../docs/adr/0230-...md") (idiom: accounts_tests.rs:12173, privacy_tests.rs:10337).
  Note in the docblock that this makes the ADR a compile input.

NORMALISATION PIPELINE (run before EVERY clause) — red-team A7/A8:
  1. blank HTML comments, KEEPING newlines (rb-67 :10362)
  2. decode &#NN; / &#xHH; / &colon; &lowbar; &num; &period; &lpar; &rpar;
  3. strip markdown backslash escapes  \X -> X
  4. strip emphasis markers * and _ OUTSIDE code spans (for the stale-claim scan only)
Line numbers are preserved throughout; [doc/html-in-section] compares LINE NUMBERS, never byte offsets (red-team A16).
PARAGRAPH SPLITTER: split on blank lines AND on list-item starts `^\s*[-*] ` (red-team A11; precondition for A1/A10).

DERIVATION SEAM  rb68p_live_emission_facts() -> &'static Vec<EmissionFact>  (OnceLock)
  per decl needle: decl occurs EXACTLY ONCE in stripped_for_scan(ACCOUNTS_RS); body from both views; evt literal SLICED from the
  strings-kept body (never transcribed); fields-builder ident derived; terminality by REUSING rb40_frozen_emit_tail()/rb65_frozen_emit_tail()
  (reviewer M6 — do NOT invent a `body_stmt_count-2` statement model, and do NOT add a third encoding of terminality).
  RECONCILIATION (red-team A14/C2, closes the green-by-construction hole): assert
    prefix_free_count("mr_log", stripped_for_scan(ACCOUNTS_RS)) == sum of per-fact emission counts
  so a THIRD emission site in a THIRD function reds INSIDE rb-68, not only via the rb-40/rb-65 censuses.
  prefix-free = next byte is not is_word_byte (kills the mr_log_breadcrumb substring).

COLLECTOR  rb68p_adr_violations(md, facts, sources, roster, floors) -> Vec<String>   NO short-circuit except the three RETURN-ALONE guards.
RETURN-ALONE GUARDS (all three fail LOUD, never open):
  [doc/parity]      odd backtick count on any line
  [live/vacuity]    facts.is_empty()            <- red-team C1: with empty facts the ENTIRE oracle measured CLEAN on a gutted doc
  [scope/section]   heading prefix "## PRV1-17 and PRV1-20" not found exactly once / body range None   <- red-team A12: measured fail-open

CLAUSES (each traces to a MEASURED finding):
  [doc/no-fence]           no ``` and no ~~~ line                                          (A15)
  [doc/no-blankish]        no whitespace-only line
  [doc/no-hidden-char]     ALLOW-LIST: chars outside {0x20..0x7E, \n} u {U+2014,U+00A7,U+2013,U+2019} are violations.
                           NOT a named blacklist — 9 of 12 invisible chars (U+2060, U+2061, U+034F, U+180E, U+3164, U+115F, U+17B4,
                           U+206A, U+FE00) survived the blacklist. Absorbs [doc/no-cr].                                    (A6, m7)
  [doc/no-hidden-carrier]  in-section: no link-reference definition `^\s*\[[^\]]*\]:\s` and no raw HTML tag `<...>`. 4 carriers
                           ([//]: # , []: # , link title, <div hidden>) measured CLEAN and parked every gated token invisibly. (A2)
  [doc/html-in-section]    a comment whose LINE RANGE intersects the section body is a violation (:94 lives elsewhere -> legal).
  [scope/roster-floor]     section names >= N distinct roster .rs files AND carries >= M decl-shaped spans (N,M derived-and-frozen;
                           today 14 and 31). Replaces the byte floor INSIDE the collector; the byte floor moves to the live-doc
                           consumer so it cannot red the small control fixtures.                                     (A5, B9)
  [scope/paragraph]        a paragraph carrying a decl span must name the file that defines it.
  [cite/no-line]           two-sided locator ban over the section body, run per-line AND over a k=2 sliding window of consecutive
                           non-blank lines joined by one space (a hard wrap defeated the per-line form in 6/6 measured cases; the
                           window measured ZERO false-REDs at k=2 and k=3 and does not reintroduce rb-67's squash false-RED). (A3)
                           LOCATOR SHAPE (must satisfy BOTH sets jointly — they pull in opposite directions):
                             RED  : path + [^A-Za-z0-9]{0,3} + optional l|L|line|lines|row|no.|#|@ + \d{1,5} terminated by
                                    end / ) / - / en-dash / , / ; / . / ff        e.g. :773  (L773)  , l. 79  (79-92)  , L79
                                    +773 ~773 [773] {773} /773 ·773  "at 773" "row 773" "no. 373" "— 773" ", lines 773-799" "773ff"
                             GREEN: digits immediately followed by whitespace + an alphabetic word  => a COUNT, not a locator
                                    "accounts.rs: 2 emissions"  "accounts.rs (2 sanctioned sites)"  "line 1 is the claim-time purge"
                             GREEN: parenthesised content that is not ^\d+(\s*[-–]\s*\d+)?$   => "accounts.rs (ADR-0235)" (rb-67's
                                    shipped honest positive `privacy.rs` (ADR-0126 idiom) must stay green).            (A4, B3, B4, B5)
  [cite/roster-coverage]   MANDATORY (not optional). TWO rosters: a SOURCE roster (from the include_str! consts — decl resolution) and
                           a CITATION-BAN roster (every \w+\.rs token appearing in the section). The ban roster is what makes
                           `accounts_tests.rs:14609` red while `accounts_tests.rs` alone stays green.               (A13, B2)
  [cite/decl-real]         every decl-shaped span occurs, squashed, in the comment+string-stripped source of the roster file NEAREST
                           IT BY BYTE DISTANCE IN THE SAME PARAGRAPH — not "any file the paragraph names" (which measured CLEAN on a
                           swapped attribution). The one-item-per-file sub-list (ADDS 5) makes this unambiguous.       (M5, A10)
  [cite/decl-sole]         that decl marker occurs EXACTLY ONCE in that source.
  [emit/pair]              POSITIONAL, not co-occurrence: for fact i, the FIRST evt-bearing span after the span containing decl_i must
                           be evt_i. Co-occurrence measured CLEAN on a full evt<->reducer payload swap in all four configurations. (A1)
  [emit/containment]       every occurrence of a DERIVED EVT LITERAL must fall inside the section body OR the `## Consequences` section.
                           The mr_log ceiling is DROPPED (it false-RED an honest Consequences mention, and \-escapes defeated it). (B7, A8)
  [emit/no-stale-claim]    WHOLE-DOCUMENT blacklist with lines 1..=10 exempt (the digest-owned header), over the emphasis/entity/
                           backslash-normalised view. Needles (concat!-assembled): "contains zero", "emits no log line at all",
                           "holds by absence", "The only logging transitively reachable". NONE collides with the corrected prose
                           (D5 forbids retraction sentences; "not by absence" does not contain "holds by absence").
                           FAILURE MESSAGE PRINTS THE LABEL AND LINE NUMBER ONLY — never the needle.            (A9, B1, B6, trap 8)

TESTS (3):
  rb68p_emission_facts_reconcile_with_the_file_census   — the derivation seam + the A14 reconciliation. NEW coverage (a third site).
  rb68p_adr0230_prv117_matches_the_live_emission_sites  — live doc; asserts facts.len()==2 BEFORE calling; byte floor lives HERE.
  rb68p_adr0230_evidence_oracle_control                 — ~35 fixtures through the SAME collector and SAME live facts.
FIXTURE RULES: every fixture goes through #[track_caller] expect_label / expect_only_label — NEVER a bare assert!(!v.is_empty()).
  EVERY clause needs at least one ISOLATING fixture whose violation list is EXACTLY that one label (red-team C3: rb-67's round-2 team
  neutralised clauses to `if false` and found 3 with no isolating fixture). At-risk clauses that need a deliberately isolated fixture:
  [doc/no-blankish], [doc/no-hidden-char] (put the char in the EVT span, not the decl span), [doc/no-fence], [cite/decl-sole] (needs a
  genuinely duplicated span — all 8 plan-named decl markers occur exactly once today, so it must be synthetic).
  POSITIVE controls required: well-formed doc · bare identifier span · module-qualified span · honest `battle.rs` mention · em dash ·
  U+2019 · U+00A7 · en dash · an HTML comment in a NON-PRV section (the :94 control) · "accounts.rs (ADR-0235)" · B3/B4/B5's three sentences.
SCAN HYGIENE (accounts_tests.rs:14403-14409): no contiguous emission call site / evt token / escaped-quote pair / block comment / raw
  string / quote-in-char-literal. Every fixture needle assembled from concat! + rb22_dq()/rb40_bs(). (Note: whole-repo .rs eval scanners
  all skip *_tests.rs — verified — so the risk is the in-file gate, not the evals.)
TRAPS: 1 pair-not-count · 2 shipped 3-stage stripper · 3 terminality via the shipped frozen tails, never includes() · 4 shape+exactly-once
  · 5 whole-file containment + no-blankish + list-aware splitter · 6 CHECK pins passed COUNT + 0 skipped + rejects "no tests to run"
  · 7 prefix-free mr_log · 8 label+line only · 9 pin no byte-exact stmt (the frozen tails own it); take literals from FORMATTED source
  · 10 no "1a." doc-list items (clippy doc_lazy_continuation, -D warnings) · 11 X3 ROSTER-pinned by NAME · 12 evt-only ceiling + pair floor.

## 5. GATES — memory/projects/gates/rb-68.gates.md (X1-X6, authored in this phase)
X3 ROSTER GROUND TRUTH (reviewer-verified by name today; v1's "24" was wrong — re-derive with `cargo nextest list`, never transcribe):
  accounts_tests.rs (13): rb40_claim_emits_one_purge_observation :14580 · rb40_claim_binds_the_purge_result :14799 ·
    rb40_evt_and_fragment_literals_are_pinned :14842 · rb40_claim_purge_fields_is_pure :14968 · rb40_no_new_bare_log_in_accounts_or_privacy
    :15129 · rb40_claim_purge_fields_is_exact :15258 · rb40_claim_purge_line_composes_into_the_envelope :15360 ·
    rb65_reaper_emits_one_cascade_observation :15697 · rb65_reaper_binds_the_purge_result :16148 · rb65_evt_and_fragment_literals_are_pinned
    :16196 · rb65_cascade_fields_is_pure :16345 · rb65_cascade_fields_is_exact :16502 · rb65_cascade_line_composes_into_the_envelope :16611
  privacy_tests.rs (8): rb40p_purge_returns_the_collected_count :7371 · rb65p_export_emits_one_observation :9421 ·
    rb65p_export_binds_the_purge_result :9797 · rb65p_export_fields_is_pure :9845 · rb65p_export_fields_is_exact :10093 ·
    rb65p_export_line_composes_into_the_envelope :10208 · rb67p_adr0220_cites_the_live_purge_declaration :10908 ·
    rb67p_adr0220_citation_oracle_control :11042
  + the 3 new rb68p_ tests = 24 total.
CHECK HARDENING (red-team C4 + memory "nextest summary on stderr"): every CHECK captures BOTH stdout and stderr, pins
  `N tests run: N passed, 0 skipped`, pins each test NAME, and rejects the literal `no tests to run`.

## 6. RISKS CARRIED
R1 [cite/no-line] is the highest-risk clause: A4's 11 spellings and B3/B4/B5's 3 honest sentences pull in opposite directions. They MUST
   be a single joint fixture set, authored together. Capture the RED against the UNEDITED doc AND against the CORRECTED draft (B8).
R2 [emit/no-stale-claim] is a blacklist and cannot read polarity (A17; ADR-0230 admits this about itself at :83-90). DISCLOSE as an
   honest limit in the ADR's new caveat paragraph — do not present it as a semantic check.
R3 [cite/no-line]'s per-line arm makes the ADR's hand-wrapping semi-load-bearing; the k=2 window bounds but does not erase this.
   Document it as an honest limit in the test banner (m8).
R4 The corrected prose must transcribe ~31 visibility keywords byte-exactly (`pub(crate) fn` vs `pub fn` vs `fn`) or [cite/decl-real]
   reds. Honest friction; budget a fix cycle for it (B8).
R5 R-rb-67-CITESHAPE is closed FOR rb-68's oracle only; it remains open in privacy_tests.rs's rb-67 collector (out of touches).
   Say so explicitly rather than implying corpus-wide closure. Follow-up: extract the markdown-oracle machinery once a THIRD consumer
   needs it (/simplify Q3) — needs an ADR, no number reserved => backlog.

## 7. RIGHT-SIZING: ONE mergeable slice. ~700-900 test lines (the clause count went UP on measured evidence, so /simplify's 600-750
target is not reachable without reopening measured bypasses; the two structural cuts it won — no separate vacuity test, no fourth
Consequences clause — are adopted). Prose: ~60 section lines + 2 Consequences bullets + 1 ARCHITECTURE paragraph. 3 tests.
FOLLOW-UP RESIDUALS (register AFTER the lenses): R-rb-68-ADR0230CITES (0230's parked cites at :53-54, :77 and later sections) ·
  R-rb-68-CITESHAPE-PORT (port rb-68's A1-A16 closures back into privacy_tests.rs's rb-67 collector) · markdown-oracle extraction (backlog).
