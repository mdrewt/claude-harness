# rb-77 — plan memo (residual R-rb-46-LIBRSMOD; ADR-0247 pre-assigned) — v1 (planner opus, pre-review)

Worktree: `projects/monster-realm/.claude/worktrees/rb-77`, branch `rb-77`, base origin/master@e06bf13 (rb-76 merged).
Orchestrator: fable@xhigh. Tier HARD. Budget target $150.
Ledger: `memory/projects/gates/rb-77.gates.md` (E1 seeded + X1-X5 authored in the PLAN phase, rb-46 shape).

## Orchestrator findings that shaped the plan (measured before planning)
- rb-76 (merged 2026-09-11, ADR-0246) ALREADY reds on the literal two-line rb-46 X4 PoC via its clause (e)
  (`guards_tests.rs` ~:3813-3836: every `#[path` line in lib.rs must be followed by a `*tests` mod). The CLASS
  still has open routes: V1 single-line `#[cfg(..)] #[path=..] mod guards;`, V2 `#[cfg_attr(.., path=..)]`,
  V3 `use guards_wasm as guards;` alias (only partially caught), V4 nested re-export, V5 a cfg'd `include!`
  inside guards.rs (rb-76's include! ban loops over a roster that EXCLUDES guards), V6 macro-generated wiring.
- No executed line-number pin on lib.rs exists; only `docs/knowledge/reducers/*.md:8` carry `lib.rs#L<n>`
  stamps → a trailing comment on lib.rs:31 (zero line shift) avoids a knowledge regen.
- `accounts_tests.rs:3603` strips STRINGS before comments → no double quote / apostrophe in the lib.rs note.

## Decisions
A. Oracle scope: CRATE-WIDE. Every non-`*tests` `mod x;` in lib.rs is bare (private, unattributed,
   unnested, unaliased); `*tests` mods must carry exactly the `#[cfg(test)]#[path=]` squashed prefix.
   `guards` is the named anti-vacuity anchor (declared exactly once); live roster floor ≥20 with
   guards/accounts/battle/movement named. No name-keyed exception table — the class is derived from the
   `tests` suffix; a future `#[cfg(feature=..)] mod dev;` must edit the class branch + argue an ADR.
B. guards.rs pinned cfg-free file-wide: exactly one `#[cfg` and it is `#[cfg(test)]`; squashed file ENDS
   with `#[cfg(test)]#[path=]modguards_tests;`; zero `#![cfg`, zero `cfg!(`, zero `include!(` (raw).
C. Architecture: pure `rb77_wiring_verdict(lib_src, guards_src) -> Result<(), String>` (non-short-circuit
   label collector over `rb76_module_squashed`), ONE live test (real files + live floors) and ONE fixture
   matrix test (11 bad fixtures asserted by exact label + 1 good control, `RB77_FIXTURE_FLOOR`). X5 is a
   MANUAL mutant sweep on the REAL files (fixture-only teeth never cover the wiring).
D. Reviewer note: ONE trailing comment on lib.rs:31 `mod guards;` (no quotes, no apostrophes, no block-
   comment markers, no attribute syntax spelled out); a 4-line `//` block above guards.rs:471.
E. ADR-0247 `docs/adr/0247-crate-root-module-wiring-is-unconditional.md`, `**Extends:** ADR-0236`, and a
   one-line `**Extended-by:** ADR-0247 (...)` added to ADR-0236's header (never `Amends:`).
F. ARCHITECTURE.md: ONE tail entry after :2273 (rb-74/rb-75 style); no ` [rb-75:` bracket; no column-0
   `**<pinned label>** (` for the 13 rb-75-pinned labels; :630 row untouched.
G. Flow: tester (opus, static-only Bash; orchestrator runs RED/bite proofs) → specialist (notes, ADR,
   ARCH, adr-digest) → lenses (reviewer, red-team, /simplify, reducer-security-auditor, verifier) →
   single full `just ci` detached → mr-gates check --timeout 2400 from the worktree.

## Planner output (verbatim)

### Verification notes (planner)
- lib.rs:24-45 banner + 21 bare `mod x;` (`mod guards;` at :31); :47-57 three `#[cfg(test)]`+`#[path]`+`mod *_tests;` triples; :351-357 rb-74's fourth at EOF. `cfg` occurrences: :47/:51/:55/:355 are `#[cfg(test)]`; :190/:200/:209 are `let cfg = ctx…` bindings — the needle must be `#[cfg` (with bracket).
- guards.rs:471-473 is the file's only `#[cfg`, and the last item.
- guards_tests.rs substrate: GUARDS_RS :757, strip_comments_and_strings :888, m22s5_squash :1646 (split_whitespace().collect() — total whitespace deletion), m22s5_assert_source_is_scannable :1662, m22s5_stripped_squashed :1695, m22s5_strip_comments_only :1746, RB76_LIB_RS :3268, rb76_module_squashed :3317, rb76_scanned_module_names :3345, rb76_path_attribute_targets :3395 (doc :3391-3394 admits the single-line form is unparsed), clause (e) :3813-3836. File ends :4313 (`// rb76-compile-red-end`) — append point.
- guards_tests.rs:4038-4060 (rb-76 on-disk superset check) already REDs if `src/guards_wasm.rs` exists undeclared; rb-76 clause (a) :3912-3933 reds a bare `mod guards_wasm;` twin (it must declare require_subject_not_deleting). Open routes: exactly V1/V2/V4/V5/V6.
- Prior art: accounts_tests.rs:5079-5117 (`rb22_lib_wires_mod_privacy`, item-span look-back banning `#[cfg(`/`#[path` on `mod privacy;`); trading_tests.rs:3687-3701 (rb-47: accounts.rs exactly one `#[cfg` + `#!`+`[cfg` ban). rb-77 = derived-roster generalization of both.
- Every roster parser strips comments first (guards_tests.rs:3346; accounts_tests.rs:3603 via strip_rust_comments(&strip_rust_strings(src)) — STRINGS FIRST). No eval derives a roster from lib.rs mod lines.
- No executed test pins a lib.rs line number (rb-74 anchors are text, :919; rb-73 pins squashed bodies). Only docs/knowledge/reducers/{init,sync_content,on_connect,on_disconnect}.md:8 stamp lib.rs#L168/189/233/329.
- ARCHITECTURE.md gated from OUTSIDE touches by sim-harness/src/bin/mr_load_driver.rs: `[rb75/bracket-roster]` = exactly 5 ` [rb-75: …]` brackets (:7682); rb75_no_historical_numeral_changed (:7879+) = each of 13 labels opens exactly one column-0 `**label** (` entry. Last tail entry rb-75 at :2273; rb-76 appended none.
- No rustfmt.toml → wrap_comments=false → trailing comment = zero line shift.
- adr-digest.mjs:198-236, 360-361, 652-672 models Extends/Extended-by REVERSE-only: `**Extended-by:** ADR-X` on 0236 obliges 0247 to carry `**Extends:** ADR-0236`. ADR-0236 header: `**Extends:** ADR-0227` (:8), `**Extended-by:** ADR-0246` (:9).

### A. Oracle scope — (ii) crate-wide, two declaration classes, no open list
`RB77_TEST_MOD_PREFIX = "#[cfg(test)]#[path=]"` (fragment-assembled). Name ends in `tests` → squashed text immediately preceding `mod` must be EXACTLY that prefix, and the byte before it `;`/`}`. Otherwise → byte immediately before `mod` must be `;` or `}`. A future `#[cfg(feature = "dev_reducers")] mod dev;` cannot land without editing this constant/branch — the ADR-worthy moment (say so in Consequences).

### B. guards.rs — pin cfg-free file-wide (do not defer V5)
G1 `[rb77/guards-cfg-count]` every `#[cfg` occurrence starts the exact literal `#[cfg(test)]`, count exactly 1; zero `#!`+`[cfg`; zero `cfg!(`. G2 `[rb77/guards-tail]` squashed guards.rs ENDS with `#[cfg(test)]#[path=]modguards_tests;`. G3 `[rb77/include-macro:guards.rs]` zero `include!(` in RAW guards.rs.

### C. Test architecture — (a) pure verdict + live test + fixture matrix
NO RED-AT-HEAD in the ordinary sense (invariant holds today; deliverable is test-side). Proof-of-teeth = (i) fixture matrix (permanent executed frozen mutants) AND (ii) X5 MANUAL sweep on the REAL lib.rs/guards.rs. Both, or the gate is forgeable by pointing the live test at a fixture.
C1 helpers (rb77_-prefixed, appended after :4313): call `rb76_module_squashed(label, src)` directly (no third stripper, ADR-0003). `rb77_declaration_sites(squashed) -> Vec<Rb77Site>`: for every `mod` index: name = ident run after `mod` (skip if empty); delim = next byte, must be `;` or `{` (skip `.model(`, `x.mode=`); preceding byte b: three bytes before exactly `pub` → visibility violation; else b ident byte → skip (inside a longer identifier); else b `.`/`:` → skip (path/field expr); else a declaration site (record b + ≤24 preceding bytes). `rb77_wiring_verdict(lib_src, guards_src) -> Result<(), String>` non-short-circuiting label collector (rb-67/68/71 pattern), Err(labels.join("\n  - ")).
C2 clauses: [rb77/roster-floor] ≥3 classified non-tests sites (parse sanity; live floor lives in the live test) · [rb77/guards-anchor] exactly one site named guards · [rb77/mod-inline-body:<n>] delim `{` · [rb77/mod-visibility:<n>] preceded by `pub` or `)` · [rb77/mod-attributed:<n>] non-tests name, preceding byte ∉ {`;`,`}`} · [rb77/testmod-prefix:<n>] tests name not preceded by exactly `#[cfg(test)]#[path=]` (itself preceded by `;`/`}`) · [rb77/mod-alias:<n>] · [rb77/cfg-not-test:<file>] any `#[cfg` not starting `#[cfg(test)]` (kills target_arch, not(..), AND `#[cfg_attr(`) · [rb77/cfg-inner:<file>] `#!`+`[cfg` · [rb77/cfg-macro:<file>] `cfg!(` · [rb77/cfg-floor:lib.rs] ≥3 `#[cfg(test)]` in lib.rs · [rb77/path-not-testmod:<file>] `#[path` not followed by exactly `=]mod`+ident ending in tests · [rb77/include-macro:<file>] `include!(` in RAW source (both files) · [rb77/macro-rules:lib.rs] `macro_rules!` in lib.rs · [rb77/guards-cfg-count], [rb77/guards-tail].
C3 needles (squashed, strings blanked): `mod guards;`→`modguards;` = ["modgua","rds;"]; `#[path = "x"]`→`#[path=]` = ["#","[path"] and ["#","[path=]mod"]; `#[cfg(test)]` = ["#[c","fg(test)]"]; generic ["#[c","fg"]; `#![cfg` = ["#!","[cfg"]; `cfg!(` = ["cf","g!("]; alias `use guards_wasm as guards;`→`useguards_wasmasguards;` needle "as"+name, NO left boundary, hit unless the byte AFTER is an ident byte (do not require `;` — misses `{guards_wasm as guards}` / `as guards,`); guards tail = [PREFIX,"modgua","rds_tests;"]; include = ["inclu","de!("]; macro_rules = ["macro_","rules!"]; target_arch (fixtures only) = ["target_","arch"].
Byte-before-`modguards;` ∈ {`;`,`}`} rejects attribute `]`, visibility `pub`/`)`, nesting/macro `{`/`(`. Does NOT catch: a second bare declaration (anchor count does); a twin inside another module's file; `[lib] path`/build.rs/RUSTFLAGS crate-root swap; proc-macro/imported-macro expansion (only anchor absence fires); files not read. `src/guards/mod.rs` beside `src/guards.rs` is a rustc ambiguity error — self-defending, say so.
C4 tests: (1) `rb77_crate_root_wires_every_module_bare_and_unconditional` live: verdict Ok on RB76_LIB_RS/GUARDS_RS + live floors (derived non-tests roster ≥20, contains guards/accounts/battle/movement). (2) `rb77_module_swap_fixtures_are_rejected_by_clause`: 11 bad + 1 good, asserted by exact label, `const RB77_FIXTURE_FLOOR: usize = 11`. No third test.
Fixtures (independent fns, no shared builder): F0 clean synthetic (5 bare mods incl guards + one proper test triple + clean guards) → Ok · F1 literal two-line PoC → cfg-not-test, mod-attributed:guards, path-not-testmod, guards-anchor · F2 V1 single-line → mod-attributed:guards, path-not-testmod · F3 V2 cfg_attr → cfg-not-test, mod-attributed:guards · F4 V3 alias trio → mod-alias:guards · F5 V4 nested → mod-inline-body:native, mod-visibility:guards · F6 V6 `wire!();` no mod guards → guards-anchor · F7 `macro_rules!` in lib.rs → macro-rules:lib.rs · F8 `pub mod guards;` → mod-visibility:guards · F9 `#[cfg(test)] #[path=..] mod guards;` → mod-attributed:guards, path-not-testmod · F10 guards.rs cfg'd include! → guards-cfg-count, include-macro:guards.rs · F11 guards.rs item after the test tail → guards-tail. Collector may raise extra labels: assert MEMBERSHIP of intended labels, never whole-message equality.

### D. Reviewer-checklist note
lib.rs: ONE trailing comment on :31 (zero shift; wrap_comments=false; every roster parser strips comments):
`mod guards; // rb-77 (ADR-0247): bare and unconditional by contract — a conditional module twin is a review stop; gated by rb77_*`
guards.rs: 4-line `//` block immediately ABOVE :471 (not the `//!` header; all inbound citations sit above):
`// rb-77 (ADR-0247): this is the ONLY conditional-compilation attribute this file` / `// may carry, and it must stay the last item. A second one, a twin of a wrapper` / `// selected by target architecture, or a textual source inclusion would swap what` / `// the wasm build compiles while every pin in guards_tests stays green.`
FORBIDDEN in both: any `"` (accounts_tests.rs:3603 strips strings BEFORE comments); apostrophes; contiguous block-comment markers either direction (lib.rs now runs through m22s5_assert_source_is_scannable :1662-1684 from rb-76); `r#`+`##`; attribute syntax spelled out (`#[cfg`, `#[path`, `target_arch`, `cfg!`); `#[rustfmt::skip]` anywhere (observability_tests scanner_teeth bans it).

### E. ADR-0247
File `docs/adr/0247-crate-root-module-wiring-is-unconditional.md`. Title: "ADR-0247 — The crate root wires every domain module bare and unconditional: a conditional-compilation module twin is a gated defect, not a reviewer convention". Header: Status Accepted · Date 2026-09-11 · Slice rb-77 (residual R-rb-46-LIBRSMOD, M-residual-backlog.spec.md#rb-77) · Supersedes — · Amends — · Extends ADR-0236 (reciprocal Extended-by added) · Subsystems security-authz, ci-gates · Decision (228 chars): "rb-77 gates the crate root: every mod in lib.rs is bare, unattributed, unaliased and unnested (test modules only via the exact cfg(test)+path prefix) and guards.rs carries one cfg, its test tail — one ordinary Rust test, no new eval."
ADR-0236 header: add `**Extended-by:** ADR-0247 (rb-77 — the crate-root module wiring that every rb-46 pin assumes is now gated; residual R-rb-46-LIBRSMOD closed)` directly after :9. Never Amends.
Rejected: clippy lint (none exists); build.rs assertion; syn AST (new dep for a 40-line invariant; ADR-0224 escape hatch); wasm32 test job (tests don't run on wasm32; the twin is selected by the very cfg); new eval (ADR-0224); guards-only scope.
Consequences: a future legitimate conditional module must edit RB77_TEST_MOD_PREFIX/class branch + argue an ADR — intended friction; message says so.
Residuals (not chased per ADR-0224): R-rb-77-CARGOSWAP (`[lib] path`/build.rs/--cfg crate-root swap); R-rb-77-SIBLINGTWIN (item-level cfg twin inside a rostered module other than guards.rs); R-rb-77-MACROWIRING (proc-macro/imported macro expansion). Already closed elsewhere (note, not residual): undeclared src/guards_wasm.rs → rb-76 :4038-4060; bare `mod guards_wasm;` → rb-76 (a) :3912-3933.

### F. ARCHITECTURE.md
One column-0 tail entry after :2273 opening `**rb-77** (residual R-rb-46-LIBRSMOD, promoted from source slice rb-46), **ADR-0247 at the supervisor-reserved number.**` closing `ADR next-free = 0248.` No ` [rb-75:` token; no column-0 `**<pinned label>** (` for 11r-c, 11r-f, rb-15, rb-17, M15a, M15b, ux2, ux2b, M14.5b, uxd2, uxd3-a, uxd3-b, 11r-h. Do NOT add rb-76's missing tail entry. :630 row unchanged. :466-485 unchanged.

### G. Ordered tasks
Tester: read substrate (:750-960, :1640-1840, :3244-3430, :3782-4069), do not modify; append rb-77 block after :4313 (banner ≤25 lines, needles, parser, verdict, 12 fixtures, 2 tests); static self-check (fragments, no block markers, no raw quote char, no r##, no rustfmt::skip, messages never quote a searched needle); hand-derive the squashed live module block + guards tail in a comment; write the X5 register: 8 mutants on the REAL files (F1,F2,F3,F4,F5,F8 lib.rs; F10,F11 guards.rs) + 2 benign controls (reordered mod list; a comment above `mod guards;`) that must stay GREEN.
Orchestrator: `cargo nextest run -p monster-realm-module -E 'test(/rb77_/)'` (stderr!); mutant sweep with copies in /tmp/rb77-orig, `sleep 1` between rows, restore from copy never `git checkout --`; `git diff --stat` afterwards.
Specialist: lib.rs:31 trailing comment; guards.rs 4-line note above :471; ADR-0247 + Extended-by line in 0236; ARCHITECTURE tail entry; fmt/clippy; `just adr-digest` + `-check`; `just knowledge-check` (expect PASS, no regen); commit; single full `just ci` detached with CI-EXIT marker (npm ci first if node_modules missing); kill by PID only.
Verifier: re-run the gates, re-execute 3 of 8 mutants, invent ONE mutant of its own against the verdict fn, fact-check ADR + ARCH entry against `git diff origin/master HEAD`.

### H. Anti-patterns
verbatim needle outside a fragment · ban without positive control · hand-enumerated roster · shared fixture builder · rewriting rb-76 helpers (read-only) · editing any gating test outside rb77_* (a red elsewhere is a finding, not a number to bump) · hand-editing DIGEST/knowledge · Amends: on 0236 · boyscout >40 lines or outside touches (no rb-76 ARCH entry, no ADR-0245 citation retarget, no README/CHANGELOG) · a block comment above `mod guards;` · `git checkout -- <file>` to restore a mutant.

### I. Right-sizing — one coherent increment; park nothing
Diff: guards_tests.rs +300..380 · lib.rs +0 lines (trailing comment) · guards.rs +4 · docs/adr/0247 ~85 (new) · docs/adr/0236 +1 · DIGEST.md +3 (generated) · ARCHITECTURE.md +2 · docs/knowledge 0 expected.
Workflow: solo standard loop + the budgeted artifact red-team pass (plan red-team + artifact red-team find disjoint sets). Top risks: verdict parser false-REDs on live lib.rs (live test first); a note char trips a stripper outside touches; ARCH entry reds rb75 archlog tests in sim-harness (check with `cargo nextest run -p <sim-harness> -E 'test(/rb75_/)'` before CI); fixture-only teeth (X5 is a live sweep); ADR-0247 raced by a sibling (check docs/adr/ before write and before final commit).

## v2 after plan review (reviewer opus APPROVE-WITH-CHANGES M1-M6/m1-m8/n1-n4; red-team opus 10 findings incl. 2 CRITICAL; /simplify applied inline by the orchestrator)

### Findings taken (and what changed)
- RT#1 CRITICAL raw identifiers: `#[r#cfg(..)]`, `#[r#path = ..]`, `r#cfg!(`, `r#include!(` all compile (rustc 1.96 measured), rustfmt+clippy clean, and defeat every `#[cfg`-literal needle → NEW clause `[rb77/raw-ident:<file>]`: the 2-byte sequence `r#` must not occur in the squashed (strings-blanked) view of either file (0 occurrences in every production .rs today; raw STRINGS are blanked before the check so only raw identifiers survive).
- RT#2 CRITICAL composite (`/* /* */ ; mod guards; */` phantom anchor + `mod r#guards;` + raw-ident attrs = VERDICT Ok, rb-76 byte-identical) → closed by raw-ident + RT#3 + RT#4.
- RT#3 HIGH lib.rs has no nested-block-comment precondition (`rb76_module_squashed` only checks opener/closer COUNT equality) → the rb-77 squash helper asserts no `*/` survives in the squashed view, as a LABEL `[rb77/scan-substrate:<file>]` (not a panic, so fixtures can assert it) — for lib.rs, guards.rs and every fixture.
- RT#4 HIGH silent `continue` in the site parser is the hole → `[rb77/mod-unparsed:<idx>]` when a depth-0 `mod` token sits at an item boundary but the `mod<ident>;` shape is unreadable (kills `mod r#guards;`).
- RT#7 MEDIUM anchor forgery by a macro token tree `wire!{ ; mod guards; }` → declaration sites are only counted at bracket depth 0 (all three bracket kinds, over the squashed view); a depth≥1 `mod` is not a site. This ALSO replaces the reviewer's M6 (drop `[rb77/macro-rules:lib.rs]` + F7 — rb-78 owns the macro_rules class; a macro cannot coexist with the bare depth-0 anchor without E0428, so replacing the anchor is the only macro attack and it reds on absence).
- RT#6 MEDIUM `include !(` whitespace-evades a RAW count → count `include!(` on the squashed view AND the raw source (OR).
- RT#8 MEDIUM fixture BODIES (not just needles) are source text other gates read → every fixture is fragment-assembled; never contiguous `#[spacetimedb::`, `mod tests {`, `type $`, `macro_rules!`; even `"` counts; no unbalanced block markers in the test file text.
- RT#9 MEDIUM the reviewer note has no permanent gate → the LIVE test (not the pure fn) asserts the raw lib.rs has exactly one line starting with `mod guards;` (fragment-assembled) that contains `ADR-0247`. An assertion about the deliverable, not a ratchet.
- RT#5 HIGH rb-76 coverage claims corrected for the ADR: (a) rb-76's on-disk superset check EXEMPTS any twin file whose stem ends in `tests`; (b) a bare `mod guards_wasm;` reds rb-76 clause (a) ONLY if the twin names the subject gate — a re-exporting twin does not. rb-77 closes V3 by the anchor (absence of a bare depth-0 `mod guards;`) + the alias clause keyed on the FROZEN name `guards` (reviewer M2: `mod guards;` + `use x as guards;` is E0255, so the real V3 deletes `mod guards;`; keying the alias needle on roster names alone would never spell `asguards`).
- R-M1 boundary: `i == 0 || prev == b';' || prev == b'}'`; `pub` look-back guarded by `i >= 3`; alias "byte after" `None` = not an ident byte.
- R-M3 `RB77_FIXTURE_FLOOR` DROPPED (ADR-0224 amendment bans numeric floors whose sole function is decay-proofing). Matrix kept: it is an ordinary unit test of a pure function's return value (ADR-0224:60-63's endorsed shape). Each fixture is its own explicit assertion.
- R-M4 ADR-0236 back-link: APPEND `, ADR-0247 (rb-77 — …)` to the EXISTING `**Extended-by:**` line (0236:9); a second line is invisible to adr-digest's first-match `extractBoldField` dangling check (comma-joined precedent 0208:8, 0227:8).
- R-M5 X5 mutants must COMPILE on native or score INVALID (never KILLED): register = M1 two-line PoC, M2 single-line, M3 cfg_attr(path=), M4 alias trio WITH a `src/guards_wasm.rs` re-export shim (INVALID if it does not build), M5 `pub mod guards;`, M6 `#[cfg(test)] #[path = "guards.rs"] mod guards;`, M7 guards.rs `#[cfg(target_arch = "wasm32")] include!("guards_wasm.rs");`, M8 guards.rs `#[r#cfg(..)]` raw-ident twin of an unpinned wrapper, M9 lib.rs RT#2 composite; controls C1 reordered mod list, C2 a `//` line above `mod guards;` — both GREEN. Runner prints KILLED / SURVIVED / INVALID per row; restore from byte copies.
- R-m8 + /simplify: G2 `[rb77/guards-tail]` DROPPED (kills nothing G1/G3 do not, once raw-ident closes the attribute route); F11 dropped. G1 collapses into the generic clauses run over guards.rs (it has its own tests-class `mod guards_tests;` site).
- R-m1 + /simplify: live floors = ≥20 production sites in lib.rs AND ≥1 tests-class site in each file (positive controls for both needle families); the 4-name list DROPPED (rb-76 (d) already names them; rb-77's parse is byte-level and INCLUDES guards — say so in the banner + ADR). `[rb77/cfg-floor]` and `[rb77/path-not-testmod]` DROPPED as redundant with `testmod-prefix` + `mod-not-bare` + `mod-unparsed` + the live floors.
- R-m2 residuals: only R-rb-77-CARGOSWAP is new (`server-module/Cargo.toml:7` `[lib]` has no `path` key; adding one is a one-line crate-root swap nothing sees). Cite rb-78 (macro class) and rb-79 (statement-level cfg) by id — do NOT mint SIBLINGTWIN/MACROWIRING. One-liners: `#[cfg(test)] #[path = <any>] mod x_tests;` pulls any file into the TEST build (filename-decides-visibility; not a wasm route); `c"…"`/`cr"…"` C-string literals are unhandled by the stripper (0 occurrences).
- R-m3 ADR rejected alternatives add (a) compiler-enforced/illegal-states (inapplicable: module wiring is not expressible in the type system) and (b) do nothing — reviewer checklist only (the residual's own disposition) with the material-harm argument (guards.rs holds the authz wrappers; a wasm-selected twin swaps them in the SHIPPED build while the native suite stays green = ADR-0224's player-data security class). Note `Cargo.toml:39` already declares `dev_reducers`, so the "future `#[cfg(feature=..)] mod`" friction is live-shaped.
- R-m4 note text: `mod guards; // rb-77 (ADR-0247): bare and unconditional by contract — a target-selected wasm twin of this module is a review stop; gated by the rb77_ tests in guards_tests.rs`. Apostrophe ban lifted (red-team verified safe); `"` ban stays. R-m5: run `cargo fmt --all --check` right after the edit. R-m6: X5 EVIDENCE = absolute path into `memory/projects/gates/rb-77.red-before.md:<line>`. R-m7: Decision ≤200 chars. R-n1..n4 taken (doc the over-approximation; append BELOW `// rb76-compile-red-end`; ledger note that E1 is name-shaped; fixture hygiene).
- Reviewer: the guards.rs note goes ABOVE `#[cfg(test)]` (:471), never between it and `mod guards_tests;` (`evals/monster-privacy.eval.mjs:2799-2813` 160-byte look-back).

### Final oracle (one pure fn, both files)
`rb77_wiring_verdict(lib_src: &str, guards_src: &str) -> Result<(), String>` — per file: squash via `rb76_module_squashed` then `[rb77/scan-substrate:<file>]` if a `*/` survives; depth-0 `mod` site parse (prev byte: `pub` look-back → not-bare; ident byte → skip (inside identifier); `.`/`:` → skip (path); else must read `mod<ident>` + delim: unreadable → `[rb77/mod-unparsed:<idx>]`, delim `{` → `[rb77/mod-inline-body:<name>]`, name ends `tests` → exact prefix `#[cfg(test)]#[path=]` itself preceded by `;`/`}`/start else `[rb77/testmod-prefix:<name>]`, otherwise prev ∉ {start,`;`,`}`} → `[rb77/mod-not-bare:<name>]`); `[rb77/cfg-not-test:<file>]` any `#[cfg` not starting `#[cfg(test)]`; `[rb77/cfg-inner:<file>]` `#![cfg`; `[rb77/cfg-macro:<file>]` `cfg!(`; `[rb77/include-macro:<file>]` (raw ∪ squashed); `[rb77/raw-ident:<file>]` `r#` in squashed. lib.rs only: `[rb77/guards-anchor]` exactly one production site named `guards`; `[rb77/mod-alias:<name>]` `as<name>` (no left boundary; byte after not an ident byte) for `guards` and every derived production name. Non-short-circuiting; `Err(labels.join("\n  - "))`.
Live test `rb77_crate_root_wires_every_module_bare_and_unconditional`: verdict Ok on real files; ≥20 production sites + ≥1 tests site per file; exactly one raw lib.rs line starting `mod guards;` containing `ADR-0247`.
Matrix test `rb77_module_swap_fixtures_are_rejected_by_clause` (each fixture an independent fragment-assembled fn; assert label MEMBERSHIP): F0 clean → Ok · F1 two-line PoC → cfg-not-test:lib.rs + mod-not-bare:guards · F2 single-line → same · F3 cfg_attr(path=) → same · F4 alias trio (no `mod guards;`) → guards-anchor + mod-alias:guards · F5 nested re-export → mod-inline-body:native + guards-anchor · F6 `wire!{ ; mod guards; }` → guards-anchor · F8 `pub mod guards;` → mod-not-bare:guards · F9 `#[cfg(test)] #[path=..] mod guards;` → mod-not-bare:guards · F10 guards.rs cfg'd include! → cfg-not-test:guards.rs + include-macro:guards.rs · F12 `#[r#cfg(..)] mod r#guards;` → raw-ident:lib.rs + mod-unparsed · F13 nested-comment phantom → scan-substrate:lib.rs · F14 `include !(` spaced in guards.rs → include-macro:guards.rs.

## v3 after tests review (reviewer opus APPROVE-WITH-CHANGES) + artifact red-team (opus, EXECUTED in /tmp/rb77-artifact)
Measured at HEAD before round 2: `2 tests run: 1 passed, 1 failed` — matrix green, live test red ONLY on the note clause (as designed); fmt + clippy clean. Specialist (sonnet) applied the two notes (lib.rs:31 trailing comment; guards.rs 4 lines above the cfg(test) tail); main checkout untouched.
Folded into tester round 2:
- RT CRITICAL #1/#2: `cfg! {..}` / `include! {..}` brace/bracket delimiters evaded the paren-hardcoded needles (measured: an unconditional `require_owner` bypass shipped to wasm with 900/900 green) → needles become `cfg!` (4 bytes) and `include!` (8 bytes), delimiter-agnostic.
- RT HIGH #4: a kept char literal `'{'` at depth 0 strands the depth counter and blinds every later declaration (floors do not notice) → new label `[rb77/char-literal-bracket:<file>]` + fixture F16 + runner row M12.
- RT HIGH #5 / R-M1: `testmod-prefix`, `cfg-inner`, `cfg-macro` had ZERO teeth (clause no-op mutants survived) → fixtures F13 (unframed test mod), F14 (`#![cfg` + brace `cfg!`), F15 (brace `include!`), F9 extended with paren `cfg!(`.
- RT #6 / R-M3,m6,m7: runner row M6 does not compile (12× E0432; the non-test lib target loses guards) → replaced with the `#[cfg(not(test))]`/`#[cfg(test)]#[path]` pair; pre-flight dirty check + note-present check; SURVIVED requires ≥2 tests run; new rows M10 (brace cfg!), M11 (brace include! + created `*_tests.rs` twin), M12 (bracket char literal).
- RT CRITICAL(class) #3: the target split can live in ANOTHER rostered file (a cfg-paired `fn` in schema.rs called from guards.rs; or a macro) — guards.rs stays textually cfg-free and every rb77 test is green. NOT closable by a two-file text scan; ADR-0247's Decision/Consequences must claim only what the scan proves (the crate-root WIRING and guards.rs's own TEXT), and residual R-rb-77-CFGROSTER is registered (crate-wide target-predicate discipline over every rostered module — the rb-79-shaped crate-wide slice), citing rb-78/rb-79.
- R-M2: two dead live assertions deleted; R-m1: F2 deleted (byte-identical input to F1 after squashing); R-m2: `rb77_squash` alias deleted; R-m3/m4/m5/n1/n2: doc corrections; R-n3/n4: fixture bindings inlined, inert `alpha` fn dropped; size target ≤ ~680 lines.
- RT LOW #7: `cr#"…"#` C raw strings false-RED `raw-ident` (loud; 0 occurrences) — disclosed in the ADR, stripper at :812 NOT modified (shared by other gates). RT LOW #8: a string literal containing a block-comment opener panics the scan (pre-existing from rb-76) — disclosed.
- Verified closed by execution (RT): raw-ident composite, nested-comment phantom, two-line/single-line/cfg_attr twins, alias trio, nested re-export, macro token tree, every `pub(..)` spelling, `#[cfg(test, )]`/`all(test)`/`any(test, target_arch)`/`testx`/spaced/comment-split cfg spellings, `std::include!`/`::core::include!`/`include/**/!(`/`include ! (`/`r#include!`, cfg on const/use/extern, glob re-export wiring, block-comment phantom note line, second cfg'd `mod guards;` at EOF. rustc itself refuses `#[core::prelude::v1::cfg(..)]` and `#[cfg{test}]`.

## v4 after impl review (reviewer opus APPROVE-WITH-CHANGES) + reducer-security-auditor (opus PASS-WITH-NOTES)
Measured after round 2: fmt clean, `2 tests run: 2 passed`, knowledge-check no drift, adr-digest-check no drift, sim-harness rb75 oracle 3/3. Production diff vs e06bf13 = two comments only (lib.rs 1/1, guards.rs 4/0); guards_tests.rs one purely additive hunk `@@ -4312,0 +4313,851 @@`.
Folded (docs only, via doc-keeper + specialist; ledger by the orchestrator): control count is THREE rows (BASELINE + C1 + C2) not two; red-team route list is SIX (macro token tree added); `mod-not-bare` prose no longer claims a `(`/`{` comparison (depth-0 sites never reach it — anchor absence fires); "exactly one `#[cfg`" → "one today; grammar admits only the `#[cfg(test)]` spelling"; guards.rs note reworded (5 lines) to stop claiming ONLY-attribute; F9 description; `include!` RAW-arm prose false-RED disclosed; CARGOSWAP mechanism corrected (build.rs injects cfgs/OUT_DIR source, does not select a crate root; RUSTFLAGS family named); CFGROSTER widened: game-core build.rs + `include!(concat!(env!("OUT_DIR")..))` at content.rs:283, the real build matrix (nextest = default features; clippy = --all-features; CI builds a second `--features dev_reducers` wasm; six live dev_reducers cfg sites; `start_wild_battle` pin-only), rb-76's paren-bound raw-only `include!(` ban over the other 20 modules, consumer-side `use crate::guards_wasm as guards;` alias (unconditional → executed native tests catch it; pin-only reducers exposed); `#[cfg(test)] #[path=<any>]` is a GATE-DISABLING route (path string blanked). Ledger X5 rewritten to the twelve-row register + three controls (the v1 text named the dropped guards-tail row); E1 comment 11→15 fixtures.
Not folded (nits, test-code style, no weakening): needle hoisting consistency (n2), a comment on the live test's recomputation (n3), `rb77_assert_rejected` doc "label or prefix" (n4). Left for a follow-up flag in the PR.
Process note: the first commit+snapshot+sweep command was BLOCKED by the harness Bash guard (`rm -rf /tmp/...` in the same line); the lenses fell back to the (stable) live tree. Re-issued without `rm -rf`.

## v5 — close-out state (2026-09-11)
Final head on branch rb-77: 9ee9775 (ADR Confirmation filled). Sweep2 at 6484d18: 13/13 KILLED, 0 INVALID, BASELINE/C1/C2 CONTROL-GREEN, TREE CLEAN (the only later change is the ADR body). Verifier re-check PASS (both blockers closed; its own surviving mutant now dies on F17). `mr-gates check --slice rb-77 --timeout 2400` from the worktree: E1 `2 tests run: 2 passed`, X1 `900 tests run: 900 passed, 0 skipped`, X2 LINT-GREEN, X4 CI-GREEN (the single full `just ci`), X5 MANUAL → rb-77.red-before.md:62. X3 first FAILED on a wrong CHECK (it wanted `**Extended-by:** ADR-0247` verbatim; the back-link is correctly appended to ADR-0236's EXISTING Extended-by line per adr-digest's first-match parsing) — CHECK re-pointed at "the Extended-by line contains ADR-0247", re-run.
Residuals registered: R-rb-77-CFGROSTER, R-rb-77-CARGOSWAP (both → backlog). Harness-guard incident recorded as a memory card (destructive spelling anywhere in a compound Bash call drops the whole call).
