# rb-78 — plan memo (residual R-rb-46-MACRORET; ADR-0248 pre-assigned) — v2 (planner opus + orchestrator; plan reviewer + plan red-team folded in)

## v2 resolutions (plan review 2026-09-12T00:35Z — reviewer: 0 BLOCKER / 3 MED / 3 LOW; red-team: 1 HIGH / 2 MED / 2 LOW)
- DROPPED the per-module "bare-name count == qualified-site count" control (reviewer MED-1: scope creep beyond MACRORET; the qualified spelling of every site is already owned per file by rb-46 A/F, m22-s5 tag pins, rb-47, rb-76, m22-s3b).
- DROPPED fn-name recovery (reviewer MED-2): labels are `[rb78/macro-above-gate:<file>:<macro>]` followed by free text that quotes the first ~48 squashed bytes of the region (which naturally shows the fn signature); no `fn-unparsed` clause.
- M2 clarified (reviewer MED-3): the macro is DEFINED in lib.rs above `mod battle;` and INVOKED above start_battle's gate in battle.rs. M9 (generated fn) DROPPED from the live register — redundant with M8 (whole-fn wrapper) and a textual return in a generated body is already rb-46's; F8 stays as a fixture. M13 added (a `use game_core::*;` glob in battle.rs, exercising the new glob-import clause on the real tree). Register = 13 mutants (M1-M13 as enumerated in ledger gate X5) + 3 controls.
- NEW clause `[rb78/glob-import:<file>]` (red-team HIGH-1 closure): any `::*` in a rostered module whose path root — the identifier immediately before `::*` — is not exactly `super`, `self` or `crate` (those re-import from files this scan already reads). Measured: the crate's only production glob is `content.rs:907 use super::*;` (an inline test module). This closes the game-core `#[macro_export] macro_rules! format` + `use game_core::*;` route with zero live cost; `use game_core::format;` (respelt), `game_core::format!(` (respelt, `:`-preceded) and `#[macro_use] extern crate` (macro-use) were already closed. R-rb-78-GLOBMACRO is therefore NOT registered; the ADR records the closure.
- EXEMPT set for a `!` + delimiter = empty name or exactly one of `if while match return break in` (red-team MED-2: `break !(x)` is legal; `else` dropped — it cannot precede a unary expression).
- Depth counter for the region boundary is BRACE-ONLY (`{`/`}`); the char-literal ban in gate-bearing files is therefore exactly `'{'` / `'}'` (rb-46 clause 0c's set) (red-team MED-3). A `'('`/`'['` literal is inert to a brace counter and cannot form `!(`.
- Disclosed, not fixed (fail-closed, loud, zero today): `format!{`/`format![` delimiter forms are NOT allow-listed; a `c"…"`/`cr"…"` C-string literal is unknown to the shared stripper and leaves a stray `c` byte (a false RED if it directly precedes `format!(`); `returnformat!(` glue.
- X5 EVIDENCE cites the §3 RESULT line of rb-78.red-before.md (interim `pending`; the runner rewrites it at close).
- "ADR-0003" in rb-76/rb-77 prose is the harness design ADR (`specs/monster-realm-v2/adr/0003-shared-rule-core-and-prediction.md`, the SSOT/rules-once decision), not a phantom — keep citing it for "no second stripper".
- Fragment-assembly rule restated for the tester: every needle naming a production symbol or a banned spelling (`format`, `macro_use`, `::*`, `r#`, the three wrapper names) is assembled from fragments in guards_tests.rs; no raw double-quote CHAR literal; no contiguous block-comment marker; no contiguous `include!`/`cfg!` spelling in prose.
- Test names fixed: pure verdict `rb78_macro_verdict(sources: &[(String, String)]) -> Result<(), String>`; live test `rb78_no_macro_expands_above_any_deletion_gate`; matrix `rb78_macro_divert_fixtures_are_rejected_by_clause`; helper `rb78_assert_rejected(case, sources, expected_labels)`.

Worktree: `projects/monster-realm/.claude/worktrees/rb-78`, branch `rb-78`, base origin/master@79e3432 (rb-77 merged).
Orchestrator: fable@xhigh. Tier HARD. Budget target $150.
Ledger: `memory/projects/gates/rb-78.gates.md` (E1 seeded + X1-X5 authored in the PLAN phase, rb-77 shape).
Evidence: `memory/projects/gates/rb-78.red-before.md` (pre-slice PoC measurement already recorded), runner `memory/projects/gates/rb-78.mutants.py` (T4).

## Orchestrator findings (measured at HEAD before planning)
- Ten deletion-gate wrapper call sites crate-wide, all fully qualified `crate::guards::<wrapper>(..)?;`: battle.rs:124 start_battle, :411 begin_encounter (subject wrapper), :558 start_wild_battle (`dev_reducers`), economy.rs:119 buy, :213 sell, pvp.rs:818 challenge_pvp, :1004 accept_challenge, ranking.rs:152 set_profile_name, trading.rs:252 propose_trade, :468 respond_trade (commitment-predates wrapper).
- Two of the ten are ALREADY macro-closed by whole-prefix equality: begin_encounter (rb-76, battle_tests.rs:7859) and respond_trade (rb-47, trading_tests.rs:4600). The other eight are open: rb-46 clause I (battle_tests.rs:6903-6940) counts textual `return` in the prefix; a macro's `return` lives in its definition outside the prefix.
- Macro invocations ABOVE a gate in the ten prefixes: ONLY `format!(` (start_battle ×3, begin_encounter ×1). Crate-wide (21 production modules + lib.rs + guards.rs): zero `r#`, zero `macro_use`/`macro_rules`/`macro_export`, zero `extern crate`, zero bare ident `format` not followed by `!`, zero path-qualified `::format!`, zero bracket char literals in any gate-bearing file (privacy.rs/content.rs/observability.rs carry them; none carries a gate).
- **PoC M1 MEASURED CI-CLEAN at HEAD (2026-09-12T00:13Z, worktree rb-78 @79e3432):** module-scope `macro_rules! rb78_admit { ($ctx:expr) => { if $ctx.sender() != crate::WILD_IDENTITY { return Ok(()); } }; }` + `rb78_admit!(ctx);` above battle.rs:124 → `cargo fmt --check` exit 0, `cargo nextest run -p monster-realm-module` → `900 tests run: 900 passed, 0 skipped`, `cargo clippy --all-targets --all-features -D warnings` exit 0. Every real player is routed around the gate; only the native host's all-zero sender ever reaches it. battle.rs restored from a byte copy (sha verified). Recorded in rb-78.red-before.md.
- No `docs/knowledge/**` line stamp points into guards.rs → a mid-file `//` block shifts nothing gated. rb-77's raw-text needles on guards.rs (`cfg!`, `include!`, `r#`, `#[cfg`) and the strings-before-comments census forbid those tokens and any double quote in the new note.
- ADR 0248 is unused on origin/master. rb-79 (next in queue) owns the `#[cfg(test)]`-on-the-gate-statement + statement-boundary class for trading.rs — this slice adds NO clause on the gate statement itself (ADR-0247 D6 precedent).

## Decisions (planner corrections A-D folded in)
A. ONE pure verdict `rb78_macro_verdict(sources) -> Result<(), String>` (non-short-circuiting label collector, rb-77 shape) over the squashed comment-stripped string-blanked view (`rb76_module_squashed`, no third stripper — ADR-0003) of every rostered module: `rb76_scanned_module_names()` (lib.rs-derived, includes `lib`) + `guards`.
B. REGION = from the last bracket-depth-0 item boundary (`;`/`}` at depth 0, or file start) before each qualified gate needle (three needles: `crate::guards::require_not_deleting(`, `..require_subject_not_deleting(`, `..require_commitment_predates_deletion(`) to the needle. The region therefore holds attributes + signature + body prefix, so a whole-fn wrapper macro or a macro-GENERATED fn puts an `ident!{`/`ident!(` inside it.
C. CLAUSES: `[rb78/macro-above-gate:<file>:<fn>:<name>]` for every `!` immediately followed by `(`/`{`/`[` in a region, walking back over ident bytes; EXEMPT an empty name or one of exactly `if while return match in else` (unary-not — a macro cannot bear a keyword name without `r#`, which is its own clause); ADMIT only the 6 bytes `format` directly before the `!`, followed by `(`, with the byte before `format` neither an ident byte nor `:` (byte-sequence allow-list; `returnformat!(` is fail-closed and disclosed). `[rb78/raw-ident:<file>:<fn>]` for `r#` in a region. `[rb78/fn-unparsed:<file>@<offset>]` when no `fn<ident>(` is readable in the region (accept the `pubfn` / `)fn` glue). Crate-wide: `[rb78/format-respelt:<file>]` — an ident-bounded `format` preceded by `:` or not followed by `!(` (catches `use x::format;`, `std::format!(`, `crate::format!(`, `macro_rules!format{`); `[rb78/format-alias:<file>]` — `asformat` with NO left boundary and a right boundary (rb-77 mod-alias shape; catches `use x::evil as format;`); `[rb78/macro-use:<file>]` — the bytes `macro_use`. Substrate: `[rb78/scan-substrate:<file>]` (surviving block-comment close marker) for every scanned file; `[rb78/char-literal-bracket:<file>]` ONLY for files carrying ≥1 gate needle (privacy.rs legitimately spells `'{'` and carries no gate).
D. NO crate-wide `macro_rules!` DEFINITION ban (every route that matters passes through a region or the respelt clause; a definition ban would red the first legitimate helper macro for no reach). NO prefix equality on the other eight sites. NO cfg/statement-boundary clause on the gate statement (rb-79). NO numeric fixture floor (ADR-0224 amendment). NO pin of the site total at 10 (rb-46/rb-76 censuses own the sets).
E. LIVE test `rb78_no_macro_expands_above_any_deletion_gate` (name TBD by tester): verdict Ok over the real sources; positive controls (not floors): each of the three needles matched ≥1 live site; the found `(file, fn)` set contains battle.rs/start_battle, battle.rs/begin_encounter, battle.rs/start_wild_battle, trading.rs/respond_trade, economy.rs/buy; per rostered module except guards.rs, bare-name count of each wrapper == qualified-site count found there (kills an alias / fn-pointer hide); the guards.rs note exists exactly once (a raw line starting `// rb-78 (ADR-0248)` within 12 lines above the unique line starting `pub(crate) fn require_not_deleting(`).
F. FIXTURE MATRIX `rb78_macro_fixtures_are_rejected_by_clause`: F0 clean control (allow-listed `format!(` above the gate, macros BELOW the gate, a macro-laden reducer WITHOUT a gate, `if!(a)`/`!=`/`!seen.insert(`) must be Ok; F1 statement `bail!(ctx);`; F2 expression `let _x=admit!(ctx);`; F3 nested inside the allowed `format!(..)`; F4 brace; F5 square; F6 in-body `macro_rules!` definition; F7 whole-fn wrapper; F8 generated fn; F9 `std::format!(`; F10 `macro_rules!format{` in another rostered module; F11 `use x as format;`; F12 `use x::format;`; F13 `r#bail!(`; F14 `#[macro_use]`; F15 bracket char literal in a gate-bearing file; F16 nested block comment; F17 signature-position `ty!()`. Each fixture writes its own full text from fragments; asserted by label MEMBERSHIP.
G. RED-BEFORE, argued: at HEAD every macro clause is green by construction (clean tree), so the live test's RED is the note clause only (rb-77's exact shape) — an ordinary TDD red proving the test runs and reads the real guards.rs. PROOF-OF-TEETH (ADR-0224) is discharged by X5 row M1 (the literal PoC, killed by `[rb78/macro-above-gate:battle.rs:start_battle:rb78_admit]`) plus the pre-slice measurement above, both in rb-78.red-before.md. The runner runs AFTER the note lands; KILLED requires the row's label in the failure output.
H. NOTE in guards.rs: a 6-line `//` block inserted after `deletion_gate`'s body (new :96-101, above `require_not_deleting`'s doc block; lines below shift +7). No double quote, no `cfg!`/`include!`/`r#`, no contiguous comment marker, no contiguous `format!(`.
I. ADR-0248 `docs/adr/0248-macro-expansion-above-a-deletion-gate-is-a-gated-defect.md`: `**Extends:** ADR-0236` + reciprocal `**Extended-by:**` appended to ADR-0236:9 (one line — the rb-77 precedent; touches-delta item). Subsystems: security-authz, ci-gates. D1 scope all ten sites (derived), D2 grammar, D3 the byte-sequence allow-list + the three respelling bans as ONE trust decision, D4 vehicle (live + matrix + X5), D5 mid-file note (why not trailing: guards.rs has no knowledge stamps), D6 anti-decisions. Alternatives: compiler-enforced (inapplicable), prefix equality ×10 (ten hand-typed literals; blind to a respelt `format`), `syn` (new dep, does not expand macros), `cargo expand` in CI (the hostile macro controls the expansion), clippy lint (none), reviewer-only (fails ADR-0224 materiality — measured CI-clean on an authz gate), new eval (barred). Residuals: R-rb-78-GLOBMACRO (a game-core `#[macro_export]` `format` reached via a glob import; zero glob imports today; subset of R-rb-77-CFGROSTER), R-rb-78-PROCMACRO (attribute/derive proc-macro needs a Cargo.toml dep → CARGOSWAP class). `?` is not a bypass route (Err only). Fail-closed false REDs disclosed: `returnformat!(` glue, a new `assert!`/`vec!` above a gate, a bracket char literal in a gate-bearing file.

## v3 — test review (2026-09-12T01:1xZ; reviewer 0 BLOCKER / 1 HIGH / 3 MED / 2 LOW; artifact red-team 1 HIGH (out of class) / 1 MED / 1 closed)
- Tester strengthens: F21 deep raw-string opener (`r###`) and F22 an unpaired block-comment opener, each asserting `[rb78/scan-substrate]` (reviewer HIGH-1: two substrate triggers had no fixture); header citation for rb-46 clause I corrected to `battle_tests.rs:6886-6929` (the HONEST RESIDUAL sentence at :6922-6924); matrix docstring count reconciled to the shipped rows; the two contiguous builder spellings in `///` prose reworded (LOW-5).
- ADR-0248 final pass must: describe F19/F20/F21/F22, replace "within twelve lines" with the shipped ordering-only anchor (seam < note < wrapper; MED-4), state that a Cyrillic-confusable macro name is refused by rustc's `mixed_script_confusables` under `-D warnings` (red-team CLOSED-3), and carry the two residuals below.
- RESIDUALS to register AFTER the impl lenses (add is not upsert): **R-rb-78-PLAINRETURN** — MEASURED CI-clean at HEAD: a plain `if me != crate::WILD_IDENTITY { return Ok(()); }` (no macro) above pvp.rs:818 (challenge_pvp), pvp.rs:1004 (accept_challenge) and ranking.rs:152 (set_profile_name) passes fmt, clippy and 901/902 (the one red being rb-78's own note clause); those files have no per-reducer textual-return census (rb-46 clause I exists only for battle/economy; rb-47/rb-76 equality only for respond_trade/begin_encounter; trading.rs's propose_trade site is rb-79's). Not rb-78's class (no macro) and outside its touches (pvp_tests.rs / ranking_tests.rs) → register, do not smuggle. **R-rb-78-NESTEDMOD** — a `mod nested;` declared INSIDE a rostered production file (`src/pvp/nested.rs`) is invisible to every lib.rs-derived census (rb-46/rb-76/rb-77/rb-78); ADR-0246 disclosed the class as unregistered; register it once.
- Not a finding: the live note check reads RAW lines (a string literal opening a physical line with the marker would satisfy it) — informational.

## v4 — domain auditors (2026-09-12T01:4xZ): reducer-security-auditor PASS, desync-guard PASS (902/902 measured on an isolated copy of 4507bfa, feature-independent, cwd-independent)
- Tester adds two region anchors (`fnchallenge_pvp(`, `fnset_profile_name(`) so the positive control covers EVERY gate-bearing module (auditor MED-1: pvp/ranking could otherwise drop out of the scan silently while the per-needle ≥1 control stayed satisfied by battle.rs).
- Note reworded to seven lines (guards.rs:96-102): "enclosing item boundary" (begin_encounter is a crate-private helper, not a reducer), "every bang-macro invocation", and the proc-macro residual named in the note itself (auditor nit 5).
- ADR-0248 final pass (doc-keeper) must additionally: replace "rb76_module_squashed" with the shipped `rb78_squashed` (same two shared primitives; preconditions re-raised as labels so a fixture can observe them — ADR-0003 still satisfied); say "every production module" rather than "crate-wide" (desync nit 2: `*tests` modules are cfg(test) and unscanned); disclose the keyword-glue false-RED class generally (`elseif!(`, `forxin!(`) and that the glob clause admits only depth-1 in-crate roots (`use crate::schema::*;` is a fail-closed false RED); note the mixed provenance of the live loader (two include_str! embeds + eighteen disk reads, sound by same-crate membership + rb-76's path-attribute clause).
- R-rb-78-PLAINRETURN registration reason (auditor): deferred under ADR-0224's one-class-per-slice rule and ADR-0248 D6 (no edit to rb-46 clause I), NOT merely "out of touches" — `rb78_region_texts()` already computes the ten regions such a census would need; the open design question is region definition (rb-46's body extractor vs rb-78's item boundary) and respond_trade's legitimate `return Ok(())`.

## X5 register (v2 — 13 mutants + 3 controls; honest CI-clean classification)
BASELINE green · M1 literal PoC start_battle (CI-clean, MEASURED) · M2 same macro DEFINED in lib.rs above `mod battle;` and INVOKED above start_battle's gate (CI-clean; assert rb-77 live test stays green) · M3 expression position `let _x = admit!(ctx);` above buy's gate (CI-clean) · M4 brace form `admit!{ctx}` above sell's gate (CI-clean) · M5 macro nested in start_battle's `format!("opponent {e}")` argument via a `$e:expr` param (CI-clean; the row the allow-list must not shelter) · M6 `std::format!(` at battle.rs:79 (prophylactic; proves format-respelt on the real tree) · M7 `#[macro_use] extern crate game_core;` in lib.rs (prophylactic; macro-use) · M8 whole-fn wrapper macro around set_profile_name (report honestly which pins also red) · M9 `$ctx:ident`-hygienic macro above challenge_pvp's gate (CI-clean — pvp.rs has no per-reducer prefix scan) · M10 `macro_rules! format` shadow inside start_battle's body (NOT CI-clean: rb-46 clause I sees the textual return; kept as defence in depth) · M11 macro above begin_encounter's gate (NOT CI-clean: rb-76 prefix equality — this measures the "2 of 10 already closed" claim) · M12 macro above start_wild_battle's gate (CI-clean; never compiled by the test build — the text scan's gain) · M13 `use game_core::*;` glob in battle.rs (prophylactic; glob-import) · C1 a second allow-listed `format!(` above a gate GREEN · C2 a bail-style macro BELOW a gate GREEN.

## Plan (phases)
1. Plan checkpoint (this memo + ledger + red-before + ADR-0248 draft) → `wip:` commit + push.
2. Tester (opus): T2 — verdict + helpers + matrix + live test appended below guards_tests.rs:5182; record the red-before (live RED on the note clause; matrix green).
3. Reviewer + red-team on the tests (parallel) → tester strengthens.
4. Specialist: T3 — the guards.rs note only. Fast gate: `cargo nextest -E 'test(/rb78_/)'` 2/2 → `just ci-fast monster-realm-module` → `just lint`.
5. Orchestrator: T4 — X5 runner (rb-77 skeleton; byte copies to /tmp/rb-78/orig; dirty-tree pre-flight; ≥1.1 s sleep; INVALID on build failure; label-required KILLED), run in FOREGROUND chunks.
6. Lenses in one batch: reviewer + /simplify + ARTIFACT red-team (feed it: glob-imported `#[macro_export]`, proc-macro attribute, `format` via a game-core re-export chain, a macro in the region of a future eleventh site) + reducer-security-auditor + desync-guard → then verifier (gating tests not weakened; one mutant of its own choosing).
7. Full `just ci` once (detached, CI-EXIT marker) → `mr-gates check --slice rb-78 --timeout 2400` from the worktree.
8. doc-keeper: ADR-0248 final + ADR-0236 reciprocal + `just adr-digest` + ARCHITECTURE.md `**rb-78** (` paragraph (ADR next-free = 0249) + `just knowledge-check`.
9. PR on mdrewt/monster-realm with `touches-delta:`, `boyscout-delta:` (none planned), `Items: none`, the `mr-gates render --format pr` line; close residual R-rb-46-MACRORET; stop at PR-open.

## touches-delta expectation
server-module/src/guards.rs, server-module/src/guards_tests.rs (declared); docs/adr/0248-*.md, docs/adr/0236-*.md (one reciprocal line), docs/adr/DIGEST.md (generated), ARCHITECTURE.md (one paragraph), docs/knowledge/** only if knowledge-check reds. battle.rs/economy.rs/pvp.rs/ranking.rs/trading.rs/lib.rs byte-identical at merge (X5 mutates them transiently; final `git status` is the proof).

## Anti-patterns (named)
Widening the matcher to silence a false RED · a numeric fixture floor · pinning the site total · a short-circuiting verdict · a crate-wide `macro_rules!` ban · scoring a non-compiling mutant KILLED · running X5 before the note lands · quoting a pinned marker (`format!(`, `include!`, `cfg!`) in guards.rs prose · rebuilding an expectation from the file under test · `git checkout --` to restore a mutant.

## TERMINAL (2026-09-12T02:2xZ) — PR #462 open, local gate green, remote CI running; supervisor owns the merge + `residuals close --pr 462`
