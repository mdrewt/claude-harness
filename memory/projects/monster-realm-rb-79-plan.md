# rb-79 plan memo — propose_trade deletion-gate prefix pin (residual R-rb-46-TRADINGCFG)

Worktree: `projects/monster-realm/.claude/worktrees/rb-79`, branch `feat/rb-79-propose-trade-gate-prefix`, base be3ff53 (rb-78 merged). ADR reserved: **0249**. Ledger: `memory/projects/gates/rb-79.gates.md` (E1 + X1..X5, LINT-CLEAN). RED-before: `memory/projects/gates/rb-79.red-before.md` §1. Runner: `memory/projects/gates/rb-79.mutants.py`.

## Diagnosis (measured, not assumed)
- ATTRIBUTE half of the residual (`#[cfg(test)]`/`#[cfg(debug_assertions)]` on the gate statement; statement-boundary; body `#[`/`cfg!(` bans; `#![cfg`; file-wide `#[cfg`==1) — ALREADY CLOSED by rb-47 (PR #429, ADR-0237): M1/M2 KILLED at HEAD by `rb47_propose_trade_gate_has_no_attribute_or_cfg_escape` + `rb47_respond_trade_carries_the_offer_age_gate`. ADR-0237:202-203 overclaims full closure; the residual row was never closed administratively.
- EARLY-EXIT half — OPEN. Seven macro-free predecessors directly above `trading.rs:252` survive fmt + clippy + 902/902: M3 sender-keyed `return Ok(())`; M4 file-scope `const X: bool = cfg!(test)` + `if !X { return Ok(()) }`; M5 plain `false` const; M6 `return Err(e).or(Ok(()))`; M7 rejection-SHAPED `return Err(e);` (passes rb-46 clause I 1==1); M8 delegation to a file-scope ungated twin; M10 `let me = crate::WILD_IDENTITY;` shadow. Mechanism: the native host's sender is the all-zero identity == `WILD_IDENTITY` (lib.rs:95); every executed test still sees the gate fire. ADR-0248 §Residuals assigns `trading.rs:252` to rb-79.
- Why a ported rb-46 clause I is NOT enough: M7 (1==1 by construction) and M10 (no `return`) survive it. Whole-prefix EQUALITY (rb-47 clause F shape, respond_trade) kills all seven.

## Design (planner, opus; orchestrator simplify pass applied)
Two NEW `#[test]` fns in `trading_tests.rs`, nothing existing edited:
1. `rb79_propose_trade_admits_no_predecessor_above_its_deletion_gate` — live oracle over `TRADING_RS`.
2. `rb79_prefix_bypass_fixtures_are_rejected_by_clause` — executed fixture matrix.
Pure verdict `rb79_prefix_verdict(file, raw) -> Result<(), String>` (non-short-circuiting label collector, rb-78 shape), clauses in order:
- L1 `[rb79/twin]` squashed file contains paren-free `fnpropose_trade` exactly once (kills M8a: a `propose_trade_*` twin the first-hit extractor would read instead — today a scan artifact, now a designed kill).
- L2 `[rb79/extract]` `m22s5_trading_fn_body` returns Some (label, not panic, so fixtures can observe it).
- L3 `[rb79/gate-count]` gate needle (plain + trailing-comma forms) exactly once in the body.
- L4 `[rb79/prefix]` squashed prefix above the gate == the frozen literal, byte for byte (kills M3-M8, M10; incidentally M1/M2 — rb-47 stays the owner of the attribute claim).
- L5 `[rb79/cfg-macro]` the bare 4 bytes `cfg!` occur zero times in the squashed FILE (delimiter-agnostic; closes the file-scope `const X = cfg!(..)` source rb-47's body-scoped ban and rb-78's region-scoped ban both miss; 0 at HEAD; disclosed fail-closed false RED).
Anti-decisions: no port of rb-46 clause I (measured weaker; a second census on the same region duplicates a sibling gate); no depth-0 re-assertion (the frozen prefix has zero braces → equality implies depth 0; m22-s5 keeps ownership); no substrate clauses (every substrate failure is fail-closed for an equality pin: unpaired `/*` → gate-count/extract label; nested comment/char literal → prefix label; trading.rs has 0 `/*`, 0 `r#` today).
Provenance of the frozen literal: HAND-DERIVED from ADR-0166 D3 (caps first, before any DB read) + ADR-0227 D3/D4 + PRV1-9 (caller binding → two caps → caller-joined lookup with its ok_or_else rejection → gate); own `concat!` split points differing from m22-s5's and rb-47 clause F's; own blank-literal constructor (never `m22s5_blank_string_literal`); runtime tie: the literal contains the cap needle twice, the joined-lookup needle once, `ctx.se|nder()` once, `return` zero times. The diff against the HEAD prefix is a CHECK, never the source. Re-derivation contract in the failure message.
Live test anti-vacuity: verdict Ok over the real file + insert needle once (fresh split) + the runtime tie + the NOTE clause (RED at HEAD): exactly one raw line of trading.rs whose trimmed text begins `// rb-79 (ADR-0249)`, ordered strictly between the unique `pub fn propose_trade(` line and the unique trimmed gate line — ORDERING ONLY, never a line-distance window. Order inside the test: verdict → controls → tie → note (weakest claim reports last).
Fixture matrix (≤16 rows; builder `rb79_fx_source(file_scope_extra, above_gate, below_gate)` assembled from its OWN fragments, never from the expected-prefix helper; F1 hand-written in full as the builder's transcription check): F0 pristine Ok; F1 M3; F2 M4 (prefix + cfg-macro); F3 M5; F4 M6; F5 M7 (documents why a census is insufficient); F6 M8; F7 M10; F8 `#[cfg(test)]` on the statement (prefix; overlap with rb-47 disclosed); F9 M8a twin (twin label); F10 gate deleted (gate-count); F11 reducer renamed (extract); F12 `cfg!{test}` brace form with consumer BELOW the gate (cfg-macro ONLY — proves L5 independent of L4); C1 comment above gate Ok; C2 early Ok BELOW the gate Ok (honest scope; m22-s5 gate-before-insert owns it); C3 trailing-comma gate spelling Ok. Assert by label MEMBERSHIP; no numeric row floor in code.
Note (production, trading.rs): IN-PLACE rewrite of the existing 4-line Guard-1a comment block (:248-251) into exactly 4 lines, the last two beginning with the marker — ZERO line shift (40 `trading.rs:NNN` doc/spec citations and 4 knowledge stamps sit below the gate; ADR-0248's :252/:468 are one day old). Wording constraints: keep the four existing claims; ≤100 cols; no double quote, no `#[`, no `cfg!`, no `/*` `*/`, no `r#`, no apostrophes. Fallback F1 (only if 4 lines cannot hold it): grow the block, `just knowledge` in the same commit, register R-rb-79-TRADINGCITES.
ADR-0249: `**Extends:** ADR-0236` (reciprocal `**Extended-by:**` appended to 0236's existing line — rb-78 precedent) + `**Amends:** ADR-0237` (narrows :202-203's closure claim; reciprocal `**Amended-by:** ADR-0249` header line in 0237 — digest enforces reciprocity as an error). Both disclosed under touches-delta. ARCHITECTURE.md: one paragraph after rb-78's (:2277), ending "ADR next-free = 0250."; never spell the note marker contiguously there. `just adr-digest` regen.
Residuals to register AFTER the lenses: R-rb-79-PREFIXFREEZE (a lockstep edit of literal + body is visible only in the PR diff), R-rb-79-CONFIRMCANCEL (confirm/cancel deliberately ungated, ADR-0227 D5 — no prefix discipline). Plus disclosure of already-owned classes: below-the-gate (m22-s5), macro (rb-78), attribute (rb-47), lib.rs swap (ADR-0247), NESTEDMOD, PROCMACRO, CFGROSTER, PLAINRETURN (pvp/ranking).

## Simplify pass (orchestrator, `/simplify` lens on the plan)
- Kept the verdict+matrix vehicle: the non-short-circuiting collector is what lets 5 claims report independently; a single multi-clause test would hide L5 behind L4 under first-failure-wins. Not speculative — 4 sibling precedents.
- Matrix trimmed to rows that each map to a MEASURED survivor, a control, or an anti-vacuity branch (twin/extract/gate-count/cfg-macro-only). No row exists "for symmetry". Tester may drop F8 if it adds nothing beyond documenting overlap; must keep F5 and F12.
- No new stripper (ADR-0003): reuse `rb47_stripped`/`rb47_squash`/`m22s5_trading_fn_body`.
- No helper generality (no per-file roster, no region iterator): one site, one file.
- Amends on 0237 is a one-header-line reciprocal edit, no body rewrite.

## Tasks (owner → done-check)
T0 baseline (done: 902/902, cfg! 0, #[cfg 1, no rb79_ symbol). T1 tester derives the literal from the ADRs, diffs vs HEAD as a check. T2 tester writes helpers + verdict. T3 tester writes matrix. T4 tester writes live test; orchestrator runs it: RED at HEAD on the note clause ONLY (`2 tests run: 1 passed, 1 failed`) → red-before §2 RUN 1. T5 orchestrator rewrites the 4-line block (4+/4-, wc -l unchanged, fmt/clippy clean, both rb79 green). T6 bite-proofs by hand (note deleted → note clause red; M3 PoC → `[rb79/prefix]` red) → §2 RUN 2/3. T7 X5 register: rows M1-M10, M8a, M11 (cfg! const consumed below gate), M12 (cfg!{test}), T1 (note deleted) + C1/C2/C3, `--clippy --filter 'test(/rb79_/)'` → §3. T8 ADR-0249 + reciprocal lines + `just adr-digest`. T9 ARCHITECTURE paragraph. T10 ledger `mr-gates check --slice rb-79 --timeout 2400` from the worktree; X5 MANUAL by hand. T11 lenses: reviewer + /simplify + red-team (artifact, own mutant) + reducer-security-auditor + desync-guard + verifier; register residuals after. Then full `just ci`, docs (doc-keeper), graph refresh, PR.

## Anti-patterns
Widening a needle; pasting the body into the pin; starts_with/contains prefix pin; line-distance window; count-only CHECK as teeth; editing gating tests during impl; appending clauses to an existing test; touching pvp.rs/ranking.rs; a second ADR number; sharing a needle constructor with an existing clause; spelling the note marker contiguously in ARCHITECTURE.md or any production needle contiguously in trading_tests.rs; hand-editing CHANGELOG.md/docs/adr/README.md; git stash/checkout in the worktree.

## Red-team targets (plan lens)
1 prefix-preserving bypass (shadow `check_trade_side_size`, make `ok_or_else` non-rejecting, reach `ctx.sender()` otherwise); 2 predecessor OUTSIDE the extracted body (between item boundary and `{`; `#[cfg_attr]`; cfg-free declaration twin); 3 cfg!-free conditional compilation (feature cfg elsewhere, build.rs/--cfg, Cargo feature — CARGOSWAP family) driving a const consumed in a legitimately refrozen prefix; 4 a twin the extractor reaches first without a prefix-extension name (mod-nested — NESTEDMOD); 5 pass-over-nothing (kill the trailing-comma form, rename, char-literal desync, unpaired comment) — each must red; 6 forge the note (second marker, marker in a string, marker above the decl, marker in trading_tests.rs); 7 forge the ledger (stub tests + revert note → X3 must red); 8 ADR truthfulness.

## Plan-review fold (reviewer REVISE-PLAN + red-team PLAN-HOLDS, 2026-09-12)
- B1: exactly ONE raw line begins with the marker `// rb-79 (ADR-0249)`; the continuation line(s) begin with a non-marker prefix. X3 `note===1` stands.
- H2: runtime tie gains two components — the frozen literal contains `crate::` ZERO times and the caller-binding token `letme=` EXACTLY once (both true at HEAD; closes a lockstep regeneration carrying M10's shadow, which introduces no `return`).
- H3: fallback F1 DELETED. Zero-shift budget = the blank line at trading.rs:247 — the block may occupy :247-251 (5 lines) with no line below :252 moving. A trailing comment on :252 is not a substitute (marker must start a trimmed line).
- H4: SIX trading.rs knowledge stamps exist (#L113 tables/trade_offer_reaper_schedule, #L176 trade_offer_reaper, #L224 propose, #L439 respond, #L490 confirm, #L765 cancel); X3 pins all six; three sit below the gate.
- H5: declared touches = server-module/src/trading.rs, server-module/src/trading_tests.rs. touches-delta (PR body): docs/adr/0249-*.md (new), docs/adr/0236-*.md (one reciprocal Extended-by header line), docs/adr/0237-*.md (one reciprocal Amended-by header line), docs/adr/DIGEST.md (generated), ARCHITECTURE.md (one paragraph), harness memory/projects/gates/rb-79.* + this memo.
- H6: note wording — additionally NO contiguous production needle: `crate::guards::require_not_deleting(`, `ctx.sender()`, `ctx.db.`, `#[spacetimedb`, `fn propose_trade`, `#!`, `r###`; keep the four Guard-1a claims in substance.
- M1 (+ /simplify): L5 file-wide `cfg!` ban CUT, with F12/M11/M12. Reason: no in-EARS kill (M4 dies to L4; a cfg! const consumed below the gate cannot route a caller around :252), a real least-surprise cost (first legitimate cfg! anywhere in trading.rs reds propose_trade's pin), and the `return`==0 tie already closes the refrozen-prefix route. rb-47's body ban + rb-78's region ban own in-body/above-gate cfg!. Recorded as an anti-decision in ADR-0249.
- M2: verdict skip semantics — L2 fail ⇒ push label, skip L3/L4; L3≠1 ⇒ push label, skip L4; the verdict contains zero unwrap/expect/panic!.
- M3: ADR-0248 D6 (:286-289) mis-attributes the attribute/statement-boundary class to rb-79 (rb-47 owns it — M1/M2 measured). ADR-0249 corrects both 0237:202-203 and 0248 D6; residual R-rb-79-ADR0248D6 registered after the lenses (0248 body is outside this slice's docs scope).
- M4: X1 pins the exact count `904 tests run: 904 passed, 0 skipped`.
- M5: F5 additionally PROVES in-crate that rb-46's census (count(return)==count(returnErr(e);)) passes on its text while `[rb79/prefix]` fires — the measured reason a census is insufficient.
- M6: the ARCHITECTURE paragraph must not open an entry for any rb-75 roster label (`**rb-15** (`, `**rb-17** (`, `**ux2** (`, `**11r-f** (`, `**M15a** (`, `**uxd2** (`, `**uxd3-a** (`, `**uxd3-b** (`, `**11r-c** (`, `**11r-h** (`, `**M15b** (`, `**ux2b** (`, `**M14.5b** (`).
- L1: R-rb-78-PLAINRETURN is PARTIALLY discharged (trading.rs:252 share); pvp/ranking remain open. L2: the three independent gate-needle copies in trading_tests.rs are the house rule — say so in code. L3: X3's Extends/Amends leg reads the header only (slice at the first `\n## `). L4: X5 EVIDENCE is an absolute harness path:line. L5: run X4 serially (account-e2e spacetime lock). L6: L4 also incidentally kills M9 (m22-s5 keeps ownership of ordering).
- Red-team #1 (MEASURED): the wire-name rename twin `#[spacetimedb::reducer(name = ...)] pub fn open_a_trade(..)` + demoting `fn propose_trade` to a dead gated fn keeps L1-L4 GREEN and is killed ONLY by `rb47_trading_reducer_roster_is_closed` (trading_tests.rs:5200) + the m22-s5 gated-reducer censuses in guards_tests.rs. DECISION: no duplicate clause (independent-pin duplication of a same-file sibling adds nothing); disclose the dependency in ADR-0249 + the live test's doc comment; add register row M13 (wire-name rename) whose designated killer is the rb-47 roster pin, so the dependency is MEASURED evidence.
- Red-team #2/#3: char-literal polarity flip and raw-string decoys are fail-closed (extract/gate-count labels) or harmless — confirmed; no substrate clause needed.
