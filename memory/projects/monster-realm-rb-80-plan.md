# rb-80 BUILD PLAN v2 — R-rb-46-ERASEWRITERS, ADR-0250 (planner opus + reviewer opus + red-team opus + /simplify inline, 2026-09-12)

Brief: /tmp/rb-80/brief.md. Worktree: /home/mdrewt/projects/ai-apps/claude-harness/projects/monster-realm/.claude/worktrees/rb-80 (branch rb-80, pushed). Baseline: 904 tests run: 904 passed.

## Measured findings (verified by the reviewer against the code)
F1 apply_quest_trigger (npc.rs:154-222; QuestComplete grants :214-217) has exactly ONE caller: talk (npc.rs:306); the only shipped quest completes on a Talk trigger -> the criterion's "quest turn-in grants" live in `talk`. Gate BOTH talk and advance_dialogue.
F2 All three files' string strippers blank the QUOTES too (raising_tests.rs::blank_heal_scan_strings:982-1006, npc_tests.rs::m22s3b_blank_strings:1743-1758, taming_tests.rs::strip_rust_strings_taming:191-260). Gate needle on the blanked view: `crate::guards::require_not_deleting(ctx,)?;` (trailing-comma form `(ctx,,)?;`). Tag clause runs on the comments-only view.
F3 rb-78 regions (guards_tests.rs:5348) cut at the previous depth-0 `;`/`}`: raising.rs:294, npc.rs:222, npc.rs:320, taming.rs:275. All four new regions are macro-free; no substrate hazard (no brace char literals, r#, block comments) in the three files.
F4 No crate-wide census counts `require_not_deleting`; only rb78_region_anchors() (guards_tests.rs:6415, [String; 7]) must grow.
F5 No line-number citation gate covers raising/npc/taming (rb74 rows are anchor-resolved). Comment citations `raising.rs:NNN`/`npc.rs:NNN` in raising_tests.rs/npc_tests.rs/content_cache_tests.rs/battle_tests.rs/economy_tests.rs/guards_tests.rs/evals/monster-dual-write/client/e2e/wallet-balance.spec.ts are ALREADY stale at HEAD (e.g. raising_tests.rs:956 says heal_party is at :324; it is at :300) and are comments only -> NOT edited (hunk cap); registered as R-rb-80-CITEDRIFT.
F6 ARCHITECTURE.md:485 ("other ERASE-policy writers (R-rb-46-ERASEWRITERS) are the remaining disclosed openers") is falsified by this slice -> replaced IN PLACE, line-count-neutral, preserving R-rb-76-CHALLENGERGRACE on :484.
F7 No `#[cfg(feature = "dev_reducers")]` test exists today; grant_bait's execution test is the first -> inline its seeds (dead_code under -D warnings).
F8 evals/dev-reducer-gating, recruit-reducer-security, npc-dialogue-quest-security, raising-reducer-security read EVERY .rs under server-module/src (sorted, *_tests.rs included) and take the FIRST `pub fn <name>(` hit. guards_tests.rs < npc.rs < npc_tests.rs < raising.rs < raising_tests.rs < taming.rs < taming_tests.rs. RULE: never write a contiguous `pub fn grant_bait(` / `pub fn heal_party(` / `pub fn talk(` / `pub fn advance_dialogue(` (or `fn <name>(`) in ANY test file — fragment-assemble every declaration needle (`["fnheal_","party"].concat()`), and the new rb-78 anchors likewise (`["fnt","alk("]`).
F9 Reducer rosters at HEAD (bare `#[spacetimedb::reducer]`, no parameters): raising.rs {care, train, heal_party, essence_train, consume_crystalized_essence} (5); npc.rs {talk, advance_dialogue, dismiss_dialogue} (3); taming.rs {attempt_recruit, grant_bait} (2). cfg census at HEAD on the stripped view: `#[cfg` raising 2 (:273 test const, :779 test mod) / npc 1 (:499) / taming 3 (:18, :280, :299); `cfg!(` 0/0/0; `debug_assertions` 0; `target_arch` 0; `WILD_IDENTITY` in raising.rs: comment-only (0 after comment stripping).
F10 The client maps NO server reject string (grep client/src for "deletion requested"/REJECT_DELETION: none) — a mid-grace player who heals/talks sees a generic reducer error; R-rb-80-REASONSCOPE stays LOW.

## Decisions
D1 heal_party -> GATE. Insert after raising.rs:306 (`};` of the joined let-else), before :307 (character lookup): one comment line + `crate::guards::require_not_deleting(ctx, "heal_party")?;`. Why: it debits player_wallet through the shop's own spend_currency (:362), consumes inventory (:370), writes monster/monster_pub (:393-394) and heal_cooldown (:410) — §4.7's trigger predicate selects it exactly as it selected buy/sell ("the builder does not get to re-decide this").
D2 talk -> GATE. After npc.rs:237 (`};` joined let-else), before :238 (character lookup). Why: the sole caller of apply_quest_trigger (QuestComplete item + currency grants), GrantItem auto-effects via apply_effects_to_db (:287), player_quest insert, player_conversation insert/update (:291-303), player_dialogue_state write (:314). IN SCOPE: the criterion names the harm ("quest turn-in grants"); ADR-0236 D3's same-file/same-class/one-line precedent; disclosed as ledger X7.
D3 advance_dialogue -> GATE. After npc.rs:336 (`};` joined let-else), before :337 (character lookup); necessarily above :341 (first write: a conversation delete) and :350/:359 (log::warn!, rb-78). Why: GrantItem dialogue effects, StartQuest inserts, state write, conversation update/delete.
D4 grant_bait (dev, `#[cfg(feature = "dev_reducers")]`) -> GATE. After taming.rs:283 (`let me = ctx.sender();`), before :284 (item_row lookup — the first DB read; no standing guard exists). Why: it IS the `grant_item` path the residual names (misattributed as "recruit"); ships in the CI-built dev wasm; the start_wild_battle precedent. Note: client/e2e/recruit.spec.ts calls grant_bait with an Active account -> unaffected.
D5 dismiss_dialogue -> CLASSIFY-OPEN (PRV1-10): its whole body deletes only the caller's own transient conversation row; gating it strands the row once talk/advance are gated. Pinned by a 73-byte body freeze (so the classification is an assertion, not a snapshot).
D6 attempt_recruit -> CLASSIFY-OPEN (PRV1-10): bait consume (:119) and, on success, a new monster/monster_pub insert happen INSIDE an already-open wild battle behind ownership + Ongoing guards — the submit_attack/use_battle_item class (battle.rs:1063 consume_one is ungated; rb-46's census refuses to gate in-battle reducers). The cascade erases monster rows regardless (PRV1-6b). Pinned: `consume_one(` == 1 inside its body.
D7 train / care / essence_train / consume_crystalized_essence -> DEFERRED BY SCOPE (not by class): §4.7's predicate selects them too (no "value creation" class line is claimed). E1 names three sites; these four are the same class and are registered as R-rb-80-RAISINGWRITERS with the measured remedy: their only precedented placement is after require_owner (raising.rs:79, ~:160, :633, :680), which sits below a u64-keyed `monster` lookup + is_in_ongoing_battle, so the native-host five-state matrix is unreachable there (admitted and refused states both return "monster not found") — they need a different proof vehicle, i.e. their own slice. Size if pulled in: 4 inserts + 4 pin tests, no execution proof.
D8 The criterion's second arm ("classify each deliberately") is used for D5-D7 and is MECHANICAL, per file, in ONE census test per file (not the unbuilt [DEL-06]): (a) file-wide bare-name count of `require_not_deleting` == 1 / 2 / 1 (a hoisted or aliased or wrapper gate in any other fn reds it; measured: every alias shape bumps the file count); (b) reducer ROSTER: count(`#[spacetimedb::reducer`) == count(`#[spacetimedb::reducer]`) (bans `reducer(name = ..)` wire-name twins) and the exact fn-name SET after each bare attribute == F9's sets (kills an ungated twin under a new or the same wire name — rb-79 register row M13's class); (c) conditional-compilation census: `#[cfg` == 2/1/3, `cfg!(` == 0, `debug_assertions` == 0, `target_arch` == 0 (kills the file-scope cfg-const-below-the-gate and the cross-file target_arch twin); (d) write-verb census per file: raising `spend_currency(` == 1, `consume_one(` == 3 (:242,:370,:729) [tester verifies], npc `grant_item(` == 2, `grant_currency(` == 1, `apply_effects_to_db(` == 3, `apply_quest_trigger(` == 2, taming `grant_item(` == 1, `consume_one(` == 1 (kills an in-file ungated twin that duplicates the writes — the below-gate `else`-wrapped delegation class); (e) D5/D6 body pins. The per-body "==0" rows for the deferred raising writers are NOT written (implied by (a) + the gated body's own count of 1; encoding a scope deferral as a CI contract was the reviewer's M3).
D9 No new wrapper, no new reason constant (ADR-0227 D2 / ADR-0246 D2), no helper-level gating (apply_effects_to_db / apply_quest_trigger take an owner identity — would need the census-contained subject wrapper and would fire twice per talk), guards.rs BYTE-IDENTICAL, no eval added or grown (ADR-0224), every in-flight reducer stays open. Uniform site comment: exactly ONE `// Deletion gate (ADR-0250 D<n>, spec para 4.7): ...` line above each gate statement (2 inserted lines per site) so knowledge stamps move predictably: raising.rs +2 below :306, npc.rs +2 below :237 and +2 below :336 (advance_dialogue's own stamp moves +2), taming.rs +2 below :283.

## Tests — 12 `rb80_` tests, all RED at HEAD; one file per production file + guards_tests.rs; each reuses ITS OWN file's strippers (ADR-0003); every production needle fragment-assembled; no raw double-quote char literal.
Prefix literals (comments stripped -> strings blanked incl. quotes -> whitespace removed; body start to gate) — HAND-TYPED in the tests (never derived from the body), byte EQUALITY (never starts_with/contains):
  heal_party:       letme=ctx.sender();letSome(p)=ctx.db.player().identity().find(me)else{returnErr(.to_string());};
  talk:             letme=ctx.sender();letSome(p)=ctx.db.player().identity().find(me)else{returnErr(.to_string());};
  advance_dialogue: letme=ctx.sender();letSome(conv)=ctx.db.player_conversation().owner_identity().find(me)else{returnErr(.to_string());};letSome(p)=ctx.db.player().identity().find(me)else{returnErr(.to_string());};
  grant_bait:       letme=ctx.sender();
Runtime ties per literal (a THIRD set of concat! splits): letme= 1/1/1/1; ctx.sender() 1/1/1/1; player().identity().find(me) 1/1/1/0; player_conversation().owner_identity().find(me) 0/0/1/0; returnErr( 1/1/2/0; .to_string()); 1/1/2/0; crate:: 0; ?; 0; { / } 1-1 / 1-1 / 2-2 / 0-0. Anti-transposition landmark per site (heal_party and talk literals are IDENTICAL): spend_currency( ==1 in heal_party; apply_quest_trigger( ==1 in talk; apply_choice( ==1 in advance_dialogue; grant_item( ==1 in grant_bait.
"Carries" pin clauses per gated site (helper `rb80_assert_gate_pinned` per file, not a #[test]): 0a paren-free `fn<name>` count == 1 in the squashed file (rb-79 [twin] shape — NOT name equality); 0b/0c landmines on the RAW file (`'"'`, `'{'`, `'}'`, `r#`); A gate needle == 1 (both spellings); C depth 0; D prev char `;` or `}`; E body-wide `#[` == 0 and `cfg!(` == 0; F bare name == 1 in the body; P prefix EQUALS the literal + the ties; R whole-body early-exit census count(`return`) == count(`returnErr(`) (7/7, 8/8, 9/9, 2/2 at HEAD — a plain `return twin(..)` below the gate reds it); G/H anchors: heal_party `player().identity().find(me)` < gate < `character().entity_id().find(p.entity_id)` < `spend_currency(` (each == 1); talk same anchors, write `apply_effects_to_db(` == 1 in body; advance_dialogue same anchors + clause W: `player_conversation().owner_identity().delete(me)` == 5 in body and its FIRST occurrence is after the gate; grant_bait: gate_at < `item_row().id().find(item_id)` < `grant_item(` (each == 1); T tag: on the comments-only (strings-intact) body, `(ctx,"<name>")?;` (and trailing-comma form) == 1 and the qualified opener == 1.
Execution tests (native host; economy_tests.rs:2754-2969 shape): five states (no row / Active / PendingDeletion / +terminal / removed), mid-grace STRANGER row seeded throughout, `acct.remove(me) == 1` between states, ordinary error pinned EXACTLY, refused verdict == `crate::guards::REJECT_DELETION_GATED` (the constant). Seeds: player row (entity_id 7); advance_dialogue also seeds `crate::schema::PlayerConversation { owner_identity: me, npc_entity_id: 1, current_node_id: "n".to_string() }` on `fx.table::<PlayerConversation>("player_conversation","owner_identity",|r| r.owner_identity)`. Ordinary errors: heal_party/talk/advance_dialogue `Err("character not found")`; grant_bait `Err("item not found")`.
 raising_tests.rs: rb80_heal_party_is_refused_only_while_the_caller_is_deletion_gated · rb80_heal_party_carries_the_deletion_gate · rb80_raising_reducer_roster_and_open_writers_are_pinned (D8 a-d for raising.rs)
 npc_tests.rs: rb80_talk_is_refused_only_while_the_caller_is_deletion_gated · rb80_advance_dialogue_is_refused_only_while_the_caller_is_deletion_gated · rb80_talk_carries_the_deletion_gate · rb80_advance_dialogue_carries_the_deletion_gate · rb80_npc_reducer_roster_and_open_writers_are_pinned (D8 a-d + D5 dismiss_dialogue body freeze)
 taming_tests.rs: rb80_grant_bait_carries_the_deletion_gate (NOT cfg-gated; text scan) · rb80_taming_reducer_roster_and_open_writers_are_pinned (D8 a-d + D6 `consume_one(` == 1 in attempt_recruit) · rb80_grant_bait_is_refused_only_while_the_caller_is_deletion_gated (`#[cfg(feature = "dev_reducers")]`, seeds inlined)
 guards_tests.rs: rb80_rb78_anchor_roster_covers_the_new_gate_bearing_modules (rb78_region_anchors().len() == 10 AND each of the three new fragments falls inside some live rb-78 region over rb78_live_sources()) + the roster edit itself: rb78_region_anchors() [String; 7] -> [String; 10] adding ["fnheal_","party("], ["fnt","alk("], ["fngrant_","bait("]; doc comment :6394-6414 and the live test doc :6454 and failure message :6522 edited IN PLACE with the same line count above :6415 (ADR-0248:173 cites guards_tests.rs:6415 — keep it true).
Counts: default features 11 rb80_ tests (X1 full = 915); with --features dev_reducers 12 (E1) and full = 916 (X2).
Taming extraction order: strip_rust_comments(&strip_rust_strings_taming(MODULE_SOURCE)) -> extract_fn_body -> squash_ws_taming (house order taming_tests.rs:300-303). npc: strip_npc_comments -> m22s3b_blank_strings -> extract_npc_fn_body -> squash_ws. raising: strip_raising_comments -> blank_heal_scan_strings -> reducer_body -> split_whitespace().collect() (eg2_scan_body shape, which runs assert_no_heal_scan_landmines).
npc_tests.rs and taming_tests.rs carry no stripper-precondition asserts today: each new test asserts the RAW-file landmines (`'"'`, `'{'`, `'}'`, `r#`, unbalanced `/*`) loudly, citing rb-78's crate-wide [rb78/scan-substrate] as the outer net.

## Tasks
T0 baseline 904 (done). T1 tester stages RED-arm blocks under /tmp/rb-80/tests/ (FILE + unique ANCHOR line + INSERT-AFTER, or OLD/NEW for the rb78 roster edit). T2 orchestrator splices (count==1 per anchor), captures RED: `-E 'test(/rb80_/)'` 11 fail (default) / 12 fail (--features dev_reducers), each on its predicted clause; rb78 live test must stay GREEN at HEAD (anchors 10 with 3 unresolved -> its anchor control fails! => record) — NOTE: extending rb78_region_anchors makes rb78_no_macro_expands_above_any_deletion_gate RED at HEAD too (anchor control), GREEN after impl. T3 impl bottom-up per file (npc.rs :336 first, then :237), cargo fmt, `-E 'test(/rb80_/)'` 11/11. T4 `--features dev_reducers -E 'test(/rb80_/)'` 12/12. T5 full 915; `--features dev_reducers` full 916; clippy --all-targets --all-features -D warnings; fmt --check. T6 mutant register (memory/projects/gates/rb-80.mutants.py + rb-80.red-before.md): M1-M4 drop each gate; M5 `let _ =` discard; M6 tag swap talk<->advance_dialogue; M7 gate below the first write (each site); M8 `#[cfg(test)]` on a gate statement; M9 unqualified import-shadowed call; M10 duplicate gate; M11 constant reject in guards (execution: admitted states); M12 inverted polarity; M13 sender-keyed early Ok above the gate; M14 file-scope cfg(debug_assertions) const + early return below the gate; M15 rejection-shaped `return Err(e);` above the gate (P kills, not a census); M16 `let me = crate::WILD_IDENTITY;` shadow; M17 wire-name twin `#[spacetimedb::reducer(name = "heal_party")]` + demoted gated fn (roster clause kills); M18 below-gate `if me != WILD { twin() } else { body }` (write-verb census kills); M19 gate added to dismiss_dialogue / attempt_recruit / train (file count kills); M20 an anchor deleted from rb78_region_anchors (guards rb80 test kills); T1 a site comment line deleted (control: GREEN — comments are not pinned); C1 comment above a gate GREEN; C2 early Ok BELOW a gate (R-rb-80-BELOWGATE, admitted) GREEN if it duplicates no write verb. T7 docs: ADR-0250 (full ADR-0104 header incl. `**Supersedes:** —` and `**Amends:** —`, `**Extends:** ADR-0236 (...)`, Subsystems security-authz, economy-quests); ADR-0236: append `, ADR-0250 (rb-80 — ...)` to its existing `**Extended-by:**` line 9 + one dated `## Amendment (2026-09-12, rb-80 — residual R-rb-46-ERASEWRITERS closed)` at EOF; ARCHITECTURE.md: :485 in-place replacement + one `**rb-80** (...)` paragraph after :2279 ending `ADR next-free = 0251.`; `just adr-digest`. T8 `just knowledge` AFTER the final fmt; `just knowledge-check`. T9 residuals registered AFTER the lenses: R-rb-80-RAISINGWRITERS (MED), R-rb-80-BELOWGATE (MED), R-rb-80-CRATEWIDEBARE (MED — no crate-wide census of the caller gate's bare name; per-file counts only cover 8 modules), R-rb-80-CITEDRIFT (LOW), R-rb-80-REASONSCOPE (LOW), R-rb-80-DEVFEATURETEST (LOW). T10 `just ci` detached with CI-EXIT marker; `mr-gates check --slice rb-80 --timeout 2400` from the worktree.

## Ledger (memory/projects/gates/rb-80.gates.md): E1 12/12 with --features dev_reducers · X1 915 default · X2 916 dev-feature full · X3 lint · X4 docs fresh + ADR/ARCH/stamp facts + rb80 test NAME SET via `cargo nextest list` · X5 just ci · X6 MANUAL mutant register · X7 MANUAL talk extension + open-set disclosure.

## Touches-delta forecast: guards_tests.rs (sibling of guards.rs), docs/adr/0236-*.md (reciprocal Extended-by fragment + EOF amendment), docs/knowledge/** (regen), CHANGELOG.md (cliff, untouched by hand), harness memory/projects/gates/rb-80.* + memory/projects/monster-realm-rb-80-plan.md. guards.rs byte-identical. NO HIDDEN DEPENDENCY.
