# rb-81 BUILD PLAN v2 — R-rb-47-ROSTER-PVP, ADR-0251 (planner opus + plan reviewer opus + plan red-team opus + /simplify inline, 2026-09-12)

Brief: /tmp/rb-81/brief.md. v1: /tmp/rb-81/plan-v1.md. Worktree: /home/mdrewt/projects/ai-apps/claude-harness/projects/monster-realm/.claude/worktrees/rb-81 (branch rb-81 = origin/master 1850d35). Baseline 915 tests run: 915 passed, 0 skipped.

## T0 — the gap, measured on the pristine tree (/tmp/rb-81/t0.log; full default nextest, byte-restore, sha OK)
A plain bare twin (`#[spacetimedb::reducer] pub fn rb81_twin_<f>(_ctx: &ReducerContext) -> Result<(), String> { Ok(()) }`) inserted above the test-module tail:
pvp SURVIVED 915/915 · battle SURVIVED 915/915 · economy SURVIVED 915/915 · ranking KILLED only by the pre-existing COUNT pin `pvp_tests.rs:1246 m17a_rl7_server_ranking_module_invariants` (prefix count == 1). So three files have NO cover and ranking has a count, not a set, and no alias/wire-name/entry-macro closure.

## Measured facts (reviewer + red-team re-measured every v1 number; red-team ports of all four strippers at /tmp/rb-81-redteam/strippers.py)
F1 Rosters: pvp {challenge_pvp, accept_challenge, decline_challenge, cancel_challenge, submit_pvp_action, battle_challenge_reaper, pvp_deadline_reaper} (pvp.rs:800/975/1085/1120/1171/1296/1329); battle {start_battle, start_wild_battle, submit_attack, swap_active, flee, use_battle_item} (:61/548/617/784/926/982; start_wild_battle is `#[cfg(feature = "dev_reducers")]`-gated at :546 — the TEXT roster is 6 in both builds, the default build compiles 5); economy {buy, sell} (:104/198); ranking {set_profile_name} (:139). Every bare attribute is IMMEDIATELY followed by `pub fn` (16/16 sites verified).
F2 Attribute PARTITION on each file's own stripped+squashed view (prefix-only needles, string-free): pvp 16 = reducer 7 + table 2 + primary_key 2 + auto_inc 2 + index( 1 + cfg( 1 + path 1; battle 12 = reducer 6 + cfg( 5 + path 1; economy 5 = reducer 2 + cfg( 1 + path 1 + allow( 1; ranking 3 = reducer 1 + cfg( 1 + path 1. Raw `#[` (unstripped) 17/13/5/4 — the deltas are one comment-borne `#[` each at pvp.rs:1386, battle.rs:1604, ranking.rs:222 (economy none). bare == prefix == `::reducer` = 7/6/2/1. `mod` identifier-boundary count on the stripped-NOT-squashed view = 1/1/1/1 (the tail; measured +1 with a `mod twin;`); `include` substring 0; `macro_rules` 0. v1's pvp arithmetic (15, cfg=2) was WRONG; pvp.rs:252 declares a PARAMETER named `reducer` (used :260/:265/:270/:281) — a bare `reducer`-token census would pin a parameter name (cite in ADR D3 against the token-census alternative).
F3 Substrate at HEAD (all four): `/*` 0 and `*/` 0; `'"'` char literal 0; raw-string openers (`r"`, `r#…"`, `br"`, `br#…"` not preceded by an identifier byte) 0; every contiguous `//` comment run (and every trailing `//` comment) has an EVEN `"` count (0 odd runs of 108/122/27/22); the two stripper orders (comments→strings vs strings→comments, each file's OWN two strippers, squashed) are byte-EQUAL. Line-count preservation under string blanking is NOT HEAD-green (pvp and ranking each lose one newline) — do not use it. The brief's "every `"` lives outside comments" is unsatisfiable at HEAD (ranking.rs:134/167/168/258 put quotes in comments) — do not write it.
F4 Tails (comments-only squashed view, strings INTACT): pvp `#[cfg(test)]#[path="pvp_tests.rs"]modpvp_tests;`; battle `#[cfg(test)]#[path="battle_tests.rs"]modbattle_tests;`; economy `#[cfg(test)]#[path="economy_tests.rs"]#[allow(unused_imports)]modeconomy_tests;`; ranking `#[cfg(test)]#[path="ranking_tests.rs"]modranking_tests;`. cfg predicate lists (same view, file order): pvp [`#[cfg(test)]`]; battle [`#[cfg(feature="dev_reducers")]` x4 (:40/:42/:44/:546), `#[cfg(test)]`]; economy [`#[cfg(test)]`]; ranking [`#[cfg(test)]`]. NOTE the stripped views LOSE the `#[path]` payload (pvp/economy keep quote bytes → `#[path=""]`; battle/ranking blank → `#[path=]`), so tail + cfg-roster MUST run on the comments-only view.
F5 Strippers: pvp_tests.rs strip_rust_comments(:64) → strip_rust_strings(:2143) → squash_ws(:2178); battle_tests.rs :49 → :93 → :2498; economy_tests.rs strip_rust_comments_economy(:936) → strip_rust_strings_economy(:976) → NO squash helper (add `rb81_squash_economy`); ranking_tests.rs stripped_for_scan(:238) = strip_rust_strings(:94, raw-string aware) → strip_rust_comments(:190) → squash_ws(:221). All four comment strippers are length-preserving (blank to spaces); the string strippers are not (pvp/economy shrink).
F6 Entry-point attribute macros re-exported by spacetimedb 2.8.1 (~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/spacetimedb-2.8.1/src/lib.rs): duration :71, client_visibility_filter :109, settings :133, table :462, reducer :712, procedure :783, view :917. `procedure`/`view` carry no `reducer` token and need no `use` line → only an exhaustive partition sees them.
F7 Citations moved by the ADR-0237 header insert (+1 below its line 9): ARCHITECTURE.md:2279 cites `ADR-0237:203-204` (repoint to :204-205, in scope); harness specs/monster-realm-v2/M-postgate-nineteenth-review-residuals.spec.md:128 cites `ADR-0237:108` (harness spec corpus — supervisor-owned; DISCLOSE as R-rb-81-SPECCITE LOW, do not edit). No gate cites 0237 line numbers (DIGEST links only).
F8 cargo-nextest 0.9.138 `--no-tests` default = fail (exit 4), so a zero-match `-E 'test(/rb81_/)'` cannot pass silently.
F9 X4 knowledge stamps into the four files (18, unmoved because no production edit): see the ledger's pinned list.

## Decisions
D1 Four per-file tests (option A): `rb81_pvp_reducer_roster_is_closed` (pvp_tests.rs), `rb81_battle_reducer_roster_is_closed` (battle_tests.rs), `rb81_economy_reducer_roster_is_closed` (economy_tests.rs), `rb81_ranking_reducer_roster_is_closed` (ranking_tests.rs). WHY: EARS "naming the file" is satisfied by the TEST NAME (a parametrized loop names only its first failing row — coarse-mutants-only-prove-the-first-assertion); each file's own strippers have their own blind spots and ADR-0003 forbids a shared/third stripper; each test file already reads its own file; rb-47/rb-80 precedent; register rows cite one designated test. (v1's `r"`-in-string justification was wrong — those are closing quotes consumed inside the string branch; the real B-hazard is quote/`r"` inside COMMENTS.)
D2 ZERO production edits: the four .rs files stay byte-identical to origin/master (18 knowledge stamps + 7 test-file readers ripple from one inserted byte). Hardening pin (ADR-0224): GREEN at HEAD by construction; teeth = T0 + the live mutant register + the automated X8 twin-register gate.
D3 Clause list per test, IN THIS ORDER — substrate, tail, cfg-roster, ROSTER(+parse), attr-any, attr-path, PARTITION, mod-census, include/macro bans, attr-raw LAST (the semantic roster clause fires before the exotic-spelling closure so a plain twin fails NAMING the twin; the stripper-free backstop is last so every register row's designated clause is meaningful — red-team R1), every assert message carrying its `[rb81/<clause>]` label and, for numeric pins, found-vs-expected per kind:
  1 `[rb81/substrate]` on the RAW source: P1 `/*`==0 && `*/`==0; P2 `'"'`==0 (bytes 0x27 0x22 0x27, assembled); P3 raw-string openers ==0 (`r"`/`r#`/`br"`/`br#` whose preceding byte is not [A-Za-z0-9_]; use `char::from(0x22u8)` — never a literal quote char); P4 every contiguous run of full-line `//` comments and every trailing `//` comment has an even `"` count; P5 squash(strings(comments(src))) == squash(comments(strings(src))) using the file's OWN two strippers. Kills red-team H1/H2/H3 (block-comment-in-string, odd quote-in-comment, raw-opener polarity attacks) — measured GREEN at HEAD on all four, RED on all three attacks with the same rules.
  2 `[rb81/tail]` the comments-only SQUASHED view (strings intact) ENDS with the exact F4 tail literal (fragment-assembled). Kills H5 (`#[cfg(all())]`+`#[path]` retarget of the test module), anything appended after the tail, and rotted/empty source (replaces v1's `len > 5000` vacuity guard).
  3 `[rb81/cfg-roster]` the ordered list of `#[cfg(...)]` predicates on the comments-only squashed view (paren-depth walk from each `#[cfg(`) == the F4 list. Kills H6 (`feature="dev_reducers"` → `all()` publishing a dev reducer in the default build); makes the roster's build-visibility claim true.
  4 `[rb81/attr-partition]` on the file's stripped+squashed view: EACH kind's count == F2 (per-kind equality, not a sum) AND total `#[` == sum of the pinned kinds (so the unknown-kind bucket is ZERO). Kills procedure/view twins (F6), `#[reducer]`/`#[stdb::reducer]`/`#[r]`/`#[::spacetimedb::reducer]`/`#[cfg_attr(..)]` twins, a demoted-fn `#[allow(dead_code)]`, the net-zero kind swap (M2), and any new attribute — a future `#[derive]`/`#[allow]` in these files is an INTENDED re-review; the FAIL message says: "this is a security census; add the attribute deliberately, then bump this kind's pin and the total in the same edit; never relax a number alone".
  5 `[rb81/roster]` bare `#[spacetimedb::reducer]` count == 7/6/2/1 AND the parsed name SET == F1 (sorted Vec equality or BTreeSet diff with missing/extra). Walker = rb-47's LOUD shape: after each bare attribute the squashed text MUST continue `pubfn<ident>(` or PANIC `[rb81/roster-parse]` (a `pub(crate) fn`/`fn` twin or an attribute stacked after the reducer attribute is a loud failure, never a skip).
  6 `[rb81/attr-any]` prefix count == bare count (wire-name twin `reducer(name = ..)`); 7 `[rb81/attr-path]` `::reducer` token count == bare count (aliased crate path, by-name import, leading `::`; zero brittleness).
  8 `[rb81/mod-census]` identifier-boundary `mod` count == 1 on the stripped-NOT-squashed view (kills `mod twin;`, `#[path] mod twin;`, and a shadowing `mod spacetimedb { pub use ::spacetimedb::reducer as table; }`); 9 `[rb81/include-ban]` substring `include` == 0 (catches include!/include_str!/std::include!) and `[rb81/macro-ban]` substring `macro_rules` == 0, same view.
  10 `[rb81/attr-raw]` raw `#[` count == 17/13/5/4 — LAST, the only stripper-free clause (belt-and-braces for the polarity class); the FAIL message names the comment-borne delta (1/1/0/1) and the remedy (bump raw+stripped together).
  CUT (simplify): `[use-line]` (every alias spelling already reds the partition + attr-path; an exact import-list literal is a re-review on every import edit for zero measured gain); `[vacuity]` (subsumed by tail); red-team's `[comment-attr]` exact-carrier-line pin (over-pin).
D4 Honest limits → ADR D-section + X7: R-rb-81-CROSSFILEMACRO (a macro_rules! defined in another module and invoked here — no file-local window); a twin in a NEW module file (`src/twin.rs` + `mod twin;` in lib.rs) is invisible to all four tests and to rb47_scanned_module_names (derived, not pinned) — fold into R-rb-80-CRATEWIDEBARE with the other nine reducer files (DEFER, not build); ranking.rs was count-covered by m17a_rl7 before this slice (rb-81 adds SET + closures; register rows M6/M9 are rb81_ranking's SOLE kills); rosters are text, not the compiled wasm's reducer table; the partition's brittleness (D1/D2 control rows) is deliberate; flipping a table to `public` moves no clause (out of EARS scope; disclose).
D5 Docs: ADR-0251 (Extends ADR-0237; Subsystems security-authz, ci-gates; Supersedes/Amends —); ADR-0237 gets ONE `**Extended-by:** ADR-0251 (...)` header line after its `**Amended-by:**` line + ONE tail `## Amendment (2026-09-12, rb-81 — residual R-rb-47-ROSTER-PVP closed)` naming the bullet by TEXT; ARCHITECTURE.md: repoint :2279's `ADR-0237:203-204` → `:204-205` and append ONE `**rb-81** (...)` paragraph ending `ADR next-free = 0252.`; `just adr-digest`. CHANGELOG untouched (cliff). No knowledge regen expected (okf-export excludes *_tests.rs; no production edit) — run `just knowledge-check` to prove it.

## Mutant register (memory/projects/gates/rb-81.mutants.py; rb-80 shape; FILES = the four; every row also deletes any file it created in `finally`; build failure = INVALID (triggers `error[E`, `could not compile`, `^error: `); each row declares its designated TEST and CLAUSE LABEL and the runner greps the nextest output for that label → KILLED / KILLED-WRONG-CLAUSE / SURVIVED / INVALID; machine line per row `ROW <id> <verdict> test=<name> clause=<label> restored=OK`; exit 0 iff every row matched its expectation)
M1a-d plain bare twin per file → [rb81/roster] (M1d also kills m17a_rl7; disclosed) · M2a-d `#[cfg(test)]`-hidden twin → partition (cfg kind) · M3 wire-name twin + real fn demoted → attr-partition (allow) or attr-any (row per file: economy) · M4 `use spacetimedb as stdb; #[stdb::reducer]` → partition · M5 `use spacetimedb::reducer; #[reducer]` → partition · M6 `use spacetimedb::{reducer as r, ..}; #[r]` (ranking) → partition, rb81_ranking SOLE killer · M7 `use spacetimedb::*; #[reducer]` → partition · M8 `#[::spacetimedb::reducer]` → partition · M9 `#[spacetimedb::procedure]` twin (ranking) → partition, SOLE killer · M10 `#[spacetimedb::view]` twin → partition (may be INVALID outside schema.rs; record honestly) · M11 `#[path = "twin.rs"] mod twin;` → mod-census · M12 bare `mod rb81_twin;` + `src/<f>/rb81_twin.rs` (NO #[path]) → mod-census · M13 `include!("twin.rs");` → include-ban · M14 in-file macro_rules! emitting the attribute → macro-ban · M15 `#[cfg_attr(all(), spacetimedb::reducer)] pub fn twin` → partition · M16 bare attr + `pub(crate) fn twin` → roster-parse PANIC · M17a-d one reducer's attribute deleted per file → roster (missing) · M18 rename `flee`→`flee_v2` → roster SET · M19 net-zero kind swap (delete one `#[auto_inc]`, add a procedure twin) → partition per-kind · M20 ranking: comment `#[reducer]` at :222 reworded + real twin (raw stays 4) → roster/partition (stripped moves) · M21 H1 double-polarity (`/*` in a string + odd `"` in a comment + twin) → substrate P1/P4 · M22 H2 comments-first polarity+net-zero (pvp) → substrate P1 · M23 H3 strings-first polarity (ranking) → substrate P4 · M24 H5 `#[cfg(all())]#[path="pvp_twin.rs"] mod pvp_tests;` → tail · M25 H6 cfg flip on start_wild_battle → cfg-roster · M26 `r"\"` raw string in code (rb-80 V1 shape) + twin → substrate P3.
Controls: C1 pristine GREEN · C2 comment naming the attribute WITHOUT `#[` GREEN · C3 `const _X: &str = "<attr text>";` GREEN · C4 rustfmt reflow of a reducer signature GREEN · D1 a comment containing a literal `#[` → expected RED on [rb81/attr-raw] (disclosed) · D2 a new `#[allow(dead_code)]` → expected RED on partition (intended re-review).

## Ledger (memory/projects/gates/rb-81.gates.md; `mr-gates check --slice rb-81 --timeout 2400` FROM the worktree)
E1 filtered nextest 4/4 · X1 full default 919 · X2 --features dev_reducers 920 · X3 lint · X4 docs fresh + ADR-0251 header + ADR-0237 reciprocal + ARCHITECTURE entry/repoint/next-free + four production files byte-identical to origin/master + no untracked file under server-module/src + 18 stamps unmoved + rb81 test NAME SET + each test reads its own file const + each carries the substrate label · X5 just ci · X6 MANUAL full register (red-before.md path:line) · X7 MANUAL disclosures · X8 AUTOMATED twin register: the runner's M1a-d rows KILLED on [rb81/roster] (node wrapper around the python runner, which runs cargo nextest) — the machine-verified tooth that four stub tests cannot fake.

## Tasks
T0 DONE. T1 runner (python; `--list`; C1 dry-run proves restore/sha). T2 tester (opus) authors the four tests as apply-ready /tmp/rb-81/tests/<file>.append.rs blocks (+ helper fns per file; every needle fragment-assembled; labels; messages) — GREEN at HEAD, RED against the twins. T3 orchestrator splices (EOF append), cargo fmt, `just ci-fast monster-realm-module`, E1/X1/X2/X3; RED capture = runner rows M1a-d at minimum (red-before.md §2). T4 artifact lenses: tests reviewer + artifact red-team + a SECOND tester agent as the adversarial executor/critic (prompt requirement) in one batch; harden. T5 full register (§3 of red-before.md). T6 docs (D5) + `just adr-digest` + `just knowledge-check`. T7 impl-phase lenses: reviewer + /simplify + red-team + reducer-security-auditor + verifier (desync-guard NOT run: no game-core/wasm/client/movement surface — say so). T8 `just ci` detached with a CI-EXIT marker; `mr-gates check`. T9 residuals AFTER the lenses (R-rb-81-CROSSFILEMACRO MED, R-rb-81-SPECCITE LOW, + whatever the lenses add). T10 PR (touches-delta + boyscout-delta none + Items: none + `mr-gates render --format pr`).

## Touches-delta forecast
docs/adr/0237-*.md (reciprocal Extended-by header line + dated tail amendment) · docs/adr/DIGEST.md (regen) · ARCHITECTURE.md (:2279 citation repoint + tail paragraph — in-scope companion) · harness: memory/projects/gates/rb-81.{gates.md,mutants.py,red-before.md,logs/} + memory/projects/monster-realm-rb-81-plan.md. NOT expected: docs/knowledge/**, evals/**, any production .rs. Hidden dependency: NONE (the harness spec citation is disclosed, not edited).

## Anti-patterns (binding for the tester)
Never spell `#[spacetimedb::reducer]`, `#[spacetimedb::table(`, `pub fn <reducer>(`, `fn <reducer>(` contiguously anywhere in a test file, assert messages included — fragment-assemble (`["#[spacetimedb", "::reducer]"].concat()`, `["challenge_", "pvp"].concat()`); no raw `"` inside a char literal (`char::from(0x22u8)`); no `/*` or `*/` in comments of the test files' new blocks beyond what the file's strippers already survive (write "block-comment opener"); no needle containing a string literal payload (the four strippers disagree on quote survival); no first-hit anchors; no count-only pin without the SET; no relaxing a number to green; no eval added/grown; every assert carries its `[rb81/<clause>]` label FIRST in the message.
