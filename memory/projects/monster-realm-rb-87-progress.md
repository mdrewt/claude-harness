# rb-87 progress memo — TERMINAL STATE reached (PR open + local gate green + ledger 8/10 met, 2 deferred); written for the supervisor's resume/audit, not a park

PR: https://github.com/mdrewt/monster-realm/pull/475 · branch feat/rb-87-export-reaper-observation · worktree .claude/worktrees/rb-87 · head 6479e28 · remote CI running · SUPERVISOR OWNS THE MERGE (gh pr merge is forbidden for the run).

## DONE
- Plan (+ /simplify, plan reviewer, plan red-team), ADR-0238 dated amendment (ADR 251 was taken by rb-81), 8 rb87_ tests (RED record rb-87.red-before.md), implementation (plan §2 byte-exact), tests round 2 (three measured CI-clean gaps closed), docs (ARCHITECTURE paragraph, knowledge regen), security audit PASS (+ one doc retruth adopted), verifier PASS, register 30/30 + 3/3 (rb-87.x7-register.md), ledger X1-X8 met with evidence on the final tree, four residuals registered, PR #475 open.

## REMAINING (supervisor)
- Watch remote CI on PR #475 (mr-ci-watch), re-execute the ledger CHECKs, squash-merge; reconcile CHANGELOG / ADR index / ARCHITECTURE across siblings; re-target R-rb-85-X10 (targeted -> rb-87, re-deferred via X10 -> backlog); decide whether a standalone ADR number is still wanted (0251 collision); post-merge code-graph refresh on the main checkout.

## BLOCKERS
- none

## exact-next-step
- supervisor: mr-gates verify --slice rb-87 FROM .claude/worktrees/rb-87, then merge on the CI event.

## mr-gates render --format park (verbatim)
# rb-87 — park memo (generated by mr-gates)

## DONE (8)
- X1: [the criterion — one ordinary Rust suite that REDs at HEAD on the silent tick] WHEN the rb87_ tests run agains
  evidence: Summary [ 0.031s] 8 tests run: 8 passed, 961 skipped
- X2: [non-regression, default features] WHEN the full server-module suite runs on the shipped tree THE SYSTEM SHALL
  evidence: Summary [ 0.301s] 969 tests run: 969 passed, 0 skipped
- X3: [non-regression, dev_reducers feature] WHEN the server-module suite runs with --features dev_reducers THE SYST
  evidence: Summary [ 0.309s] 970 tests run: 970 passed, 0 skipped
- X4: [compile-level + lint] WHEN the lint gate runs (fmt --check, clippy -D warnings --all-targets --all-features, 
  evidence: rb87-X4:LINT-GREEN
- X5: [touches boundary + docs truthful] WHEN the tracked AND untracked changes vs the merge-base with origin/master
  evidence: rb87-X5:DOCS-FRESH gitok=true nonempty=true amd=true noadr=true arch=1 named=true nextfree=true tick=true fiel
- X6: [full gate] WHEN the complete just ci runs in the slice worktree THE SYSTEM SHALL exit 0 (lint, typecheck, ful
  evidence: rb87-X6:CI-GREEN
- X7: [proof-of-teeth — live mutant register on the REAL privacy.rs] WHEN each Rust mutant is applied one at a time 
  evidence: /home/mdrewt/projects/ai-apps/claude-harness/memory/projects/gates/rb-87.x7-register.md:42
- X8: [proof-of-teeth, automated subset at merge] WHEN the four headline register rows run (in runner order: the int
  evidence: rb87-X8:MUTANTS-KILLED 4/4 restored=OK labels=[rb87/evt-partition,rb87/fields-value,rb87/emit-count-in-fn,rb87

## REMAINING (0) — the exact next steps

## DEFERRED (2)
- X10 -> backlog — the alarm consumes the line this slice creates and cannot precede it; its files (ops/observability/rules/recording.rules.yml, ops/observability/grafana/provisioning/alerting/*, ops/observability/checks/stack-config-checks*.mjs, evals/observability-stack-config.eval.mjs) are a distinct file family outside the inherited touches with their own rb-66/rb-84 test surface (a new rule or panel forces stack-config-checks fixtures + a checkEventHasQueriedConsumer-style consumer test). This slice supplies exactly the read/planned/reaped counts and the hourly dead-man cadence that alarm reads; R-rb-85-X10's byte-growth threshold rides with it. Two facts the alarm author must build on (reviewer M2 + NIT): a cap at its bound is a necessary-not-sufficient HINT — `planned`==16 may be exactly-sixteen rather than truncated, `read`==256 may still have drained everything (whole-stamp deletes take tails, so `reaped` can exceed `read`), and a saturated stamp cap can show `read` far below 256 — so the rule must key on consecutive ticks (the exact pre-truncation stamp count is R-rb-87-BACKLOGAMBIG); and the singleton is armed by init/sync_content, so a fresh publish's first beat is up to an hour out — the missing-beat rule needs a startup grace. The module's 256/16 caps are value-pinned in Rust but nothing couples them to a rule file: the alarm must cite EXPORT_REAP_MAX_DELETE_PER_TICK / EXPORT_REAP_MAX_STAMPS_PER_TICK by name so a reviewed cap change is findable (red-team F10).
- X9 -> backlog — the same mechanism rb-85 and rb-86 recorded as R-rb-85-X9 (unpromoted, harness residual log): datastore_index_scan_range_bsatn is still undefined in server-module/src/native_host_tests.rs (a test path reaching the helper link-fails the WHOLE lib-test binary), datastore_delete_by_index_scan_point_bsatn aborts there, ctx.database_identity() is unstubbed, and no capturing log sink exists in the native host. rb-87's executable oracles stop one hop short of the reducer: the fields fragment by value and the composed envelope through the shipped build_log_line; the reducer wiring is pinned by equality. The tests red-team MEASURED the other half of that gap: an early `return` inside observability::mr_log (outside touches) silences every line in the crate — the reaper beat, the rb-65 data_export line, the heartbeat — and the full 969-test server-module suite still passes; nothing anywhere proves delivery of a line, so the dead-man design rests on the heartbeat pipeline being watched (registered as R-rb-87-MRLOGDELIVERY). Modelling the range scan, the point delete, the database identity and a log sink is that residual's own slice.
