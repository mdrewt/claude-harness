Run the Monster Realm v2 build loop in this harness per `specs/monster-realm-v2/build-loop-prompt.md`, the project `AGENTS.md`, and `standards/`, using the real harness agents/commands — do not shortcut its procedure, subagent orchestration, engineering invariants, or Definition of Done. Triage complexity first, then route per `docs/routing.md`. **Scope: exactly ONE mergeable slice — `<SLICE>` (<TARGET_DESC>) — then stop. Right-size it: if planning shows the slice is large (many files / several EARS criteria), ship the smallest coherent mergeable increment and park the remainder as the next slice — smaller, focused PRs review better. Stay strictly within this slice's declared `touches:` path-set — <TOUCHES> — plus these ALWAYS-in-scope companions: (a) sibling test files of any declared code file (e.g. `foo_tests.rs` next to `foo.rs`, `*.test.ts` next to a declared `.ts`), and (b) the standard doc outputs `docs/adr/**` (your reserved ADR number only), `docs/knowledge/**` entries for reducers/modules you touched, `CHANGELOG.md`, `ARCHITECTURE.md`. List EVERY file you touched beyond the literal declared set in the PR body under a `touches-delta:` heading so the supervisor can audit it mechanically. Any OTHER file outside the declared set is a hidden dependency: stop, record it in the handoff, and let the supervisor re-serialize (a concurrent sibling may own those files).**

**Boy Scout Rule (per `standards/principles.md`, scoped for the supervised loop):** WITHIN this slice's declared `touches:` set (+ the ALWAYS-in-scope companions), leave files better than you found them — bounded, in-file improvements (naming of FILE-LOCAL symbols only — renaming anything exported/imported elsewhere is a refactor needing its own slice, and at N≥2 can break a sibling building against pre-rename master; dead code, comment accuracy, small code smells) that ride the SAME reviewer/red-team/verifier gates as everything else. Attribute each under a **`boyscout-delta:`** PR-body heading (`file:line-range — one-line why`); the label is attribution ONLY, never a lighter-review exemption — the reviewer verifies entries are truthful and in-cap, and a substantive change mislabeled as boyscout is a red flag. **Cap:** ~40 changed lines / ≤3 hunks of boyscout-attributed diff per slice (a ceiling, not a target); excess becomes a follow-up flag in the handoff, never a merge blocker and never hunk-split to dodge the cap. **Intent boundary:** a file outside `touches:` that the task REQUIRES is still a hidden-dependency STOP (rule above); a file outside `touches:` that merely invites cleanup gets a follow-up flag — do NOT touch it. Gating tests are never boyscout targets, and interface/ADR-freeze micro-slices are boyscout-EXEMPT entirely — a frozen seam that parallel siblings build against must be a crisp, minimal diff. (Missing tests for behavior you changed are core DoD, not boyscout — they don't count against the cap.)

**If `master` CI is red, fix/revert it to green instead and stop — this overrides the slice scope AND the Boy Scout rule; a red master is never a cleanup opportunity.**

**Worktree:** if the handoff names a parked worktree/branch for this slice, **resume in it** (continue from its `wip:` commits); else `git worktree add` a fresh isolated one at `<WT_ROOT>/<SLICE>` from the latest `origin/<BASE_BRANCH>`. Leave the main checkout on `<BASE_BRANCH>`. **NEVER run mutating git commands (`stash`, `checkout`, `commit`, `reset`, `clean`, …) against the main checkout path — not even for diagnosis** (a run's `git -C <main-checkout> stash pop` once left a conflicted index that blocked the merge pipeline); read-only commands (`log`, `diff`, `show`) are fine. All mutations happen in YOUR worktree only.

<RESUME_BLOCK>

<TIER_BLOCK><REPO_BLOCK>

**Checkpoint + cooperative stop (IMPORTANT):** commit a `wip:` checkpoint to the slice branch at every phase boundary (**after the plan + ADRs are written** — so a planning-phase park never re-does planning; after tests are written; after each green increment; before long CI/merge waits) **and `git push` it immediately — the branch must never sit unpushed.** As the orchestrator, **between phases and whenever you are waiting (e.g. on CI), check for the files `/tmp/mr_stop_<SLICE>` or `/tmp/mr_stop_all`.** If either exists: do NOT start new work — finish the current atomic step, `git add -A && git commit -m "wip(<SLICE>): checkpoint — rate-limit stop"` (skip if nothing to commit), `git push`, update the handoff (`memory/projects/monster-realm-handoff.md`) with the exact resume point (branch, any open PR, the next step), then **exit cleanly.** **On ANY park (stop-flag, blocker, or otherwise ending without a PR): also write `memory/projects/monster-realm-<SLICE>-progress.md`** — structured DONE / REMAINING / BLOCKERS / exact-next-step, ~1 page — so a resume pass starts from a memo instead of re-discovering state from the diff. The PR squash-merge collapses all `wip:` commits into one Conventional Commit, so checkpoints never reach `master`.

**Test-first, gated:** **If the slice deliverable IS tests** (deflake / smoke / e2e-only slices): the tester lens is STILL mandatory — invoke a separate `tester` agent to adversarially execute and critique the tests (repeat-run ≥5× for flake work, attempt to break the assertions, confirm the proof-of-teeth still bites); the supervisor’s pre-merge orchestration audit requires a tester-role invocation on every non-doc slice and a missing one forces an extra paid review pass. `planner` plans → `reviewer`+`red-team`+`/simplify` review the plan (`/adr` new patterns) → the **`tester` (a different agent than the implementer)** writes meaningful tests from the EARS criteria — unit/property, integration, contract, the harness evals incl. **proof-of-teeth that actually bite** — starting red → `specialist` implements red→green **without editing the gating tests** → `just ci` green → `reviewer`+`/simplify`+`red-team`+ the domain auditors (`reducer-security-auditor`/`desync-guard`)+`verifier` review and re-run the **full** suite, and **the `verifier` asserts the gating tests were not weakened RED→green** (no deleted/`skip`/`xit`/`.only`/`#[ignore]`'d tests, no removed assertions, proof-of-teeth still bite) → `doc-keeper` updates memory + writes the ADR **at the supervisor-assigned number `<ADR_NUM>`** (see doc-aggregation note). Independent read-only lenses in parallel; tests-before-impl and the verifier-gated merge serial; subagents depth = 1, N = 2–3.<SKILLS_BLOCK>

**Doc-aggregation (fan-out safety):** a sibling slice may be running concurrently on a disjoint file set. To avoid doc-merge collisions: **do NOT hand-edit `CHANGELOG.md`** (it is `git cliff`-generated from your Conventional Commit messages — write good `feat:`/`fix:` commits instead); **do NOT touch `docs/adr/README.md`** (the supervisor owns the ADR index); if you author an ADR, name it `docs/adr/<ADR_NUM>-<slug>.md` using the **supervisor-assigned number above** (never pick your own); keep any `ARCHITECTURE.md` edit to a **minimal, targeted** addition in the relevant section. The supervisor reconciles `CHANGELOG`/ADR-index/`ARCHITECTURE` across siblings at the serial merge.

**Uphold the invariants:** SSOT + functional-core/imperative-shell (rules live once in `game-core`); content is data, not code; additive schema (ADR-0006); illegal states unrepresentable, parse-don't-validate at boundaries, exhaustive `match`, injected clocks/RNG; server-authoritative/intent-only/reject-not-clamp, stakes-classified RLS (ADR-0015 — must-never-leak data in private tables), netcode smoothness (ADR-0013); loose coupling, YAGNI; record every non-obvious "why" as an ADR.

**Definition of done (the authority):** **this slice's gate** (see REPO above) green and meaningful — for a project slice that is `just ci` (lint, typecheck, full unit/integration/contract suite, evals, coverage + mutation on changed lines — extend mutation to the whole changed module where feasible, security clean, benchmarks in budget) · every EARS criterion tested · every gate has a proof-of-teeth fixture that bites · `/simplify` + `/review` closed it · domain auditors green · ADR/changelog/memory/`ARCHITECTURE.md` updated, spec reflects reality. **Budget is ample — favor thoroughness over frugality: prefer more mutation coverage, an added adversarial/red-team lens, and the full eval suite where they catch *distinct* defect classes; add lenses that find different bugs, not redundant agents re-running the same checks.**

**Merge (supervisor-owned — pipeline the wait):** run **this slice's gate** (see REPO above) green **locally before opening the PR** — a remote-CI red on a check local CI also runs is a wasted round-trip to avoid (ReDoS / `detect-non-literal-regexp` has bitten twice; prefer `String.indexOf`/literal patterns over dynamic `RegExp`). Open the slice PR on `<PR_REPO>`, then **STOP — your terminal state is 'PR open + local gate green + remote CI running (where remote CI exists)'. `gh pr merge` is FORBIDDEN for you — it is a supervisor-only command (runs have merged early, before remote CI, bypassing the audit gate; retro 2026-07-24). The supervisor owns the merge** (it delegates the CI wait to `mr-ci-watch` and squash-merges on the resulting event — one Conventional Commit, linear), so the next slice can start during the remote-CI wait; **do not sit waiting on remote CI or merge yourself** under the supervisor. If branch protection blocks the PR, or you can't reach **local** green after **~4–5 red→fix cycles**, **PARK**: commit + push safe progress, leave the PR open, document the blocker — never force-merge or bypass protection.

**Valid stopping points (exhaustive — do NOT end your session anywhere else):** (1) terminal state — PR open + this slice's local gate green; (2) a stop-flag park (checkpoint committed + pushed, handoff updated); (3) an explicit PARK/blocker after the bounded fix-cycles (progress committed + PUSHED, blocker documented in the handoff). **Ending with a mere progress summary mid-slice is a failure mode, not a completion.** If you notice you are about to summarize-and-stop and none of the three conditions holds, continue working instead. (A wrapper may auto-resume you with a reminder if you stop early anyway.)

End with a structured summary: slice · merged|parked|blocked|stopped · PR URL · CI · what changed · ADRs · risks.

**Foreground-only execution (IMPORTANT — added 2026-07-15 after m16.5e lost 3 attempts/$41 to this):** run every subagent and long build/test step in the FOREGROUND of your turn — never end your turn while subagents, builds, or test runs are still executing in the background. Ending the turn with work in flight kills the session at the runtime's turn ceiling and loses the in-flight work. If you are waiting on something long (CI, big build), poll it inside the same turn or checkpoint (`wip:` commit + push + handoff) and exit cleanly at a valid stopping point instead.

PR description MUST include a line `Items: <feedback-ledger ids, or none>` (mechanical ledger linkage; census + reconcile read it).

**BUDGET: $<CAP_USD> for this slice (advisory thresholds; only the 125% hard ceiling is mechanical).**
Check for these flags BEFORE every subagent spawn and every new top-level task:
`/tmp/mr_warn_<SLICE>` = LANDING PATTERN: converge on finishing, no new scope, no new subagent fan-outs.
`/tmp/mr_stop_<SLICE>` = STOP: current subagent finishes only its current task, spawn nothing new, commit+push WIP, write the handoff, exit cleanly. Blowing the cap parks the slice and counts against it (sizing failure).
