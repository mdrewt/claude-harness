# Native supervisor — Monster Realm v2 (cutover 2026-07-21, Drew-directed)

**PRIMARY:** WSL-native cron loop. **FALLBACK:** the Cowork/DC scheduled task (daily 09:00, stands down while the native heartbeat is <2h old).

## Components
- `mr-native-tick.sh` (this dir) — cron entry point. Mechanical gates in pure bash (kill-switch → flock → live-chain fast-path → rate-limit reset → human-activity probe → chain-mutex TTL); spawns `claude -p` ONLY when an action may be needed. Gate log: `mr-native-tick.log` (rotates at ~1MB).
- `mr-supervisor-prompt-native.md` (this dir) — the decision run's full supervisor prompt (native-adapted; incl. resource locations + gotchas). Decision model: **sonnet** (`SUP_MODEL` env to override); rooted runs stay **opus** via `mr-launch.sh` (unchanged).
- Cron: `0 * * * *` hourly WATCHDOG + `@reboot sleep 60` (user crontab, mdrewt). Since 2026-07-24 v3 the loop is **event-driven**: finished/crashed runs, CI conclusions, and rate-limit resets fire ticks directly via `$MEM/pending-events/` + `mr-native-tick.sh <event-file>` (sources: cron|done|ci|crash|reset|manual; events REQUEUE on contention, never drop). Cron is the safety net only.
- Offload tools (all in `$MEM`; keep judgment in the LLM, mechanics in scripts): `mr-situation` (HINTS-ONLY JSON state bundle + budget governor; 45s cache; window anchored to `.weekly-reset-anchor`) · `mr-record` (validated ledger/handoff writes; wrapper-reconcile rows carry finished-run cost) · `mr-audit` (mechanical pre-merge audits; FLAGGED/AUDIT-ERROR = mandatory LLM read; hard tier always FLAGGED) · `mr-spawn` (brief+lock+detached launch+asserts from vars.json) · `mr-ci-watch` / `mr-reset-watch` (zero-token watchers that wake ticks). Bridges disable: `MR_NO_EVENT_BRIDGE=1`; escalation disable: `MR_NO_ESCALATE=1`. · `mr-ollama` (local ornith:35b via ollama, $0): ADVISORY-ONLY event-bundle summaries (`summarize-run`, grounded on the brief scope, strict no-inference rule) + `triage` fallback when the claude CLI itself is down (auth outages). Never a gate; `MR_NO_OLLAMA=1` disables. Measured: summary ~14s, triage ~5s, ping 18s cold.
- Model/effort routing (2026-07-24): decision ticks sonnet@medium · routine slices opus@high · HARD slices fable@xhigh (guard: fable ≤45% of weekly window) · content opus@medium · wrapper escalates a failing opus run's final attempt to fable@xhigh (budget-guarded, identical-signature-guarded). Budget governor: SOFT-PAUSE >90% of $1,250/window (no new launches), HARD-STOP >97% (wrapper stands down pre-spawn). Governor knobs live in **`mr-budget-config.json`** (single source; bounds-validated; readers fall back to identical built-in defaults on a missing/invalid file) — recalibrate it + `.weekly-reset-anchor` at the next real park.
- WSL keepalive: `C:\Users\mdrewt\.wslconfig` (`instanceIdleTimeout=-1`, `vmIdleTimeout=-1`; effective on next WSL restart — do NOT `wsl --shutdown` while runs are live) + Startup-folder `wsl-ubuntu-boot.cmd` (boots the VM at Windows logon; schtasks needed admin, Startup folder does not).
- Health signals (epoch secs, this dir) — the fallback and `mr-supervisor-status` read these:
  - `.native-supervisor-tick-alive` — bumped at the top of EVERY tick incl. standdowns. Pure "cron is firing" liveness.
  - `.native-supervisor-heartbeat` — the fallback's stand-down gate (fresh <2h = healthy). Written at tick top ONLY while healthy; **suppressed while the most recent decision run failed and has not since succeeded**, so a logged-out / erroring loop goes stale and WAKES the fallback instead of masking the failure behind a fresh timestamp.
  - `.native-supervisor-last-success` — epoch of the last decision run that exited rc=0 (clears the failure latch, refreshes the heartbeat).
  - `.native-supervisor-last-failure` (+ `.native-supervisor-last-failure-epoch`) — set on rc!=0 with the reason (e.g. `Not logged in`); logged as `SPAWN-FAIL`. This latch is what suppresses the heartbeat until the next success.

## Controls
- **Helper commands** (`~/.local/bin`, on PATH — added 2026-07-24):
  - `mr-supervisor-status` — one-glance health: enabled/paused, schedule, **live runs (in-flight decision tick / building slices)**, heartbeat & tick-alive age, last success/failure, recent ticks, **budget/governor + fable share + pending events + watchers**. Add `-f` (raw) or `--pretty` (readable: assistant text + tool calls + brief results) to live-tail the most recent task log (the active slice build); `--log` prints its path.
  - `mr-supervisor-chat` — resume the Claude session behind any past run/tick (`list`, `<slice>`, `tick`, or `<slice> -p "question"`). Conversation memory persists; prompt cache expires after minutes-to-an-hour (a later resume re-pays ingestion but skips re-derivation). For live steering from your phone: `claude --resume <sid>` then `/remote-control` (QR → Claude app / claude.ai/code).
  - `mr-supervisor-disable` / `mr-supervisor-enable` — pause / resume the loop (toggles `.native-supervisor-disabled`; paused ticks log SKIP and exit).
  - `mr-supervisor-run` — run ONE tick now, detached; overrides the pause via `MR_FORCE=1` but still respects the concurrency / rate-limit / live-run gates.
- **Pause native loop (manual):** `touch $MEM/.native-supervisor-disabled` (ticks log SKIP and exit). Resume: remove the flag. `MR_FORCE=1 bash mr-native-tick.sh` bypasses ONLY this gate for an on-demand run.
- **Full rollback to DC:** remove the two crontab lines (`crontab -e`), delete the heartbeat file, re-raise the Cowork task cadence via Cowork.
- **Test a tick without spending:** `MR_TICK_DRYRUN=1 bash mr-native-tick.sh` (prints DECISION-NEEDED instead of spawning).
- Decision-run transcripts: `/tmp/mr_native_tick_native-<ts>-<pid>.log`.

## Test evidence (2026-07-21)
bash -n PASS · kill-switch PASS · flock-overlap PASS · live-chain fast-path PASS (fixture faketest lock) · rate-limit/human-probe exercised live (stood down on ptc5b's dying writes — correct conservative behavior) · cron-env headless claude PASS (`NATIVE_OK`, RC=0) · crontab + cron daemon PASS · .wslconfig + startup cmd PASS · state directive + heartbeat PASS. End-to-end: first real spawn expected at the 16:30Z tick (merge of ptc5b PR #236).

## End-to-end PROVEN (2026-07-21T16:30–17:00Z, first production cycle)
16:30 cron tick: gates cleared -> SPAWNED sonnet decision run. Decision run: verified PR #236 from live state -> both audits CLEAN (orchestration: 6 Agent calls incl. tester/reviewer/red-team/verifier, opus confirmed; gating-test: 4 proof-of-teeth added, none weakened) -> squash-MERGED ptc5b (a80912c, master CI GREEN) -> composite-LAUNCHED ptc5c (leader 902320 detached, opus asserted, ADR-0139) -> ledger/handoff/state recorded -> mutex released -> rc=0. 17:00 tick: fast-path STANDDOWN live-chain ptc5c — zero-cost, no claude spawn. Full lifecycle verified in production.

## Maintenance log
- 2026-07-21T19:23Z: gate-2 hardened to accept rate_limit_resets_at as epoch OR ISO-8601 (decision runs write ISO; the original float-only parse would have leaked one sonnet spawn per tick during parked windows). Live-verified against the active seven_day park (resets 2026-07-24T00:00Z).
- 2026-07-21 corpus tidy: handoff archived (48.8KB -> newest entries only); mr-state queue consolidated (stale c.5 progress refreshed, DC notes collapsed to one FALLBACK-ONLY entry); fallback task prompt rewritten to a 3.5KB delegating stub — supervisor logic SSOT is mr-supervisor-prompt-native.md (header marks it).
- 2026-07-24T~12:15Z (Drew-directed): **silent-standdown bug fixed + cadence/controls update.** WSL `claude` CLI had been logged out since ~Jul 22 -> every tick spawned `claude -p` -> instant `Not logged in` rc=1 (no work, no tokens), but the heartbeat was written unconditionally at tick top so the fallback always saw it fresh and stood down (~2 days undetected). Fix in `mr-native-tick.sh`: heartbeat is now an HONEST health signal (suppressed while the last decision run failed and has not since succeeded) + new markers `.native-supervisor-tick-alive` / `-last-success` / `-last-failure(-epoch)`, with `SPAWN-FAIL rc=.. reason=..` logged. Verified all four marker paths deterministically + live dry-run. Cadence `*/30`->hourly (`0 * * * *`). Added `~/.local/bin/mr-supervisor-{status,disable,enable,run}` + `MR_FORCE=1` override on gate -1 (bypasses the pause ONLY; other gates still apply). Backups: `mr-native-tick.sh.bak.*`, `crontab.bak.*`, this README `.bak.*`. Optional follow-up: have the fallback Step 1 also read `.native-supervisor-last-failure` for instant (vs <=2h) detection.
- 2026-07-24T~17:30Z (Drew-directed, plan+impl dual-reviewed by subagent lenses): **v3 — event-driven + offload + routing/governor.** Tick v3 (EVENT payloads, pending-events requeue, per-source logging, debounce, reconcile-before-gates incl. dead-pid locks w/ ledger-dup+age guards, sonnet@medium, stream-json cost telemetry, SUPERVISOR ledger rows wrapper-owned, auth toast). Launch v2 (effort arg, ATTEMPT markers, auth fast-fail, identical-signature dead-stop, haiku triage, escalate-final-attempt opus→fable@xhigh budget-guarded, done/crash event bridges). New: mr-situation/mr-record/mr-audit/mr-spawn/mr-ci-watch/mr-reset-watch + mr-supervisor-chat; status v5 budget view. SSOT gained Model&effort routing / Budget governor / Offload tools; briefs gained <TIER_BLOCK>. Governor window anchored to plan reset (`.weekly-reset-anchor`, seeded 2026-07-24T00:00Z) after live test caught rolling-7d overcounting post-reset. Ledger: 3 duplicate reconcile rows removed (ptc5c/e/g), 2 genuine backfills kept (ptc5b $161.24, ptc5f $41.10). Review fixes: flock-fd9 inheritance wedge closed in all children; empty-epoch arithmetic guard; mr-record strict flag values; mr-spawn env-based lock write + kill-before-retry; ci-watch pid-identity check. Rollback: *.bak.v2.20260724T153207Z. Fallback Cowork task rewritten (failure-latch escalation + digest + native-tick-first).
- 2026-07-24T~19:30Z: **mr-ollama local-model offload added** (Drew-directed). ornith:35b (21GB, 33/67 CPU/GPU split on the 16GB RTX 5080, think:false mode). Wired: launch v2 fire_event appends an advisory summary to done/crash bundles; haiku-triage falls back to local triage when the claude CLI is unavailable (the Jul-22-24 auth-outage class). Guardrails: always-rc0 + OLLAMA-UNAVAILABLE degrade, output filtered/capped (2400ch), advisory-only doctrine in SSOT, brief-scope grounding + strict no-inference rule (ungrounded v1 confabulated a slice description in test — kept as a cautionary example of why local output never gates). UTF-8 truncation decode fix (errors=replace).
- 2026-07-24T~21:00Z final pre-run tidy (Drew Q&A, plan+impl reviewed): budget knobs consolidated to `mr-budget-config.json` (drift-proofing; malformed-config fallback tested). Tick debounce KEPT after review overturned a dead-code claim (real case: fast event-tick decision at :5x + hourly cron at :00). Impl-spec marked historical. Portability documented below.

## Portability (other projects under `projects/`)

The MECHANISM is generic (event ticks, pending-events, locks, health markers, mr-audit --repo, mr-record, mr-ollama, watchers). The BINDING is monster-realm. To port, parameterize exactly these points: (1) `PROJ` path + `mdrewt/monster-realm` gh slugs in mr-native-tick.sh / mr-situation / mr-ci-watch / mr-launch.sh / mr-spawn; (2) ledger/handoff filenames still hardcoded in mr-situation (`mr-record` is already parameterized via `MR_RECORD_LEDGER`/`MR_RECORD_HANDOFF`); (3) the SSOT prompt + brief template, which are ~80% project process-knowledge and need per-project versions; (4) helper commands' hardcoded `$MEM` references. Recommended shape when a second project actually exists (YAGNI until then): a per-project config dir (`$MEM/<project>/`) + `MR_PROJECT` env selecting it, with the scripts reading paths/slugs/prompts from it.
