# rb-109 plan — native-host range-scan + index-point-delete model, and the deferred X9 execution proof

REV 1 (planner, opus, 2026-09-21). Worktree `.claude/worktrees/rb-109`, branch `rb-109` from origin/master
@786c222. Baseline `cargo nextest run -p monster-realm-module`: 1011/1011 (1012/1012 with `--features
dev_reducers`). Ledger: `memory/projects/gates/rb-109.gates.md` (one seeded gate, E1). No ADR number
allocated → dated body-only amendments on ADR-0238 (closes R-rb-85-X9) and ADR-0222 (the native host), a
one-word retruth in ADR-0265:33.

Orchestrator amendments to the planner's text are marked **[ORCH]**.

## 0. Verified facts

- V1 bindings lowering: `filter(..=cutoff)` → raw `datastore_index_scan_range_bsatn(index_id: u32,
  prefix_ptr, prefix_len, prefix_elems: u16 /*ColId repr(transparent)*/, rstart_ptr, rstart_len, rend_ptr,
  rend_len, out: *mut u32) -> u16`; single-column ⇒ prefix empty, prefix_elems 0; bounds are BSATN
  `Bound<T>`: tag 0 Included(x)+BSATN(x), 1 Excluded, 2 Unbounded (no payload) — sats `ser/impls.rs:124-128`.
  `.delete(stamp)` with a bare i64 → `datastore_delete_by_index_scan_point_bsatn(index_id, point_ptr,
  point_len, out: *mut u32 /*rows deleted*/) -> u16`.
- V2 **inbound line citations INTO native_host_tests.rs from files OUTSIDE touches** (comments, not gates):
  evolution_tests.rs:5471,:6339 → :293 (`unmodelled`); rb73_session_tests.rs:387 → :351-354 (table scan);
  battle_tests.rs:7025,:7405 + economy_tests.rs:2797 → :311-319 (`index_id_from_name`); ADR-0265:33 → :14-18.
- V3 privacy.rs comment edits must be LINE-COUNT-NEUTRAL: ADR-0265:219 cites privacy.rs:1832-1834 (below
  the ≈1728 comment); `scripts/okf-export.mjs` stamps `resource: privacy.rs#L<line>` for the reaper table
  (:1740) and reducer (:1826) into docs/knowledge/**.
- V4 `K: Serialize + DeserializeOwned + Ord` on `table_keyed` is caller-compatible (every caller keys on
  `Identity` or `u64`; `spacetimedb::DeserializeOwned` re-exported at spacetimedb-2.8.1/src/lib.rs:43).
- V5 `[rb85/accessor-ratchet]`/`[rb85/handle-ratchet]` skip privacy_tests.rs BY NAME (privacy_tests.rs
  :14308-14310) but NOT native_host_tests.rs → it keeps naming no accessor / row type / `__TableHandle`.
- V6 T7's needle is `reap_expired_export_bundles(` (with paren) over stripped code → revising to `== 1`
  forces EXACTLY ONE call site in privacy_tests.rs.
- V7 **[ORCH]** monster-privacy / account-privacy evals exclude `*_tests.rs` files under their `[SCOPE]`
  rules (cfg(test)-declared) — checked before test authoring; T5's direct accessor call in privacy_tests.rs
  is therefore expected clean, verified by the full `just ci`.

## 1. Decisions

- **D1 (HARD): native_host_tests.rs lines 1–450 stay line-count-identical; every net new line lands at
  451+.** Module-doc retruth (:15-23) is an in-place reflow within 9 lines; `unmodelled()` message
  (:295-297) within 3 lines; `use` line :56 gains `DeserializeOwned` in place; `table_keyed` generics :207
  tighten in place; the registration line :217 stays ONE line; `Handle::rows()` in a NEW impl block at EOF;
  design prose in a `// ---` banner at EOF. Verification: `sed -n '293p;295p;297p;311p;319p;351p;354p;450p'`
  byte-identical before/after except the retruthed message lines. Fallback = hidden-dependency STOP naming
  evolution_tests.rs, rb73_session_tests.rs, battle_tests.rs, economy_tests.rs.
- **[ORCH] D1a — comparator storage:** prefer changing `index_table: HashMap<u32, u32>` (:75) to
  `HashMap<u32, (u32, KeyCmp)>` on the SAME line (doc comment reflowed in place), with
  `type KeyCmp = fn(&[u8], &[u8]) -> std::cmp::Ordering;` declared at EOF, over the planner's second
  `static INDEX_CMP` + lock-order rule: one lock, one map, the point scan's `Some(&table_id)` becomes
  `Some(&(table_id, _))` on its line. Either satisfies D1; the implementer picks the one that stays
  line-neutral and simplest.
- **D2** range model correct for all three bound kinds on each side; `prefix_elems != 0` → `unmodelled`;
  unregistered index → empty iterator (point-scan precedent).
- **D3** compare DECODED values (i64 BSATN is LE two's complement; byte order ≠ value order).
- **D4** ascending order is a MODEL of the btree contract, not a live-host observation — say so in the ADR
  amendment; register residual R-rb-109-ORDERMODEL after the lenses.

## 2. Host model (implementer) — native_host_tests.rs

2.1 module doc :15-23 → eleven symbols defined once; seven implemented (two name lookups, index point AND
range scans, iterator advance/close, index-point delete); four unmodelled (table scan, insert, update,
delete_all_by_eq). Keep: never spells the cfg(test) literal; never names accessor/row type/table attr.
2.2 `unmodelled()` :295-297 → "single-column index point and range reads and index-point deletes only".
2.3 :56 `use spacetimedb::{DeserializeOwned, Identity, ReducerContext, Serialize};` · :207 `K: Serialize +
DeserializeOwned + Ord` · :217 one-line registration binding table id + comparator.
2.4 EOF: `KeyCmp` alias, `cmp_keys::<K>` (bsatn::from_slice both, `.cmp`), `Handle::rows() -> Vec<R> where
R: DeserializeOwned` (store order), the new range-scan extern, the real point-delete body.
2.5 `datastore_index_scan_range_bsatn`: parse bounds (tag byte; `[]`/2 ⇒ Unbounded); resolve table (absent
⇒ empty iterator, return 0); filter rows by cmp per side; STABLE `sort_by` ascending; clone; `open_iter`;
write id; return 0.
2.6 `datastore_delete_by_index_scan_point_bsatn`: resolve table (absent ⇒ write 0); `retain` out rows with
key bytes == point; write removed count as u32; return 0.
2.7 EOF banner: Bound tag encoding + citation, decoded-value rationale, stable-sort rationale, D4.

## 3. Tests (tester) — privacy_tests.rs, `rb109_` prefix, one `// ====` section at EOF (rb-107 conventions)

Hygiene (rb22p_scan_hygiene :965): line comments only; no `/* */`, no `r"`, no print/debug macro tokens, no
`\"`, no `"` inside any comment in the section. ONE helper call site: `fn rb109_tick(ctx, now) ->
(usize, usize, usize)` is the only place `reap_expired_export_bundles(` appears. Fixture:
`fx.table_keyed::<crate::schema::ExportBundle, i64>("export_bundle", "created_at_ms", |r| r.created_at_ms)`.
Constants read live by real name. Never spell `accessor = export_bundle`.

| # | name | clause | assertions |
|---|---|---|---|
| T1 | rb109_oversized_tick_is_bounded_and_reaps_the_oldest_bundles_whole | no abort; ≤ cap; fairness | 26 bundles × 17 = 442 rows, 26 owners, payload 8 KiB (drives BUFFER_TOO_SMALL), 20 expired stamps `cutoff - 1000*i` (i=0 is AT cutoff), 6 live `cutoff+1+1000*j`, seeded DESCENDING; one tick → read==256, planned==16, reaped==272 (>read); survivors == exact (owner, stamp, chunk_id) triples: 102 live + the 68 rows of the FOUR NEWEST expired stamps (an unsorted host leaves the four OLDEST — counts identical, sets disjoint) |
| T2 | rb109_tick_deletes_exactly_the_expired_set_including_the_cutoff_stamp | delete exactly the expired | 5 expired (85 rows, one AT cutoff), 3 live (51, one at cutoff+1) → (85,5,85); survivors == 51 live by chunk_id set |
| T3 | rb109_repeated_ticks_drain_to_the_live_set | bounded progress | T1 population; ticks until reaped==0 (cap 10, loud panic): (256,16,272),(68,4,68),(0,0,0); each read≤256 ∧ planned≤16; final == 6 live bundles |
| T4 | rb109_zero_expired_and_empty_tables_report_a_zero_tick | no-op | (0,0,0) on empty and on all-live; row set unchanged |
| T5 | rb109_host_range_model_honours_all_three_bound_kinds_and_negative_keys | model control | stamps [-5000,-1,0,1,5000,9000] seeded descending; direct `ctx.db.export_bundle().created_at_ms().filter(..)`: `..=0`→3, `..0`→2, `0..`→4, `(-1)..=1`→3; `(-5000)..=9000` yields ASCENDING |
| T6 | rb109_test_roster_is_closed | ledger anchor | mirror rb107_test_roster_is_closed (per-name, both adjacency forms, attr walker, declaration total over closed test+helper rosters) |

Pin revision (pre-authorised by its own doc :14404-14410): `rb85_helper_is_never_named_outside_privacy_rs`
second clause `tests, 0` → `tests, 1`; retruth its doc + message naming `rb109_tick`. First clause unchanged.
No new helper may start with rb48_/rb85_/rb86_/rb87_/rb107_. Totals 1011 → 1017 (1012 → 1018).

## 4. Prose retruth (comment/doc only)

privacy.rs (line-neutral): :1728 "models ten syscalls" → eleven (row-count clause stays true); :1902-1908
drop "the execution proof is deferred, ledger X9" → "modelled and executed against the native host in rb-109
(privacy_tests.rs T1) — a model of the btree contract, not a live-host observation".
privacy_tests.rs: :7497, :10181, :11573-11578, :12762, :12934-12935, :13377, :13527, :14394-14399,
:14432-14438, :15288, :15660, :15964, :17926, :18826-18830, :20249, :20321 (check each).
native_host_tests.rs :15-23, :295-297. ARCHITECTURE.md: append one **rb-109** paragraph in the M22-residual
run (rb-107/rb-108 entries); do not rewrite :161-163. ADR-0222: append `## Amendment (2026-09-21, rb-109)`
one paragraph. ADR-0265:33 "exactly ten" → eleven. ADR-0238: §5 below; also retruth :319, :510, :626.
Sweep: `rg -n 'exactly ten|models ten|deferred.*X9|X9 .*deferred|defines exactly ten' server-module/src/
privacy.rs server-module/src/privacy_tests.rs server-module/src/native_host_tests.rs docs/adr/0265-*.md
docs/adr/0238-*.md ARCHITECTURE.md` → zero stale hits.

## 5. ADR-0238 amendment (append, body-only)

`## Amendment (2026-09-21, rb-109 — residual R-rb-85-X9 closed)`: discharges the X9 → backlog paragraph
(:372-378); corrects :319/:510/:626 (the HELPER is executable now; the REDUCER still is not —
`database_identity()` unstubbed). Decision: host gains the range scan (three bound kinds, decoded-value
comparator, stable ascending sort) and a real point delete; `table_keyed` K gains `DeserializeOwned + Ord`;
`Handle::rows()` read-back; 11/7/4. MEASURED: 20 expired bundles seeded descending → one tick reads 256,
plans the 16 OLDEST, reaps 272 whole, leaves the four newest expired + every live row — honestly a model of
the btree contract. Consequences: T7 0→1 as pre-authorised; suite 1011→1017; the `.iter()` regression is now
LOUD (process abort) not merely text-pinned. Rejected: modelling the table scan (it is the ban); a Host
field/second static that shifts out-of-touch citations (D1); calling the reducer (LINK failure).

## 6. RED/GREEN staging → `memory/projects/gates/rb-109.red-before.md`

S1 pin-only RED: only the 0→1 clause on the pre-slice tree → `[rb85/helper-name-tests]` fails (found 0,
want 1), 1011 run / 1010 passed / 1 failed. S2 LINK RED: full test block + read-back API + comparator but
WITHOUT the two syscall bodies → whole lib-test binary fails to link (`undefined symbol:
datastore_index_scan_range_bsatn`), zero tests run — the measured mechanism behind T7's message. S3 GREEN:
1017/1017 (1018 dev_reducers).

## 7. Mutant register (`memory/projects/gates/rb-109.mutants.py`, orchestrator-run, each RED)

M1 helper body → rb-48 `.iter()` sweep (THE spec's RED): T1/T2/T3 abort via unmodelled table scan
(collateral: rb85_helper_body_exact, rb22p_owner_scoped_filter_never_iter). M2 drop `.take` → T1 read 340.
M3 `..=cutoff`→`..cutoff` → T2 (85→68). M4 host sort removed → T1 survivor set + T5 order. M5 point delete
writes 0 → T2 reaped 0, T3 overrun. M6 point delete ignores key → T2 survivors. M7 range ignores end bound →
T2 read 136, T5. M8 stamp-cap truncate dropped → T1 planned 20. M9 comparator on raw bytes → T5 negatives.
C1–C3 controls (unchanged tree; comment-only edit; decoy `fn rb109_probe()` → roster RED).

## 8. Ledger CHECKs (cwd = worktree; PATH prefix as rb-108)

E1: nextest `-E` over the six rb109_ tests, `2>&1 | tail -3`, EXPECT `/Summary \[[^\]]*\] 6 tests run: 6
passed, [0-9]+ skipped/`. **[ORCH]** X2 SUITE-GREEN = the rb-108 X3 `node -e` full-nextest form pinning
`1017 tests run: 1017 passed, 0 skipped` and `1018 …` (NOT a `just ci` CHECK — 120 s cap, and the supervisor
re-executes every CHECK). X3 CLIPPY-GREEN: `cargo clippy -p monster-realm-module --all-targets -- -D
warnings 2>&1 | tail -1` EXPECT `/Finished/`. X4 T7-REVISION: `-E 'test(=privacy::privacy_tests::
rb85_helper_is_never_named_outside_privacy_rs)'` EXPECT `/1 test run: 1 passed/`. X5 M1-ABORT: `MANUAL:`
citing rb-109.red-before.md §M1. X6 DOCS-FRESH: `node -e` checking privacy.rs numstat added==deleted, the D1
sed lines, and `just knowledge-check` → prints `rb109-X6:DOCS-FRESH`.

## 9. Docs / residual

`just knowledge-check` (regen + commit if drift). ARCHITECTURE.md paragraph. ADR amendments (0238, 0222,
0265). Residual AFTER lenses: R-rb-109-ORDERMODEL — the btree ascending yield is an expectation about the
real datastore never observed on a live instance; closing it needs an account-e2e-tier probe.

## 10. Touches

native_host_tests.rs, privacy.rs (comments, line-neutral), privacy_tests.rs — declared. docs/adr/0238,
0222, 0265, ARCHITECTURE.md, docs/knowledge/** (if drift) — always-in-scope. Harness-side:
memory/projects/gates/rb-109.*, rb-109.plan.md, handoff. No hidden-dependency STOP under D1.

## 11. Anti-patterns

Vacuous host model (count-only assertions); two helper call sites; assert-no-panic as oracle; growing the
module doc; naming an accessor in native_host_tests.rs; `"` in a comment / block comment / raw string /
print macro in the new section; rbNN_-prefixed helpers; calling the reducer; rewriting rb-41 history;
`git checkout`/`stash` in the loop.

## 12. Risks

R1 wrong bound-tag parse (T5 + cited encoding). R2 D1 slip (sed check, X6). R3 bound tightening breaks a
caller (`cargo check --tests` first). R4 an eval objects to `export_bundle()` in privacy_tests.rs (V7 says
no; fallback: drop T5's direct calls, keep T1's survivor pin). R5 8 KiB × 442 slow (measure; floor 2 KiB).
R6 stranded quoter (sweep). R7 sibling `just ci` (never `pkill -x just`).

## 13. Workflow

tester (opus) → tests reviewer + tests red-team (artifact lens, hunting model vacuity) → implementer (opus,
separate agent) → reviewer + /simplify + red-team + reducer-security-auditor + verifier. desync-guard: no
game-core/wasm/client surface — not run, stated.

---

# REV 2 (2026-09-21) — after plan reviewer (opus) + plan red-team (opus) + /simplify. DELTAS to REV 1 above; REV 2 wins on conflict.

## Decisions changed / added

- **D1a is THE decision** (reviewer M3): comparator stored in `index_table`'s value — `index_table: HashMap<u32, (u32, KeyCmp)>` at :81 (the field is :81, not :75), `type KeyCmp = fn(&[u8], &[u8]) -> std::cmp::Ordering;` declared at EOF, registration :217 `h.index_table.insert(index_id, (table_id, cmp_keys::<K>));` (66 cols), point-scan arm :337 `Some(&(table_id, _)) => h`. The planner's second static + lock order is STRUCK.
- **D5 (reviewer B1 / red-team F3a) — THE WRITE WALL STAYS FOR UNREGISTERED TABLES.** `datastore_delete_by_index_scan_point_bsatn` is real ONLY when `index_table` has the index id; an unregistered index → `unmodelled(..)` (abort), asymmetric with the read path on purpose: a read of an unregistered table must yield nothing so multi-table predicates run, but no test in the crate needs a WRITE to a table it did not register to succeed, and the abort is the kill mechanism that rb73_session_tests.rs:493-501 and accounts_tests.rs:20449-20454 rest on. New wall, stated everywhere: "three writes abort unconditionally (insert, update, delete_all_by_eq); the index-point delete aborts unless the fixture registered the index; the table scan aborts."
- **D6 (reviewer M2 + red-team F9) — a both-`Unbounded` range aborts.** `(Unbounded, Unbounded)` is a sorted full scan — the `.iter()` shape wearing a range — so the model `unmodelled()`s it, keeping the wall spelling-independent. Host-side oracles in tests use an explicit bounded range covering the fixture's known stamps (`lo..=hi`), never `..`.
- **D7 (reviewer M4, red-team F16/F17) — strict bound parse + strict out-param.** Bound parse is an explicit tag match: `0` Included(rest), `1` Excluded(rest), `2` Unbounded (rest must be empty), `_ => unmodelled`; a payload that fails `bsatn::from_slice::<K>` → `unmodelled`; the tag byte is stripped BEFORE the comparator sees the payload; an empty bound slice is an error, not Unbounded. The point delete writes EXACTLY a `u32` on EVERY `0`-returning path.
- **D8 (red-team F8, reviewer m3) — exactly ONE new `#[no_mangle]`.** `nm` on the built lib-test binary shows no reference to `datastore_delete_by_index_scan_range_bsatn` (the `if const { is_point_scan }` branch is eliminated), so it is NOT defined; neither is `datastore_table_row_count`. Both stay link-fatal if reached; say so in the ADR-0222 amendment. Count: ELEVEN symbols, SEVEN implemented (two name lookups, index point scan, index range scan, iterator advance/close, index-point delete on a registered index), FOUR unmodelled (table scan, insert, update, delete_all_by_eq).
- **D9 (red-team F11/F12) — iterator lifecycle is observable, and handle methods never run inside a live iteration.** New `Fixture::open_iters() -> usize` at EOF; tests assert 0 after every tick. `Handle::rows()` doc says: `host()` is a non-reentrant Mutex — calling any handle method while a `ctx.db…filter(..)` iterator is live deadlocks the test (collect first, assert after).
- **D2 stays as a NAMED YAGNI exception** (/simplify): three bound kinds on EACH side are modelled because T6 exercises every one of them (incl. Excluded-START via the `(Bound, Bound)` terminator); if T6 is ever dropped, D2 shrinks to loud-abort-on-unsupported.
- **Stable sort is TESTED or DROPPED** (red-team F14): T6 seeds a same-stamp pair and asserts seed order preserved; `sort_by` (stable) it is.
- **Payload sizing retruthed** (red-team F5): `IterBuf` starts at 65 536 B (ROW_ITER_CHUNK_SIZE*2) and `clear()` keeps capacity, so an 8 KiB row never reaches `BUFFER_TOO_SMALL`; T1 keeps 8 KiB as "large payload_json" (spec wording, ~7 rows per advance), and T7 seeds one row with `payload_json` ≥ 65 461 B to drive the reserve-and-retry loop. Note for the mutant runner: a host writing `0` instead of `next.len()` on TOO_SMALL makes `RowIter::read` spin forever — a HANG, not a red.

## Tests — REV 2 roster (EIGHT tests; totals 1011 → 1019, 1012 → 1020)

1. `rb109_oversized_tick_is_bounded_and_reaps_the_oldest_bundles_whole` — REV 1 T1 plus: **[rb109/seed-order]** seed in a deterministic INTERLEAVED order (neither ascending nor descending store order may reproduce the sorted window) and assert from the LIVE store (`t.rows()`) that the first 256 stamps in store order ≠ the 256 smallest stamps (red-team F1); **[rb109/host-unbounded]** `ctx.db.export_bundle().created_at_ms().filter(lo..=cutoff).count() == 340` with `lo` = the oldest seeded stamp — the host must not cap its own scan (red-team F2, mutant M10); **[rb109/iters-closed]** `fx.open_iters() == 0` after the tick; survivor comparison as a SORTED Vec of `(owner, stamp, chunk_id)` (F13).
2. `rb109_tick_deletes_exactly_the_expired_set_including_the_cutoff_stamp` — REV 1 T2 (85/5/85; one stamp AT cutoff; sorted survivor set == the 51 live).
3. `rb109_repeated_ticks_drain_to_the_live_set` — REV 1 T3 + `open_iters() == 0` after every tick.
4. `rb109_zero_expired_and_empty_tables_report_a_zero_tick` — REV 1 T4 + POSITIVE CONTROL (F9): after the all-live tick, `filter(lo..=hi).count() == N` through the host, so `0 reaped` is distinguishable from `0 readable`.
5. `rb109_stamp_cap_binds_when_the_window_holds_more_than_sixteen_stamps` (NEW; reviewer M1 / red-team F6) — 20 expired bundles × 13 chunks = 260 rows (+ a few live): window 256 = 19 whole stamps + a 9-row straddler → 20 distinct stamps → `planned == 16`, `reaped == 208`, survivors = the 4 newest expired stamps (52 rows) + live. Kills M8 (truncate dropped → planned 20, reaped 260).
6. `rb109_host_range_model_honours_every_bound_kind_negative_keys_and_ties` — REV 1 T5 with `i64` SUFFIXES on every literal (reviewer M5), plus `(std::ops::Bound::Excluded(-1i64), std::ops::Bound::Included(1i64))` → [0, 1] and `(Bound::Excluded(-5000i64), Bound::Unbounded)` → 5 rows (F4, mutant M11), plus a same-stamp pair asserting seed order (F14). Stamps `[-5000, -1, 0, 1, 5000, 9000]` seeded descending.
7. `rb109_host_iterators_close_alias_free_and_refill_on_oversized_rows` (NEW; F5/F11) — two iterators held live and interleaved (`a.next(); b.next(); a.next(); …`) with both sequences asserted (catches `open_iter` id aliasing); after both are dropped `open_iters() == 0`; one row with `payload_json` ≥ 65 461 B read back through `filter(lo..=hi)` (the reserve-and-retry leg).
8. `rb109_test_roster_is_closed` — ALL EIGHT rb107 clauses (reviewer B2): per-name declared once, roster-dup pairwise, both adjacency forms, the attribute walker + its control fixtures, `[rb109/label-census]` (every `[rb109/…]` label appears in exactly one test's panic block), the declaration total over a closed test roster + a closed helper roster, and a DEPENDENCY roster naming `rb85_helper_is_never_named_outside_privacy_rs`, `rb85_helper_body_exact`, `rb22p_scan_hygiene`.

T7-revision (`rb85_helper_is_never_named_outside_privacy_rs`, red-team F10): second clause `tests, 0 → 1` AND a new clause counting the PAREN-LESS identifier `reap_expired_export_bundles` in `stripped_for_scan(PRIVACY_TESTS_RS)` == 1 (closes the fn-item-binding second call); message states honestly that a text count proves no execution — the eight rb109_ value oracles do. The single sanctioned spelling is `crate::privacy::reap_expired_export_bundles(ctx, now)` inside `fn rb109_tick`. Hygiene per the ACTUAL gate (`privacy_tests.rs:972-1006`): no `/*`, `*/`, `r#`, `log::`, `println!`, `print!`, `dbg!`, `\"`; keep `"` out of comments anyway.

## Mutant register REV 2 (all orchestrator-run; each RED unless noted)

M1 `.iter()` sweep → abort (THE spec RED) · M2 drop `.take` → T1 read 340 · M3 `..cutoff` → T2 · M4 host sort removed → T1 survivors/seed-order + T6 order · M5 point delete writes 0 → T2/T3 · M6 point delete ignores key → T2 survivors · M7 range ignores end bound → T2 read, T6 · M8 stamp truncate dropped → **T5** (planned 20) · M9 byte comparator → T6 negatives · **M10** host truncates its scan at 256 → T1 `[rb109/host-unbounded]` · **M11** start-side Excluded parsed as Included → T6 · **M12** `out` written as `usize` → UB, may stay GREEN (named, not relied on) · **M13** host writes 0 on TOO_SMALL → HANG (documented, run with a timeout) · **M14** unregistered-index delete returns 0 instead of aborting → (no rb109_ red; the wall is prose-pinned + the rb-72/rb-73 mechanism narratives; disclosed) · C1–C3 controls.

## Prose retruth REV 2 (adds to §4)

IN TOUCHES: native_host_tests.rs :14-18 must carry the eleven/seven sentence (ADR-0265:33 cites :14-18), :205-206 ("The write wall is unchanged: … still aborts") and :235 ("(unmodelled) write syscalls") retruthed IN PLACE (D1), `unmodelled()` message :295-297 must LOSE ≥ 14 chars elsewhere to fit the new wording in 3 lines at ≤100 cols (red-team F7). ARCHITECTURE.md:161-162 retruthed IN PLACE ("eleven … seven implemented, the table scan + three writes panicking, the point delete real on a registered index") + the appended rb-109 paragraph (reviewer M6). Sweep needles widened: `rg -n 'exactly ten|models ten|ten symbols|ten syscalls|four write|four WRITE|all four writes|every write syscall|write syscalls are|deferred.*X9|X9 .*deferred|defines exactly ten' server-module/src docs/adr ARCHITECTURE.md`.
OUT OF TOUCHES — **NOT edited; recorded as stale + residual + PR follow-up (the rb-41 / ADR-0222-amendment precedent)**: rb73_session_tests.rs:33, :493-501, :638-641; accounts_tests.rs:20318, :20329-20337, :20349-20359, :20449-20454, :20966; trading_tests.rs:4902, :4995, :5009; npc_tests.rs:2152, :2629, :2998; taming_tests.rs:581, :997; raising_tests.rs:3341, :3733; evolution_tests.rs:67, :5470-5473, :6337-6340, :6541, :6556, :7168; battle_tests.rs:7702, :7971; guards_tests.rs:4120. Under D5 every "unregistered table write aborts" mechanism claim stays TRUE; the imprecise "all four write syscalls are unmodelled" wording and the rb-72 Leg-A M5 narrative (accounts_tests.rs:20329-20337 — that mutant's kill now lands as the `[rb72/post-player]` assertion rather than an abort, because `player` IS registered there) are the stale carriers. Residual: **R-rb-109-WRITEWALLPROSE** (LOW). This is a comment-truth follow-up, not a file the criterion requires — the intent-boundary rule says flag, do not touch.

## Ledger REV 2 (/simplify: four gates + the seeded one; Touches line written)

Touches: server-module/src/native_host_tests.rs, server-module/src/privacy.rs, server-module/src/privacy_tests.rs (+ docs/adr/0238, 0222, 0265 amendments; ARCHITECTURE.md; docs/knowledge/** if drift).
E1 nextest `-E` over the EIGHT rb109_ names, EXPECT `8 tests run: 8 passed`; EVIDENCE must say the "pre-slice `.iter()` body" is mutant M1 (the tree at 786c222 already ships the bounded read — reviewer n3). X2 SUITE totals `1019`/`1020` via the rb-108 `node -e` form. X4 T7 revision `1 test run: 1 passed`. X5 `MANUAL:` M1 abort → `memory/projects/gates/rb-109.red-before.md` §M1. X3 (clippy) and X6 (docs-fresh) DROPPED — `just ci` covers both, and every CHECK costs the supervisor a re-run.

## ADR SSOT split (/simplify)

ADR-0222 amendment = the HOST record (design, D5 wall, D8 symbol count, D9 lifecycle, the comparator is fixture-typed not schema-typed and the candidate list is eager while the bound proven is the module's decode count — red-team F15 / reviewer m5 — and the out-of-touches stale-site list). ADR-0238 amendment = the X9 CLOSURE record (what is discharged, the fairness MEASUREMENT stated as a model of the btree contract, T7 revision, totals, residuals R-rb-109-ORDERMODEL + pointer to WRITEWALLPROSE). Each cross-references the other in one line; nothing is stated twice. ADR-0265:33 one-word retruth. Also mark ADR-0238 :304, :319, :510, :543, :625 sentences as superseded (its own convention).

## Risks added

R8 `unmodelled()` message arithmetic (F7) — do it before any code. R9 clippy `too_many_arguments` on a 9-param `extern "C"` (exempt for non-Rust ABI — confirm at the first clippy run). R10 `Handle` STRUCT bounds must NOT tighten — only `Fixture::table_keyed`'s (12 `Handle<'_, T[, u64]>` annotations across the crate; reviewer m11). R11 an oracle spelled `filter(..)` deadlocks nothing but ABORTS under D6 — use `lo..=hi`.
