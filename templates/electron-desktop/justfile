set windows-shell := ["cmd.exe", "/c"]
setup:
    npm run setup

lint:
    npm run lint

typecheck:
    npm run typecheck

test:
    npm run test

eval:
    npm run eval

security:
    npm run security

build:
    npm run build

start:
    npm run start

# No pure-logic modules to mutate yet (Electron runtime glue; security posture is
# gated by the eval + security.test). Add logic + tests, then wire StrykerJS here.
mutate:
    @echo "mutate: no pure-logic modules to mutate yet (see justfile comment)"

changelog:
    git cliff -o CHANGELOG.md

ci:
    npm run ci
