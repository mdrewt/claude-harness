{
  "name": "{{NAME}}",
  "version": "0.1.0",
  "type": "module",
  "description": "{{DESCRIPTION}}",
  "license": "MIT",
  "engines": { "node": ">=24.13.1 <25" },
  "scripts": {
    "setup": "npm install",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "build": "tsc -p tsconfig.build.json",
    "eval": "node evals/run.mjs",
    "security": "node scripts/check-secrets.mjs .",
    "mutate": "stryker run",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run eval && npm run security"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@stryker-mutator/core": "^8.2.0",
    "@stryker-mutator/vitest-runner": "^8.2.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
