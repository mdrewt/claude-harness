{
  "name": "{{NAME}}", "version": "0.1.0", "type": "module", "license": "MIT",
  "description": "{{DESCRIPTION}}",
  "scripts": {
    "setup": "npm install", "dev": "vite",
    "lint": "biome check .", "format": "biome format --write .", "typecheck": "tsc --noEmit",
    "test": "vitest run", "eval": "node evals/run.mjs",
    "security": "node scripts/check-secrets.mjs .", "build": "tsc --noEmit && vite build",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run eval && npm run security"
  },
  "dependencies": { "pixi.js": "^8.2.0" },
  "devDependencies": { "@biomejs/biome": "^2.0.0", "typescript": "^5.5.0", "vite": "^5.3.0", "vitest": "^2.0.0" }
}
