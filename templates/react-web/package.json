{
  "name": "{{NAME}}", "version": "0.1.0", "type": "module", "license": "MIT",
  "description": "{{DESCRIPTION}}",
  "engines": { "node": ">=24.13.1 <25" },
  "scripts": {
    "setup": "npm install", "dev": "vite",
    "lint": "biome check .", "format": "biome format --write .", "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage", "eval": "node evals/run.mjs",
    "security": "node scripts/check-secrets.mjs .",
    "mutate": "stryker run",
    "build": "tsc --noEmit && vite build",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run eval && npm run security"
  },
  "dependencies": { "react": "^18.3.0", "react-dom": "^18.3.0" },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@stryker-mutator/core": "^8.2.0", "@stryker-mutator/vitest-runner": "^8.2.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.5.0", "vite": "^5.3.0", "@vitejs/plugin-react": "^4.3.0",
    "vitest": "^2.0.0", "jsdom": "^24.0.0",
    "@types/react": "^18.3.0", "@types/react-dom": "^18.3.0",
    "@testing-library/react": "^16.0.0", "@testing-library/jest-dom": "^6.4.0"
  }
}
